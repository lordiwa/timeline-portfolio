---
phase: 04-chapters-0-2-4-5
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - public/assets/ch2-bg.png
  - src/components/Chapter2Content.vue
  - src/components/FlashBanner.vue
  - src/components/ScrollShell.vue
  - src/styles/chapter-themes.css
  - src/data/chapters.js
  - src/data/projects.js
  - src/i18n/es.json
  - src/i18n/en.json
  - tests/components/Chapter2Content.test.js
  - tests/components/FlashBanner.test.js
autonomous: false
requirements:
  - ART-02
  - A11Y-06
  - I18N-05
user_setup:
  - service: pixelforge-mcp
    why: "Generación de ch2-bg.png (forge_background full-frame opaco Flash era)"
    env_vars:
      - name: GEMINI_API_KEY
        source: "ya configurada"
  - service: adobe-mcp
    why: "Crop 480×270 + opcional HSL adjust para harmonizar paleta del bg con tokens ch2"
must_haves:
  truths:
    - "Al hacer scroll a ch2, el visitante ve un background Flash era 2009 (banner+browser chrome+vector gradients) generado por pixelforge"
    - "FlashBanner.vue renderiza header skeumorphic con orange→purple gradient + drop shadow + emboss"
    - "Ch2 muestra 1-3 ProjectCards filtrados por chapterEra===2 con variant `[data-chapter='2']` Flash-era CSS"
    - "Toggle ES↔EN cambia bio + flavor + project titles/descs sin layout shift"
    - "El bg consume `--bg-image: url('/assets/ch2-bg.png')` y BackgroundLayers crossfade lo aplica al entrar al chapter"
  artifacts:
    - path: "public/assets/ch2-bg.png"
      provides: "Flash era 2009 background full-frame 480×270 (pixelforge forge_background)"
    - path: "src/components/Chapter2Content.vue"
      provides: "Wrapper layout ch2 + FlashBanner embed + bio + projects filtrados era===2"
      min_lines: 60
    - path: "src/components/FlashBanner.vue"
      provides: "Header skeumorphic Flash era con linear-gradient orange→deep + chrome dots + emboss"
      min_lines: 30
    - path: "src/styles/chapter-themes.css"
      provides: "[data-chapter='2'] .project-card variant + [data-chapter='2'] .flash-banner block + --bg-image declared"
    - path: "src/data/chapters.js"
      provides: "chapters[2].palette poblada con §5.1 hex (5-8 colors) + opcional bgImage field"
      contains: "palette: \\["
    - path: "src/data/projects.js"
      provides: "1-3 items chapterEra===2 con shape D3-03 (BlueLizard/Matte/Joju)"
    - path: "tests/components/Chapter2Content.test.js"
      provides: "DOM contract + FlashBanner embed + projects filter ch2 + bg-image asserts + locale switch"
    - path: "tests/components/FlashBanner.test.js"
      provides: "Skeumorphic CSS markers (linear-gradient orange + inset highlight + box-shadow)"
  key_links:
    - from: "src/components/Chapter2Content.vue"
      to: "src/components/FlashBanner.vue, src/components/ProjectCard.vue"
      via: "imports + template embed"
      pattern: "<FlashBanner|<ProjectCard"
    - from: "src/styles/chapter-themes.css [data-chapter='2']"
      to: "public/assets/ch2-bg.png"
      via: "--bg-image: url(...)"
      pattern: "--bg-image:\\s*url\\(['\"]?/assets/ch2-bg\\.png"
    - from: "src/components/BackgroundLayers.vue (Phase 2 D2-04)"
      to: "ch2 --bg-image"
      via: "useBackgroundMorph consume var(--bg-image) at activeChapter===2"
      pattern: "background-image:\\s*var\\(--bg-image"
---

<objective>
W2 entrega **ch2 Flash era 2009** completo: 1 background full-frame pixel-art + FlashBanner skeumorphic + Chapter2Content wrapper + 1-3 ProjectCards con variant Flash-era + paleta §5.1 lockeada en `chapters[2].palette` + i18n keys de proyectos era.

Purpose: Ch2 es el primer chapter de la "era profesional" (gameplay programmer BlueLizard/Matte/Joju) — entrega evidencia visual de Flash era (banner skeumorphic + paleta orange-purple distintiva) y abre el patrón "pixelforge full-frame opaco + BackgroundLayers crossfade" que ch5 hereda.

Output: 1 PNG generado + 2 SFCs nuevos + extensiones a 4 archivos existentes (ScrollShell wire + chapter-themes.css variant + chapters.js palette + projects.js items + es.json/en.json keys) + 2 test files.

**MVP vertical slice (W2):** Tras esta wave el visitante hace scroll a ch2 y ve: banner Flash naranja, background pixel-art con vibe vector 2009, lista de proyectos BlueLizard/Matte/Joju como cards con gradient skeumorphic, bio y flavor en idioma activo. Los siguientes (ch4/ch5) siguen en placeholder hasta W3/W4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md
@.planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md
@.planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md
@.planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md
@CLAUDE.md
@src/components/Chapter3Content.vue
@src/components/ProjectCard.vue
@src/components/BackgroundLayers.vue
@src/components/ScrollShell.vue
@src/styles/chapter-themes.css
@src/data/chapters.js
@src/data/projects.js
@src/i18n/es.json
@src/i18n/en.json
@tests/components/Chapter3Content.test.js
@tests/components/ProjectCard.test.js
@.claude/agents/artist-creator.md
@.claude/skills/crear-arte-pixelforge.md

<interfaces>
<!-- Contratos clave W2 -->

From src/data/projects.js — shape D3-03 verbatim (de Chapter3Content.test.js fixture):
```javascript
{
  id: 'ch2-bluelizard',
  chapterEra: 2,
  year: 2009,
  titleKey: 'projects.ch2-bluelizard.title',
  descKey: 'projects.ch2-bluelizard.desc',
  link: 'https://...' || null,
  imageSrc: null,
  role: 'Gameplay Programmer' || null,
  techStack: ['ActionScript 3', 'Flash CS5'] || null,
  planetSprite: null,    // Phase 5 ch6 only
  planetOrbit: null,
  planetColor: null,
}
```

From src/styles/chapter-themes.css — [data-chapter="2"] block stub Phase 2:
```css
[data-chapter="2"] {
  --c-bg: #2a1a4a;
  --c-fg: #e0c0ff;
  --c-accent: #ff8800;
  --c-border: #8060c0;
  --c-focus: #ffaa00;
  --c-surface: #1a0a3a;  /* opcional, planner añade si falta */
  /* --bg-image: url('/assets/ch2-bg.png');  ← AÑADIR W2 */
}
```

From UI-SPEC §6.4 — FlashBanner DOM:
```html
<header class="flash-banner">
  <div class="flash-banner-chrome" aria-hidden="true"></div>
  <h2 class="flash-banner-title">{{ t('chapters.2.bannerTitle') }}</h2>
  <p class="flash-banner-subtitle">{{ t('chapters.2.bannerSubtitle') }}</p>
</header>
```

Visual: linear-gradient orange→deep + chrome dots pseudo-elements top-left + 2px solid amber border + box-shadow drop + inset highlight + text-shadow embossed title.

From RESEARCH §Pattern 5 (W2 — ch2-bg.png prompt):
```
forge_background(
  description: "pixel art 2009 Flash era banner with vector gradients,
                glossy buttons, browser chrome (Internet Explorer or Firefox 3 era),
                retro Web 2.0 aesthetic, solid filled shapes, NO outlines,
                flat shading, era-authentic 2009 vibe",
  palette: chapters[2].palette,
  aspect: "16:9",
  outputPath: "public/assets/ch2-bg.png"
)
optimize_sprite(size: "480x270", ...)
```

From PATTERNS.md §4 (FlashBanner skeumorphic CSS):
- `background: linear-gradient(to bottom, #ff8800 0%, #cc6600 50%, #883300 100%)` orange→deep.
- `border: 2px solid #ffaa00` amber.
- `box-shadow: 0 8px 24px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.3)`.
- `text-shadow: 0 1px 0 rgba(255,255,255,0.7)` para embossed title.
- Chrome dots: 3 pseudo-elementos `top: 8px; left: 8px` red/yellow/green 8×8 round.

i18n keys nuevas Phase 4 W2:
```
chapters.2.flavor                     "Era Flash 2009. ActionScript, banners vector, gameplay arcade."
chapters.2.bannerTitle                "Era Flash · 2009"
chapters.2.bannerSubtitle             "BlueLizard / Matte CG / Joju Games"
projects.ch2-bluelizard.title         "BlueLizard Games"
projects.ch2-bluelizard.desc          "..."
projects.ch2-matte.title              "Matte CG"
projects.ch2-matte.desc               "..."
projects.ch2-joju.title               "Joju Games"
projects.ch2-joju.desc                "..."
```
Cantidad real (1, 2 ó 3 items) la decide Rafael en §2.1 — checkpoint:human-input.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-input" gate="blocking">
  <name>Task 1: Gate W2 — §2.1 proyectos ch2 Flash + §5.1 paleta Flash</name>
  <what-needed>
    Dos inputs de `CONTENT-CHECKLIST.md` para arrancar W2:

    **1. §5.1 paleta ch2 Flash (5-8 hex)** — el `palette:` argument que pixelforge usará para `ch2-bg.png`. Stub Phase 2 disponible si Rafael no entrega: `['#2a1a4a', '#e0c0ff', '#ff8800', '#8060c0', '#ffaa00']`. Si Rafael refina 1-2 hex, registrar la versión final.

    **2. §2.1 proyectos ch2 (1-3 items)** — para cada proyecto:
    ```
    Proyecto N (id sugerido: ch2-bluelizard | ch2-matte | ch2-joju):
    - title (ES):
    - title (EN):
    - desc breve (ES, 1-2 frases):
    - desc breve (EN):
    - role: (e.g., "Gameplay Programmer")
    - techStack: (array strings, e.g., ["ActionScript 3", "Flash CS5"])
    - link: (URL o null)
    ```

    Si §2.1 está vacío al execute → W2 bloquea aquí. W3 (ch4) y W4 (ch5) NO dependen — pueden arrancar después de su propio gate. W1 (ch0/ch1) ya completo.

    **Fallback acceptable:** Si Rafael responde "usa stubs" → ejecutor genera 1 proyecto placeholder por estudio con `title` = nombre del estudio, `desc` = "PENDING — Rafael completará en checklist post-merge". El test parity sigue pasando porque las keys existen en ambos idiomas; W5 sign-off captura el TODO.
  </what-needed>
  <how-to-verify>
    1. Lee `.planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md` (o equivalente) §2.1 y §5.1.
    2. Si vacío, pregunta a Rafael directamente:
       > "Para W2 (ch2 Flash) necesito:
       > - §5.1 paleta ch2 (5-8 hex; stub disponible si OK)
       > - §2.1 proyectos Flash era: 1-3 items con title ES/EN + desc ES/EN + role + techStack
       > Responde 'usa stubs' si prefieres placeholders editables después, o entrega los datos."
    3. Registrar los datos recibidos (palette + lista de proyectos) para Tasks 2-3.
  </how-to-verify>
  <resume-signal>
    Rafael responde con:
    - Paleta §5.1 final (o 'usa stubs').
    - Lista de proyectos §2.1 (o 'usa stubs ch2-bluelizard, ch2-matte, ch2-joju con PENDING desc').

    Continuar Task 2 con los datos.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generar ch2-bg.png + actualizar chapters.js palette + chapter-themes.css --bg-image</name>
  <files>
    public/assets/ch2-bg.png,
    src/data/chapters.js,
    src/styles/chapter-themes.css
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 5" lines 627-639 (W2 prompt verbatim)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §5 (color tokens ch2 — 60/30/10 distribution + acento reserved-for) + §13 pixel art constraints
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §"src/data/chapters.js (MODIFICADO)" + §"src/styles/chapter-themes.css (MODIFICADO)" sección target 1 y 2
    - .claude/skills/crear-arte-pixelforge.md (ToolSearch + forge_background opaco + optimize_sprite)
    - CLAUDE.md §6.1 (siluetas planas), §6.3 (forge_background NO forge_sprite para opacos)
    - src/data/chapters.js (líneas 35-43 ch2 stub con palette: [])
    - src/styles/chapter-themes.css (búsqueda `[data-chapter="2"]` block — actualmente stub Phase 2)
  </read_first>
  <action>
    **Delegar a `artist-creator` agent** o ejecutar como assistant principal.

    **2.1 Cargar tools deferred pixelforge** (si no ya cargados en sesión):
    ```
    ToolSearch(query="select:mcp__pixelforge__forge_background,mcp__pixelforge__forge_sprite,mcp__pixelforge__process_sprite,mcp__pixelforge__optimize_sprite", max_results=10)
    ```

    **2.2 Generar `public/assets/ch2-bg.png`:**

    Llamar `mcp__pixelforge__forge_background` con:
    - `description`: verbatim de RESEARCH §Pattern 5 W2: `"pixel art 2009 Flash era banner with vector gradients, glossy buttons, browser chrome (Internet Explorer or Firefox 3 era), retro Web 2.0 aesthetic, solid filled shapes, NO outlines, flat shading, era-authentic 2009 vibe"`. Añadir al final `"480x270 resolution, 16:9 aspect"`.
    - `palette`: la paleta §5.1 recibida en Task 1 (o stub `['#2a1a4a', '#e0c0ff', '#ff8800', '#8060c0', '#ffaa00']`). Pasar verbatim.
    - `aspect`: `"16:9"`.
    - `outputPath`: `"public/assets/ch2-bg.png"`.

    **2.3 Post-process:**

    - `mcp__pixelforge__optimize_sprite` con `inputPath: "public/assets/ch2-bg.png"`, `outputPath: "public/assets/ch2-bg.png"`, `size: "480x270"` (nearest-neighbor downscale).
    - Adobe MCP opcional si el output necesita HSL adjust: `adobe_mandatory_init()` → `image_adjust_hsl(saturation: ±5)` o `image_adjust_color_temperature` si la paleta drifteó del §5.1 expected. Solo si visible-eye distinto al stub.

    **2.4 Actualizar `src/data/chapters.js`:**

    Buscar el bloque `id: 2` (lines ~34-43) y reemplazar `palette: []` con `palette: [...§5.1 hex array]`. Opcional añadir `bgImage: '/assets/ch2-bg.png'` field (data-driven path — pero el chapter-themes.css ya lo declara via `--bg-image`, así que es opcional para consistency). **Recommended:** dejar solo `palette:` actualizado, mantener CSS-driven `--bg-image` para minimizar cambios.

    **2.5 Actualizar `src/styles/chapter-themes.css`:**

    Buscar el bloque `[data-chapter="2"]` (Phase 2 stub) y añadir DENTRO del bloque:
    ```css
    --bg-image: url('/assets/ch2-bg.png');
    ```
    al lado de los demás custom props (justo después de `--c-focus: #ffaa00;`).

    Si la paleta §5.1 final difiere 1-2 hex del stub Phase 2, también actualizar `--c-bg/--c-fg/--c-accent/--c-border/--c-focus` con los valores aprobados Rafael — pero **solo si difieren**; si Rafael dijo "usa stubs", dejar Phase 2 values intactos.

    **2.6 Smoke `npm run dev`:**

    Visitar `?ch=2` → el BackgroundLayers (Phase 2 D2-04) debe crossfadear al entrar y mostrar `ch2-bg.png` como fondo. `useBackgroundMorph` consume `var(--bg-image)` automáticamente — sin código nuevo.

    **2.7 Tests:**

    `npm run test:run -- tests/assets/asset-naming.test.js` debe seguir verde — `ch2-bg.png` matchea regex `^ch[0-6]-(bust|bg|...)\.png$`.
  </action>
  <verify>
    <automated>npm run test:run -- tests/assets/asset-naming.test.js tests/data/chapters.test.js tests/styles/themes-file.test.js</automated>
    Esperado:
    - asset-naming T1 verde (ch2-bg.png matchea).
    - chapters.test.js: `chapters[2].palette.length >= 5` debería pasar (si existing test lo cubre — si no, no rompe nada).
    - themes-file.test.js: 7 themes blocks aún presentes.

    Adicional manual: `Test-Path public/assets/ch2-bg.png` → True. Tamaño 30-60 KB rango esperado.
  </verify>
  <done>
    - `ch2-bg.png` 480×270 generado, paleta §5.1 aplicada.
    - `chapters[2].palette` poblada con array hex.
    - `chapter-themes.css [data-chapter="2"]` declara `--bg-image: url('/assets/ch2-bg.png')`.
    - Smoke `npm run dev ?ch=2` muestra el bg al entrar al chapter.
    - Commits: `art(04-03): generate ch2-bg.png`, `feat(04-03): update ch2 palette + --bg-image declaration`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear FlashBanner + Chapter2Content + projects ch2 + i18n + wire ScrollShell + tests</name>
  <files>
    src/components/FlashBanner.vue,
    src/components/Chapter2Content.vue,
    src/components/ScrollShell.vue,
    src/data/projects.js,
    src/styles/chapter-themes.css,
    src/i18n/es.json,
    src/i18n/en.json,
    tests/components/FlashBanner.test.js,
    tests/components/Chapter2Content.test.js
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §4 (FlashBanner skeumorphic CSS verbatim de chapter-themes.css existente .project-card lines 158-247) + §1 (Chapter2Content reuse Chapter3Content layout)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §6.4 FlashBanner DOM + §6.9 ProjectCard variants ch2 (linear-gradient orange→deep)
    - src/components/Chapter3Content.vue (clonable verbatim)
    - src/components/ProjectCard.vue (componente shared — los project items ch2 lo consumen via [data-chapter="2"] cascade variant)
    - src/styles/chapter-themes.css @layer components lines 158-247 (.project-card base + Phase 3 web 2.0 skeumorphic)
    - src/data/projects.js (current empty array — Phase 3 carry-forward shape)
    - tests/components/Chapter3Content.test.js (T1-T6 mount + projects filter pattern)
    - tests/components/ProjectCard.test.js (props validator + CSS markers pattern)
    - tests/i18n/parity.test.js (qué keys nuevas valida)
  </read_first>
  <behavior>
    **FlashBanner:**
    - `<script setup>` con `useI18n` import + `defineProps({})` vacío opcional.
    - Template: `<header class="flash-banner"><div class="flash-banner-chrome" aria-hidden="true"></div><h2 class="flash-banner-title">{{ t('chapters.2.bannerTitle') }}</h2><p class="flash-banner-subtitle">{{ t('chapters.2.bannerSubtitle') }}</p></header>`.
    - El styling NO va en `<style scoped>` del SFC — va en `chapter-themes.css @layer components` bajo `[data-chapter="2"] .flash-banner { ... }` para que la cascade aplique tokens del theme correctamente. Esto es CRÍTICO: si lo metes en scoped, los tokens `var(--c-accent)` resolverán pero la convención del proyecto (Phase 3 ProjectCard idem) es que styling era-specific vive en `chapter-themes.css`.

    **Chapter2Content:**
    - Layout 2-col/stacked clonado Chapter3Content cambiando `ch3` → `ch2`.
    - `const chapter = chapters[2]` + `const ch2Projects = computed(() => projects.filter(p => p.chapterEra === 2))`.
    - Template: dentro de `.ch2-content`, `<FlashBanner />` PRIMERO (header arriba), después `.ch2-bio` con `t(bio.coreKey)` + `<p class="ch2-flavor">{{ t('chapters.2.flavor') }}</p>`, después `<div v-if="ch2Projects.length > 0" class="ch2-projects">` con `<ProjectCard v-for="p in ch2Projects" :key="p.id" :project="p" />`.
    - El fondo del chapter viene de BackgroundLayers (D2-04 global) consumiendo `--bg-image` — NO necesita `background-image` en el SFC scoped.

    **chapter-themes.css extensions (@layer components):**

    Añadir al final del archivo (después del último bloque .project-card existente Phase 3):

    1. `[data-chapter="2"] .flash-banner` block — skeumorphic Flash CSS:
       ```css
       [data-chapter="2"] .flash-banner {
         position: relative;
         border-radius: 8px;
         padding: var(--sp-lg);
         background: linear-gradient(to bottom, #ff8800 0%, #cc6600 50%, #883300 100%);
         border: 2px solid #ffaa00;
         box-shadow: 0 8px 24px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.3);
         margin-bottom: var(--sp-md);
       }
       [data-chapter="2"] .flash-banner-chrome {
         position: absolute;
         top: 8px;
         left: 8px;
         width: 32px;
         height: 8px;
         /* 3 dots simulando window controls */
         background:
           radial-gradient(circle 4px at 4px center, #ff5f56, transparent 5px),
           radial-gradient(circle 4px at 16px center, #ffbd2e, transparent 5px),
           radial-gradient(circle 4px at 28px center, #27c93f, transparent 5px);
       }
       [data-chapter="2"] .flash-banner-title {
         font-family: 'Verdana', 'Trebuchet MS', sans-serif;
         color: #ffffff;
         text-shadow: 0 1px 0 rgba(0,0,0,0.4), 0 -1px 0 rgba(255,255,255,0.4);
         margin: 0 0 var(--sp-sm) 0;
         font-size: clamp(1.5rem, 4vw, 2.5rem);
         font-weight: 700;
       }
       [data-chapter="2"] .flash-banner-subtitle {
         font-family: 'Verdana', 'Trebuchet MS', sans-serif;
         color: rgba(255,255,255,0.85);
         margin: 0;
         font-size: 1rem;
       }
       ```

    2. `[data-chapter="2"] .project-card` variant — UI-SPEC §6.9 verbatim:
       ```css
       [data-chapter="2"] .project-card {
         background:
           linear-gradient(to bottom, rgba(255,136,0,0.18) 0%, rgba(128,96,192,0.08) 50%, rgba(0,0,0,0.1) 100%),
           var(--c-surface, #1a0a3a);
         border: 2px solid var(--c-accent);  /* #ff8800 orange */
         box-shadow: 0 6px 16px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.4);
       }
       [data-chapter="2"] .project-card-title {
         font-family: 'Verdana', 'Trebuchet MS', sans-serif;
         color: var(--c-fg);
         text-shadow: 0 1px 0 rgba(255,255,255,0.4);
       }
       [data-chapter="2"] .project-card-link {
         background: linear-gradient(to bottom, var(--c-accent), color-mix(in srgb, var(--c-accent) 60%, #000));
         border-radius: 4px;
       }
       ```

    **projects.js items:**
    - Añadir 1-3 items con shape D3-03 verbatim (id, chapterEra=2, year=2009, titleKey, descKey, link/null, imageSrc=null, role string, techStack array, planet* null).

    **i18n keys (paridad ES + EN):**
    - `chapters.2.flavor`, `chapters.2.bannerTitle`, `chapters.2.bannerSubtitle`.
    - Por cada proyecto en projects.js: `projects.{id}.title` + `projects.{id}.desc` en ambos idiomas.

    **ScrollShell wire:**
    - Añadir `import Chapter2Content from './Chapter2Content.vue'`.
    - Añadir `<Chapter2Content v-else-if="ch.id === 2" />` en la cadena después de ch1 y antes de ch3.

    **Tests:**

    `FlashBanner.test.js` (analog ProjectCard.test.js pattern + readFileSync CSS markers):
    - T1: mount con i18n → `<header class="flash-banner">` exists, `.flash-banner-chrome` + `.flash-banner-title` + `.flash-banner-subtitle` exist.
    - T2: locale ES vs EN — title cambia al toggle.
    - T3: chapter-themes.css source contiene `[data-chapter="2"] .flash-banner` block con `linear-gradient` + `box-shadow` + `inset 0 1px 0` (markers skeumorphic).
    - T4: source contiene `.flash-banner-chrome` con `radial-gradient` (chrome dots).

    `Chapter2Content.test.js` (analog Chapter3Content.test.js T1-T6):
    - T1: `.ch2-layout` + `.ch2-meta` + `.ch2-content` exist.
    - T2: avatar src `'/assets/ch2-bust.png'` + alt resuelve.
    - T3: `<FlashBanner>` embebido (findComponent).
    - T4: mock projects con 2 items ch2 + 1 item ch4 → `findAllComponents(ProjectCard).length === 2` (filter funciona).
    - T5: locale ES→EN, bio + flavor cambian.
    - T6: CSS readFileSync (chapter-themes.css) contiene `[data-chapter="2"] .project-card` block con `linear-gradient` orange variant.
  </behavior>
  <action>
    **3.1 Crear `src/components/FlashBanner.vue`** según behavior block. SFC minimal: solo template + script (CSS vive en chapter-themes.css).

    **3.2 Crear `src/components/Chapter2Content.vue`** clonando Chapter3Content.vue con cambios mecánicos `ch3` → `ch2`, embed `<FlashBanner />`, añadir `<p class="ch2-flavor">`. Avatar src será `/assets/ch2-bust.png` (existe post-W0).

    **3.3 Extender `src/styles/chapter-themes.css @layer components`** añadiendo los dos bloques `[data-chapter="2"] .flash-banner ...` y `[data-chapter="2"] .project-card ...` según behavior block.

    **3.4 Añadir items a `src/data/projects.js`:**
    - Si Rafael entregó §2.1 datos reales → usar exactos.
    - Si "usa stubs" → 3 items placeholder con shape D3-03 + `desc` = `"PENDING — Rafael CONTENT-CHECKLIST §2.1"`:
      ```javascript
      { id: 'ch2-bluelizard', chapterEra: 2, year: 2009, titleKey: 'projects.ch2-bluelizard.title',
        descKey: 'projects.ch2-bluelizard.desc', link: null, imageSrc: null,
        role: 'Gameplay Programmer', techStack: ['ActionScript 3', 'Flash CS5'],
        planetSprite: null, planetOrbit: null, planetColor: null },
      // ch2-matte, ch2-joju análogos
      ```

    **3.5 Añadir i18n keys a es.json + en.json:**

    ES:
    ```json
    "chapters": {
      "2": { ..., "flavor": "Era Flash 2009. ActionScript, banners vector, gameplay arcade." }
    },
    "projects": {
      "ch2-bluelizard": { "title": "BlueLizard Games", "desc": "..." },
      ...
    }
    ```

    EN paridad — `chapters.2.flavor`: `"Flash era 2009. ActionScript, vector banners, arcade gameplay."`; project titles/descs en EN.

    Si stubs: `desc` = `"PENDING — fill in CONTENT-CHECKLIST §2.1"` (ES) / `"PENDING — fill in CONTENT-CHECKLIST §2.1"` (EN paridad).

    Adicional UI-SPEC §11.6:
    - `chapters.2.bannerTitle` ES `"Era Flash · 2009"` / EN `"Flash Era · 2009"`.
    - `chapters.2.bannerSubtitle` ES + EN ambos iguales `"BlueLizard / Matte CG / Joju Games"` (nombres propios sin traducción).

    **3.6 Modificar `src/components/ScrollShell.vue`:**

    Imports: añadir `import Chapter2Content from './Chapter2Content.vue'`.

    Template: en la cadena v-if/v-else-if (ya tiene ch0, ch1, ch3 post-W1), añadir entre ch1 y ch3:
    ```vue
    <Chapter2Content v-else-if="ch.id === 2" />
    ```

    **3.7 Crear `tests/components/FlashBanner.test.js`** — mount con createTestI18n + readFileSync sobre chapter-themes.css para asserts skeumorphic markers.

    **3.8 Crear `tests/components/Chapter2Content.test.js`** clonando Chapter3Content.test.js con cambios mecánicos + T-ch2-projects-filter (mock projects mixto ch2/ch4 verifica filter).

    **3.9 Smoke `npm run dev ?ch=2`** — confirmar visualmente:
    - Banner Flash naranja con chrome dots en top-left.
    - Background pixel-art `ch2-bg.png` crossfade aplicado al entrar.
    - Project cards con gradient orange-purple.
    - Toggle ES↔EN funcional.
  </action>
  <verify>
    <automated>npm run test:run -- tests/components/FlashBanner.test.js tests/components/Chapter2Content.test.js tests/components/ProjectCard.test.js tests/i18n/parity.test.js tests/data/projects.test.js</automated>
    Esperado:
    - FlashBanner T1-T4 PASS.
    - Chapter2Content T1-T6 PASS (incluido projects filter).
    - ProjectCard.test.js sin regresión (variant ch2 no debería romper tests existentes).
    - parity test PASS (todas las nuevas keys ch2 + projects paridad ES↔EN).
    - projects.test.js: si existe schema test (shape D3-03 enforce) — verificar los 1-3 nuevos items cumplen shape.
    - Suite full: `npm run test:run` ~245 (post-W1) → ~260 PASS.
  </verify>
  <done>
    - 2 SFCs nuevos: FlashBanner, Chapter2Content.
    - 2 test files: FlashBanner.test.js, Chapter2Content.test.js.
    - chapter-themes.css extended con `[data-chapter="2"]` `.flash-banner` + `.project-card` variants.
    - projects.js con 1-3 items ch2.
    - i18n: chapters.2.{flavor,bannerTitle,bannerSubtitle} + projects.ch2-*.{title,desc} en ES + EN paridad.
    - ScrollShell.vue: wire-up Chapter2Content.
    - Smoke `npm run dev ?ch=2` visualmente correcto.
    - Commits atómicos: `feat(04-03): FlashBanner SFC`, `feat(04-03): Chapter2Content + projects + i18n`, `feat(04-03): wire ch2 in ScrollShell`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixelforge palette → asset | Paleta `chapters[2].palette` autor-controlado |
| projects.js content → Vue template | `t()` resuelve i18n autor-controlado, cero v-html |
| project.link → external nav | Anchor tags requieren rel="noopener noreferrer" + target="_blank" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | XSS-CONTENT | flavor/banner/project desc en i18n keys | mitigate | Vue auto-escapa `{{ t(...) }}`; cero v-html. ProjectCard.vue Phase 3 ya valida shape D3-03 con validator. |
| T-04-07 | Phishing | `project.link` external en projects.js | mitigate | ProjectCard.vue (Phase 3) ya aplica `rel="noopener noreferrer"` + `target="_blank"` para `<a>` tags. T-CON-03 test existente verifica. |
</threat_model>

<verification>
## Wave-level checks W2

1. **Asset-naming guard verde:** `ch2-bg.png` matchea regex `^ch[0-6]-bg\.png$`.
2. **Suite no regresión:** full suite ~260 PASS (≈+15 W2 tests).
3. **ART-02 cumplido:** ch2-bg.png existe + chapter-themes.css declara `--bg-image: url('/assets/ch2-bg.png')`.
4. **i18n parity verde:** todas las keys ch2 + projects.ch2-* en ambos idiomas.
5. **Build verde:** `npm run build` completa.
6. **Smoke manual:** `?ch=2` muestra banner Flash + bg pixel-art + project cards orange + toggle ES↔EN funcional.

## Out of scope W2
- Refinement de bg si Rafael ve drift visual → opcional iterar in-place, NO blocking.
- Animación del banner (Flash era hover effects en buttons) → no requerido por D-06 (interaction-derived ≤150ms ya cubre hovers default).
- Mobile testing en hardware iOS — deferred Plan 07 Phase 1.
</verification>

<success_criteria>
- `public/assets/ch2-bg.png` generado, 480×270, paleta §5.1 aplicada.
- 2 SFCs: FlashBanner + Chapter2Content (era-signature + wrapper).
- chapter-themes.css extended con `[data-chapter="2"]` `.flash-banner` + `.project-card` variants completos.
- 1-3 items ch2 en projects.js + keys i18n correspondientes (ES + EN paridad).
- ScrollShell wire-up ch2.
- Tests `FlashBanner.test.js` + `Chapter2Content.test.js` verdes.
- Suite full no regresión.
- Smoke `npm run dev ?ch=2` visualmente correcto.
</success_criteria>

<output>
After completion, create `.planning/phases/04-chapters-0-2-4-5/04-03-SUMMARY.md` con:
- ch2-bg.png details (file size, paleta usada).
- FlashBanner + Chapter2Content paths + LOC.
- Items ch2 añadidos a projects.js (lista de ids).
- i18n keys nuevas (lista).
- Smoke screenshot path o descripción.
- Lecciones para W3 (ch4 multi-layer) y W4 (ch5 hero).
</output>
