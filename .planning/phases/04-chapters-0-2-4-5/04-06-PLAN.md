---
phase: 04-chapters-0-2-4-5
plan: 06
type: execute
wave: 5
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - src/i18n/es.json
  - src/i18n/en.json
  - tests/i18n/parity.test.js
  - tests/integration/theme-bleed-phase4.test.js
  - tests/components/ScrollShell.theme-isolation-phase4.test.js
  - tests/i18n/layout-shift-phase4.test.js
  - .planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md
  - .planning/phases/04-chapters-0-2-4-5/04-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - A11Y-06
  - THM-04
  - THM-05
  - I18N-05
  - A11Y-04
  - A11Y-07
  - CORE-09
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "Los 7 alt-text avatar.busts.{N}.alt están refreshed con texto era-accurate en ES + EN (D4-11)"
    - "Test parity.test.js T4 valida que ningún alt contiene 'PENDING' ni 'Placeholder' (W5 sign-off guard)"
    - "Theme isolation cross-chapter (ch4 parallax + ch5 scroll-reveal) NO bleed durante snap"
    - "Layout shift ES vs EN: cada Chapter{N}Content (N=0,1,2,4,5) mantiene height comparable cross-locale (no overflow visible)"
    - "Manual checklist 04-MANUAL-CHECKLIST.md cubre los 9 manual-only items (visual coherence busts, parallax depth ch4, marquee+starfield ch1, terminal ch0, FlashBanner ch2, FloatingPanel ch4, ScrollRevealCard ch5, alt-text ratification, ES vs EN layout, contrast audit ch2/4/5, privacy ch4-bg-stars-far privacy guard, iOS preventive mitigations)"
    - "Rafael firma §10 sign-off con verdict: PASS | PASS-with-observations"
    - "Build verde y suite global verde (≈315+ tests)"
  artifacts:
    - path: "src/i18n/es.json"
      provides: "avatar.busts.{0..6}.alt refresh con drafts era-accurate D4-11 §8.2"
    - path: "src/i18n/en.json"
      provides: "avatar.busts.{0..6}.alt refresh EN paridad"
    - path: "tests/i18n/parity.test.js"
      provides: "T4 extension: alt-text no contiene PENDING/Placeholder"
    - path: "tests/integration/theme-bleed-phase4.test.js"
      provides: "Integration: parallax ch4 + scroll-reveal ch5 contained en su section, no bleed durante snap entre chapters adyacentes"
    - path: "tests/components/ScrollShell.theme-isolation-phase4.test.js"
      provides: "Mount full ScrollShell con mocks; verifica que cada section data-chapter='N' es boundary CSS scope correcto"
    - path: "tests/i18n/layout-shift-phase4.test.js"
      provides: "Snapshot length comparator ES vs EN per Chapter{N}Content para detectar overflow potencial"
    - path: ".planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md"
      provides: "Checklist manual humano: 11 items + §10 sign-off Rafael verdict PASS"
    - path: ".planning/phases/04-chapters-0-2-4-5/04-VERIFICATION.md"
      provides: "Verification report final Phase 4 (suite test results + manual gates) — actualiza el draft VALIDATION.md a estado final"
  key_links:
    - from: "tests/i18n/parity.test.js T4"
      to: "src/i18n/{es,en}.json avatar.busts.*.alt"
      via: "for loop sobre 0..6 verifica no contiene placeholder text"
      pattern: "avatar\\.busts\\.\\d+\\.alt"
    - from: "04-MANUAL-CHECKLIST.md §10"
      to: "STATE.md + ROADMAP.md (Phase 4 closed)"
      via: "Rafael firma → planner-actor actualiza state final"
      pattern: "verdict:\\s*PASS"
---

<objective>
W5 cierra Phase 4 con tres acciones complementarias:

1. **Refresh i18n alt-text era-accurate** (D4-11) — Reemplazar los placeholders Phase 2 `avatar.busts.{0..6}.alt` con drafts redactados por el ejecutor (RESEARCH §Pattern 8 + UI-SPEC §8.2 templates) + extender `parity.test.js` con T4 guard contra reintroducción de placeholders.

2. **Tests de integración cross-chapter** — Verificar programáticamente que (a) parallax ch4 NO se filtra a ch3/ch5 durante snap, (b) scroll-reveal ch5 NO triggers cuando ch3 activo, (c) layout shift ES vs EN per chapter no produce overflow.

3. **Manual checklist + sign-off Rafael** — Generar `04-MANUAL-CHECKLIST.md` derivado de VALIDATION.md §"Manual-Only Verifications" + UI-SPEC §A11Y + RESEARCH §Open Questions. Rafael ejecuta los 11 items + firma §10. Generar `04-VERIFICATION.md` final con resultados de suite + manual gates → cierra phase.

Purpose: Sin W5 el motor programático Phase 4 está completo pero el contrato visual no está validado por humano. Phase 4 entrega chapters era-auténticos cuyo "se siente correcto" solo el ojo de Rafael puede ratificar. Sin W5 sign-off, no se puede declarar Phase 4 PASS y arrancar Phase 5.

Output: 2 archivos i18n actualizados (~14 keys refreshed) + 3 test files nuevos (1 parity extension + 2 integration) + 04-MANUAL-CHECKLIST.md (canonical artifact) + 04-VERIFICATION.md (final report) + STATE.md + ROADMAP.md actualizados.

**MVP vertical slice (W5):** Tras esta wave: visitante con screen reader oye alt-text era-accurate al pasar por cada chapter ("Rafael at 10 in front of a CRT monitor..."); ejecutor next-phase (Phase 5) tiene un Phase 4 cerrado con verdict PASS de Rafael; cualquier regresión visual cross-chapter es detectada por tests automated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md
@.planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md
@.planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md
@.planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md
@.planning/phases/04-chapters-0-2-4-5/04-VALIDATION.md
@.planning/phases/02-theme-system-i18n/02-MANUAL-CHECKLIST.md
@CLAUDE.md
@src/i18n/es.json
@src/i18n/en.json
@tests/i18n/parity.test.js
@tests/components/ScrollShell.theme-isolation.test.js
@tests/components/Chapter0Content.test.js
@tests/components/Chapter1Content.test.js
@tests/components/Chapter2Content.test.js
@tests/components/Chapter4Content.test.js
@tests/components/Chapter5Content.test.js

<interfaces>
<!-- Contratos clave W5 -->

From UI-SPEC §8.2 — alt-text drafts era-accurate (ES + EN paridad):

ES:
- `avatar.busts.0.alt`: "Rafael a los 10 años frente a un monitor CRT, era de comandos en terminal (1995)"
- `avatar.busts.1.alt`: "Rafael adolescente a los 17, en una página web HTML 90s con marquees y fondo estrellado (2001)"
- `avatar.busts.2.alt`: "Rafael a los 22, programador de gameplay en la era Flash, banner vector colorido (2009)"
- `avatar.busts.3.alt`: "Rafael a los 26, web 2.0 era diseñador y líder de equipo en Pink Parrot (2013)"
- `avatar.busts.4.alt`: "Rafael a los 30, era AR/VR con paneles holográficos flotantes (2015-2018)"
- `avatar.busts.5.alt`: "Rafael a los 36, era moderna animada, líder frontend (2022-2023)"
- `avatar.busts.6.alt`: "Rafael a los 40, era de convergencia QA + IA, ambiente espacial pixel art (2026)"

EN paridad:
- `avatar.busts.0.alt`: "Rafael at 10 in front of a CRT monitor, command-line era (1995)"
- `avatar.busts.1.alt`: "Rafael as a teenager at 17, on a 90s HTML page with marquees and starry background (2001)"
- `avatar.busts.2.alt`: "Rafael at 22, gameplay programmer in the Flash era, colorful vector banner (2009)"
- `avatar.busts.3.alt`: "Rafael at 26, web 2.0 era designer and team lead at Pink Parrot (2013)"
- `avatar.busts.4.alt`: "Rafael at 30, AR/VR era with floating holographic panels (2015-2018)"
- `avatar.busts.5.alt`: "Rafael at 36, modern animated era, frontend lead (2022-2023)"
- `avatar.busts.6.alt`: "Rafael at 40, QA + AI convergence era, pixel art space ambient (2026)"

From RESEARCH §Pattern 9 — theme isolation test approach:
```javascript
// tests/components/ScrollShell.theme-isolation-phase4.test.js
it('T1 — ch4 ParallaxLayers does not bleed visually into ch3 viewport during snap', () => {
  expect(wrapper.find('[data-chapter="4"] .parallax-layers').exists()).toBe(true)
  expect(wrapper.find('[data-chapter="3"] .parallax-layers').exists()).toBe(false)
})
it('T2 — ch5 ScrollRevealCard does not trigger reveal when ch3 is active', () => {
  // IO mocked to never fire if section not in viewport
})
```

From RESEARCH §Pattern 8 — parity T4 test extension:
```javascript
it('T4 — avatar.busts.N.alt NO contiene placeholder text (W5 sign-off guard)', () => {
  for (let i = 0; i <= 6; i++) {
    const altEs = es.avatar?.busts[String(i)]?.alt || ''
    const altEn = en.avatar?.busts[String(i)]?.alt || ''
    expect(altEs).not.toMatch(/^Placeholder/i)
    expect(altEs).not.toMatch(/PENDING/)
    expect(altEn).not.toMatch(/^Placeholder/i)
    expect(altEn).not.toMatch(/PENDING/)
  }
})
```

From Phase 2 02-MANUAL-CHECKLIST.md (analog estructural):
- 10 sections (visual coherence, FOUT/contrast/CLS/cross-browser/i18n e2e/regression visual, performance, etc.).
- §10 sign-off con `verdict:` field firmado por Rafael.

From VALIDATION.md §"Manual-Only Verifications" — 11 ítems para W5 checklist (avatar busts coherence, parallax depth ch4, marquee+starfield ch1 era-accuracy, cursor ch0 era-accuracy, FlashBanner ch2 era-accuracy, FloatingPanel glass ch4, ScrollRevealCard motion ch5, alt-text era-accurate ratification, ES vs EN layout no-overflow, contrast 4.5:1 ch2/4/5, privacy `public/references/` exclusion, iOS preventive mitigations).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh i18n alt-text era-accurate + extend parity test T4 guard</name>
  <files>
    src/i18n/es.json,
    src/i18n/en.json,
    tests/i18n/parity.test.js
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §8.2 (alt-text drafts ES + EN — los 7 valores verbatim)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 8: A11Y-06 alt-text bilingüe era-accurate" + §"tests/i18n/avatar-busts-parity.test.js (extension)"
    - .planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md §D4-11 (Claude deriva drafts; Rafael ratifica W5 manual checklist)
    - src/i18n/es.json (current avatar.busts.{0..6}.alt — placeholder Phase 2)
    - src/i18n/en.json (idem)
    - tests/i18n/parity.test.js (current T1-T3 — extender T4)
  </read_first>
  <action>
    **1.1 Actualizar `src/i18n/es.json`** sección `avatar.busts.{0..6}.alt` con los 7 drafts ES de UI-SPEC §8.2. Mantener la jerarquía existente (`avatar.busts[N].alt`) — sólo reemplazar los valores.

    **1.2 Actualizar `src/i18n/en.json`** análogo con los 7 drafts EN. Paridad enforced por T1-T3 existentes de parity.test.js.

    **1.3 Extender `tests/i18n/parity.test.js`** añadiendo T4 al final del `describe` block:

    ```javascript
    it('T4 — avatar.busts.N.alt NO contiene placeholder text (W5 sign-off guard)', () => {
      for (let i = 0; i <= 6; i++) {
        const altEs = es.avatar?.busts?.[String(i)]?.alt || ''
        const altEn = en.avatar?.busts?.[String(i)]?.alt || ''
        expect(altEs).not.toMatch(/^Placeholder/i)
        expect(altEs).not.toMatch(/PENDING/i)
        expect(altEn).not.toMatch(/^Placeholder/i)
        expect(altEn).not.toMatch(/PENDING/i)
        expect(altEs.length).toBeGreaterThan(20)  // sanity: descripciones era-accurate son >20 chars
        expect(altEn.length).toBeGreaterThan(20)
      }
    })
    ```

    Notas:
    - Si Rafael en Task 4 (manual checklist) refina algún alt-text, el cambio se aplica al JSON sin tocar el test.
    - El test bloquea contra reintroducción accidental de "PENDING" / "Placeholder" en cualquier commit futuro.

    **1.4 Smoke:**

    - `npm run test:run -- tests/i18n/parity.test.js` debe tener T1-T4 verdes.
    - `npm run dev` y visitar `?ch=0` ... `?ch=6` con DevTools accessibility tree para verificar que el alt-text se anuncia correctamente al lector de pantalla (Chrome → DevTools → Accessibility tab → buscar el `<img class="chN-avatar">`).
  </action>
  <verify>
    <automated>npm run test:run -- tests/i18n/parity.test.js</automated>
    Esperado: T1-T4 PASS (T4 nuevo — guard activo).
  </verify>
  <done>
    - 14 keys i18n refrescados (7 chapters × 2 idiomas).
    - parity.test.js T4 nuevo verde — guard contra placeholder.
    - Commits atómicos: `feat(04-06): refresh avatar alt-text era-accurate ES+EN`, `test(04-06): parity T4 guard contra placeholder text`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tests de integración cross-chapter (theme isolation + layout shift ES/EN)</name>
  <files>
    tests/integration/theme-bleed-phase4.test.js,
    tests/components/ScrollShell.theme-isolation-phase4.test.js,
    tests/i18n/layout-shift-phase4.test.js
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 9: Theme isolation con parallax + scroll-reveal activos" (lines 887-911)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Validation Architecture" Gaps Wave 5 (qué tests específicos espera)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §9.3 layout shift mitigation (I18N-05)
    - tests/components/ScrollShell.theme-isolation.test.js (Phase 2 analog — qué patrones usa para mount full shell con themes)
    - tests/i18n/parity.test.js (estructura describe + flatten + Set comparator)
    - src/components/ScrollShell.vue (post-W4: 6 sections con Chapter{N}Content components)
    - tests/i18n/test-helpers.js (createTestI18n helper)
  </read_first>
  <behavior>
    **`tests/integration/theme-bleed-phase4.test.js`** (architectural — readFileSync source-level):

    - Verifica que el SFC `ParallaxLayers.vue` tiene `position: absolute; inset: 0` con z-index <= 3 (contenido dentro del section).
    - Verifica que `Chapter4Content.vue` tiene `.ch4-layout { position: relative; overflow: hidden }` (containerización del parallax absolute).
    - Verifica que `Chapter5Content.vue` NO importa ParallaxLayers ni FloatingPanel (no bleed reverse).
    - Verifica que `ScrollShell.vue` no aplica `position: absolute` ni overflow hacking a las section elements (cada section es boundary CSS scope).

    T1: Source ParallaxLayers.vue contiene `position: absolute` Y `inset: 0` en `.parallax-layers` CSS.
    T2: Source Chapter4Content.vue contiene `position: relative` Y `overflow: hidden` en `.ch4-layout`.
    T3: Source Chapter5Content.vue NO contiene imports de `ParallaxLayers` ni `FloatingPanel`.
    T4: Source Chapter4Content.vue NO contiene imports de `ScrollRevealCard` (otra dirección de leak prevention).
    T5: chapter-themes.css contiene `[data-chapter="N"]` boundary para cada N en 0..6 (no global theme leak).

    **`tests/components/ScrollShell.theme-isolation-phase4.test.js`** (mount-based):

    - Mount ScrollShell con mocks de scrollState + prm + bgMorph.
    - Verifica que cada `<section data-chapter="N">` renderiza el Chapter{N}Content correcto via v-if branches.
    - Verifica que ch4 section contiene `.parallax-layers` pero ch3 section NO.
    - Verifica que ch5 section contiene `.scroll-reveal-card` pero ch4 section NO.

    T1: Full mount ScrollShell renderea 7 sections (ch0..ch6).
    T2: section ch4 (`wrapper.find('[data-chapter="4"]')`) contiene `.parallax-layers` element child.
    T3: section ch3 (`wrapper.find('[data-chapter="3"]')`) NO contiene `.parallax-layers`.
    T4: section ch5 (`wrapper.find('[data-chapter="5"]')`) contiene `.scroll-reveal-card` element (header reveal card al menos).
    T5: section ch6 (`wrapper.find('[data-chapter="6"]')`) renderea el placeholder genérico (no Chapter6Content — Phase 5).

    **`tests/i18n/layout-shift-phase4.test.js`** (snapshot length comparator):

    - Mount cada Chapter{N}Content (N=0,1,2,4,5) con locale='es' y luego con locale='en'.
    - Compara `wrapper.html().length` (proxy de text content length).
    - Verifica que la diferencia ES vs EN está en un rango razonable (ES ~20-30% más largo expected).

    T1: Chapter0Content.vue render ES vs EN — diferencia <50% absoluta (no overflow extreme).
    T2: Chapter1Content.vue idem.
    T3: Chapter2Content.vue idem.
    T4: Chapter4Content.vue idem.
    T5: Chapter5Content.vue idem.

    **Note:** Estos tests son sanity-checks; el manual checklist W5 §3 es donde Rafael valida visualmente cross-locale.
  </behavior>
  <action>
    **2.1 Crear `tests/integration/theme-bleed-phase4.test.js`** según behavior T1-T5. Patrón: readFileSync sobre los SFCs + regex/string matches.

    **2.2 Crear `tests/components/ScrollShell.theme-isolation-phase4.test.js`** según behavior T1-T5. Mount full ScrollShell con mocks:

    ```javascript
    function mountShell() {
      const i18n = createTestI18n({ locale: 'es' })
      return mount(ScrollShell, {
        global: {
          plugins: [i18n],
          provide: {
            scrollState: { activeChapter: ref(3), scrollProgress: ref(3/7), scrollToChapter: vi.fn() },
            prm: { prefersReduced: ref(false) },
            bgMorph: { layerA: { chapter: ref(3), opacity: ref(1) }, layerB: { chapter: ref(3), opacity: ref(0) } },
          },
        },
      })
    }
    ```

    Para T2-T4 (parallax/scroll-reveal containment): el mount renderiza los components solo si el v-if branch lo permite. Como ch3 está active y los demás aún se renderean en el v-for (DOM tiene los 7 sections), ch4 section debería tener `.parallax-layers` dentro pero ch3 NO.

    **2.3 Crear `tests/i18n/layout-shift-phase4.test.js`** según behavior. Helper para mountar cada Chapter{N}Content con i18n.locale = 'es' / 'en' y comparar `.html().length`.

    Mocks de projects:
    ```javascript
    vi.mock('@/data/projects', () => ({
      projects: [
        // 1-2 items per chapter mock que sí matchean chapterEra
        { id: 'ch2-test', chapterEra: 2, year: 2009, titleKey: 'projects.ch2-test.title', descKey: 'projects.ch2-test.desc', ... },
        { id: 'ch4-test', chapterEra: 4, ... },
        { id: 'ch5-test', chapterEra: 5, ... },
      ]
    }))
    ```

    Y añadir keys de mock a createTestI18n para que `t()` resuelva.

    **2.4 Smoke:**

    `npm run test:run -- tests/integration tests/components/ScrollShell.theme-isolation-phase4.test.js tests/i18n/layout-shift-phase4.test.js`.
  </action>
  <verify>
    <automated>npm run test:run -- tests/integration/theme-bleed-phase4.test.js tests/components/ScrollShell.theme-isolation-phase4.test.js tests/i18n/layout-shift-phase4.test.js</automated>
    Esperado: T1-T5 de cada archivo PASS.

    Adicional: `npm run test:run` full suite → ~315 PASS (≈+15 W5 tests).
  </verify>
  <done>
    - 3 test files de integración creados.
    - ~15 tests nuevos pasando.
    - Suite full verde.
    - Commits atómicos por test file: `test(04-06): integration theme-bleed Phase 4`, `test(04-06): ScrollShell theme-isolation Phase 4`, `test(04-06): i18n layout-shift Phase 4`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generar 04-MANUAL-CHECKLIST.md + 04-VERIFICATION.md draft</name>
  <files>
    .planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md,
    .planning/phases/04-chapters-0-2-4-5/04-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-VALIDATION.md (Manual-Only Verifications table — 11 items canónicos)
    - .planning/phases/02-theme-system-i18n/02-MANUAL-CHECKLIST.md (analog estructural — 10 sections + §10 sign-off)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Validation Architecture" + §"Open Questions" (Q3 mobile parallax+backdrop perf + Q5 iOS marquee)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §8 A11Y design contracts (focus rings + alt-text + tab order) + §9.4 iOS specific (Plan 07 deferred)
    - .planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md §D4-11 (Rafael ratifica alt-text W5)
  </read_first>
  <action>
    **3.1 Crear `.planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md`** con la siguiente estructura (clonar la organización de 02-MANUAL-CHECKLIST.md):

    ```markdown
    # Phase 4 — Manual Verification Checklist

    > Ejecutado por Rafael al cerrar Phase 4. Cada §N tiene un objetivo verifiable + criterios de PASS + espacio para observaciones. §10 firma sign-off final.

    ## §1 — Avatar busts 7-batch consistency visual (ART-01, D4-01)

    **Objetivo:** Validar que los 7 busts pixel-art representan al mismo personaje envejeciendo coherentemente.

    **Cómo verificar:**
    1. Abre los 7 PNGs lado a lado:
       ```powershell
       Start-Process public\assets\ch0-bust.png
       Start-Process public\assets\ch1-bust.png
       Start-Process public\assets\ch2-bust.png
       Start-Process public\assets\ch3-bust.png
       Start-Process public\assets\ch4-bust.png
       Start-Process public\assets\ch5-bust.png
       Start-Process public\assets\ch6-bust.png
       ```
    2. Para cada bust valida:
       - **Identity:** ¿Es reconociblemente Rafael (basado en fotos referencia 2011/2016/2022/2026)?
       - **Aging progression:** ch0 (niño ~10) → ch1 (adolescente ~17) → ch2 (~22) → ch3 (~26) → ch4 (~30) → ch5 (~36) → ch6 (~40). Cada bust debe verse mayor que el anterior.
       - **Era markers:** ropa/peinado/accesorios apropiados a cada era.
       - **Style consistency:** mismo nivel de detalle pixel-art (16-bit chunky), mismo skin tone base.

    **Criterios de PASS:** ≥6 de los 7 busts cumplen identity + aging + era + style. Si 1 falla, registrar en observaciones + opcional regenerar.

    **Observaciones Rafael:**
    ```
    [Espacio para Rafael]
    ```

    ## §2 — Parallax depth perceivable ch4 (ART-03, SC-1)

    **Objetivo:** Validar que las 4 capas de ch4 se mueven con velocidades distintas (depth effect).

    **Cómo verificar:**
    1. `npm run dev` → http://127.0.0.1:5173?ch=4
    2. Hacer scroll dentro de ch4 (mouse wheel suave) — observar las capas:
       - **stars-far** debería moverse muy lento.
       - **planet-mid** un poco más rápido.
       - **panels-fg** más rápido aún.
       - **ships-near** la más rápida.
    3. DevTools → Rendering panel → "Emulate CSS media feature prefers-reduced-motion: reduce" → re-scroll. Bajo PRM, todas las capas se mueven uniforme (sin depth differential).
    4. Mobile: redimensionar viewport a 375×667. Aún debe sentirse depth (puede ser menos pronunciado).

    **Criterios de PASS:** Depth perceptible default; uniform bajo PRM.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §3 — Marquee + starfield ch1 era-accuracy (ART-07, SC-1)

    **Objetivo:** Verificar feel GeoCities/Angelfire 90s.

    **Cómo verificar:**
    1. `?ch=1` → observar:
       - `<marquee>` scrolling left con texto bilingüe.
       - Starfield twinkling sutil.
       - Tabla legacy `border="1"` con caption.
       - Comic Neue font.
       - Magenta sobre navy (era-authentic tradeoff THM-05 documentado).
    2. Toggle PRM → marquee swap a `<span>` estático centrado; starfield estático.
    3. Comparar mentalmente con archive.org sites circa 1999 (https://web.archive.org/web/1999*/geocities.com).

    **Criterios de PASS:** Feel reconocible "página personal 90s" + PRM swap funcional.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §4 — Cursor parpadeante ch0 era-accuracy (ART-07, SC-1)

    **Objetivo:** CRT P1 phosphor look + cursor steps animation.

    **Cómo verificar:**
    1. `?ch=0` → observar:
       - Texto VT323 verde fósforo (#00ff41) sobre negro.
       - Cursor parpadeante en steps (binary opacity 1↔0).
       - 4 líneas terminal con staggered reveal (animation-delay 0/1.5/3/4.5s).
    2. PRM → cursor estático, líneas instant render.

    **Criterios de PASS:** Feel "MS-DOS prompt 1995" + PRM compliance.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §5 — FlashBanner ch2 era-accuracy (ART-02, SC-1)

    **Objetivo:** Skeumorphic Flash era 2009 vibe.

    **Cómo verificar:**
    1. `?ch=2` → observar:
       - FlashBanner orange→deep gradient + chrome dots (3 round pseudo-elements).
       - Background pixel-art `ch2-bg.png` crossfade aplicado.
       - Project cards con linear-gradient orange-purple + thicker border 2px.
       - Verdana/Trebuchet font para el banner.
    2. Comparar mentalmente con archive.org circa 2009 (sitios con Flash embedded).

    **Criterios de PASS:** Feel "Flash banner 2009" + bg pixel-art coherente con paleta §5.1.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §6 — FloatingPanel glass aesthetic ch4 (ART-03)

    **Objetivo:** AR/VR glass holographic + backdrop-filter blur.

    **Cómo verificar:**
    1. `?ch=4` → observar FloatingPanels con:
       - Background semi-transparente cyan-tinted.
       - Border cyan #00ffff 1px solid.
       - Backdrop-filter blur visible (Chrome moderno).
       - Box-shadow holographic glow + inset highlight.
       - Audiowide font para titles.
    2. Firefox: si no soporta backdrop-filter, fallback opaque background-color readable.
    3. Mobile <600px: blur reducido a 6px (Pitfall 3 perf budget).

    **Criterios de PASS:** Glass feel + fallback funcional cross-browser.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §7 — ScrollRevealCard motion ch5 (SC-1)

    **Objetivo:** Modern reveal animation + PRM compliance.

    **Cómo verificar:**
    1. `?ch=5` → scroll down dentro del chapter.
    2. Observar:
       - Header card fade+slide-in (300ms ease).
       - Cards staggered 100/200/300/400/500ms delay.
       - Modern flat aesthetic (no skeumorphic depth).
    3. PRM → todos los cards instant render desde mount (sin transition).

    **Criterios de PASS:** Reveal modern + PRM instant funcional.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §8 — Avatar busts alt text era-accurate ES/EN ratification (A11Y-06, D4-11)

    **Objetivo:** Rafael ratifica que los drafts del ejecutor capturan correctamente cada bust.

    **Cómo verificar:**
    1. Abre `src/i18n/es.json` y `src/i18n/en.json` → busca `avatar.busts`.
    2. Para cada chapter (0..6):
       - Lee el ES alt-text.
       - Mira el bust correspondiente en `public/assets/chN-bust.png`.
       - Valida que la descripción match al bust generado (edad, contexto era, expresión).
       - Lee el EN paridad — confirma fidelity.
    3. Si algún alt-text necesita refinar: edita directamente el JSON; el test `parity.test.js` T4 garantiza no romper paridad ni reintroducir placeholders.

    **Criterios de PASS:** Los 7 alt-text ES + EN reflejan fielmente los busts; cualquier refine que Rafael haga queda commiteado.

    **Observaciones:**
    ```
    [Rafael — registrar cambios si refina algún alt]
    ```

    ## §9 — ES vs EN layout no-overflow (I18N-05)

    **Objetivo:** ES ~20-30% más largo que EN no debe romper layouts.

    **Cómo verificar:**
    1. `npm run dev` → para cada chapter (0,1,2,3,4,5) en ambos idiomas (LangToggle top-right):
       - Desktop 1440×900: ¿Algún texto se desborda visible? ¿Layout shift al toggle?
       - Mobile 375×667 (DevTools responsive): ¿Tabla ch1, FlashBanner ch2, FloatingPanels ch4, ScrollRevealCards ch5 se ajustan?
    2. Especial atención a:
       - Terminal scroll lines ch0 (VT323 corre estrecho — ES más largo OK).
       - FloatingPanel ch4 con techStack array (ES "Programador" vs EN "Programmer" minor diff).
       - ScrollRevealCard ch5 cards (5 cards × 100ms staggered).

    **Criterios de PASS:** ningún text overflow visible + no layout shift CLS al toggle locale.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §10 — Contrast audit 4.5:1 ch2/ch4/ch5 (A11Y-04, THM-05)

    **Objetivo:** Verificar WCAG AA 4.5:1 en los chapters cuyo paleta se finalizó W2/W3/W4.

    **Cómo verificar:**
    1. Chrome DevTools → Lighthouse → Accessibility audit en `?ch=2`, `?ch=4`, `?ch=5`. O usar axe DevTools extension.
    2. Verificar que ningún issue de contrast aparece para text/foreground pairs.
    3. Para ch4 FloatingPanel: backdrop-filter blur puede reducir contrast aparente del texto sobre parallax — verificar 4.5:1 contra el `rgba(10, 15, 46, 0.4)` fallback (escenario sin blur).
    4. Ch1 magenta-on-navy ya tiene tradeoff documentado Phase 2 (3.2:1 + 18px+ compensation) — NO falla la audit si está marked as exempted.

    **Criterios de PASS:** No issues nuevos en ch2/4/5. Tradeoffs documentados quedan documentados.

    **Observaciones:**
    ```
    [Rafael — pegar axe results]
    ```

    ## §11 — Privacy public/references/ exclusion (T-PRIVACY-01, D4-02)

    **Objetivo:** Las 6 fotos NO están commiteadas al repo.

    **Cómo verificar:**
    ```powershell
    # 1. Listar archivos en public/references/ (existen en disco)
    Get-ChildItem public\references\

    # 2. Verificar que git las trata como ignored
    git check-ignore public/references/2011.jpg
    # Debería retornar: public/references/2011.jpg

    # 3. Confirmar NO están en git history
    git log --all --full-history -- public/references/2011.jpg
    # Debería retornar vacío
    ```

    **Criterios de PASS:** los 3 checks confirmados; las fotos viven en disk local pero git las ignora.

    **Observaciones:**
    ```
    [Rafael]
    ```

    ## §12 — iOS preventive mitigations documented (Plan 07 deferred)

    **Objetivo:** Documentar qué pasaría en iOS Safari sin hardware para test.

    **Riesgos heredados:**
    - `<marquee>` ch1: legacy support iOS 3.2+. Si Apple deshabilita marquee, fallback PRM-swap está pronto.
    - Parallax 4 layers ch4 + backdrop-filter: posible jank Snapdragon 7xx / A12 iPhone. Mobile blur ya reducido a 6px (Pitfall 3).
    - IntersectionObserver ch5: soportado iOS 12.2+.

    **Acción:** No-op para Rafael. Solo confirmar que el código tiene las mitigaciones preventivas. Marcar §12 PASS automáticamente.

    **Observaciones:**
    ```
    [iOS smoke test deferred Plan 07 Phase 1 — sin hardware]
    ```

    ## §13 — Sign-off

    | Field | Value |
    |-------|-------|
    | Reviewer | Rafael Matovelle |
    | Date | YYYY-MM-DD |
    | Verdict | [ ] PASS / [ ] PASS-with-observations / [ ] FAIL |
    | Observations summary | |

    Si verdict = PASS-with-observations: detallar items §N con polish opcional para Phase 5+ o post-merge.

    Si verdict = FAIL: detallar items §N que requieren iteración inmediata (re-execute W0/W1/W2/W3/W4 plan correspondiente).

    Si verdict = PASS: planner-actor actualiza STATE.md + ROADMAP.md marcando Phase 4 como cerrada.
    ```

    **3.2 Crear `.planning/phases/04-chapters-0-2-4-5/04-VERIFICATION.md`** (final report — sustituye al draft VALIDATION.md como verification artifact):

    ```markdown
    ---
    phase: 4
    slug: chapters-0-2-4-5
    status: pending-human-gate
    verdict: TBD
    suite_baseline: ~216 (post-Phase 3)
    suite_final: TBD (W5 ejecutará para registrar valor exacto)
    created: <date>
    ---

    # Phase 4 — Verification Report

    ## Programmatic Results

    | Plan | Wave | Status | New Tests | Suite Total |
    |------|------|--------|-----------|-------------|
    | 04-01 | W0 — Avatar batch 7 busts + gitignore + asset-naming | TBD | +4 | TBD |
    | 04-02 | W1 — ch0 + ch1 CSS-only + TerminalScroll + MarqueeBanner + StarfieldBg | TBD | +15 | TBD |
    | 04-03 | W2 — ch2 Flash + FlashBanner + bg | TBD | +10 | TBD |
    | 04-04 | W3 — ch4 AR/VR + ParallaxLayers + FloatingPanel + 4 layers | TBD | +20 | TBD |
    | 04-05 | W4 — ch5 Modern + ScrollRevealCard + hero | TBD | +15 | TBD |
    | 04-06 | W5 — alt-text refresh + integration tests + manual checklist | TBD | +15 | TBD |

    ## Test Suite Summary

    - Baseline pre-Phase 4: ~216 tests verde.
    - Phase 4 added: ~80 tests.
    - Final: TBD (W5 executor rellena tras `npm run test:run`).

    ## Build Status

    - `npm run build`: TBD (W5 verifica final).

    ## Manual Gate Status

    Ver `.planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md`.

    - §1-§12 status: TBD por Rafael.
    - §13 sign-off: TBD.

    ## Requirements Coverage

    | Req | Plan(s) | Programmatic | Manual gate |
    |-----|---------|-------------|-------------|
    | ART-02 (5 bgs ch2-6) | 04-03, 04-04, 04-05 | asset-naming.test.js + Chapter{2,4,5}Content.test.js | §5, §6 |
    | ART-03 (parallax ch4) | 04-04 | ParallaxLayers.test.js + Chapter4Content.test.js | §2 |
    | ART-04 (ch6-bust herencia Phase 5) | 04-01 | asset-naming.test.js T2 ch6 | §1 |
    | ART-07 (ch0/ch1 cero pixel art) | 04-02 | Chapter{0,1}Content.test.js T6 source guard | §3, §4 |
    | A11Y-06 (alt-text era-accurate) | 04-02, 04-06 | parity.test.js T4 guard | §8 |
    | CORE-09 (PRM respetado) | 04-02, 04-04, 04-05 | MarqueeBanner.test.js T3 + ParallaxLayers.test.js T5 + ScrollRevealCard.test.js T3 | §2, §3, §4, §7 |
    | THM-04 (no theme bleed) | 04-06 | ScrollShell.theme-isolation-phase4.test.js | (implicit en visual review) |
    | THM-05 (contrast tradeoffs) | 04-06 | (architectural) | §10 |
    | I18N-05 (ES vs EN layout) | 04-06 | layout-shift-phase4.test.js | §9 |
    | A11Y-04 (contrast 4.5:1 ch2/4/5) | 04-06 | (manual axe audit) | §10 |
    | CORE-04, CORE-05 (atomic commits) | TODOS | git log review | (implícito) |

    ## Threat Mitigation Status

    | Threat ID | Status |
    |-----------|--------|
    | T-04-01 Privacy public/references/ | mitigated (gitignore + check-ignore verified §11) |
    | T-04-02 Palette hex validation | accepted (autor-controlado) |
    | T-04-04/06/08/11 XSS-CONTENT i18n | mitigated (no v-html convention) |
    | T-04-07/10/13 Phishing external links | mitigated (rel=noopener noreferrer ProjectCard) |
    | T-04-09 DoS-perf mobile parallax+blur | mitigated (mobile blur 6px + will-change selective; iOS deferred) |

    ## Deferred Items

    - iOS smoke test confirmatorio (Plan 07 Phase 1 deferred — sin hardware).
    - Phase 6 deploy: `firebase.json` ignore glob para `public/references/` (D4-02 caveat).
    - Optional polish ch3 con proyectos reales (D4 Deferred Ideas — si Rafael completa CONTENT-CHECKLIST §2.2 en paralelo, fold-in opcional Phase 6 pre-deploy).

    ## Recommendation

    TBD por executor W5 tras Rafael firma §13.

    Si PASS → Phase 4 cerrada. Siguiente: `/gsd-execute-phase 5` (Phaser ch6).
    Si PASS-with-observations → registrar items en STATE.md Deferred, cerrar Phase 4.
    Si FAIL → ciclo gap-closure W{N} apropiado.
    ```
  </action>
  <verify>
    <automated>npm run test:run</automated>
    Full suite expected: ~315 tests PASS (W4 baseline ~300 + W5 +15).

    Adicional verificación manual:
    - Verificar que 04-MANUAL-CHECKLIST.md tiene 12 sections + §13 sign-off block.
    - Verificar que 04-VERIFICATION.md tiene placeholders TBD donde W5 executor rellena tras smoke.
  </verify>
  <done>
    - `04-MANUAL-CHECKLIST.md` creado con 12 sections + §13 sign-off.
    - `04-VERIFICATION.md` creado en estado `pending-human-gate` con tablas Requirements + Threats.
    - Commit: `docs(04-06): generate manual checklist + verification draft`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Rafael ejecuta 04-MANUAL-CHECKLIST.md y firma §13 sign-off</name>
  <what-built>
    Phase 4 motor programático completo: 6 plans ejecutados, ~80 tests nuevos, 11 SFCs nuevos, 13 pixel-art assets generados, i18n keys refreshed. El gate humano cierra la fase.
  </what-built>
  <how-to-verify>
    1. Rafael abre `.planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md`.

    2. Ejecuta secuencialmente §1-§12 con sus criterios PASS. Para cada uno:
       - Sigue los pasos "Cómo verificar".
       - Marca el resultado en "Observaciones".
       - Si necesita refinar algún alt-text (§8), edita directamente `src/i18n/es.json` / `en.json` — el test `parity.test.js` T4 garantiza no romper.

    3. Cuando complete §1-§12, llena §13:
       - **Verdict:** PASS / PASS-with-observations / FAIL.
       - **Date:** fecha actual.
       - **Observations summary:** 1-3 frases.

    4. Si Rafael necesita re-generar algún bust por drift de identity (§1 falla) → loop back a `/gsd-execute-phase 4` con `--gaps-only` y selectivamente regenerar el bust en cuestión.

    5. Si Rafael ve issue grave en parallax mobile (§2 jank) → loop back W3 plan 04-04 con flag de "fallback 3 layers" registered.

    **Time estimate:** 30-60 minutos para los 12 items + sign-off.
  </how-to-verify>
  <resume-signal>
    Rafael edita `04-MANUAL-CHECKLIST.md` §13 con:
    - `verdict: PASS` (o `PASS-with-observations` o `FAIL`)
    - `date: YYYY-MM-DD`
    - observations summary

    Tras commit del checklist firmado, ejecutor cierra Task 5.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Finalizar Phase 4 — actualizar VERIFICATION.md + STATE.md + ROADMAP.md</name>
  <files>
    .planning/phases/04-chapters-0-2-4-5/04-VERIFICATION.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md (verdict §13 firmado por Rafael)
    - .planning/phases/04-chapters-0-2-4-5/04-VERIFICATION.md (draft con TBD placeholders Task 3)
    - .planning/STATE.md (current state — actualizar Phase 4 status)
    - .planning/ROADMAP.md (Phase 4 entry — marcar checked)
  </read_first>
  <action>
    **5.1 Ejecutar `npm run test:run`** y registrar conteo total exacto.

    **5.2 Ejecutar `npm run build`** y registrar status verde/fail.

    **5.3 Actualizar `04-VERIFICATION.md`:**

    - Reemplazar `TBD` en suite_final + por-plan rows con valores reales.
    - Reemplazar `pending-human-gate` con `status: closed` (o `closed-with-observations` si verdict así).
    - Reemplazar `verdict: TBD` con el valor firmado por Rafael en §13.
    - Sección "Recommendation" → completar con la decisión final (continuar Phase 5 / FAIL-cycle / etc.).
    - Sección "Manual Gate Status" → completar §1-§13 con los valores que Rafael dejó en el checklist.

    **5.4 Actualizar `.planning/STATE.md`:**

    - Mover Phase 4 de "In Progress" a "Phase 4 status: 6/6 plans + manual gate FIRMADO" (clonando lo que se hizo para Phase 2 en STATE.md actual).
    - Actualizar progress counts.
    - Actualizar Current Position → Phase 4 cerrada, siguiente `/gsd-execute-phase 5` o `/gsd-discuss-phase 5`.
    - Registrar Phase 4 Verification Results table (clonar estructura Phase 2 con 6 plans).
    - Si verdict = PASS-with-observations: añadir items deferred a "Deferred Items".

    **5.5 Actualizar `.planning/ROADMAP.md`:**

    - Phase 4 entry: cambiar `[ ]` → `[x]` con date YYYY-MM-DD.
    - Status: `Closed (verdict: PASS)` o `Closed-with-deferred-observations (verdict: PASS-with-observations: ...)`.
    - Plans count: `6/6`.
    - Actualizar table at the end "Plans Complete" para Phase 4.

    **5.6 Commit final:**

    `docs(04-06): Phase 4 closed — verdict <PASS|PASS-with-observations>, STATE+ROADMAP+VERIFICATION updated`.

    **5.7 Resumen al usuario:**

    > "Phase 4 cerrada. {N} plans ejecutados, {M} tests pasando, build verde. Rafael verdict: {verdict}. Next: `/gsd-execute-phase 5` (Phaser ch6) cuando esté listo."
  </action>
  <verify>
    <automated>npm run test:run</automated>
    Esperado: full suite verde con conteo final registrado en 04-VERIFICATION.md.

    Manual:
    - `git log --oneline -20` muestra commits Phase 4 todos atómicos por task.
    - `cat .planning/STATE.md | head -50` muestra Phase 4 cerrada.
    - `cat .planning/ROADMAP.md | grep -A2 "Phase 4"` muestra `[x]`.
  </verify>
  <done>
    - 04-VERIFICATION.md con status closed + verdict + conteos reales + manual gate filled.
    - STATE.md actualizado: Phase 4 6/6 + manual gate firmado.
    - ROADMAP.md: Phase 4 `[x]` con date + verdict.
    - Phase 4 cerrada oficialmente.
    - Commit final consolidado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rafael edita JSON i18n directamente | autor-controlado, mismo trust que workflow inicial |
| Manual checklist verdict → cierra Phase | requiere firma explícita §13 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | XSS-i18n | refresh alt-text en i18n | mitigate | Vue auto-escape `t()`; cero v-html convention; parity T4 guard contra placeholder. |
| T-04-15 | Premature close | Skip §13 sign-off accidentally | mitigate | Task 5 explicit checks §13 verdict field exists antes de actualizar STATE/ROADMAP. |
</threat_model>

<verification>
## Wave-level checks W5

1. **i18n parity T1-T4 verde:** alt-text refreshed + guard contra placeholder activo.
2. **Integration tests verdes:** theme-bleed + ScrollShell isolation + layout-shift.
3. **Suite total verde:** ~315 PASS.
4. **Build verde:** `npm run build` completa.
5. **Manual checklist completo:** §1-§12 evaluados por Rafael + §13 verdict firmado.
6. **Phase 4 cerrada:** STATE.md + ROADMAP.md + VERIFICATION.md sincronizados con verdict.

## Out of scope W5

- Refactor del código Phase 4 — los plans 04-01..04-05 ya entregaron; W5 NO modifica components, solo i18n + tests + docs.
- Polish visual de busts si Rafael ve drift menor — opcional registrar como deferred, NO bloquear sign-off salvo issue grave.
- Phase 5 / 6 work — fuera del scope.
- Phase 6 firebase.json ignore glob para `public/references/` (D4-02 caveat) — Phase 6 lo aplicará.
</verification>

<success_criteria>
- 14 i18n keys avatar alt refreshed era-accurate ES + EN paridad.
- parity.test.js T4 nuevo guard activo.
- 3 test files integration: theme-bleed + ScrollShell isolation + layout-shift Phase 4.
- 04-MANUAL-CHECKLIST.md con 12 sections + §13 sign-off block.
- 04-VERIFICATION.md final report con conteos reales + manual gate + threat mitigation status + requirements coverage matrix.
- STATE.md + ROADMAP.md actualizados marcando Phase 4 cerrada.
- Rafael firmó §13 con verdict PASS / PASS-with-observations.
- Full suite verde + build verde.
</success_criteria>

<output>
After completion, create `.planning/phases/04-chapters-0-2-4-5/04-06-SUMMARY.md` con:
- alt-text refreshed (lista de los 14 keys).
- 3 test files integration creados con cobertura.
- 04-MANUAL-CHECKLIST.md sections + §13 verdict registrado.
- Phase 4 final stats: total plans, total tests added, total assets generated, total LOC added.
- Phase 5 readiness: qué hereda Phase 5 de Phase 4 (busts ch6, paletas confirmadas, patrones validados ParallaxLayers/IO).
- Lecciones para Phase 5 / 6 (qué workflows funcionaron, qué se podría optimizar).
</output>
