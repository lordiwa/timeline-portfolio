---
phase: 04-chapters-0-2-4-5
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-03
files_modified:
  - public/assets/ch4-bg-stars-far.png
  - public/assets/ch4-bg-planet-mid.png
  - public/assets/ch4-fg-panels.png
  - public/assets/ch4-fg-ships.png
  - src/components/Chapter4Content.vue
  - src/components/ParallaxLayers.vue
  - src/components/FloatingPanel.vue
  - src/components/ScrollShell.vue
  - src/styles/chapter-themes.css
  - src/data/chapters.js
  - src/data/projects.js
  - src/i18n/es.json
  - src/i18n/en.json
  - tests/components/Chapter4Content.test.js
  - tests/components/ParallaxLayers.test.js
  - tests/components/FloatingPanel.test.js
autonomous: false
requirements:
  - ART-02
  - ART-03
  - A11Y-06
  - CORE-09
  - I18N-05
user_setup:
  - service: pixelforge-mcp
    why: "Generación de 4 layers ch4 (3 backgrounds opacos + 1 sprite alpha para ships)"
  - service: adobe-mcp
    why: "image_adjust_hsl/temperature para harmonizar paleta cross-layer (desaturar capas lejanas, calentar cercanas); image_remove_background fallback si bg removal de pixelforge no entrega alpha clean en ships y panels"
must_haves:
  truths:
    - "Al hacer scroll a ch4, el visitante ve 4 capas parallax (stars-far → planet-mid → panels-fg → ships-near) con depth perceptible"
    - "Las capas se mueven con factor escalonado [0.2, 0.5, 0.8, 1.0] en respuesta a scrollProgress; stars-far es la más lenta"
    - "Bajo prefers-reduced-motion, todas las capas se mueven con factor 1.0 uniforme (sin diferencial = scroll natural sin depth effect)"
    - "FloatingPanel renderiza glass holográfico cyan con backdrop-filter blur (+ fallback opaco con @supports)"
    - "Mobile reduce blur a 6px (Pitfall 3) y soporta 3 layers cómodamente"
    - "Los ProjectCards ch4 se envuelven en FloatingPanel (no ProjectCard variant directo — D4-04)"
  artifacts:
    - path: "public/assets/ch4-bg-stars-far.png"
      provides: "Layer más lejana (factor 0.2) — deep stars + dark nebula, forge_background opaco"
    - path: "public/assets/ch4-bg-planet-mid.png"
      provides: "Layer mid (factor 0.5) — planeta/nebula silhouette, forge_background opaco"
    - path: "public/assets/ch4-fg-panels.png"
      provides: "Layer foreground panels (factor 0.8) — paneles holográficos translúcidos, forge_sprite + bg_remove (Pitfall 2)"
    - path: "public/assets/ch4-fg-ships.png"
      provides: "Layer ships near (factor 1.0) — 2 naves estáticas con alpha, forge_sprite + bg_remove"
    - path: "src/components/ParallaxLayers.vue"
      provides: "Wrapper 4 capas absolute con transform translateY scroll-driven; inject scrollState + prm; localProgress clamp((scrollProgress*7)-4, 0, 1) (Pitfall 6)"
      min_lines: 60
    - path: "src/components/FloatingPanel.vue"
      provides: "Glass holográfico card con backdrop-filter blur + @supports fallback + slot wrapper"
      min_lines: 30
    - path: "src/components/Chapter4Content.vue"
      provides: "Wrapper layout ch4 + ParallaxLayers (z-index 0) + .ch4-content (z-index 4) con FloatingPanels"
      min_lines: 70
    - path: "src/styles/chapter-themes.css"
      provides: "[data-chapter='4'] tokens finales + .floating-panel block + .parallax-layer styles"
    - path: "tests/components/ParallaxLayers.test.js"
      provides: "4 layers + translateY factor math + PRM uniform factor + aria-hidden + will-change marker"
    - path: "tests/components/FloatingPanel.test.js"
      provides: "Slot rendering + @supports backdrop-filter source-level marker + fallback rgba"
    - path: "tests/components/Chapter4Content.test.js"
      provides: "ParallaxLayers + FloatingPanel embeds + projects filter ch4 + locale switch"
  key_links:
    - from: "src/components/ParallaxLayers.vue"
      to: "App.vue provide('scrollState'), provide('prm')"
      via: "inject('scrollState') → scrollProgress reactive ref; inject('prm') → prefersReduced"
      pattern: "inject\\('scrollState'\\)|inject\\('prm'\\)"
    - from: "src/components/ParallaxLayers.vue"
      to: "public/assets/ch4-bg-stars-far.png + 3 more"
      via: "<img :src=layer.src loading='lazy'> (Pitfall 7 — NO background-image CSS)"
      pattern: "<img[^>]*src=\\[?layer\\.src"
    - from: "src/components/Chapter4Content.vue"
      to: "src/components/ParallaxLayers.vue, src/components/FloatingPanel.vue"
      via: "import + template embed; ParallaxLayers z-index:0, FloatingPanel z-index:4"
      pattern: "<ParallaxLayers|<FloatingPanel"
---

<objective>
W3 entrega **ch4 AR/VR 2015-18** — el chapter más complejo de Phase 4: 4 capas parallax depth-staggered + paneles glass holográficos + ships estáticos + naves espaciales sin character animation (PHA-08 carry-forward).

Purpose: Ch4 es la "señal" visual más fuerte del salto era — AR/VR justifica el extra effort en multi-layer + backdrop-filter. Si W3 sale bien, valida que el patrón "transform-driven parallax sin GSAP" es viable end-to-end en este stack (lección clave para Phase 5 escena Phaser que reutiliza patrones similares con WebGL).

Output: 4 PNGs generados con palette ch4 §5.3 (3 backgrounds opacos + 1 sprite alpha para ships) + ParallaxLayers self-contained dentro del section + FloatingPanel reusable + Chapter4Content wrapper + project items AR/VR + extension chapter-themes.css.

**MVP vertical slice (W3):** Tras esta wave el visitante hace scroll a ch4 y ve: cielo estrellado moviéndose lento + planeta a mitad de velocidad + paneles holográficos casi al ritmo + 2 naves al frente + FloatingPanels cyan-glass con info de proyectos AR/VR/Metrodigi. Bajo PRM, todas las capas se mueven uniforme sin depth effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md
@.planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md
@.planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md
@.planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md
@CLAUDE.md
@src/components/Chapter3Content.vue
@src/components/BackgroundLayers.vue
@src/components/StickyAvatar.vue
@src/components/ProjectCard.vue
@src/components/ScrollShell.vue
@src/composables/useScrollState.js
@src/composables/usePRM.js
@src/styles/chapter-themes.css
@src/data/chapters.js
@src/data/projects.js
@tests/components/BackgroundLayers.test.js
@tests/components/Chapter3Content.test.js
@tests/components/ProjectCard.test.js
@.claude/agents/artist-creator.md
@.claude/agents/artist-editor.md
@.claude/skills/crear-arte-pixelforge.md
@.claude/skills/editar-arte-adobe.md

<interfaces>
<!-- Contratos clave W3 -->

From src/composables/useScrollState.js — `scrollProgress` ref readonly 0..1 global sobre los 7 chapters:
```javascript
import { useRafFn } from '@vueuse/core'
// scrollProgress es ref(0..1) reactive provisto via App.vue provide('scrollState', { activeChapter, scrollProgress, scrollToChapter })
```

From src/composables/usePRM.js:
```javascript
const { prefersReduced } = inject('prm')  // ref(boolean), reactive a media query change
```

From RESEARCH §Pitfall 6 — localProgress crítico:
```javascript
// scrollProgress global 0..1 sobre 7 chapters; ch4 ocupa rango [4/7, 5/7] aprox.
// Mapear a localProgress 0..1 dentro del chapter 4:
const localProgress = computed(() => {
  return Math.min(1, Math.max(0, (scrollProgress.value * 7) - 4))
})
// Usar localProgress en el cálculo translateY (NO scrollProgress global).
```

From RESEARCH §Pattern 1 + Pitfall 7 — usar `<img loading="lazy">` NO `background-image` CSS:
```vue
<img v-for="layer in layers" :src="layer.src" loading="lazy" alt="" aria-hidden="true"
     class="parallax-layer" :class="`parallax-layer--${layer.name}`"
     :style="{ transform: getTransform(layer.factor) }" />
```

From RESEARCH §Pattern 7 — FloatingPanel CSS estructura (vive en chapter-themes.css @layer components):
```css
[data-chapter="4"] .floating-panel {
  position: relative;
  z-index: 4;
  padding: var(--sp-lg);
  background-color: rgba(10, 15, 46, 0.4);  /* fallback opaco */
  border: 1px solid var(--c-accent);
  border-radius: 8px;
  box-shadow: 0 0 20px rgba(0,255,255,0.3), inset 0 1px 0 rgba(255,255,255,0.1);
  color: var(--c-fg);
}
@supports ((backdrop-filter: blur(10px)) or (-webkit-backdrop-filter: blur(10px))) {
  [data-chapter="4"] .floating-panel {
    background-color: rgba(10, 15, 46, 0.15);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }
}
@media (max-width: 599px) {
  @supports ((backdrop-filter: blur(10px)) or (-webkit-backdrop-filter: blur(10px))) {
    [data-chapter="4"] .floating-panel {
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
    }
  }
}
```

From RESEARCH §Pitfall 2 — panels-fg usa forge_sprite + bg_remove, NO forge_background:
Los paneles holográficos diseñados con **transparencias ENTRE paneles** requieren alpha
para que la capa planet-mid se vea por las zonas vacías. `forge_background` cubre todo
opaco → planet queda oculto. Usar `forge_sprite` con prompt explícito sobre paneles
posicionados con transparencia entre ellos + `process_sprite(bg_remove)`.

Layer table (UI-SPEC §6.6):
| Layer | Asset | Factor | Tool pixelforge |
|-------|-------|--------|------------------|
| stars-far | ch4-bg-stars-far.png | 0.2 | forge_background |
| planet-mid | ch4-bg-planet-mid.png | 0.5 | forge_background |
| panels-fg | ch4-fg-panels.png | 0.8 | forge_sprite + bg_remove (Pitfall 2) |
| ships-near | ch4-fg-ships.png | 1.0 | forge_sprite + bg_remove |

From PATTERNS.md §5 — ParallaxLayers analog BackgroundLayers (HUD invariante inject + aria-hidden + absolute inset 0 + pointer-events none).

i18n keys nuevas Phase 4 W3:
```
chapters.4.flavor                       "Era AR/VR 2015-18. Paneles holográficos, profundidad espacial, AR aplicado."
chapters.4.parallaxAria                 "Escena AR/VR con paneles flotantes" (opcional)
projects.ch4-arvr-own.title             "ARVR Studio" (o nombre que Rafael especifique)
projects.ch4-arvr-own.desc              "..."
projects.ch4-metrodigi.title            "Metrodigi"
projects.ch4-metrodigi.desc             "..."
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-input" gate="blocking">
  <name>Task 1: Gate W3 — §2.3 proyectos ch4 AR/VR + §5.3 paleta AR/VR</name>
  <what-needed>
    Dos inputs del `CONTENT-CHECKLIST.md` para arrancar W3:

    **1. §5.3 paleta ch4 AR/VR (5-8 hex)** — la `palette:` argument para los 4 pixelforge calls. Stub Phase 2: `['#0a0f2e', '#b0d0ff', '#00ffff', '#2050a0', '#142050']` (deep space blue + cyan accent). Si Rafael refina (p.ej. añadir purple para Metrodigi era), registrar.

    **2. §2.3 proyectos ch4 (1-3 items)** — esperado 1-2 items:
    ```
    Proyecto 1 — ARVR (empresa propia AR/VR):
    - title (ES/EN)
    - desc 1-2 frases (ES/EN)
    - role: (e.g., "Founder / Tech Lead")
    - techStack: (e.g., ["Unity", "ARKit", "Vuforia"])
    - link: URL o null

    Proyecto 2 — Metrodigi:
    - title, desc, role, techStack, link
    ```

    Si vacío: fallback `'usa stubs'` con PENDING desc (mismo patrón W2).
  </what-needed>
  <how-to-verify>
    1. Lee CONTENT-CHECKLIST §2.3 + §5.3. Si presentes → registra para Tasks 2-3.
    2. Si vacíos → pregunta a Rafael:
       > "W3 (ch4 AR/VR multi-layer) necesita:
       > - §5.3 paleta (5-8 hex; stub Phase 2 disponible)
       > - §2.3 proyectos AR/VR (1-3 items con title/desc ES+EN + role + techStack)
       > Responde 'usa stubs' o entrega los datos."
  </how-to-verify>
  <resume-signal>
    Rafael responde con palette + lista de proyectos (o 'usa stubs'). Continuar Task 2.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generar 4 layers pixelforge ch4 + chapters.js palette + chapter-themes.css ch4 tokens</name>
  <files>
    public/assets/ch4-bg-stars-far.png,
    public/assets/ch4-bg-planet-mid.png,
    public/assets/ch4-fg-panels.png,
    public/assets/ch4-fg-ships.png,
    src/data/chapters.js,
    src/styles/chapter-themes.css
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 5" lines 644-697 (4 prompts verbatim por layer)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pitfall 2" (panels-fg requiere forge_sprite NO forge_background — transparencia entre paneles)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §6.6 (layer table) + §5 (color tokens ch4 — 60/30/10 deep space + cyan accent)
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §"src/styles/chapter-themes.css (MODIFICADO)" sección target 1 (tokens stub a finalizar)
    - .claude/skills/crear-arte-pixelforge.md + .claude/skills/editar-arte-adobe.md
    - CLAUDE.md §6.1 (capas intermedias siluetas planas — críticamente importante para panels-fg)
    - CLAUDE.md §6.2 (bg presets nombrados para forge_sprite)
    - CLAUDE.md §6.3 (forge_background opacos, forge_sprite alpha)
    - src/data/chapters.js (líneas ~54-63 ch4 stub palette:[])
    - src/styles/chapter-themes.css (búsqueda `[data-chapter="4"]` block — Phase 2 stub)
  </read_first>
  <action>
    **Delegar a `artist-creator` agent.** Loop secuencial sobre las 4 capas (orden: far → near para que el output de stars-far pueda servir como reference de paleta para los siguientes via Adobe HSL harmonization).

    **2.1 Cargar tools deferred pixelforge** (si no ya cargados):
    ```
    ToolSearch(query="select:mcp__pixelforge__forge_background,mcp__pixelforge__forge_sprite,mcp__pixelforge__process_sprite,mcp__pixelforge__optimize_sprite", max_results=10)
    ```

    **2.2 Generar capa stars-far (factor 0.2, layer más lejana):**

    `mcp__pixelforge__forge_background` con:
    - `description`: `"pixel art deep space, distant stars, dark nebula background, solid filled silhouettes, NO outlines, deep blue-black, scattered tiny bright pixels representing far stars, full-frame opaque, 480x270 resolution, 16:9 aspect"`.
    - `palette`: paleta §5.3 (recibida Task 1 o stub `['#0a0f2e', '#b0d0ff', '#00ffff', '#2050a0', '#142050']`).
    - `aspect`: `"16:9"`.
    - `outputPath`: `"public/assets/ch4-bg-stars-far.png"`.

    Luego `optimize_sprite(size: "480x270")`.

    **2.3 Generar capa planet-mid (factor 0.5):**

    `mcp__pixelforge__forge_background` con:
    - `description`: `"pixel art planet on horizon, large planet/moon silhouette mid-distance, simple two-tone shading, NO surface detail, solid filled curved silhouette, deep space background visible around planet edges, 480x270 resolution"`.
    - Mismo `palette`, `aspect`, output `"public/assets/ch4-bg-planet-mid.png"`.

    Luego `optimize_sprite`.

    **2.4 Generar capa panels-fg (factor 0.8) — CRÍTICO Pitfall 2:**

    Usar `mcp__pixelforge__forge_sprite` (NO forge_background) para que tenga alpha entre paneles:
    - `description`: `"2-3 holographic floating panels positioned around the frame (top-left, mid-right, bottom-center), transparent empty space between panels, solid filled glass panels with thin glow borders, AR/VR HUD aesthetic, NO internal grid pattern, NO detailed UI elements inside panels, NO connectors between panels"`.
    - `background`: `"night"` (preset nombrado per CLAUDE.md §6.2 — para que process_sprite bg_remove limpie el "night" bg → alpha donde NO hay panel).
    - `palette`: §5.3.
    - `references`: `["public/assets/ch4-bg-stars-far.png"]` (anchor de paleta cross-layer).
    - `outputPath`: `"public/assets/ch4-fg-panels.png"`.

    Luego `process_sprite(inputPath: "public/assets/ch4-fg-panels.png", operation: "bg_remove")` para limpiar el "night" bg → alpha.

    Luego `optimize_sprite(size: "480x270")`.

    Adobe MCP fallback si bg_remove deja artefactos (`adobe_mandatory_init` → `image_remove_background`).

    **2.5 Generar capa ships-near (factor 1.0):**

    `forge_sprite`:
    - `description`: `"pixel art 2 retro spaceships flying through space, side view, solid filled silhouettes with simple two-tone shading, NO detailed cockpit windows, NO internal lineart, 1 ship positioned upper-left and 1 ship lower-right diagonal direction, transparent background between ships and around them"`.
    - `background`: `"night"`.
    - `palette`: §5.3.
    - `references`: `["public/assets/ch4-bg-stars-far.png"]`.
    - `outputPath`: `"public/assets/ch4-fg-ships.png"`.

    Luego `process_sprite(bg_remove)` + `optimize_sprite(480x270)`.

    **2.6 Adobe MCP HSL harmonization (opcional pero recommended):**

    Para reforzar la depth perception:
    - `image_adjust_hsl` sobre `ch4-bg-stars-far.png` con `saturation: -15` (más desaturada — sensación de lejanía).
    - `image_adjust_color_temperature` sobre `ch4-bg-stars-far.png` con `temperature: -10` (más fría — atmospheric perspective).
    - `ch4-fg-ships.png`: dejar como está (cercana = saturación normal).

    Saltar este step si las paletas §5.3 ya producen output coherente al primer call.

    **2.7 Actualizar `src/data/chapters.js`:**

    Bloque `id: 4` (~lines 54-63): reemplazar `palette: []` con la paleta §5.3 final.

    **2.8 Actualizar `src/styles/chapter-themes.css`:**

    Buscar `[data-chapter="4"]` block (Phase 2 stub). Si la paleta §5.3 difiere de los tokens stub (`--c-bg: #0a0f2e`, `--c-fg: #b0d0ff`, etc.), actualizar.

    **NO añadir `--bg-image`** — ch4 es self-contained parallax (D4-07); BackgroundLayers no se usa para ch4. El background global cuando se está en ch4 puede mantener el `--c-bg` del block (color sólido detrás de las parallax layers en caso de gap de carga lazy).

    **2.9 Smoke test naming:**

    `npm run test:run -- tests/assets/asset-naming.test.js` debe pasar — los 4 nuevos PNGs matchean el regex específico (`ch4-bg-stars-far`, `ch4-bg-planet-mid`, `ch4-fg-panels`, `ch4-fg-ships` están en el enumerated regex Task 2 de 04-01).

    **2.10 Smoke `npm run dev ?ch=4`:**

    Antes de Task 3, los 4 PNGs solo existen pero no se renderizan (ParallaxLayers no existe aún). Verificar que los archivos existen y tienen tamaño razonable (30-80 KB cada uno).
  </action>
  <verify>
    <automated>npm run test:run -- tests/assets/asset-naming.test.js tests/data/chapters.test.js</automated>
    Esperado:
    - asset-naming T1 verde (4 nuevos PNGs matchean enumerated regex).
    - chapters.test.js: chapters[4].palette.length >= 5.

    Manual smoke: `Get-ChildItem public/assets/ch4-*.png` → 4 files; cada uno 480×270; ch4-fg-panels.png y ch4-fg-ships.png tienen alpha channel (PNG-32, no PNG-24).
  </verify>
  <done>
    - 4 PNGs ch4 generados con paleta §5.3.
    - chapters[4].palette poblada.
    - chapter-themes.css tokens ch4 finalizados (si difirieron del stub).
    - Adobe HSL aplicado si la depth perception lo requería.
    - Commits: `art(04-04): generate ch4-bg-stars-far`, `...planet-mid`, `...fg-panels (Pitfall 2 forge_sprite+bg_remove)`, `...fg-ships`, `feat(04-04): update ch4 palette + tokens`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Crear ParallaxLayers + FloatingPanel components + tests (TDD heavy)</name>
  <files>
    src/components/ParallaxLayers.vue,
    src/components/FloatingPanel.vue,
    src/styles/chapter-themes.css,
    tests/components/ParallaxLayers.test.js,
    tests/components/FloatingPanel.test.js
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 1: Multi-Layer Parallax" lines 281-352 (source completo)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pitfall 6: scrollProgress global vs local" (localProgress = clamp((scrollProgress*7)-4, 0, 1))
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pitfall 7: loading=lazy NO funciona en background-image" → usar <img> tag
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 7: FloatingPanel" lines 798-845 (CSS @supports backdrop-filter + fallback + mobile blur reduction)
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §5 (ParallaxLayers analog BackgroundLayers) + §6 (FloatingPanel analog ProjectCard structural) + §10 (test pattern BackgroundLayers.test.js)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §6.5 FloatingPanel DOM + §6.6 ParallaxLayers DOM + §12 motion contract Phase 4
    - src/components/BackgroundLayers.vue (analog estructural — inject + aria-hidden + absolute inset 0)
    - src/composables/useScrollState.js (export shape de scrollProgress)
    - src/composables/usePRM.js (export shape de prefersReduced)
    - tests/components/BackgroundLayers.test.js (analog mount helper con provide mutable refs + readFileSync CSS)
    - tests/components/ProjectCard.test.js (analog slot mount + readFileSync chapter-themes.css markers)
    - tests/setup.js (mocks globales — verificar si IntersectionObserver y ResizeObserver están mockeados; @vueuse/core useIntersectionObserver es el que importa ScrollRevealCard W4, no este task)
    - CLAUDE.md §6.1 (siluetas planas — recordatorio que el output ch4 ya está validado)
  </read_first>
  <behavior>
    **ParallaxLayers.vue:**

    Script:
    - `import { inject, computed } from 'vue'`.
    - `const { scrollProgress } = inject('scrollState')`.
    - `const { prefersReduced } = inject('prm')`.
    - Const `layers` array:
      ```javascript
      const layers = [
        { src: '/assets/ch4-bg-stars-far.png',  factor: 0.2, name: 'stars'  },
        { src: '/assets/ch4-bg-planet-mid.png', factor: 0.5, name: 'planet' },
        { src: '/assets/ch4-fg-panels.png',     factor: 0.8, name: 'panels' },
        { src: '/assets/ch4-fg-ships.png',      factor: 1.0, name: 'ships'  },
      ]
      ```
    - `const localProgress = computed(() => Math.min(1, Math.max(0, (scrollProgress.value * 7) - 4)))` (Pitfall 6).
    - `function getTransform(factor) { if (prefersReduced.value) return 'translateY(' + ((localProgress.value - 0.5) * 100) + 'vh)'; return 'translateY(' + ((localProgress.value - 0.5) * factor * 100) + 'vh)' }`.

    Template:
    ```vue
    <div class="parallax-layers" aria-hidden="true">
      <img v-for="layer in layers" :key="layer.name"
           :src="layer.src" loading="lazy" alt=""
           :class="['parallax-layer', `parallax-layer--${layer.name}`]"
           :style="{ transform: getTransform(layer.factor) }" />
    </div>
    ```

    Style scoped:
    - `.parallax-layers { position: absolute; inset: 0; overflow: hidden; pointer-events: none; z-index: 0; }`
    - `.parallax-layer { position: absolute; inset: 0; width: 100%; height: 120%; object-fit: cover; object-position: center; image-rendering: pixelated; will-change: transform; }` (height:120% para overflow vertical sin reveal de edge durante translate).
    - `.parallax-layer--stars  { z-index: 0; }`, `.parallax-layer--planet { z-index: 1; }`, `.parallax-layer--panels { z-index: 2; }`, `.parallax-layer--ships  { z-index: 3; }`.

    **FloatingPanel.vue:**

    Script: minimal `defineProps({ title: { type: String, default: '' } })`.

    Template:
    ```vue
    <article class="floating-panel">
      <h3 v-if="title" class="floating-panel__title">{{ title }}</h3>
      <slot />
    </article>
    ```

    Style scoped: VACÍO o solo class container. El styling vive en chapter-themes.css `@layer components` para que aplique tokens ch4 via cascade.

    **chapter-themes.css `@layer components` extensions:**

    Añadir AL FINAL del `@layer components` block:
    ```css
    [data-chapter="4"] .floating-panel {
      position: relative;
      z-index: 4;
      padding: var(--sp-lg);
      background-color: rgba(10, 15, 46, 0.4);
      border: 1px solid var(--c-accent);
      border-radius: 8px;
      box-shadow: 0 0 20px rgba(0, 255, 255, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.1);
      color: var(--c-fg);
      margin-bottom: var(--sp-md);
    }
    @supports ((backdrop-filter: blur(10px)) or (-webkit-backdrop-filter: blur(10px))) {
      [data-chapter="4"] .floating-panel {
        background-color: rgba(10, 15, 46, 0.15);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
      }
    }
    @media (max-width: 599px) {
      @supports ((backdrop-filter: blur(10px)) or (-webkit-backdrop-filter: blur(10px))) {
        [data-chapter="4"] .floating-panel {
          backdrop-filter: blur(6px);
          -webkit-backdrop-filter: blur(6px);
        }
      }
    }
    [data-chapter="4"] .floating-panel__title {
      font-family: 'Audiowide', 'Eurostile', sans-serif;
      color: var(--c-accent);
      margin: 0 0 var(--sp-sm) 0;
      font-size: 1.25rem;
    }
    ```

    **Tests behavior:**

    `ParallaxLayers.test.js` (analog BackgroundLayers.test.js mount helper):
    - Helper `mountParallax({ scrollProgress = 0, prefersReduced = false } = {})` con `provide: { scrollState: { scrollProgress: ref(scrollProgress) }, prm: { prefersReduced: computed(() => prefersReduced) } }`.
    - T1 — renders 4 .parallax-layer img elements con clases stars/planet/panels/ships.
    - T2 — con `scrollProgress=4/7 ≈ 0.571` (centro de ch4): localProgress ≈ 0; (0 - 0.5) * factor * 100 → stars=-10vh, planet=-25vh, panels=-40vh, ships=-50vh. Verificar via attribute style.transform contiene esos vh values. **Más simple test:** con `scrollProgress=0.5` (medio del shell pero fuera de ch4 range): `localProgress = clamp(0.5*7 - 4, 0, 1) = clamp(-0.5, 0, 1) = 0`. Entonces (0-0.5)*factor*100 = -10, -25, -40, -50vh respectivamente. Verificar.
    - T3 — con `scrollProgress=0` (top): localProgress=0, idem T2 valores.
    - T4 — con `scrollProgress=5/7 ≈ 0.714`: localProgress = clamp(5-4, 0, 1) = 1. (1-0.5)*factor*100 → +10, +25, +40, +50vh. Verificar.
    - T5 — PRM: con `prefersReduced=true`, **todos** los layers usan factor uniforme. Verificar que `.parallax-layer--stars` y `.parallax-layer--ships` retornan **mismo** translateY value. Para `scrollProgress=0.5`, localProgress=0, factor uniforme 1.0 → all layers translateY(-50vh).
    - T6 — `<div class="parallax-layers">` tiene `aria-hidden="true"`.
    - T7 — Source SFC contiene `will-change: transform` marker (readFileSync verifies compositor hint).
    - T8 — Source contiene `loading="lazy"` en el `<img>` tag (Pitfall 7 verified).
    - T9 — Source contiene `inject('scrollState')` y `inject('prm')` imports.

    `FloatingPanel.test.js` (analog ProjectCard.test.js slot pattern):
    - Helper `mountPanel({ title = '', slot = '<p>Content</p>' } = {})`.
    - T1 — slot renderiza: `wrapper.html()` contiene `<p>Content</p>`.
    - T2 — title prop rendea h3: `mountPanel({ title: 'My Panel' })` → `wrapper.find('h3.floating-panel__title').text() === 'My Panel'`.
    - T3 — sin title: `mountPanel()` → `<h3>` NO existe (v-if).
    - T4 — chapter-themes.css source contiene `[data-chapter="4"] .floating-panel` block.
    - T5 — source contiene `@supports ((backdrop-filter: blur(10px))` (feature detection).
    - T6 — source contiene fallback `background-color: rgba(10, 15, 46, 0.4)` OUTSIDE el @supports block (Pitfall 3 compliance).
    - T7 — source contiene `@media (max-width: 599px)` con blur(6px) (mobile perf budget).
  </behavior>
  <action>
    **3.1 Crear `src/components/ParallaxLayers.vue`** copiando RESEARCH §Pattern 1 source y aplicando los siguientes ajustes versus el RESEARCH:

    - **Pitfall 6 aplicado:** computed `localProgress` envuelve `scrollProgress` con clamp.
    - **Pitfall 7 aplicado:** template usa `<img>` con `loading="lazy"` en lugar de `background-image: url()`. CSS adicional `.parallax-layer { object-fit: cover; height: 120% }` para overflow vertical.

    **3.2 Crear `src/components/FloatingPanel.vue`** según behavior — SFC mínimo, styling fuera del scoped.

    **3.3 Extender `src/styles/chapter-themes.css @layer components`** con bloque `[data-chapter="4"] .floating-panel` completo (con @supports + @media mobile blur reduction).

    **3.4 Crear `tests/components/ParallaxLayers.test.js`** con T1-T9 según behavior. Helper mountParallax exact.

    **3.5 Crear `tests/components/FloatingPanel.test.js`** con T1-T7 según behavior.

    **Orden TDD:** Tests rojos primero (RED), después implementaciones (GREEN), refactor solo si necesario.
  </action>
  <verify>
    <automated>npm run test:run -- tests/components/ParallaxLayers.test.js tests/components/FloatingPanel.test.js</automated>
    Esperado:
    - ParallaxLayers T1-T9 PASS (especialmente T2-T5 math verifications).
    - FloatingPanel T1-T7 PASS (especialmente T5-T7 CSS @supports markers).
  </verify>
  <done>
    - ParallaxLayers.vue + FloatingPanel.vue creados según specs Pitfall 6 + Pitfall 7 + Pitfall 3.
    - chapter-themes.css extended con `[data-chapter="4"] .floating-panel` block completo.
    - 2 test files con cobertura math + PRM + CSS markers + accessibility.
    - Commits: `feat(04-04): ParallaxLayers (Pitfall 6/7 applied)`, `feat(04-04): FloatingPanel + @supports backdrop-filter`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Crear Chapter4Content wrapper + projects + i18n + ScrollShell wire + tests</name>
  <files>
    src/components/Chapter4Content.vue,
    src/components/ScrollShell.vue,
    src/data/projects.js,
    src/i18n/es.json,
    src/i18n/en.json,
    tests/components/Chapter4Content.test.js
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §1 "Chapter4Content" per-chapter changes table (parallax z-index 0, FloatingPanel z-index 4, position relative en .ch4-layout)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Code Examples Ejemplo 1: Chapter4Content.vue — ensamble parallax + glass panels" lines 1167-1230 (source code completo)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §6.8 wrapper common structure + per-chapter projects rendering table (ch4 usa FloatingPanel NO ProjectCard)
    - src/components/Chapter3Content.vue (base layout clone)
    - src/components/ScrollShell.vue (wire pattern existente)
    - src/data/projects.js (shape D3-03 carry-forward)
    - tests/components/Chapter3Content.test.js (T1-T6 pattern)
  </read_first>
  <behavior>
    **Chapter4Content:**

    Layout: 2-col desktop/stacked mobile clonado Chapter3Content cambiando `ch3` → `ch4`. **CRÍTICO:**
    - `.ch4-layout { position: relative; overflow: hidden }` — para contener ParallaxLayers absolute.
    - Dentro del layout, ParallaxLayers PRIMERO como first-child (sin que `.ch4-meta` ni `.ch4-content` lo overflowear visualmente; ParallaxLayers ya tiene `z-index: 0` y los content z-index 4+).
    - `.ch4-content { position: relative; z-index: 4 }` para garantizar que el bio + project cards quedan ENCIMA de las parallax layers.

    Imports:
    - `import { computed } from 'vue'`.
    - `import { useI18n } from 'vue-i18n'`.
    - `import { chapters } from '@/data/chapters'`.
    - `import { projects } from '@/data/projects'`.
    - `import { bio } from '@/data/bio'`.
    - `import ParallaxLayers from './ParallaxLayers.vue'`.
    - `import FloatingPanel from './FloatingPanel.vue'`.

    Const:
    - `const chapter = chapters[4]`.
    - `const ch4Projects = computed(() => projects.filter(p => p.chapterEra === 4))`.

    Template structure:
    ```vue
    <div class="ch4-layout">
      <ParallaxLayers />

      <aside class="ch4-meta">
        <img class="ch4-avatar" :src="chapter.avatarSrc" :alt="t('avatar.busts.4.alt')"
             width="160" height="192" loading="lazy" />
        <p class="ch4-year">{{ chapter.year }}</p>
        <p class="ch4-era">{{ t(chapter.eraKey) }}</p>
      </aside>

      <div class="ch4-content">
        <FloatingPanel :title="t(chapter.titleKey)">
          <p class="ch4-flavor">{{ t('chapters.4.flavor') }}</p>
          <p class="ch4-bio">{{ t(bio.coreKey) }}</p>
        </FloatingPanel>

        <FloatingPanel v-for="project in ch4Projects" :key="project.id"
                       :title="t(project.titleKey)">
          <p class="ch4-project-desc">{{ t(project.descKey) }}</p>
          <p v-if="project.role" class="ch4-project-role">{{ project.role }}</p>
          <ul v-if="project.techStack" class="ch4-project-tech">
            <li v-for="tech in project.techStack" :key="tech">{{ tech }}</li>
          </ul>
          <a v-if="project.link" :href="project.link" target="_blank" rel="noopener noreferrer"
             class="ch4-project-link">{{ t('ui.openProject') }}</a>
        </FloatingPanel>
      </div>
    </div>
    ```

    **NO** usar `<ProjectCard>` — ch4 usa `FloatingPanel` con slot directamente (D4-04 + UI-SPEC §6.8 explica por qué: glass holographic es estructuralmente distinto de ProjectCard skeumorphic, FloatingPanel es dedicated component).

    `<style scoped>` clonado de Chapter3Content cambiando ch3 → ch4 + añadir:
    - `.ch4-layout { position: relative; overflow: hidden; }` (override del padding del Chapter3Content original — ParallaxLayers necesita full-bleed).
    - `.ch4-content { position: relative; z-index: 4; }` (encima de parallax).
    - `.ch4-flavor`, `.ch4-bio`, `.ch4-project-desc`, `.ch4-project-role`, `.ch4-project-tech` con colors `var(--c-fg)` y `var(--c-accent)` ch4.

    **projects.js items:** Añadir 1-2 items chapterEra===4 con shape D3-03 verbatim:
    ```javascript
    { id: 'ch4-arvr-own', chapterEra: 4, year: 2015, titleKey: 'projects.ch4-arvr-own.title',
      descKey: 'projects.ch4-arvr-own.desc', link: null, imageSrc: null,
      role: 'Founder / Tech Lead', techStack: ['Unity', 'ARKit', 'Vuforia'],
      planetSprite: null, planetOrbit: null, planetColor: null },
    { id: 'ch4-metrodigi', chapterEra: 4, year: 2018, titleKey: 'projects.ch4-metrodigi.title',
      descKey: 'projects.ch4-metrodigi.desc', link: null, imageSrc: null,
      role: 'Frontend Engineer', techStack: ['React', 'WebGL'],
      planetSprite: null, planetOrbit: null, planetColor: null },
    ```
    Si Rafael entregó datos reales, usar exactos.

    **i18n keys ES + EN:**
    - `chapters.4.flavor` ES: `"Era AR/VR 2015-18. Paneles holográficos, profundidad espacial, AR aplicado a publicidad y entrenamiento."` / EN: `"AR/VR era 2015-18. Holographic panels, spatial depth, AR applied to advertising and training."`.
    - `chapters.4.parallaxAria` opcional ES: `"Escena AR/VR con paneles flotantes"` / EN: `"AR/VR scene with floating panels"`.
    - `projects.ch4-arvr-own.{title,desc}` + `projects.ch4-metrodigi.{title,desc}` (or stubs PENDING).

    **ScrollShell wire:**
    - Imports: `import Chapter4Content from './Chapter4Content.vue'`.
    - Template: añadir `<Chapter4Content v-else-if="ch.id === 4" />` después de ch3 branch y antes del `<div v-else>` placeholder.

    **Tests:**

    `Chapter4Content.test.js` (analog Chapter3Content.test.js T1-T6 + ch4-specific):
    - T1: `.ch4-layout`, `.ch4-meta`, `.ch4-content` exist; `.ch4-layout` tiene `position: relative` (verificable via CSS readFileSync source).
    - T2: avatar src `/assets/ch4-bust.png` + alt resuelve a `t('avatar.busts.4.alt')`.
    - T3: `<ParallaxLayers>` embebido (findComponent); `<FloatingPanel>` count ≥ 1 (panel principal + N projects).
    - T4: mock projects con 1 item ch4 + 1 item ch5 → `findAllComponents(FloatingPanel).length === 2` (panel principal + 1 project; filter ch5 excluido).
    - T5: locale ES→EN, flavor + bio actualiza.
    - T6: CSS readFileSync (Chapter4Content.vue): contiene `position: relative` en `.ch4-layout` + `z-index: 4` en `.ch4-content`.
    - T7: NO usa `<ProjectCard>` (los projects van vía FloatingPanel slot, NO via ProjectCard) — `findAllComponents(ProjectCard).length === 0`.
  </behavior>
  <action>
    **4.1 Crear `src/components/Chapter4Content.vue`** según behavior + RESEARCH Ejemplo 1.

    **4.2 Modificar `src/data/projects.js`** añadiendo 1-2 items ch4 (shape D3-03).

    **4.3 Añadir i18n keys** a es.json + en.json (paridad).

    **4.4 Modificar `src/components/ScrollShell.vue`** wire-up Chapter4Content.

    **4.5 Crear `tests/components/Chapter4Content.test.js`** con T1-T7 según behavior.

    **4.6 Smoke `npm run dev ?ch=4`:**
    - Visitar el chapter en desktop.
    - Hacer scroll DENTRO del chapter (o mover el mouse para gatillar scrollProgress).
    - Verificar visualmente:
      - 4 capas se mueven con velocidades distintas (stars-far lento, ships-near rápido).
      - FloatingPanels con backdrop-filter blur visible (Chrome moderno).
      - Toggle PRM en DevTools (Rendering panel → Emulate prefers-reduced-motion) → todas las capas se mueven uniforme.
      - Mobile <600px: blur reducido (6px), aún coherente.

    Documentar smoke en SUMMARY.
  </action>
  <verify>
    <automated>npm run test:run -- tests/components/Chapter4Content.test.js tests/components/ParallaxLayers.test.js tests/components/FloatingPanel.test.js tests/i18n/parity.test.js</automated>
    Esperado:
    - Chapter4Content T1-T7 PASS.
    - ParallaxLayers + FloatingPanel sin regresión.
    - parity test PASS.

    Full suite: ~260 (post-W2) → ~280 PASS (≈+20 W3 tests).
  </verify>
  <done>
    - Chapter4Content.vue creado con parallax z-index 0 + content z-index 4 + filter chapterEra===4 + FloatingPanel slot por proyecto.
    - 1-2 items ch4 en projects.js + keys i18n correspondientes.
    - ScrollShell wire-up Chapter4Content.
    - Smoke `npm run dev ?ch=4` visualmente correcto en default + PRM.
    - Suite verde.
    - Commits: `feat(04-04): Chapter4Content + projects + i18n`, `feat(04-04): wire ch4 in ScrollShell`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixelforge palette → 4 assets | §5.3 autor-controlado |
| project.link external ch4 | `rel="noopener noreferrer"` aplicado al anchor |
| scrollProgress global → ParallaxLayers translate | `scrollProgress` es readonly ref; cero user-input runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | XSS-CONTENT | flavor + project desc en i18n | mitigate | Vue auto-escapa; cero v-html. |
| T-04-09 | DoS-perf | 4 parallax layers + backdrop-filter en mobile (Pitfall 3) | mitigate | `@media (max-width: 599px)` reduce blur a 6px; `will-change: transform` solo en parallax layers (no globally); W5 manual checklist incluye FPS check en mobile (deferred iOS). |
| T-04-10 | Phishing | project.link external | mitigate | `<a>` con target="_blank" + rel="noopener noreferrer" pattern (heredado Phase 3 ProjectCard). En Chapter4Content los anchors hand-rolled — aplicar mismo pattern explícitamente. |
</threat_model>

<verification>
## Wave-level checks W3

1. **Asset-naming guard:** 4 nuevos PNGs ch4 matchean enumerated regex.
2. **Suite no regresión:** full suite ~280 PASS.
3. **ART-02 + ART-03 cumplidos:** 4 layers existen + ParallaxLayers renderiza con factor escalonado + FloatingPanel glass.
4. **PRM compliance (D4-10d):** ParallaxLayers.test.js T5 verifica factor uniforme bajo PRM.
5. **Pitfall 7:** ParallaxLayers usa `<img loading="lazy">` NO `background-image` CSS.
6. **Pitfall 3:** FloatingPanel.test.js T7 verifica mobile blur reduction.
7. **Build verde:** `npm run build` completa.
8. **Smoke manual:** `?ch=4` muestra depth perceptible + FloatingPanels con glass + PRM uniform funcional.

## Out of scope W3
- Performance benchmark en iOS real — deferred Plan 07.
- Animación de ships (moving across screen) — D4-04 + PHA-08: naves estáticas Phase 4 (Phase 5 ch6 podría animarlas con Phaser).
- Fallback a 3 layers si W3 smoke reporta jank mobile en hardware Rafael — opcional iterar in-place.
</verification>

<success_criteria>
- 4 PNGs ch4 generados con paleta §5.3 (3 forge_background + 1 forge_sprite + bg_remove panels Pitfall 2).
- 3 SFCs nuevos: ParallaxLayers (Pitfall 6 + 7 aplicados), FloatingPanel (@supports backdrop-filter + mobile blur reduction), Chapter4Content (parallax z-0 + content z-4 + FloatingPanel slot per project).
- 3 test files con cobertura math + PRM + CSS markers + i18n.
- chapter-themes.css extended con `[data-chapter="4"] .floating-panel` block completo.
- 1-2 items ch4 en projects.js + keys i18n.
- ScrollShell wire-up Chapter4Content.
- Smoke `npm run dev ?ch=4` confirma depth perceptible + glass + PRM uniform.
</success_criteria>

<output>
After completion, create `.planning/phases/04-chapters-0-2-4-5/04-04-SUMMARY.md` con:
- 4 PNGs ch4 details (file sizes, paleta usada, Adobe HSL aplicado o no).
- ParallaxLayers + FloatingPanel + Chapter4Content paths + LOC.
- Items ch4 añadidos a projects.js.
- i18n keys nuevas (lista).
- Smoke screenshot / descripción del depth effect (qué layers se ven moverse y a qué velocidad).
- PRM verification result (toggle DevTools → all layers uniform).
- Lecciones para W4 (ch5 hero + ScrollRevealCard).
</output>
