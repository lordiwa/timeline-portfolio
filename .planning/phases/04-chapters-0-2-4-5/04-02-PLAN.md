---
phase: 04-chapters-0-2-4-5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Chapter0Content.vue
  - src/components/Chapter1Content.vue
  - src/components/TerminalScroll.vue
  - src/components/MarqueeBanner.vue
  - src/components/StarfieldBg.vue
  - src/components/ScrollShell.vue
  - src/styles/chapter-themes.css
  - src/i18n/es.json
  - src/i18n/en.json
  - tests/components/Chapter0Content.test.js
  - tests/components/Chapter1Content.test.js
  - tests/components/TerminalScroll.test.js
  - tests/components/MarqueeBanner.test.js
autonomous: true
requirements:
  - ART-07
  - A11Y-06
  - CORE-09
  - THM-04
  - I18N-05
must_haves:
  truths:
    - "Al hacer scroll a ch0, el visitante ve terminal CRT con cursor parpadeante + texto VT323 verde sobre negro"
    - "Al hacer scroll a ch1, el visitante ve marquee bilingüe scrolling + starfield twinkleando + Comic Neue magenta sobre navy"
    - "Bajo prefers-reduced-motion: ch0 cursor estático, ch1 marquee swap a span estático, ch1 starfield sin twinkle"
    - "Ch0 y ch1 NO contienen pixel art (ART-07 enforced) — son 100% CSS"
    - "Toggle ES↔EN actualiza el contenido de ambos chapters sin layout shift visible"
  artifacts:
    - path: "src/components/Chapter0Content.vue"
      provides: "Wrapper layout 2-col/stacked ch0 + embed TerminalScroll + bio i18n + ART-07 (zero pixel art bg)"
      min_lines: 50
    - path: "src/components/Chapter1Content.vue"
      provides: "Wrapper layout ch1 + embed MarqueeBanner + StarfieldBg + tabla legacy + bio i18n"
      min_lines: 60
    - path: "src/components/TerminalScroll.vue"
      provides: "CSS-only terminal CRT con cursor blink + lines staggered animation-delay; PRM via @media"
      min_lines: 40
    - path: "src/components/MarqueeBanner.vue"
      provides: "<marquee> real deprecated + v-if swap a <span> bajo PRM (D4-05); inject prm consume"
      min_lines: 40
    - path: "src/components/StarfieldBg.vue"
      provides: "CSS-only radial-gradient repeated + twinkle keyframes; PRM @media branch"
      min_lines: 25
    - path: "src/styles/chapter-themes.css"
      provides: "Reglas @layer components para .terminal-scroll, .marquee-banner, .starfield-bg, .ch0-layout, .ch1-layout"
    - path: "src/components/ScrollShell.vue"
      provides: "Wire <Chapter0Content v-if='ch.id===0'> + <Chapter1Content v-else-if='ch.id===1'> + imports nuevos"
    - path: "tests/components/Chapter0Content.test.js"
      provides: "ART-07 guard (no <img> ch0-bg*) + DOM contract + i18n alt + TerminalScroll embed + CSS PRM marker"
    - path: "tests/components/Chapter1Content.test.js"
      provides: "ART-07 guard + DOM contract + i18n + MarqueeBanner embed + PRM v-if swap test (mutar prefersReduced)"
    - path: "tests/components/TerminalScroll.test.js"
      provides: "CSS markers @keyframes terminal-cursor-blink + @media PRM source-level"
    - path: "tests/components/MarqueeBanner.test.js"
      provides: "<marquee> exists default + <span class='marquee-banner__static'> al mutar prefersReduced=true; starfield markers source-level"
  key_links:
    - from: "src/components/Chapter0Content.vue"
      to: "src/components/TerminalScroll.vue"
      via: "import + template embed"
      pattern: "<TerminalScroll"
    - from: "src/components/Chapter1Content.vue"
      to: "src/components/MarqueeBanner.vue, src/components/StarfieldBg.vue"
      via: "imports + template embed"
      pattern: "<MarqueeBanner|<StarfieldBg"
    - from: "src/components/MarqueeBanner.vue"
      to: "App.vue provide('prm')"
      via: "inject('prm') → prefersReduced ref"
      pattern: "inject\\('prm'\\)"
    - from: "src/components/ScrollShell.vue"
      to: "Chapter0Content, Chapter1Content"
      via: "v-if branches en el section v-for"
      pattern: "ch\\.id === 0|ch\\.id === 1"
---

<objective>
W1 entrega los **dos chapters CSS-only (ch0 + ch1)** que arrancan el arco visual del portafolio sin tocar pixel art. Es la primera wave que cierra ART-07 y la única paralelizable con W0 (los components no dependen de busts; el avatar in-content consume `chapter.avatarSrc` que apunta a `/assets/ch{N}-bust.png` y muestra `alt` aunque el PNG todavía no exista — Phase 3 ya validó ese fallback).

Purpose: Sin ch0/ch1, el visitante que arranca en ch3 y hace scroll up ve placeholders genéricos "1995 · Terminal" / "2001 · HTML 90s" — pierde la primera mitad de la narrativa "tres décadas de tecnología". Los dos chapters son CSS-only puros (terminal CRT + marquee+starfield) y entregan máximo signature visual con cero costo de pixelforge.

Output: 5 components Vue nuevos (2 wrappers + 3 era-signature) + 4 test files + extension de `chapter-themes.css @layer components` + 2 wire branches en `ScrollShell.vue` + ~6 keys nuevas en i18n.

**MVP vertical slice (W1):** Después de esta wave, el visitante puede hacer scroll up desde ch3 y ver ch0 y ch1 completos con identidad era-auténtica, marquee scrolling, starfield twinkleando, terminal con cursor parpadeante, y toggle ES↔EN funcional en ambos. Los chapters ch2/ch4/ch5 siguen en placeholder genérico (W2-W4 los completan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md
@.planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md
@.planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md
@.planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md
@CLAUDE.md
@src/components/Chapter3Content.vue
@src/components/StickyAvatar.vue
@src/components/StickyTimeline.vue
@src/components/ScrollShell.vue
@src/styles/chapter-themes.css
@src/i18n/es.json
@src/i18n/en.json
@tests/components/Chapter3Content.test.js
@tests/components/StickyAvatar.test.js
@tests/i18n/test-helpers.js

<interfaces>
<!-- Contratos clave para los components nuevos -->

From src/components/Chapter3Content.vue — script shape a clonar (D3-04 locked):
```vue
<script setup>
import { computed } from 'vue'
import { useI18n } from 'vue-i18n'
import { chapters } from '@/data/chapters'
import { projects } from '@/data/projects'
import { bio } from '@/data/bio'
import ProjectCard from './ProjectCard.vue'

const { t } = useI18n()
const chapter = chapters[3]  // ← cambiar N per chapter
const ch3Projects = computed(() => projects.filter(p => p.chapterEra === 3))
</script>
```

From src/components/StickyAvatar.vue — pattern inject('prm') para PRM:
```javascript
import { inject } from 'vue'
const { prefersReduced } = inject('prm')
// uso: v-if="!prefersReduced" o prefersReduced.value en script
```

From RESEARCH §Pattern 2 (TerminalScroll) — CSS keyframes esperados:
- `@keyframes terminal-reveal` (lines staggered fade-in via animation-delay)
- `@keyframes terminal-cursor-blink` (steps(2) opacity 1↔0)
- `@media (prefers-reduced-motion: reduce) { .terminal-line { opacity:1; animation:none } .terminal-cursor { animation:none; opacity:1 } }`

From RESEARCH §Pattern 3 (MarqueeBanner) — template branch crítico:
```vue
<marquee v-if="!prefersReduced" behavior="scroll" direction="left" scrollamount="6" class="marquee-banner__scroll">
  {{ t('chapters.1.marqueeText') }}
</marquee>
<span v-else class="marquee-banner__static">{{ t('chapters.1.marqueeText') }}</span>
```
**CRÍTICO:** usar `v-if` NO `v-show` — `<marquee>` debe desaparecer del DOM para que el browser deje de scrollearlo (D4-05).

From UI-SPEC §6.3 — StarfieldBg DOM:
```html
<div class="starfield-bg" aria-hidden="true"></div>
```
CSS: 6-12 radial-gradients distribuidos + animation: twinkle 3s ease-in-out infinite alternate + @media PRM animation:none.

From tests/components/Chapter3Content.test.js — mount + i18n helper pattern:
```javascript
import { mount, flushPromises } from '@vue/test-utils'
import { createTestI18n } from '../i18n/test-helpers.js'

function mountCh3({ locale = 'es', stubs } = {}) {
  const i18n = createTestI18n({ locale })
  const wrapper = mount(Chapter3Content, { global: { plugins: [i18n], stubs } })
  return { wrapper, i18n }
}
```

i18n keys nuevas Phase 4 W1 (paridad ES + EN obligatoria — `tests/i18n/parity.test.js` enforza):
```
chapters.0.flavor               (string ES + EN — descriptor era)
chapters.0.terminal.line1       (cada línea del terminal scroll, p.ej. "$ whoami")
chapters.0.terminal.line2
chapters.0.terminal.line3
chapters.0.terminal.line4
chapters.1.flavor
chapters.1.marqueeText          (single string del marquee)
chapters.1.marqueeAria          (aria-label compensation D4-05)
chapters.1.tableLabel           (label tabla legacy "ME 101" GeoCities-style)
```
**No tocar avatar.busts.{0..6}.alt aquí** — W5 los refresca con drafts era-accurate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear TerminalScroll + StarfieldBg + MarqueeBanner (era-signature components)</name>
  <files>
    src/components/TerminalScroll.vue,
    src/components/StarfieldBg.vue,
    src/components/MarqueeBanner.vue,
    src/styles/chapter-themes.css,
    src/i18n/es.json,
    src/i18n/en.json,
    tests/components/TerminalScroll.test.js,
    tests/components/MarqueeBanner.test.js
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 2: TerminalScroll" (lines 365-427 — código completo)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 3: MarqueeBanner" (lines 442-527 — crítico v-if swap + starfield radial-gradient pattern)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §6.1 TerminalScroll DOM + §6.2 MarqueeBanner DOM + §6.3 StarfieldBg DOM
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §2 (TerminalScroll analog: BackgroundLayers + StickyAvatar PRM CSS branch), §3 (MarqueeBanner analog: StickyAvatar PRM v-if pattern), §10 (test pattern StickyAvatar.test.js mount helper con prm mutable)
    - src/components/StickyAvatar.vue (lines 32-79 inject + watch + PRM branch; lines 156-167 @media CSS defensive)
    - src/components/StickyTimeline.vue (lines 38-44 simple inject prm)
    - src/styles/chapter-themes.css (current `[data-chapter="0"]` block ch0 ya complete con `--c-bg: #000000`, `--c-fg: #00ff41`; `[data-chapter="1"]` ya complete con `--c-bg: #000080`, `--c-fg: #ff00ff`, `--c-accent: #ffff00`; @layer components al final donde añadir reglas)
    - tests/components/StickyAvatar.test.js (lines 1-80 — mount helper con `provide: { prm: { prefersReduced: ref(...) } }` mutable + readFileSync para asserts CSS)
    - tests/i18n/test-helpers.js (createTestI18n helper)
    - src/i18n/es.json + en.json (current structure — añadir keys siguiendo jerarquía existente)
    - CLAUDE.md §6 errores críticos
  </read_first>
  <behavior>
    **TerminalScroll:**
    - Renderiza `<pre>` con 4 `<span class="terminal-line">` (lines 1-4) cada uno con `animation-delay` escalonado 0s, 1.5s, 3s, 4.5s para staggered reveal.
    - `<span class="terminal-cursor" aria-hidden="true">█</span>` al final, animation `terminal-cursor-blink` 1s steps(2) infinite.
    - Bajo `@media (prefers-reduced-motion: reduce)`: ambos animations `none`, opacity:1.
    - Consume i18n: `t('chapters.0.terminal.line1')` ... line4.
    - NO consume `inject('prm')` — PRM se maneja 100% via CSS @media (simplificación porque no hay JS state que controlar — Pattern 2 lo confirma).

    **StarfieldBg:**
    - Único `<div class="starfield-bg" aria-hidden="true"></div>`.
    - CSS: 6-8 `radial-gradient` distribuidos por el frame (white 2px + light pink 1.5px + soft yellow 2px combos), `background-size: 200px 200px`, `background-repeat: repeat`.
    - `animation: starfield-twinkle 4s ease-in-out infinite alternate` con keyframes que mutan SOLO `opacity` 0.7↔1.0 (NUNCA `box-shadow` — anti-pattern Tobias Ahlin verified).
    - Bajo PRM `@media`: `animation: none; opacity: 1`.
    - Sin consume i18n (decorativo `aria-hidden`).

    **MarqueeBanner:**
    - Template branch `v-if="!prefersReduced"`:
      ```vue
      <marquee behavior="scroll" direction="left" scrollamount="6" class="marquee-banner__scroll" :aria-label="t('chapters.1.marqueeAria')">
        {{ t('chapters.1.marqueeText') }}
      </marquee>
      ```
      con `<!-- eslint-disable-next-line vue/no-deprecated-html-element-is -->` comment encima (D4-05 era-authenticity intentional).
    - Branch `v-else`:
      ```vue
      <span class="marquee-banner__static">{{ t('chapters.1.marqueeText') }}</span>
      ```
    - **Crítico:** `v-if`/`v-else` (NO v-show) para que `<marquee>` salga del DOM bajo PRM y el browser deje de scrollearlo.
    - Consume `inject('prm')` + `useI18n()`.

    **Tests behavior expected:**
    - `TerminalScroll.test.js`:
      - T1: monta sin errores, renderiza 4 `.terminal-line` spans.
      - T2: source CSS contiene `@keyframes terminal-cursor-blink` con `steps(2)`.
      - T3: source CSS contiene `@media (prefers-reduced-motion: reduce)` + `.terminal-cursor { animation: none }`.
      - T4: source CSS contiene `@keyframes terminal-reveal`.
      - T5: locale ES vs EN — al cambiar `i18n.global.locale.value = 'en'` y `flushPromises()`, las líneas cambian.
    - `MarqueeBanner.test.js`:
      - T1: con `prefersReduced=ref(false)` mount → `<marquee>` existe en DOM.
      - T2: con `prefersReduced=ref(false)` → `.marquee-banner__static` NO existe.
      - T3: mutar `prefersReduced.value = true` + `flushPromises()` → `<marquee>` desaparece, `.marquee-banner__static` aparece.
      - T4: source CSS contiene `radial-gradient` markers (starfield-style via class si MarqueeBanner provee starfield internamente, o test va a StarfieldBg) — **decisión:** StarfieldBg es un componente independiente; los markers radial-gradient se testean en `tests/components/StarfieldBg.test.js` (opcional W1) o en el component file source directamente via readFileSync de StarfieldBg.vue. Por simplicidad, T4 verifica `MarqueeBanner.vue` source contiene `inject('prm')` import y los dos branches `v-if`/`v-else`.
      - T5: aria-label del marquee `t('chapters.1.marqueeAria')` resuelve a string no vacío.

    Nota: no creamos `tests/components/StarfieldBg.test.js` dedicado en este task — el component es trivial (~25 LOC CSS-only sin lógica). Si Task 2 lo requiere para Chapter1Content.test.js, lo crea ahí.
  </behavior>
  <action>
    Delegar a `frontend-dev` agent o ejecutar como assistant principal.

    **1.1 Crear `src/components/TerminalScroll.vue`** copiando RESEARCH §Pattern 2 source completo. Ajustar:
    - Imports: `import { useI18n } from 'vue-i18n'`.
    - Const `lines` array con 4 items `{ key: 'chapters.0.terminal.line1', delay: 0 }` ... delay 4.5s.
    - Tokens CSS via `var(--c-fg)` y `var(--c-bg)` (heredados del `[data-chapter="0"]` cascade — funcionan porque el component está dentro de `<section data-chapter="0">`).
    - `<style scoped>` con `@keyframes terminal-reveal`, `@keyframes terminal-cursor-blink` con `steps(2)`, `@media (prefers-reduced-motion: reduce)` block.
    - Font-family: `'VT323', ui-monospace, monospace` (heredado Phase 2 W4 self-hosted, ya cargado via `[data-chapter="0"]` block — el component solo necesita declarar font-family).

    **1.2 Crear `src/components/StarfieldBg.vue`** siguiendo UI-SPEC §6.3:
    - `<script setup>` vacío excepto `defineProps({})` opcional.
    - Template `<div class="starfield-bg" aria-hidden="true"></div>`.
    - `<style scoped>` con 6-8 `radial-gradient(Xpx Xpx at X% Y%, color N%, transparent N%)` combos en `background-image`, `background-size: 200px 200px`, `background-repeat: repeat`, `animation: starfield-twinkle 4s ease-in-out infinite alternate`.
    - `@keyframes starfield-twinkle { from { opacity: 0.7 } to { opacity: 1.0 } }`.
    - `@media (prefers-reduced-motion: reduce) { .starfield-bg { animation: none; opacity: 1 } }`.
    - `position: absolute; inset: 0; z-index: -1` para que viva detrás del content del chapter sin pointer-events.

    **1.3 Crear `src/components/MarqueeBanner.vue`** copiando RESEARCH §Pattern 3 source. Crítico:
    - `import { inject } from 'vue'` + `import { useI18n } from 'vue-i18n'`.
    - `const { prefersReduced } = inject('prm')` + `const { t } = useI18n()`.
    - Template con `v-if="!prefersReduced"` `<marquee>` + `<!-- eslint-disable-next-line vue/no-deprecated-html-element-is -->` comment encima.
    - `v-else` `<span class="marquee-banner__static">`.
    - `<style scoped>` con `font-family: 'Comic Neue', 'Comic Sans MS', cursive` (Phase 2 W4 self-hosted, declarado en `[data-chapter="1"]` block).

    **1.4 Extender `src/styles/chapter-themes.css @layer components`** añadiendo AL FINAL del archivo (después del bloque `.project-card` existente):

    Reglas para `[data-chapter="0"] .terminal-scroll` (font-size, padding, color, background — RESEARCH §Pattern 2 styles), `[data-chapter="1"] .marquee-banner` (background `var(--c-accent)`, padding, border, color — RESEARCH §Pattern 3 styles base), `[data-chapter="1"] .starfield-bg` (caps + media query si planner decide moverlas aquí desde scoped — recommended: dejar scoped en cada SFC para simplicidad). Si dejas scoped, NO toques chapter-themes.css en este task — solo cambia el archivo si añades **per-chapter overrides** que no caben en scoped (p.ej. `[data-chapter="N"] .marquee-banner { ... }` para override token — opcional).

    **Decisión recomendada:** dejar todo en `<style scoped>` de cada SFC para W1 (los components consumen `var(--c-fg)` etc. heredados del cascade). Tocar `chapter-themes.css` solo si Task 2 (wrappers) necesita override `[data-chapter="N"]` específico que el scoped no alcanza.

    **1.5 Añadir i18n keys** a `src/i18n/es.json` y `src/i18n/en.json` (paridad enforced por `tests/i18n/parity.test.js`):

    ES:
    ```json
    {
      "chapters": {
        "0": { ..., "flavor": "1995. Monitor CRT phosphor. Mi primer prompt de comandos.",
               "terminal": {
                 "line1": "$ whoami",
                 "line2": "Rafael Matovelle, 10 años, 1995",
                 "line3": "$ cat about.txt",
                 "line4": "Mi primera computadora. Mi primer 'Hello World'."
               } },
        "1": { ..., "flavor": "GeoCities, Angelfire, tablas anidadas y marquees por doquier.",
               "marqueeText": "★ Bienvenido a mi página web!! ★ 🚧 En construcción 🚧",
               "marqueeAria": "Banner decorativo era HTML 90s",
               "tableLabel": "Mis cosas favoritas (1999)" }
      }
    }
    ```

    EN (paridad enforced):
    ```json
    {
      "chapters": {
        "0": { ..., "flavor": "1995. CRT phosphor monitor. My first command prompt.",
               "terminal": { "line1": "$ whoami", "line2": "Rafael Matovelle, age 10, 1995",
                             "line3": "$ cat about.txt", "line4": "My first computer. My first 'Hello World'." } },
        "1": { ..., "flavor": "GeoCities, Angelfire, nested tables and marquees everywhere.",
               "marqueeText": "★ Welcome to my website!! ★ 🚧 Under construction 🚧",
               "marqueeAria": "Decorative banner — 90s HTML era",
               "tableLabel": "My favorite things (1999)" }
      }
    }
    ```

    **1.6 Crear `tests/components/TerminalScroll.test.js`** siguiendo PATTERNS.md §10 (analog StickyAvatar.test.js mount helper). Asserts T1-T5 según behavior block.

    **1.7 Crear `tests/components/MarqueeBanner.test.js`** con `provide: { prm: { prefersReduced: ref(false) } }` mutable. Asserts T1-T5 según behavior block. Para T3 mutar: `prefersReduced.value = true; await flushPromises();` luego asserts swap.

    **Orden TDD:** Tests rojos primero (RED commit), luego components (GREEN commit), luego css/styles refactor si necesario. Commits separados.
  </action>
  <verify>
    <automated>npm run test:run -- tests/components/TerminalScroll.test.js tests/components/MarqueeBanner.test.js tests/i18n/parity.test.js</automated>
    Esperado:
    - TerminalScroll T1-T5 PASS.
    - MarqueeBanner T1-T5 PASS (especialmente T3 PRM swap).
    - parity test PASS (todas las nuevas keys ES↔EN paridad).
  </verify>
  <done>
    - 3 SFCs creados: TerminalScroll.vue, StarfieldBg.vue, MarqueeBanner.vue.
    - 2 test files creados con cobertura PRM + CSS markers + i18n locale switch.
    - 9 i18n keys nuevas en ambos archivos (paridad ES/EN).
    - Suite no regresión: 220 (post-W0) → 220 + ~10 tests nuevos = ~230 PASS.
    - Commits atómicos por SFC + test (preferible 3 commits `feat(04-02): TerminalScroll`, `feat(04-02): StarfieldBg`, `feat(04-02): MarqueeBanner + PRM v-if swap`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear Chapter0Content + Chapter1Content wrappers + wire en ScrollShell</name>
  <files>
    src/components/Chapter0Content.vue,
    src/components/Chapter1Content.vue,
    src/components/ScrollShell.vue,
    src/styles/chapter-themes.css,
    tests/components/Chapter0Content.test.js,
    tests/components/Chapter1Content.test.js
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §1 "Chapter{N}Content wrappers" (excerpt completo Chapter3Content lines 19-89 + per-chapter table)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §6.8 (Chapter{N}Content wrappers — common structure + per-chapter projects rendering decision table)
    - src/components/Chapter3Content.vue (analog exact — 184 LOC completas)
    - src/components/ScrollShell.vue (lines 24-35 chapters array + lines 76-90 template section v-for con placeholder)
    - tests/components/Chapter3Content.test.js (analog exact — T1-T6 cobertura: DOM contract, avatar src, projects filter, locale switch, CSS markers)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Validation Architecture" Gaps Wave 1 (qué tests Phase 4 W1 espera)
    - src/styles/chapter-themes.css (`[data-chapter="0"]` + `[data-chapter="1"]` blocks — verificar tokens disponibles)
  </read_first>
  <behavior>
    **Chapter0Content:**
    - Layout 2-col desktop (200px aside + 1fr content) / stacked mobile <600px — clonado Chapter3Content.
    - `<aside class="ch0-meta">` con `<img class="ch0-avatar" :src="chapter.avatarSrc" :alt="t('avatar.busts.0.alt')" width="160" height="192" loading="lazy" />` + year + era.
    - `<div class="ch0-content">`:
      - `<TerminalScroll />` embed PRIMERO (arriba del bio para hero de era).
      - `<div class="ch0-bio"><p>{{ t(bio.coreKey) }}</p></div>` (bio core compartida).
      - `<p class="ch0-flavor">{{ t('chapters.0.flavor') }}</p>` (texto era-flavor).
      - **NO renderea `.ch0-projects`** — ch0 sin proyectos (CONTENT-CHECKLIST §2.6 documenta esto).
    - **ART-07 enforcement:** ningún `<img>` o `background-image` referenciando `/assets/ch0-bg*.png` o `parallax/`. Solo el avatar `ch0-bust.png` (que aún puede no existir post-W0 si execute paralelo — alt provee fallback).
    - `<style scoped>`: clones del CSS de Chapter3Content cambiando `ch3` → `ch0` en class names; tokens via cascade `[data-chapter="0"]`.

    **Chapter1Content:**
    - Misma estructura que ch0 pero:
      - `<StarfieldBg />` PRIMERO (posicionado absolute con `inset: 0; z-index: -1` desde su `<style scoped>` — vive detrás del content).
      - `<MarqueeBanner />` después del aside meta, encima del bio.
      - `<div class="ch1-bio">` con bio core + flavor era.
      - **NO renderea `.ch1-projects`** (CONTENT-CHECKLIST §2.6).
      - Opcional: `<table border="1" class="ch1-legacy-table">` con `<caption>{{ t('chapters.1.tableLabel') }}</caption>` y 2-3 rows de "Mis cosas favoritas" era-flavor en celdas — añade era-authenticity GeoCities sin reclamar info crítica de Rafael.
    - **ART-07 enforcement:** sin `<img>` ni `bg-image` pixel-art.
    - Position relative en `.ch1-layout` para que StarfieldBg absolute esté contenido.

    **ScrollShell wire:**
    - Imports: añadir 2 `import Chapter0Content from './Chapter0Content.vue'` y `import Chapter1Content from './Chapter1Content.vue'` al lado del existente Chapter3Content import.
    - Template: reemplazar el `<Chapter3Content v-if="ch.id === 3" />` actual con cadena `v-if`/`v-else-if` que cubre ch0, ch1, ch3 — los demás (ch2/4/5/6) siguen en placeholder genérico hasta W2-W4.
    - Mantener `<div v-else class="chapter-placeholder">...` para ch2/4/5/6.

    **chapter-themes.css `@layer components`:**
    - Si los `<style scoped>` de Chapter0Content/Chapter1Content NO alcanzan a tokens compartidos (ya consumen `var(--c-fg)` heredados), no tocar este archivo en Task 2.
    - Si Task 2 descubre que necesita reglas `[data-chapter="0"] .ch0-meta { ... }` que no caben en scoped — añadirlas al `@layer components` final.

    **Tests:**
    - `Chapter0Content.test.js` T1-T6 análogo a Chapter3Content.test.js + T-ART07 (no img src `/assets/ch0-bg*`).
      - T1: `.ch0-layout` + `.ch0-meta` + `.ch0-content` exist.
      - T2: avatar img `src === '/assets/ch0-bust.png'` + alt `t('avatar.busts.0.alt')` resuelve.
      - T3: `<TerminalScroll>` component embebido (findComponent).
      - T4: NO renderea projects (no `<ProjectCard>` found).
      - T5: locale ES→EN toggle actualiza flavor text.
      - T6: source SFC NO contiene `url\(.*ch0-bg.*\)` ni `<img.*src=.*parallax/` regex (ART-07 enforcement).
    - `Chapter1Content.test.js` T1-T6 + T-PRM-MARQUEE (clone del T3 de MarqueeBanner pero a nivel wrapper).
      - T1-T2 idem.
      - T3: `<MarqueeBanner>` + `<StarfieldBg>` components embebidos.
      - T4: NO renderea projects.
      - T5: locale switch.
      - T6: ART-07 source guard.
      - T7: PRM mutable mock — `provide: { prm: { prefersReduced: ref(true) } }` → `<marquee>` no existe en DOM, `<span class="marquee-banner__static">` sí.
  </behavior>
  <action>
    **2.1 Crear `src/components/Chapter0Content.vue`** copiando Chapter3Content.vue verbatim. Cambios:

    - Reemplazar `ch3` → `ch0` en class names (`ch3-layout` → `ch0-layout`, etc.).
    - `const chapter = chapters[0]`.
    - `import TerminalScroll from './TerminalScroll.vue'`.
    - Remover `import ProjectCard from ...` + remover `import { projects }` + remover `const ch3Projects = computed(...)`.
    - En template: añadir `<TerminalScroll />` al inicio de `.ch0-content` antes del `.ch0-bio`.
    - Añadir `<p class="ch0-flavor">{{ t('chapters.0.flavor') }}</p>` en `.ch0-content`.
    - Remover el bloque `.ch0-projects` v-for ProjectCard (ch0 sin proyectos).
    - `<style scoped>` clonar Chapter3Content CSS reemplazando `ch3` → `ch0`. Avatar src será `/assets/ch0-bust.png` — si no existe aún (W0 en paralelo), browser muestra alt + fallback color del border.

    **2.2 Crear `src/components/Chapter1Content.vue`** análogo:

    - `const chapter = chapters[1]`.
    - Imports: `import MarqueeBanner from './MarqueeBanner.vue'`, `import StarfieldBg from './StarfieldBg.vue'`.
    - Template: dentro de `.ch1-layout`:
      - `<StarfieldBg />` PRIMERO (su CSS scoped lo posiciona absolute detrás).
      - `<aside class="ch1-meta">` con avatar + year + era.
      - `<div class="ch1-content">`:
        - `<MarqueeBanner />` arriba.
        - `<div class="ch1-bio">` con bio core + flavor.
        - Opcional: `<table border="1" class="ch1-legacy-table">` con caption + 2 rows era-flavor.
    - `<style scoped>` con `position: relative` en `.ch1-layout` (para contener StarfieldBg absolute). Resto clonado de Chapter3Content cambiando ch3 → ch1.
    - Border de la tabla legacy: `border: 1px solid var(--c-fg);` (magenta sobre navy era-auténtico).

    **2.3 Modificar `src/components/ScrollShell.vue`:**

    Imports section (lines 23-24 zone):
    ```vue
    import Chapter0Content from './Chapter0Content.vue'
    import Chapter1Content from './Chapter1Content.vue'
    import Chapter3Content from './Chapter3Content.vue'
    ```

    Template (lines 85-88 zone): reemplazar
    ```vue
    <Chapter3Content v-if="ch.id === 3" />
    <div v-else class="chapter-placeholder">
    ```
    con
    ```vue
    <Chapter0Content v-if="ch.id === 0" />
    <Chapter1Content v-else-if="ch.id === 1" />
    <Chapter3Content v-else-if="ch.id === 3" />
    <div v-else class="chapter-placeholder">
    ```

    No tocar lo demás de ScrollShell.

    **2.4 Crear `tests/components/Chapter0Content.test.js`** copiando `tests/components/Chapter3Content.test.js` verbatim. Cambios:

    - `import Chapter0Content from '@/components/Chapter0Content.vue'`.
    - Helper `mountCh0` análogo a mountCh3.
    - `vi.mock('@/data/projects')` con mock vacío `projects: []` (ch0 sin proyectos).
    - T1: assert `.ch0-layout`, `.ch0-meta`, `.ch0-content` exist.
    - T2: `wrapper.find('img.ch0-avatar').attributes('src')` === `'/assets/ch0-bust.png'`; alt resuelve a `t('avatar.busts.0.alt')` (no vacío).
    - T3: `wrapper.findComponent(TerminalScroll).exists()` === true.
    - T4: `wrapper.findAllComponents(ProjectCard).length` === 0.
    - T5: locale switch ES→EN, flavor text cambia.
    - T6: `CH0_SOURCE` readFileSync, regex `/url\(.*ch0-bg|<img[^>]*src=["'][^"']*parallax/` NO match (ART-07).

    **2.5 Crear `tests/components/Chapter1Content.test.js`** análogo. Adicional:

    - T7: mount con `provide: { prm: { prefersReduced: ref(true) } }`; verificar `wrapper.find('marquee').exists()` === false; `wrapper.find('.marquee-banner__static').exists()` === true.
    - Para T7 necesitas pasar el provide through `mount(Chapter1Content, { global: { plugins: [i18n], provide: { prm: ... } } })`. Verifica que MarqueeBanner recibe el inject correctamente.

    **2.6 Smoke `npm run dev`:**

    Tras todos los commits, ejecutar `npm run dev` y navegar a `http://127.0.0.1:5173?ch=0` y `?ch=1` para validar:
    - ch0 muestra terminal con cursor parpadeando, texto VT323 verde sobre negro.
    - ch1 muestra marquee scrolling + starfield twinkleando + tabla legacy.
    - Toggle ES↔EN actualiza ambos sin layout shift visible.
    - Documentar smoke en SUMMARY.
  </action>
  <verify>
    <automated>npm run test:run -- tests/components/Chapter0Content.test.js tests/components/Chapter1Content.test.js tests/components/ScrollShell.test.js tests/i18n/parity.test.js</automated>
    Esperado:
    - Chapter0Content T1-T6 PASS.
    - Chapter1Content T1-T7 PASS (incluido T7 PRM marquee swap a nivel wrapper).
    - ScrollShell.test.js sin regresión (los tests existentes Phase 1+2 no deberían romper porque solo añadimos branches al v-if).
    - parity test PASS.

    Adicional: `npm run test:run` (full suite) — verificar 220+ tests (post W0) → ~245 PASS (≈+15 W1 tests).
  </verify>
  <done>
    - 2 wrappers Chapter0Content + Chapter1Content + 2 test files creados.
    - ScrollShell wire-up: 5 v-else-if branches en lugar de placeholder generic para ch0/ch1/ch3 (ch2/4/5 placeholder hasta W2-W4).
    - Suite no regresión: full suite verde con +15 tests aprox.
    - Manual smoke `npm run dev` confirma ch0 + ch1 funcionales visualmente en ES + EN + PRM.
    - Commits atómicos: `feat(04-02): Chapter0Content + TerminalScroll wire`, `feat(04-02): Chapter1Content + MarqueeBanner + StarfieldBg wire`, `feat(04-02): wire ch0/ch1 in ScrollShell`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n key resolution → Vue template | Texto resuelto por `t()` se interpola directamente; cero v-html (ya enforced por test convención Phase 3) |
| `<marquee>` content → DOM | Legacy tag deprecated; contenido viene de i18n autor-controlado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | XSS-CONTENT | flavor/marquee text en `chapters.{0,1}.*` keys | mitigate | Vue template auto-escapa `{{ t(...) }}`; NO usar `v-html` en ningún Chapter{N}Content (convención Phase 3 ya enforced). Source-level guard via grep en Task 2 verifica ausencia de v-html. |
| T-04-05 | Tampering | `<marquee>` tag legacy (D4-05 deliberado) | accept | Contenido es i18n autor-controlado (`t('chapters.1.marqueeText')`), no raw HTML. Era-authenticity tradeoff documentado (D4-05). |
</threat_model>

<verification>
## Wave-level checks W1

1. **Suite verde:** `npm run test:run` → tests Phase 3 (216) + W0 (4) + W1 (~15) = ~235 PASS.
2. **ART-07 enforced:** Chapter0Content.test.js T6 + Chapter1Content.test.js T6 verifican ausencia de pixel-art bg references en SFC source.
3. **PRM compliance:** MarqueeBanner.test.js T3 + Chapter1Content.test.js T7 verifican swap a `<span>` bajo PRM (D4-05 + D4-10b).
4. **i18n parity:** todas las keys nuevas (chapters.{0,1}.flavor + .terminal.* + .marquee*) están en ES y EN — `tests/i18n/parity.test.js` enforza.
5. **Theme isolation no regresión:** `tests/components/ScrollShell.theme-isolation.test.js` (Phase 2 W2) sigue verde — el StarfieldBg interno ch1 vive dentro del section, no escapa al snap container.
6. **Build verde:** `npm run build` completa sin warnings nuevos.

## Out of scope W1
- Tabla legacy ch1 con info crítica de Rafael — el "tableLabel" + 2 rows flavor es suficiente, info real diferida.
- Animation typing del terminal (line-by-line) — RESEARCH §Pattern 2 documenta como opcional; W1 entrega staggered fade-in via animation-delay, no typing per-char.
- Tests dedicados para StarfieldBg — el component es trivial; verificación implicit vía Chapter1Content.test.js T3 (`findComponent`).
</verification>

<success_criteria>
- 5 SFCs nuevos: TerminalScroll, StarfieldBg, MarqueeBanner, Chapter0Content, Chapter1Content.
- 4 test files: TerminalScroll.test.js, MarqueeBanner.test.js, Chapter0Content.test.js, Chapter1Content.test.js.
- ScrollShell.vue wire-up: ch0/ch1/ch3 con component, resto placeholder.
- chapter-themes.css: opcional reglas `@layer components` per chapter si scoped no alcanza (en general NO necesario W1).
- ~9 i18n keys nuevas en ES + EN paridad.
- Smoke manual `npm run dev` confirma visualmente: terminal CRT ch0, marquee+starfield ch1, toggle ES↔EN, PRM swap funcional.
- Full suite verde sin regresión Phase 1/2/3 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-chapters-0-2-4-5/04-02-SUMMARY.md` con:
- 5 SFCs creados con paths + LOC aprox + i18n keys consumidos.
- 4 test files con cobertura (qué T-N qué req cubre).
- Diff resumen de ScrollShell.vue (5 lines wire + 2 imports).
- i18n keys añadidas (lista ES + EN paridad).
- Smoke `npm run dev` screenshot path o descripción del output ch0/ch1.
- Lecciones para W2/W3/W4 (qué patrones del wrapper Chapter3Content escalaron bien, qué hubo que ajustar).
</output>
