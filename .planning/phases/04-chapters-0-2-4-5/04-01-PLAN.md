---
phase: 04-chapters-0-2-4-5
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - public/assets/ch0-bust.png
  - public/assets/ch1-bust.png
  - public/assets/ch2-bust.png
  - public/assets/ch3-bust.png
  - public/assets/ch4-bust.png
  - public/assets/ch5-bust.png
  - public/assets/ch6-bust.png
  - .gitignore
  - tests/assets/asset-naming.test.js
autonomous: false
requirements:
  - ART-01
  - ART-04
  - A11Y-06
user_setup:
  - service: pixelforge-mcp
    why: "Generación de pixel art via Gemini flash-image (nano-banana-2). Verificar /project:verificar-mcps antes de ejecutar W0."
    env_vars:
      - name: GEMINI_API_KEY
        source: "ya configurada (heredada Phase 3); setx en PowerShell si no existe"
    dashboard_config:
      - task: "Verificar billing habilitado en Google Cloud project asociado al GEMINI_API_KEY"
        location: "console.cloud.google.com → Billing"
  - service: adobe-mcp
    why: "Post-process opcional para crop/HSL/bg removal cuando pixelforge no entrega alpha clean"
    dashboard_config:
      - task: "adobe_mandatory_init() debe responder antes de cualquier image_* call"
        location: "MCP runtime"
must_haves:
  truths:
    - "Los 7 busts pixel art existen en public/assets/ch{0..6}-bust.png"
    - "El personaje es reconociblemente el mismo a través de las 7 edades (~10, ~17, ~22, ~26, ~30, ~36, ~40)"
    - "El estilo pixel art (16-bit chunky, edges definidos, sin antialiasing) es consistente entre los 7 busts"
    - "Las fotos referencia en public/references/ NO se commitean al repo (privacy gate D4-02)"
    - "El test asset-naming valida que cualquier .png en public/assets/ cumple regex ch[0-6]-{descriptor}.png"
  artifacts:
    - path: "public/assets/ch0-bust.png"
      provides: "Bust niño ~10 años, era Terminal 1995, aging-down de 2011.jpg"
    - path: "public/assets/ch1-bust.png"
      provides: "Bust adolescente ~17, era HTML 90s 2001, aging-down de 2011.jpg"
    - path: "public/assets/ch2-bust.png"
      provides: "Bust joven ~22, era Flash 2009, leve aging-down 2011.jpg"
    - path: "public/assets/ch3-bust.png"
      provides: "Bust ~26 anchor (validation gate), era Web 2.0 2013, 2011.jpg anchor"
    - path: "public/assets/ch4-bust.png"
      provides: "Bust ~30, era AR/VR 2015-18, 2016.jpeg anchor + 2019.jpg aux"
    - path: "public/assets/ch5-bust.png"
      provides: "Bust ~36, era Modern 2022-23, 2022.jpeg anchor + 2024.jpg aux"
    - path: "public/assets/ch6-bust.png"
      provides: "Bust ~40, era Phaser 2026 (heredado por Phase 5), 2026.jpg anchor"
    - path: ".gitignore"
      provides: "Entry 'public/references/' que excluye las 6 fotos del repo"
      contains: "public/references/"
    - path: "tests/assets/asset-naming.test.js"
      provides: "Test arquitectural ART-05: regex ch[0-6]-{descriptor}[-{variant}].png sobre public/assets/"
  key_links:
    - from: "src/components/StickyAvatar.vue"
      to: "public/assets/ch{N}-bust.png"
      via: "consumed via chapters[activeChapter].avatarSrc"
      pattern: "ch[0-6]-bust\\.png"
    - from: "src/components/Chapter3Content.vue (y futuros Chapter{0,1,2,4,5}Content)"
      to: "public/assets/ch{N}-bust.png"
      via: "chapter.avatarSrc en <img :src> del aside.chN-meta"
      pattern: "chapter\\.avatarSrc"
    - from: "tests/assets/asset-naming.test.js"
      to: "public/assets/*.png"
      via: "readdirSync + regex validation"
      pattern: "^ch\\[0-6\\]-.*\\.png\\$"
---

<objective>
W0 entrega los **7 avatar busts pixel art** que cierran ART-01 (deferred Plan 03-05 de Phase 3) + ART-04 (ch6-bust herencia para Phase 5), y crea el **gate de naming convention** que protegerá los assets de las waves siguientes.

Purpose: Sin los 7 busts, ningún `Chapter{N}Content` puede mostrar el avatar real (todos quedan en placeholder ch3-style); el StickyAvatar Phase 1 ya consume `chapters[activeChapter].avatarSrc`. Sin asset-naming guard, los assets de W2/W3/W4 pueden driftear (`ch4_ships.png` underscore, `ch4-bg.png` descriptor genérico) y romper convención ART-05 en producción.

Output: 7 PNGs commiteados en `public/assets/` + `.gitignore` entry de privacy + 1 test arquitectural ejecutable que cubre los 13 assets esperados al final de Phase 4.

**Phase Goal (MVP user story):**
**As a** visitante del portafolio, **I want to** ver al avatar de Rafael envejecer reconociblemente al hacer scroll por los 7 chapters, **so that** entienda en segundos que estoy mirando a una persona real cuya carrera atraviesa tres décadas de tecnología.

**MVP vertical slice (W0):** Después de esta wave, el visitante que abra el sitio y haga scroll ya ve un avatar diferente y reconocible en cada chapter — la "máquina del tiempo del avatar" funciona end-to-end aunque el contenido y el styling era-auténtico de los chapters 0/1/2/4/5 todavía estén pendientes (W1-W4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md
@.planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md
@.planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md
@.planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md
@CLAUDE.md
@.claude/agents/artist-creator.md
@.claude/agents/artist-editor.md
@.claude/skills/crear-arte-pixelforge.md
@.claude/skills/editar-arte-adobe.md
@src/data/chapters.js
@src/components/StickyAvatar.vue
@tests/styles/themes-file.test.js
@tests/styles/fonts-bundle.test.js

<interfaces>
<!-- Contratos clave que el executor necesita sin explorar codebase -->

From src/data/chapters.js — shape de cada chapter (D3-04 locked):
```javascript
{
  id: 0..6,
  year: number,
  era: string,
  eraKey: 'chapters.N.era',
  titleKey: 'chapters.N.title',
  avatarSrc: '/assets/chN-bust.png',  // ← lo que esta wave puebla
  palette: []  // ambient era palette (NO se usa para busts — ver Pitfall 1)
}
```

Paleta humana §5.6 (D4-CONTEXT y RESEARCH Pitfall 1) — se pide al gate humano:
```
['<skin-base>', '<skin-shadow>', '<hair-base>', '<hair-highlight>',
 '<clothing-1>', '<clothing-2>', '<eye>', '<line-art>']
```
Esta paleta humana **NO va en chapters[N].palette** (esa es ambient). Se pasa
directamente como `palette:` argument al call de `forge_sprite` para los 7 busts.

From .claude/skills/crear-arte-pixelforge.md — tools deferred:
```
ToolSearch(query="select:mcp__pixelforge__forge_background,mcp__pixelforge__forge_sprite,mcp__pixelforge__forge_animation,mcp__pixelforge__process_sprite,mcp__pixelforge__optimize_sprite", max_results=10)
```
Llamar ANTES de cualquier forge_*. Si InputValidationError → pixelforge no instalado.

From CLAUDE.md §6.2 — presets bg nombrados (NUNCA "black"/"white"):
`"night"`, `"forest"`, `"sky"`, `"dungeon"`, `"ocean"`, `"sand"`, `"snow"`, `"lava"`

From RESEARCH Pitfall 1 — palette mapping ambient vs human:
`chapters[N].palette` es ambient (`['#000000','#00ff41']` para ch0 etc.). Si se pasa
a forge_sprite para un bust humano, el personaje sale verde fósforo (no humano).
**Usar paleta humana §5.6** como `palette:` argument para los 7 busts.

Fotos referencia ya entregadas en public/references/ (D4-02 privacy gate):
- 2011.jpg (~24) — anchor ch2/ch3, aging-down para ch0/ch1
- 2016.jpeg (~29) — anchor ch4
- 2019.jpg (~32) — identity aux
- 2022.jpeg (~36) — anchor ch5
- 2024.jpg (~38) — identity aux
- 2026.jpg (~40) — anchor ch6
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-input" gate="blocking">
  <name>Task 1: Gate — Paleta humana §5.6 + verificación pixelforge MCP</name>
  <what-needed>
    Antes de generar arte W0 necesitamos dos inputs verificables:

    1. **Paleta humana §5.6** del CONTENT-CHECKLIST (Phase 3 carry-forward). Esta paleta es el `palette:` argument que va a las 7 calls de pixelforge para los busts. **NO** confundir con `chapters[N].palette` que es ambient — ver Pitfall 1 en RESEARCH.

       Formato esperado en `.planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md §5.6` (o equivalente):
       ```
       Paleta humana avatar (5-8 hex):
       - Skin base:     #__________
       - Skin shadow:   #__________
       - Hair base:     #__________
       - Hair highlight:#__________
       - Clothing 1:    #__________  (era-neutral camiseta/polo)
       - Clothing 2:    #__________  (era-neutral pantalón/jeans)
       - Eye color:     #__________
       - Line-art:      #__________  (opcional — bordes pixel art)
       ```

    2. **pixelforge MCP funcional**. Ejecutar `/project:verificar-mcps` (CLAUDE.md §3) o smoke test:
       ```
       ToolSearch(query="select:mcp__pixelforge__forge_background,mcp__pixelforge__forge_sprite,mcp__pixelforge__forge_animation,mcp__pixelforge__process_sprite,mcp__pixelforge__optimize_sprite", max_results=10)
       ```
       Si InputValidationError → seguir CLAUDE.md §4.2 instalación limpia.

    3. **Confirmar fotos referencia presentes** en `public/references/`:
       ```powershell
       Get-ChildItem public\references\
       ```
       Esperado: `2011.jpg`, `2016.jpeg`, `2019.jpg`, `2022.jpeg`, `2024.jpg`, `2026.jpg` (D4-02).
  </what-needed>
  <how-to-verify>
    1. Lee `.planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md` (si existe) o pregunta a Rafael por la paleta humana §5.6.
    2. Ejecuta `ToolSearch(...)` con los 5 tools de pixelforge — debe devolver los 5 schemas.
    3. Ejecuta `Get-ChildItem public/references/` — deben aparecer las 6 fotos.
    4. Si los 3 inputs presentes → registrar paleta humana §5.6 en una nota local (la usarán las Tasks 3 y 4) y resumir al usuario los 6-8 hex que entrarán a pixelforge.
  </how-to-verify>
  <resume-signal>
    Rafael responde con:
    - "Paleta humana §5.6: skin=#XXXXXX, hair=#XXXXXX, ..." (mínimo 5 hex)
    - "pixelforge OK" / "pixelforge falla con: <error>"
    - "Fotos OK" / "Falta foto: <path>"

    Si los 3 OK → continuar Task 2. Si pixelforge falla → bloquear W0 hasta resolver; W1 (ch0+ch1 CSS-only) puede arrancar en paralelo. Si paleta §5.6 falta → bloquear W0 only.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Crear test arquitectural asset-naming + .gitignore privacy entry</name>
  <files>tests/assets/asset-naming.test.js, .gitignore</files>
  <read_first>
    - tests/styles/themes-file.test.js (analog exact — readFileSync + regex matchAll + Set unique pattern)
    - tests/styles/fonts-bundle.test.js (analog exact — readdirSync + existsSync defensive)
    - .planning/phases/04-chapters-0-2-4-5/04-PATTERNS.md §"8. tests/assets/asset-naming.test.js" (excerpt + cambios)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pitfall 8: Asset naming drift (SC-2)"
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §13.4 (naming patterns Phase 4)
    - CLAUDE.md §"7.4 Correr el proyecto" + ART-05 (ch{N}-{descriptor}[-{variant}].png)
    - .gitignore actual (leer para insertar al final sin duplicar)
  </read_first>
  <action>
    **2.1 Crear `tests/assets/asset-naming.test.js`** siguiendo PATTERNS.md §8 con los siguientes asserts:

    - Imports: `describe, it, expect` de `vitest`; `readdirSync, existsSync` de `node:fs`; `resolve` de `node:path`.
    - Constante `ASSETS_DIR = resolve(process.cwd(), 'public/assets')`.
    - Constante `ASSET_NAMING_REGEX = /^ch[0-6]-(bust|bg|bg-stars-far|bg-planet-mid|fg-panels|fg-ships|hero)\.png$/` — enumeración explícita en lugar de wildcard para que un commit accidental de `ch4-particle.png` rompa el test (ART-05 strict).
    - Describe block: `'Asset naming convention (ART-05)'`.
    - **T1 — `'todos los .png en public/assets/ matchean el regex Phase 4'`:** `readdirSync(ASSETS_DIR).filter(f => f.endsWith('.png'))` → filtrar los que NO matchean regex → expect array vacío.
    - **T2 — `'los 7 busts ch{0..6}-bust.png existen al finalizar W0'`:** for i 0..6, `expect(existsSync(resolve(ASSETS_DIR, 'ch' + i + '-bust.png'))).toBe(true)`. **Importante:** este test ESPERA correr DESPUÉS de Task 4 (busts generados). En Task 2 commit inicial, T2 rompe — eso es lo correcto: el test es Wave-0 gate del checker. Documentar en JSDoc del describe que T2 es "wave-0-gate: red until ch{N}-bust.png exist".
    - **T3 — `'no hay .png con underscore en el nombre (ART-05 strict)'`:** filtrar archivos con `_` → expect vacío.
    - **T4 — `'public/references/ NO contiene .png commiteados (privacy gate D4-02)'`:** `existsSync(resolve(process.cwd(), 'public/references'))` puede ser true en dev (Rafael), pero `git check-ignore public/references` debería retornarlo como ignored. **No automable cross-platform en vitest sin shell call** — convertir T4 en assertion sobre `.gitignore` source: `readFileSync('.gitignore', 'utf8')` debe contener línea `public/references/` (sin comentario). Hacer T4 source-level.

    Estilo: comentarios JSDoc explicando ART-05 + D4-02 + el "wave-0-gate red→green" para T2.

    **2.2 Actualizar `.gitignore`** — al final del archivo (NO en medio), añadir bloque comentado:
    ```
    # Phase 4 D4-02 privacy gate — fotos de referencia Rafael (pixelforge avatar batch).
    # Vite las sirve en dev local pero NO se commitean. Phase 6 deploy añade glob en firebase.json.
    public/references/
    ```

    Verificar antes de escribir si la línea `public/references/` ya existe — si sí, no duplicar (idempotente).
  </action>
  <verify>
    <automated>npm run test:run -- tests/assets/asset-naming.test.js</automated>
    Esperado en este commit:
    - T1: PASS (no hay aún ningún .png Phase 4 — el directorio puede tener solo placeholders Phase 3 vacío)
    - T2: **FAIL** (busts no existen aún — wave-0-gate red intentionally)
    - T3: PASS (no underscores)
    - T4: PASS (gitignore entry presente)

    Documentar en commit message que T2 quedará rojo hasta Task 4 (post-batch). Adicional smoke: `git check-ignore public/references/2011.jpg` retorna `public/references/2011.jpg` (verificado en bash).
  </verify>
  <done>
    - `tests/assets/asset-naming.test.js` creado con T1-T4 ejecutables.
    - `.gitignore` contiene `public/references/` al final, sin duplicado si ya existía.
    - `git check-ignore public/references/2011.jpg` confirma exclusión.
    - Commit atómico: `test(04-01): add asset-naming guard + gitignore privacy (D4-02)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validation bust ch3 (anchor + human review gate)</name>
  <files>public/assets/ch3-bust.png</files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 4: Pixelforge batch 7 busts" (lines 532-617 — BASE_PROMPT_AVATAR + DIFFS + EXECUTION FLOW Step 0)
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pitfall 1: pixelforge palette mapping ambient vs human"
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pitfall 4: pixelforge no acepta foto JPG como references"
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §7 (avatar 7-bust visual spec — dimensiones 80×96, palette humana §5.6, identity markers ch3 "~26 adulto profesional joven")
    - .claude/skills/crear-arte-pixelforge.md (ToolSearch primero; presets bg nombrados §6.2; `forge_sprite` + bg removal flow)
    - .claude/agents/artist-creator.md (responsabilidades)
    - CLAUDE.md §6.1 (siluetas planas), §6.2 (bg presets), §6.3 (forge_background opacos, forge_sprite alpha)
    - public/references/2011.jpg (anchor primario edad ~24, interpolación a ~26)
  </read_first>
  <action>
    **Delegar a `artist-creator` agent** (sub-agent invocation) con el siguiente prompt estructurado. Si no se puede delegar via Task tool, ejecutar como assistant principal siguiendo CLAUDE.md §2.3 routing reglas.

    **3.1 Cargar tools deferred:**
    ```
    ToolSearch(query="select:mcp__pixelforge__forge_background,mcp__pixelforge__forge_sprite,mcp__pixelforge__forge_animation,mcp__pixelforge__process_sprite,mcp__pixelforge__optimize_sprite", max_results=10)
    ```

    **3.2 Generar ch3 anchor bust:**

    Llamar `mcp__pixelforge__forge_sprite` con:
    - `description`: construir desde RESEARCH §Pattern 4 BASE_PROMPT_AVATAR + DIFF_ch3:
      `"pixel art character bust portrait, 16-bit style, [skin tone from paleta humana §5.6 — usar hex base entregado en Task 1], [build, facial features matching 2011.jpg reference], warm friendly expression, pixelated clean edges, 96x96 px output, transparent background applied, consistent character identity. Age ~26 years old, semi-professional Web 2.0 era clothing, light beard or clean shaven, confident expression."`
    - `background`: `"sky"` (preset nombrado per CLAUDE.md §6.2 — NUNCA "black"/"white")
    - `references`: `["public/references/2011.jpg"]` (anchor primario, edad ~24, interpolar a ~26)
    - `palette`: **array de la paleta humana §5.6** recibido en Task 1 gate (NO `chapters[3].palette` — ver Pitfall 1). Pasar verbatim los 5-8 hex.
    - `outputPath`: `"public/assets/ch3-bust.png"`
    - `size`: `96` (pixelforge default — UI-SPEC §7.1 source dimension)

    **3.3 Post-process pipeline:**

    - `mcp__pixelforge__process_sprite` con `inputPath: "public/assets/ch3-bust.png"`, `operation: "bg_remove"` (limpiar el preset "sky" → alpha).
    - `mcp__pixelforge__optimize_sprite` con `inputPath` y `outputPath` ambos `"public/assets/ch3-bust.png"`, `size: "96x96"` (nearest-neighbor).
    - Si bg removal deja artefactos: delegar a `artist-editor` con `mcp__claude_ai_Adobe_for_creativity__adobe_mandatory_init` → `image_remove_background` → `image_crop_and_resize` (96×96 exact).

    **3.4 Tests post-art:**

    - `npm run test:run -- tests/assets/asset-naming.test.js` — T2 debería PASS solo para ch3 (parcial). Los otros 6 siguen rojos hasta Task 4.

    **3.5 GATE humano (checkpoint embebido):**

    Mostrar al usuario el output (path + invitar a abrir el PNG en visor). Responder con descripción verbal:
    > "ch3-bust generado. Edad aparente ~26. Identity markers: [pelo/ojos/expresión]. Por favor abre `public/assets/ch3-bust.png` y confirma:
    > - ¿Es reconociblemente Rafael (basado en 2011.jpg)?
    > - ¿La edad aparente está cerca de ~26?
    > - ¿El estilo pixel art es coherente con la estética del proyecto?
    > Responde 'aprobado' o 'iterar con: <ajuste>'."

    **Si Rafael responde 'iterar':** re-prompt con DIFF ajustado o reference adicional (p.ej. `references: ["public/references/2011.jpg", "public/references/2016.jpeg"]` para interpolar). Repetir hasta aprobación. Documentar iteraciones en commit message.

    **Si pixelforge rechaza JPG references (Pitfall 4):** pivot a `description:` verbal sin `references:` array — describir foto en palabras ("based on a man in his early 20s, brown hair, slight build, friendly expression"). Documentar en commit.
  </action>
  <verify>
    <automated>npm run test:run -- tests/assets/asset-naming.test.js</automated>
    Adicional:
    - `Test-Path public/assets/ch3-bust.png` (PowerShell) → True
    - PNG file size > 1KB (no corrupted/empty)
    - PNG dimensions 96×96 (verificable con Node `sharp` o Adobe MCP preview)

    **Manual gate (resume-signal):** Rafael responde 'aprobado' tras review visual.
  </verify>
  <done>
    - `public/assets/ch3-bust.png` existe, 96×96, transparent bg.
    - Rafael aprobó visualmente (identity coherente con 2011.jpg, edad ~26, estilo pixel art).
    - T2 del asset-naming test parcial-verde (ch3-bust.png present).
    - Commit atómico: `art(04-01): generate ch3-bust anchor (validation gate aprobado)`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Batch 6 busts restantes (ch0, ch1, ch2, ch4, ch5, ch6) con identity anchors</name>
  <files>
    public/assets/ch0-bust.png,
    public/assets/ch1-bust.png,
    public/assets/ch2-bust.png,
    public/assets/ch4-bust.png,
    public/assets/ch5-bust.png,
    public/assets/ch6-bust.png
  </files>
  <read_first>
    - .planning/phases/04-chapters-0-2-4-5/04-RESEARCH.md §"Pattern 4: Pixelforge batch 7 busts" (DIFFS per chapter table lines 555-569 + EXECUTION FLOW Steps 1-7 lines 591-608)
    - .planning/phases/04-chapters-0-2-4-5/04-CONTEXT.md §D4-03 (foto→bust mapping por edad)
    - .planning/phases/04-chapters-0-2-4-5/04-UI-SPEC.md §7.3 (aging progression markers per bust + style consistency markers)
    - public/assets/ch3-bust.png (acabado de generar Task 3 — sirve como identity anchor bidirectional)
    - CLAUDE.md §6.1-§6.4 errores pixelforge
    - .claude/skills/crear-arte-pixelforge.md
  </read_first>
  <action>
    **Delegar a `artist-creator` agent.** Loop secuencial sobre los 6 chapters restantes en orden: **ch0, ch1, ch2 (aging-down primero), ch4, ch5, ch6 (aging-up después)**. El ch3-bust.png recién aprobado es el identity anchor bidirectional.

    **4.1 Por cada chapter N en [0, 1, 2, 4, 5, 6]:**

    Llamar `mcp__pixelforge__forge_sprite` con parámetros específicos según D4-03 mapping (verbatim de RESEARCH §Pattern 4):

    | N | DIFF (edad + era visual) | references array | bg preset |
    |---|--------------------------|------------------|-----------|
    | 0 | "age ~10 years old, short dark hair, simple childhood clothing (striped t-shirt), wide curious eyes, slight baby face, 1995 era" | `["public/references/2011.jpg", "public/assets/ch3-bust.png"]` | `"sky"` |
    | 1 | "age ~17 years old, slightly longer hair, casual 2000s teen clothing (band tee or hoodie), faint adolescent features, 2001-2004 era" | `["public/references/2011.jpg", "public/assets/ch3-bust.png"]` | `"sky"` |
    | 2 | "age ~22 years old, casual programmer clothing 2009 era (graphic tee, jeans), clean shaven, young adult, Flash dev era" | `["public/references/2011.jpg", "public/assets/ch3-bust.png"]` | `"sky"` |
    | 4 | "age ~30 years old, professional attire (button-down or polo), short beard, mature expression, AR/VR era 2015-18" | `["public/references/2016.jpeg", "public/assets/ch3-bust.png", "public/references/2019.jpg"]` | `"sky"` |
    | 5 | "age ~36 years old, slightly grayed temples, modern professional attire, well-groomed beard, 2022-23 era" | `["public/references/2022.jpeg", "public/references/2019.jpg", "public/references/2024.jpg"]` | `"sky"` |
    | 6 | "age ~40 years old, distinguished, some gray hair, confident leadership expression, 2026" | `["public/references/2026.jpg", "public/references/2024.jpg"]` | `"sky"` |

    Construcción del `description` final: BASE_PROMPT_AVATAR (RESEARCH §Pattern 4 lines 549-553) + DIFF de la tabla.

    Para **TODOS** los 6 calls:
    - `palette`: **paleta humana §5.6** recibida en Task 1 (misma para los 7 busts — Pitfall 1).
    - `size`: `96`.
    - `outputPath`: `public/assets/ch{N}-bust.png`.

    **4.2 Post-process por bust:**

    - `mcp__pixelforge__process_sprite(operation: "bg_remove")` para cada output.
    - `mcp__pixelforge__optimize_sprite(size: "96x96")` para cada output.
    - Adobe MCP fallback si bg removal deja artefactos: `adobe_mandatory_init()` → `image_remove_background` → `image_crop_and_resize` 96×96.

    **4.3 Identity drift detection (in-loop):**

    Después de cada bust, hacer una verificación heurística rápida (filesize razonable 5-15KB para 96×96 PNG; dimensiones exactas). Si pixelforge entrega algo sospechoso (filesize >50KB sugiere alpha mal aplicado, <1KB sugiere generación vacía), re-prompt con references más explícitos.

    **4.4 NO regenerar ch3-bust.png** — fue aprobado en Task 3.

    **4.5 Estrategia de fallback si pixelforge falla mid-batch:**
    - Si call N falla → reportar al usuario qué bust faltó, NO bloquear los demás.
    - Commits parciales OK: cada bust generado es su propio commit `art(04-01): generate ch{N}-bust.png`.
    - Si pixelforge falla totalmente → bloquear W0 y continuar W1 (CSS-only, no depende de art).
  </action>
  <verify>
    <automated>npm run test:run -- tests/assets/asset-naming.test.js</automated>
    Esperado:
    - T1: PASS (todos los .png cumplen regex).
    - T2: **PASS** (los 7 ch{0..6}-bust.png existen — wave-0-gate green ahora).
    - T3: PASS (no underscores).
    - T4: PASS (.gitignore unchanged).

    Adicional manual smoke:
    - `Get-ChildItem public/assets/ch*-bust.png | Measure-Object` → 7 files.
    - Tamaño total estimado: 35-105 KB (5-15 KB × 7).
  </verify>
  <done>
    - 6 nuevos busts existen en `public/assets/ch{0,1,2,4,5,6}-bust.png`, 96×96, alpha clean.
    - Test asset-naming T2 verde para los 7 busts.
    - 6 commits atómicos `art(04-01): generate ch{N}-bust.png` (uno per chapter para permitir rollback selectivo).
    - O 1 commit batch `art(04-01): generate 6 remaining busts (ch0/1/2/4/5/6) — D4-03 anchors aplicados` si el agente prefiere atomicidad por wave.
  </done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 5: Human review — coherencia visual de los 7 busts (RESUME inmediato)</name>
  <what-built>
    7 busts pixel-art generados con paleta humana §5.6, references escalonadas (D4-03), bg transparente, 96×96 px. El test `asset-naming.test.js` verifica presencia + naming; el ojo humano valida coherencia de personaje + aging progression.
  </what-built>
  <how-to-verify>
    1. Abre cada bust en `public/assets/`:
       ```powershell
       Get-ChildItem public/assets/ch*-bust.png | ForEach-Object { Start-Process $_.FullName }
       ```
       O carga el dev server (`npm run dev`) y haz scroll por los 7 chapters — el `StickyAvatar.vue` swappea automáticamente.

    2. Para cada bust, valida:
       - **Identity coherence:** ¿Es el mismo personaje? Cara, ojos, estructura general consistentes.
       - **Aging progression:** ch0 (niño ~10) → ch1 (adolescente ~17) → ch2 (~22) → ch3 (~26 anchor) → ch4 (~30) → ch5 (~36) → ch6 (~40). Cada bust debe verse claramente mayor que el anterior.
       - **Era-specific markers:** ropa/peinado/accesorios coherentes con la era (camiseta rayada años 90 ch0; estética Web 2.0 ch3; barba moderna ch5; etc.).
       - **Style consistency:** mismo nivel de detalle pixel-art (16-bit chunky), mismo skin tone base (paleta §5.6 aplicada uniformemente).

    3. Si un bust drifteó (no se parece a Rafael, edad incorrecta, ropa anacrónica) → registrar en el resume-signal qué bust(s) y qué ajuste pedir.
  </how-to-verify>
  <resume-signal>
    Rafael responde con:
    - "Aprobado los 7" → marcar W0 cerrado, sign-off W5 ratificará alt-text.
    - "Re-generar ch{N}: <razón>" → executor vuelve a Task 4 solo para ese bust con DIFF ajustado.
    - "Aprobado con observaciones: <list>" → registrar en STATE.md como deferred polish, continuar a W1.

    **Non-blocking:** Si Rafael no responde, W1 (ch0+ch1 CSS-only) NO depende de aprobación visual de busts — puede arrancar en paralelo. W2/W3/W4 SÍ dependen porque cada Chapter{N}Content consume el bust correspondiente.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → repo (commit) | Las fotos de Rafael viven en disco pero NO deben entrar al repo |
| disk → Firebase (deploy, Phase 6) | Las fotos NO deben llegar a producción aunque queden en local |
| dev server (Vite) → red local | Vite sirve `public/references/` en `http://localhost:5173/references/` — aceptable porque solo Rafael accede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `public/references/2011.jpg` ... `2026.jpg` (6 fotos Rafael) | mitigate | `.gitignore` entry `public/references/` añadida en Task 2; verificación `git check-ignore public/references/2011.jpg`. Phase 6 deploy gate añade `firebase.json` ignore glob (D4-02 caveat). |
| T-04-02 | Injection-adjacent | Paleta hex en `palette:` argument de pixelforge | accept | Paleta viene de CONTENT-CHECKLIST §5.6 autor-controlado (Rafael); cero user-input runtime. Validación heurística: cada hex debe matchear `/^#[0-9a-f]{6}$/i` antes de pasar a pixelforge (validar in-loop sin tirar error si falla, solo warn). |
| T-04-03 | Tampering | `references:` array de pixelforge acepta paths del filesystem | accept | Paths son hardcoded literal en código, no construidos desde user-input. pixelforge corre local (no rede backend). |
</threat_model>

<verification>
## Wave-level checks (al finalizar W0)

1. **Asset-naming guard verde:** `npm run test:run -- tests/assets/asset-naming.test.js` → 4/4 tests PASS.
2. **Suite global no regresión:** `npm run test:run` → todos los 216 tests Phase 3 siguen PASS + 4 nuevos asset-naming → 220 PASS.
3. **Build verde:** `npm run build` completa sin errores. Verifica que Vite no copia accidentalmente `public/references/` a `dist/` (debería NO copiarlos si están en `public/` pero Vite respeta solo `.gitignore` para `dist/` exclusion limitedly — **Phase 6 task de deploy validará exclusión de `dist/references/`**).
4. **Privacy gate funcional:** `git check-ignore public/references/2011.jpg` retorna el path (lo trata como ignored).
5. **7 busts presentes y razonablemente sized:** `Get-ChildItem public/assets/ch*-bust.png | Measure-Object -Property Length -Sum` → 7 files, total 35-105 KB rango.

## Out of scope para W0
- Polish HSL/saturación de busts si Rafael lo pide → deferred a W5 manual checklist polish.
- Alt-text era-accurate en es.json/en.json → keys actuales son placeholder; W5 los refresca con drafts §8.2 + Rafael ratifica.
</verification>

<success_criteria>
- 7 PNGs en `public/assets/ch{0..6}-bust.png`, 96×96, alpha clean, paleta humana §5.6 aplicada uniformemente.
- Personaje reconocible y aging progresión verificable a ojo (gate Task 5 Rafael).
- `tests/assets/asset-naming.test.js` 4/4 PASS — guard arquitectural activo para W2/W3/W4.
- `.gitignore` excluye `public/references/` (D4-02 privacy).
- Suite global no regresión: 216 → 220 tests PASS.
- `StickyAvatar.vue` (Phase 1) muestra el bust correcto al scrollear (manual smoke `npm run dev`).
</success_criteria>

<output>
After completion, create `.planning/phases/04-chapters-0-2-4-5/04-01-SUMMARY.md` con:
- Estado de cada bust (path, dimensiones, iteraciones requeridas, references usadas)
- Si pixelforge rechazó JPG references (Pitfall 4) → documentar pivot a description verbal.
- Si paleta §5.6 difirió de la documentada en CONTENT-CHECKLIST → registrar la versión final usada.
- Lecciones para W2/W3/W4 (qué presets bg funcionaron, qué prompts produjeron drift, etc.).
- Smoke `npm run dev` screenshot path o descripción del avatar swap visible.
</output>
