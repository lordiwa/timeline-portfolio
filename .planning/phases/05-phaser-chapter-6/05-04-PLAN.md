---
phase: 05-phaser-chapter-6
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - src/components/Chapter6Content.vue
  - src/components/ScrollShell.vue
  - src/styles/chapter-themes.css
autonomous: true
requirements:
  - PHA-01
  - PHA-02
  - PHA-04
  - PHA-06
  - PHA-07
  - PHA-09
  - CON-04
  - MOB-03
  - A11Y-02
  - A11Y-05
must_haves:
  truths:
    - "`src/components/Chapter6Content.vue` existe y declara `shallowRef(null)` para game state (NUNCA `ref()` ni `reactive()` — Pitfall 1)."
    - "watch(activeChapter) con `{ immediate: true, flush: 'post' }`: cuando v===6 && !game.value, await dynamic `import('@/phaser')` + `createGame(canvasHostRef.value, { prefersReduced })`; cuando v !== 6 && game.value, `game.value.destroy(true, false)` + reset arrivalDone/activeProject."
    - "Bridge IN: `game.value.events.on('vue:show-project', id => activeProject.value = id)` + `game.value.events.on('vue:arrival-complete', () => arrivalDone.value = true)` (PHA-07)."
    - "Bridge OUT: `watch(locale, l => game.value?.events.emit('locale-changed', l))` con null-guard defensive (PHA-06)."
    - "HMR guard: `import.meta.hot?.dispose(() => { game.value?.destroy(true, false); game.value = null })` (Pitfall 3)."
    - "`useResizeObserver(document.documentElement, ...)` recalcula zoom integer + invoca `game.value.scale.setZoom(newZoom)` solo si cambió (Pitfall 8 mitigation) (PHA-09, MOB-03)."
    - "Template renderea `<div class=\"ch6-layout\">` con `<div ref=\"canvasHost\" class=\"ch6-canvas-host\" aria-hidden=\"true\">`, 3 `<button class=\"ch6-planet-trigger sr-only\">` keyboard A11Y (D5-06), `<p v-if=\"arrivalDone\" class=\"ch6-mantra\">{{ t('chapters.6.mantra') }}</p>` + import dinámico de `ProjectOverlay` (placeholder componente en W3 — el componente real es W4)."
    - "`src/components/ScrollShell.vue` importa Chapter6Content + añade `<Chapter6Content v-else-if=\"ch.id === 6\" />` reemplazando el placeholder de chapter 6."
    - "`src/styles/chapter-themes.css` @layer components añade reglas para `.ch6-layout` (position:relative, width 100%, height 100%, **NO overflow:hidden** — Pattern 12 mitigation chapter-overlap bug), `.ch6-canvas-host` (position:absolute, inset:0), `.ch6-mantra` (Pattern 11: bottom 80px, font Audiowide, color #ffd95c, animation mantra-fade-in 400ms, @media PRM animation:none opacity:1)."
    - "Tests components/Chapter6Content + Chapter6Content-lazy + Chapter6Content-bridge + Chapter6Content-resize + Chapter6Content-prm + a11y/keyboard-planet-buttons + integration/chapter-overlap-ch6 todos verdes."
  artifacts:
    - path: "src/components/Chapter6Content.vue"
      provides: "Era-specific component ch6 con Phaser lifecycle + Vue bridge + mantra + 3 sr-only buttons + ProjectOverlay v-if"
      contains: "shallowRef"
      min_lines: 80
    - path: "src/components/ScrollShell.vue"
      provides: "v-else-if=\"ch.id === 6\" wired a Chapter6Content"
      contains: "Chapter6Content"
    - path: "src/styles/chapter-themes.css"
      provides: "@layer components reglas .ch6-layout + .ch6-canvas-host + .ch6-mantra"
      contains: ".ch6-mantra"
  key_links:
    - from: "src/components/Chapter6Content.vue"
      to: "src/phaser/index.js"
      via: "dynamic import('@/phaser') en watch(activeChapter) — Vite splittable"
      pattern: "await\\s+import\\(['\"]@/phaser['\"]\\)"
    - from: "src/components/Chapter6Content.vue"
      to: "src/data/projects.js"
      via: "computed filter chapterEra=6 para 3 sr-only buttons keyboard A11Y"
      pattern: "projects\\.filter\\(.*chapterEra.*===\\s*6"
    - from: "src/components/ScrollShell.vue"
      to: "src/components/Chapter6Content.vue"
      via: "import + v-else-if branch"
      pattern: "Chapter6Content"
    - from: "src/styles/chapter-themes.css .ch6-layout"
      to: "Pattern 12 mitigation"
      via: "absence of overflow:hidden in .ch6-layout block"
      pattern: "\\.ch6-layout\\s*\\{[^}]*\\}"
---

<objective>
W3 monta el "shell Vue" del chapter 6: el componente `Chapter6Content.vue` que orquesta el lifecycle Phaser (lazy import + mount + destroy + HMR dispose), el bridge bidireccional (Vue ↔ Phaser para locale + project click + arrival complete), el ResizeObserver para zoom integer (Pitfall 8 mitigation), los 3 sr-only buttons keyboard A11Y (D5-06), el mantra HTML/Vue v-if (D5-03 + CON-04), y un placeholder `<ProjectOverlay>` v-if (componente real implementado en W4). Wire en ScrollShell + reglas CSS chapter-overlap-defensive en chapter-themes.css.

Purpose: dejar el chapter 6 funcionalmente "completo" en términos de Phaser lifecycle + Vue integration. ProjectOverlay v-if se renderea como stub (W4 lo implementa completo). Al final de W3, abrir el sitio + scroll a ch6 muestra: BackgroundLayers crossfade (W0 + W1), canvas Phaser monta (W2 + W3), escena con planets/ships/arrival visible, click en planet emite event y `activeProject` se setea (overlay placeholder), mantra HTML aparece al fin del arrival.

Output: 2 archivos modificados (ScrollShell.vue, chapter-themes.css) + 1 archivo nuevo (Chapter6Content.vue). Tests Chapter6Content suite + keyboard A11Y + chapter-overlap defensive verdes.

**Slice-end-to-end check:** tras este plan, dev preview de ch6 renderea la escena Phaser real con planets visibles + ships en loop + mantra fade-in al fin del arrival. Click en planet abre placeholder overlay (W4 lo polished). Avance VISIBLE major end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-phaser-chapter-6/05-CONTEXT.md
@.planning/phases/05-phaser-chapter-6/05-RESEARCH.md
@.planning/phases/05-phaser-chapter-6/05-PATTERNS.md
@.planning/phases/05-phaser-chapter-6/05-03-SUMMARY.md
@src/components/ScrollShell.vue
@src/components/Chapter4Content.vue
@src/components/SkipLink.vue
@src/styles/chapter-themes.css
@src/composables/useScrollState.js
@src/composables/usePRM.js
@src/data/projects.js
@src/data/chapters.js
@tests/components/Chapter6Content.test.js
@tests/components/Chapter6Content-lazy.test.js
@tests/components/Chapter6Content-bridge.test.js
@tests/components/Chapter6Content-resize.test.js
@tests/components/Chapter6Content-prm.test.js
@tests/a11y/keyboard-planet-buttons.test.js
@tests/integration/chapter-overlap-ch6.test.js

<interfaces>
<!-- Chapter6Content.vue contract (consumed by ScrollShell.vue v-else-if branch). -->

Component contract:
```vue
<Chapter6Content v-else-if="ch.id === 6" />
<!-- No props expected — consume injects scrollState (activeChapter) + prm (prefersReduced) -->
```

Injects required (provided by App.vue Phase 1):
- `scrollState`: { activeChapter: Ref<number>, scrollProgress, scrollToChapter }
- `prm`: { prefersReduced: Ref<boolean> }

State refs internal:
- `game: ShallowRef<Phaser.Game | null>` — PHA-01 (NEVER ref()/reactive())
- `arrivalDone: Ref<boolean>` — toggled by 'vue:arrival-complete' event
- `activeProject: Ref<string | null>` — toggled by 'vue:show-project' event
- `ch6Projects: ComputedRef<Project[]>` — projects.filter(p => p.chapterEra === 6)
- `canvasHostRef: TemplateRef<HTMLDivElement>` — Phaser parent element

ScrollShell.vue diff:
- Add: `import Chapter6Content from './Chapter6Content.vue'`
- Add: `<Chapter6Content v-else-if="ch.id === 6" />` BEFORE `<div v-else class="chapter-placeholder">`
- Note: el `<div v-else>` placeholder se queda como dead-branch defensive (no hay otro chapter sin component custom post-Phase 5, pero mantener defensive evita crash si alguien añade ch7).

ProjectOverlay placeholder import (W3):
- Importar `ProjectOverlay` como dynamic `defineAsyncComponent(() => import('./ProjectOverlay.vue'))` O import directo si el archivo existe en W3 como stub mínimo.
- W3 puede crear un stub mínimo de ProjectOverlay.vue (10 LOC: solo emite 'close' en click) que W4 reemplaza completo.
- Alternativa: usar `<component :is="activeProject ? ProjectOverlayLazy : null" />` para evitar crash si el archivo falta.
- Decisión recommended: crear stub mínimo `ProjectOverlay.vue` en W3 (5-10 LOC: section role=dialog + close button + emit close on click/Escape) → W4 reemplaza con la implementación synthwave completa. Stub permite render visible end-to-end W3.
</interfaces>

<analog_pattern>
**`Chapter4Content.vue`** (Plan 04-04) es el closest analog para el shell Vue (inject scrollState + prm, computed projects.filter(p => p.chapterEra === N), SFC structure). 05-PATTERNS líneas 194-303 mapea match ~40%. Diferencias clave:
- Chapter4Content usa layout 2-col (grid) — Chapter6Content NO (canvas full-bleed D5-09).
- Chapter4Content NO usa Phaser — Chapter6Content sí (lifecycle Phaser único en esta era).
- Chapter4Content tiene `aside.ch4-meta` con avatar — Chapter6Content NO (StickyAvatar Phase 1 ya provee chapters[6].avatarSrc global).
- Chapter4Content.layout tiene `overflow: hidden` para contener parallax — Chapter6Content NO (Pattern 12 mitigation chapter-overlap bug).

**`SkipLink.vue`** establece el patrón `.sr-only` CSS — Chapter6Content reusa la misma clase global (no duplica).

**Chapter6Content skeleton verbatim** disponible en 05-RESEARCH líneas 399-500 + 05-PATTERNS líneas 224-298.

**ScrollShell.vue diff** trivial: 1 import + 1 v-else-if branch (analog Phase 4 Plans 04-02/03/04/05 que añadieron Chapter{0,1,2,3,4,5}Content).
</analog_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/components/Chapter6Content.vue — lifecycle Phaser + bridge + ResizeObserver + 3 sr-only buttons + mantra v-if</name>
  <files>src/components/Chapter6Content.vue, src/components/ProjectOverlay.vue</files>
  <behavior>
    - Chapter6Content.vue setup: imports `shallowRef`, `ref`, `watch`, `onBeforeUnmount`, `inject`, `useTemplateRef`, `nextTick`, `computed` desde 'vue'; `useI18n` desde 'vue-i18n'; `useResizeObserver` desde '@vueuse/core'; `chapters` desde '@/data/chapters'; `projects` desde '@/data/projects'; `ProjectOverlay` desde './ProjectOverlay.vue' (stub creado en este mismo task).
    - `const { activeChapter } = inject('scrollState')`; `const { prefersReduced } = inject('prm')`; `const { locale, t } = useI18n()`.
    - `const game = shallowRef(null)` (PHA-01).
    - `const canvasHostRef = useTemplateRef('canvasHost')`.
    - `const arrivalDone = ref(false)`; `const activeProject = ref(null)`.
    - `const ch6Projects = computed(() => projects.filter(p => p.chapterEra === 6))`.
    - watch(activeChapter, async (v) => { ... }, { immediate: true, flush: 'post' }): mount/destroy lifecycle Pattern 1 verbatim. Defensive `if (!canvasHostRef.value) await nextTick()`. `const { createGame } = await import('@/phaser')`. registra listeners 'vue:show-project' + 'vue:arrival-complete'. destroy(true, false) en el else branch + reset state.
    - watch(locale, l => game.value?.events.emit('locale-changed', l)) — null-guard defensive (PHA-06).
    - useResizeObserver(document.documentElement, () => { if (!game.value) return; recompute zoom integer; if newZoom !== game.value.scale.zoom → game.value.scale.setZoom(newZoom) }) — Pitfall 8 mitigation (PHA-09, MOB-03).
    - if (import.meta.hot) import.meta.hot.dispose(...) → destroy(true, false) (Pitfall 3).
    - onBeforeUnmount(() => game.value?.destroy(true, false)) — defensive cleanup.
    - Template: `<div class="ch6-layout">` con `<div ref="canvasHost" class="ch6-canvas-host" aria-hidden="true" />` + `<button v-for="p in ch6Projects" class="ch6-planet-trigger sr-only" :aria-label="t('ui.openProject') + ': ' + t(p.titleKey)" @click="activeProject = p.id" type="button">` (D5-06) + `<p v-if="arrivalDone" class="ch6-mantra">{{ t('chapters.6.mantra') }}</p>` (D5-03) + `<ProjectOverlay v-if="activeProject" :project-id="activeProject" @close="activeProject = null" />` (W4 reemplaza el stub).
    - `<style scoped>` mínimo (la mayoría del styling va a chapter-themes.css @layer components Task 2).

    - ProjectOverlay.vue STUB (creado en este task — W4 reemplaza completo):
      - Template mínimo: `<section role="dialog" aria-modal="true" class="project-overlay" @click="emit('close')"><button type="button" :aria-label="t('ui.closeOverlay')" @click.stop="emit('close')">×</button><p>STUB · W4 replaces · projectId: {{ projectId }}</p></section>`.
      - Props: `projectId: { type: String, required: true }`.
      - Emits: `['close']`.
      - Mínimo viable para que Chapter6Content render no crashee en W3 + tests Chapter6Content-bridge verifiquen el v-if. Test ProjectOverlay.test.js sigue RED hasta W4.
  </behavior>
  <action>Crear `src/components/Chapter6Content.vue` siguiendo RESEARCH §Pattern 1 skeleton (líneas 399-500) verbatim. Estructura: `<script setup>` con imports y reactive state, watch lifecycle, watch locale, useResizeObserver, HMR guard, onBeforeUnmount; `<template>` con .ch6-layout + canvas host + sr-only buttons + mantra v-if + ProjectOverlay v-if; `<style scoped>` mínimo solo para reglas que NO van al global @layer components (e.g., posicionamiento de la sr-only override si necesario).

  Crear `src/components/ProjectOverlay.vue` STUB en este mismo task con el template mínimo descrito en behavior — propósito único: permitir render end-to-end W3 sin crash y permitir tests Chapter6Content-bridge verde (el v-if=activeProject) renderea algo. W4 reemplaza completo con synthwave glass + focus trap + ESC. STUB tiene `<script setup>defineProps(['projectId']); defineEmits(['close']); const { t } = useI18n()</script>` minimal.

  Verificar manualmente que el código pasa los tests Chapter6Content suite:
  - Chapter6Content.test.js T1 (PHA-04 lazy import called when activeChapter=6).
  - T2 (PHA-02 destroy(true, false) called when activeChapter !== 6).
  - T3 (shallowRef regex en source).
  - T4 (registra 2 listeners en game.events).
  - Chapter6Content-lazy.test.js T1 (await import('@/phaser') string-literal).
  - T2 (NO top-level import Phaser).
  - Chapter6Content-bridge.test.js T1-T3 (vue:show-project → activeProject set, vue:arrival-complete → arrivalDone, activeChapter reset).
  - Chapter6Content-resize.test.js T1 (useResizeObserver(document.documentElement)), T2 (setZoom only if newZoom !== current — Pitfall 8 guard).
  - Chapter6Content-prm.test.js T1 (prefersReduced.value pasado a createGame), T2 (mantra @media PRM en CSS).
  - a11y/keyboard-planet-buttons.test.js T1-T3 (3 sr-only buttons + aria-label + Tab nav order).

  Per D5-09 (canvas full-bleed) + D5-11 (lifecycle pattern verbatim) + Pitfall 1 (shallowRef) + Pitfall 2 (canvas ref defensive) + Pitfall 3 (HMR dispose) + Pitfall 8 (resize loop guard).</action>
  <verify>
    <automated>npm run test -- --run tests/components/Chapter6Content tests/components/Chapter6Content-lazy tests/components/Chapter6Content-bridge tests/components/Chapter6Content-resize tests/components/Chapter6Content-prm tests/a11y/keyboard-planet-buttons</automated>
  </verify>
  <done>Chapter6Content.vue creado con lifecycle Phaser completo PHA-01/02/04/06/07/09; ProjectOverlay.vue stub mínimo creado; 6 archivos test (Chapter6Content suite + keyboard a11y) verdes; suite global no regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ScrollShell.vue + finalizar chapter-themes.css @layer components (ch6-layout + canvas-host + mantra + Pattern 12 mitigation)</name>
  <files>src/components/ScrollShell.vue, src/styles/chapter-themes.css</files>
  <behavior>
    - ScrollShell.vue imports `Chapter6Content` desde './Chapter6Content.vue' después de Chapter5Content.
    - Template añade `<Chapter6Content v-else-if="ch.id === 6" />` antes del `<div v-else class="chapter-placeholder">`. El v-else placeholder se queda como defensive (dead-branch — no afecta render porque ch6 ahora tiene component).
    - chapter-themes.css @layer components añade bloque ch6 con reglas:
      - `[data-chapter="6"] .ch6-layout { position: relative; width: 100%; height: 100%; /* NO overflow:hidden — Pattern 12 mitigation chapter-overlap bug deferred Phase 4 */ }`
      - `[data-chapter="6"] .ch6-canvas-host { position: absolute; inset: 0; }`
      - `[data-chapter="6"] .ch6-canvas-host canvas { display: block; }` (no margin/border/etc — image-rendering pixelated ya global en index.html)
      - `[data-chapter="6"] .ch6-mantra { position: absolute; bottom: calc(80px + env(safe-area-inset-bottom, 0)); left: 50%; transform: translateX(-50%); font-family: 'Audiowide', sans-serif; font-size: clamp(1rem, 3vw, 1.5rem); color: #ffd95c; text-shadow: 0 0 12px rgba(255, 217, 92, 0.6); opacity: 0; animation: mantra-fade-in 400ms ease-out forwards; pointer-events: none; text-align: center; white-space: nowrap; z-index: 40; }`
      - `@keyframes mantra-fade-in { from { opacity: 0; transform: translate(-50%, 8px); } to { opacity: 1; transform: translate(-50%, 0); } }`
      - `@media (prefers-reduced-motion: reduce) { [data-chapter="6"] .ch6-mantra { animation: none; opacity: 1; } }` (D5-08, A11Y-05).
      - `.ch6-planet-trigger.sr-only` puede reusar `.sr-only` global (Phase 2 SkipLink establece la clase) — verificar; si no existe global, declarar inline aquí (es práctica común evitar duplicación).
    - integration/chapter-overlap-ch6.test.js T1-T4 verdes (Pattern 12 verifications).
    - components/Chapter6Content-prm.test.js T2 verde (mantra @media PRM presente).
  </behavior>
  <action>Editar `src/components/ScrollShell.vue`: añadir import `Chapter6Content` en el bloque de imports después de Chapter5Content (línea ~29). Añadir branch `<Chapter6Content v-else-if="ch.id === 6" />` en el template después de Chapter5Content y antes del `<div v-else class="chapter-placeholder">` (línea ~95). Mantener el placeholder div como dead-branch defensive (comentar `<!-- defensive: dead branch post-Phase 5; protects against future ch7+ -->`).

  Editar `src/styles/chapter-themes.css`: localizar el `@layer components` block (línea ~158). Después de las reglas Phase 4 (FlashBanner, etc.), antes del cierre del `}` de @layer components, añadir un sub-bloque "Phase 5 — ch6 canvas + mantra (D5-03, D5-09, Pattern 12)" con las reglas del behavior. Verificar visualmente que la regla `.ch6-layout` NO tiene `overflow: hidden` (Pattern 12 mitigation chapter-overlap bug — el integration test T2/T3 lo verifica via regex).

  Per D5-09 (canvas full-bleed) + Pattern 12 (chapter-overlap mitigation) + D5-03 (mantra HTML) + D5-08 (PRM heuristic mantra sin fade).</action>
  <verify>
    <automated>npm run test -- --run tests/integration/chapter-overlap-ch6.test.js tests/components/Chapter6Content-prm.test.js tests/styles/themes-file.test.js</automated>
  </verify>
  <done>ScrollShell.vue importa + wires Chapter6Content; chapter-themes.css declara @layer components reglas .ch6-* sin overflow:hidden en .ch6-layout; integration/chapter-overlap-ch6.test.js T1-T4 verdes; themes-file.test.js sigue verde; suite global no regression. **Slice end-to-end check manual:** `npm run dev` + scroll a ch6 = canvas Phaser visible con planets/ships/arrival + mantra HTML fade-in al fin + click planet abre stub overlay (mensaje "STUB · W4 replaces").</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vue Chapter6Content → Phaser sandbox | watch lifecycle controla mount/destroy; el game NO debe acceder al Vue tree directo |
| Bridge events (game.events) | Trust: solo Chapter6Content emite/escucha; otros componentes no acceden |
| ResizeObserver → game.scale | API call controlado por if (!game.value) return guard |
| HMR dev → game instances | dispose callback cleanup previene leak en dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W3-01 | DoS | HMR accumulates game instances (Pitfall 3) | mitigate | `import.meta.hot?.dispose(() => game.value?.destroy(true, false))` declared |
| T-05-W3-02 | DoS | ResizeObserver infinite loop (Pitfall 8) | mitigate | `if (newZoom !== game.value.scale.zoom)` guard breaks the loop |
| T-05-W3-03 | Tampering | Bridge event 'vue:show-project' con id inválido | mitigate | W4 ProjectOverlay (próxima wave) usa `projects.find(p => p.id === id)` con null guard; aquí stub solo muestra texto |
| T-05-W3-04 | Spoofing | locale-changed event emit fuera de Chapter6Content | mitigate | watch(locale) en Chapter6Content único emitter; otros componentes no acceden a `game.value` (privado al component) |
| T-05-W3-05 | DoS | Bundle Phaser cargado en initial chunk (PHA-04 violation) | mitigate | `await import('@/phaser')` dynamic string-literal — Chapter6Content-lazy.test.js T1 verifica regex; T2 confirma absence de top-level `import Phaser` |
| T-05-W3-06 | Information disclosure | Pixel-perfect render NOT achieved en HiDPI | accept | jsdom no verifica; W5 manual checklist confirma con Rafael en monitor Retina/HiDPI |
| T-05-W3-07 | Repudiation | Chapter overlap bug regression no detected | mitigate | integration/chapter-overlap-ch6.test.js T1-T4 source-regex defensive enforcement |
</threat_model>

<verification>
- `npm run test -- --run tests/components/Chapter6Content tests/components/Chapter6Content-lazy tests/components/Chapter6Content-bridge tests/components/Chapter6Content-resize tests/components/Chapter6Content-prm tests/a11y/keyboard-planet-buttons tests/integration/chapter-overlap-ch6.test.js`: todos verdes.
- `npm run test:run` global: 151 baseline + Phase 5 W0-W3 verdes; tests RED restantes (ProjectOverlay + focus-trap) verdes en W4.
- `npm run dev` + browser scroll a ch6: visualmente la escena Phaser renderea (planets + ships + arrival camera + mantra fade-in) + click planet emite event + stub overlay aparece.
- `npm run build`: chunk Phaser tree-shaken separado en `dist/assets/*phaser*.js`. (Verificación opcional W3; mandatory W5 manual checklist.)
- Frontmatter validation: `gsd-sdk query frontmatter.validate ".planning/phases/05-phaser-chapter-6/05-04-PLAN.md" --schema plan`.
</verification>

<success_criteria>
- src/components/Chapter6Content.vue declara `shallowRef(null)` para game (PHA-01 enforced).
- watch(activeChapter) lifecycle: lazy import Phaser, mount/destroy idempotent, listeners IN registrados.
- watch(locale) bridge OUT con null-guard.
- useResizeObserver(document.documentElement) con Pitfall 8 guard (setZoom only if changed).
- import.meta.hot?.dispose() declared (Pitfall 3 mitigation).
- 3 sr-only buttons keyboard A11Y con aria-label i18nificado (D5-06).
- mantra HTML v-if=arrivalDone con CSS animation 400ms + PRM override animation:none (D5-03 + D5-08).
- ScrollShell.vue wired con `<Chapter6Content v-else-if="ch.id === 6" />`.
- chapter-themes.css `[data-chapter="6"] .ch6-layout` SIN overflow:hidden (Pattern 12 mitigation verified by test).
- ProjectOverlay.vue stub mínimo creado (W4 reemplaza completo).
- 7 archivos test verdes (Chapter6Content suite 5 + keyboard a11y + chapter-overlap-ch6 integration).
</success_criteria>

<output>
After completion, create `.planning/phases/05-phaser-chapter-6/05-04-SUMMARY.md` que incluya: confirmación de Pattern 12 mitigation (.ch6-layout NO tiene overflow:hidden), chunk size Phaser verificado en build (target ≤200KB gzipped), screenshot/notes del primer dev preview con scene visible + arrival animation + mantra HTML + stub overlay click feedback.
</output>
