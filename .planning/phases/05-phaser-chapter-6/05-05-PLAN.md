---
phase: 05-phaser-chapter-6
plan: 05
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-04
files_modified:
  - src/components/ProjectOverlay.vue
  - src/styles/chapter-themes.css
autonomous: true
requirements:
  - CON-04
  - PHA-07
  - A11Y-05
must_haves:
  truths:
    - "`src/components/ProjectOverlay.vue` es modal Vue synthwave completo: `role=\"dialog\" aria-modal=\"true\"` + backdrop blur deep purple + card con border cyan + double glow (cyan + pink) (D5-07)."
    - "**Template incluye null guard `v-if=\"project\"` wrapper** que previene crash si `projects.find(...)` retorna undefined (threat T-05-W4-01 mitigation; Warning 9 RESOLVED). Mount con `projectId='ch6-nonexistent'` NO crashea — render queda vacío."
    - "ESC key cierra: `document.addEventListener('keydown', handleKeydown)` con handler emit('close') si key === 'Escape'."
    - "Click outside (backdrop) cierra: `handleBackdropClick` valida `e.target === overlayRef.value` (no en card interior)."
    - "Close button con `aria-label=\"Cerrar\"` (i18nificado via `t('ui.closeOverlay')`); ESC y click outside también disparan emit('close')."
    - "Focus trap manual (RESEARCH §Pattern 10, ~30 LOC): keydown Tab cycla entre focusables querySelectorAll; Shift+Tab desde primer focusable → último; Tab desde último → primero."
    - "onMounted captura `lastFocusedEl = document.activeElement` + setTimeout focus al closeBtn; onBeforeUnmount restore focus a lastFocusedEl (RESEARCH §Pattern 10)."
    - "Card renderea title + year + role + techStack chips + descripción + link `target=\"_blank\" rel=\"noopener noreferrer\"` (T-05-W4-02 tabnabbing mitigation)."
    - "Mobile fullscreen (@media max-width 599px): `max-width: 100vw; height: 100dvh; border-radius: 0; border-left: none; border-right: none`."
    - "Animación fade+scale 200ms ease-out al abrir; instant bajo `@media (prefers-reduced-motion: reduce)` (D5-08, A11Y-05)."
    - "Tests components/ProjectOverlay.test.js (T1-T6) + a11y/focus-trap.test.js verdes."
  artifacts:
    - path: "src/components/ProjectOverlay.vue"
      provides: "Modal Vue synthwave neon-glow con focus trap manual + ESC/click-outside/restore focus + null guard projectId"
      contains: "role=\"dialog\""
      min_lines: 80
    - path: "src/styles/chapter-themes.css"
      provides: "@layer components reglas .project-overlay (backdrop blur + card glow doble cyan+pink + mobile fullscreen + PRM no-animation)"
      contains: ".project-overlay"
  key_links:
    - from: "src/components/ProjectOverlay.vue"
      to: "src/data/projects.js"
      via: "computed projects.find(p => p.id === props.projectId) + v-if=\"project\" null guard"
      pattern: "projects\\.find\\("
    - from: "src/components/ProjectOverlay.vue"
      to: "i18n.global.t"
      via: "useI18n() — title + close aria + tech labels"
      pattern: "useI18n"
    - from: "src/components/Chapter6Content.vue"
      to: "src/components/ProjectOverlay.vue"
      via: "v-if=\"activeProject\" :project-id @close"
      pattern: "<ProjectOverlay"
---

<objective>
W4 reemplaza el stub `ProjectOverlay.vue` (W3) con el componente synthwave completo: modal Vue con backdrop blur deep purple + card border cyan + double glow (cyan + pink) + focus trap manual (~30 LOC en lugar de @vueuse/integrations useFocusTrap — decisión Open Q3 RESOLVED documentada en Don't Hand-Roll §10) + ESC cierra + click-outside cierra + restore focus al planet-trigger originador + mobile fullscreen responsive + PRM instant + **null guard `v-if="project"` para tolerar projectId inválido** (Warning 9 RESOLVED).

Purpose: cerrar el bucle Vue↔Phaser → overlay UI. Click en un planet en SpaceScene (W2) emite `show-project` (W2) → Chapter6Content.vue setea `activeProject = id` (W3) → ProjectOverlay v-if (W3 stub) renderea con el contenido del proyecto correcto y A11Y completo + tolera projectIds inválidos sin crash.

Output: 1 archivo Vue reemplazado completamente (ProjectOverlay.vue) + 1 archivo CSS modificado (chapter-themes.css @layer components). Tests ProjectOverlay (T1-T6) + focus-trap verdes.

**Slice-end-to-end check:** tras este plan, abrir el sitio + scroll a ch6 + click en cualquier planet → overlay synthwave aparece con título/role/techStack/desc del proyecto correcto + ESC cierra + click outside cierra + click close-X cierra + restore focus al planet-button trigger + Tab dentro del overlay cycla entre close + (eventual) link. Slice COMPLETO end-to-end ch6. Adicionalmente: mount con projectId inválido (defensive test T6) NO crashea.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-phaser-chapter-6/05-CONTEXT.md
@.planning/phases/05-phaser-chapter-6/05-RESEARCH.md
@.planning/phases/05-phaser-chapter-6/05-PATTERNS.md
@.planning/phases/05-phaser-chapter-6/05-04-SUMMARY.md
@src/components/ProjectOverlay.vue
@src/components/FloatingPanel.vue
@src/components/ContactHUD.vue
@src/components/LangToggle.vue
@src/components/SkipLink.vue
@src/styles/chapter-themes.css
@src/data/projects.js
@src/i18n/es.json
@src/i18n/en.json
@tests/components/ProjectOverlay.test.js
@tests/a11y/focus-trap.test.js

<interfaces>
<!-- ProjectOverlay.vue contract (already consumed by Chapter6Content.vue W3 via stub). -->

Component contract (consumer = Chapter6Content.vue W3):
```vue
<ProjectOverlay
  v-if="activeProject"
  :project-id="activeProject"
  @close="activeProject = null"
/>
```

Props:
```
{ projectId: { type: String, required: true } }  // 'ch6-ar-vr' | 'ch6-remoose' | 'ch6-software-mind' | (defensive: any string — null guard tolerates invalid)
```

Emits:
```
['close']  // disparado por ESC, click-outside backdrop, click close button
```

Data lookup with null guard (Warning 9 RESOLVED):
```
const project = computed(() => projects.find(p => p.id === props.projectId))
// project.value: { titleKey, descKey, year, role, techStack[], link } | undefined
// Template usa v-if="project" wrapper para evitar crash si lookup retorna undefined.
```

i18n keys consumidas (W0 las creó):
- `t('ui.closeOverlay')` — aria-label del close button
- `t(project.titleKey)` — title del card (solo dentro de v-if=project)
- `t(project.descKey)` — descripción (solo dentro de v-if=project)
- `t('ui.openProject')` — link "Visit project →" (opcional, si project.link existe)

DOM/A11Y contract:
- `role="dialog"` `aria-modal="true"` `aria-labelledby="project-${projectId}-title"` en backdrop root.
- `<button ref="closeBtn">` primer focusable (focus al mount) — siempre presente (incluso si project es undefined, para que el modal tenga al menos un focus target).
- `<h2 id="project-${projectId}-title">` para aria-labelledby binding (dentro de v-if=project).
- `<a target="_blank" rel="noopener noreferrer">` para link (T-05-W4-02).

Focus trap manual contract (decisión Open Q3 RESOLVED — vs @vueuse/integrations):
- Manual ~30 LOC chosen sobre @vueuse/integrations useFocusTrap (peer dep focus-trap ~8KB).
- focusables = overlayRef.querySelectorAll('a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])').
- Tab desde último → first.focus() + preventDefault.
- Shift+Tab desde primero → last.focus() + preventDefault.

Null guard contract (Warning 9 RESOLVED — threat T-05-W4-01 mitigation):
- Template usa `v-if="project"` wrapper sobre el contenido del card (title/year/role/techStack/desc/link).
- Si `project` es undefined (projectId no existe en projects.js), el modal renderea solo el shell (backdrop + close button) sin contenido.
- ESC + click-outside + close button siguen funcionando para cerrar.
- Test T6 verifica: mount con `projectId="ch6-nonexistent"` → no crash; `wrapper.find('.project-overlay__title').exists()` === false.
</interfaces>

<analog_pattern>
**NO direct analog** (D5-07 explicit "NO reusa ProjectCard ni FloatingPanel"). Pero loose analogs:

- **FloatingPanel.vue** (Phase 4 ch4): pattern de minimal SFC con title prop + slot — referencia estructural (5-15 LOC), pero styling era distinta (glass holographic vs synthwave neon).
- **ContactHUD.vue** (Phase 3): position:fixed + z-index layering + aria-label patterns + SVG inline icons — referencia para `role="dialog"` layout structure.
- **LangToggle.vue**: keydown handler pattern (Phase 2) — referencia para event listener cleanup.
- **SkipLink.vue**: `.sr-only` + focus-visible pattern (Phase 1) — referencia para focus management básico.

**Source-of-truth verbatim**: RESEARCH §Pattern 10 líneas 947-1059 — skeleton completo de ProjectOverlay copy + adapt.

**Focus trap pattern**: RESEARCH líneas 979-995 manual implementation chosen over `@vueuse/integrations useFocusTrap` per Don't Hand-Roll table (8KB peer dep vs ~30 LOC manual is leaner — scope is 3-5 focusables, no complex tab order).
</analog_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reemplazar src/components/ProjectOverlay.vue (stub W3 → componente synthwave completo con focus trap manual + null guard)</name>
  <files>src/components/ProjectOverlay.vue</files>
  <behavior>
    - El componente respeta el contract de interfaces (props projectId, emits close).
    - `<script setup>` imports: ref, onMounted, onBeforeUnmount, computed, inject, useTemplateRef desde 'vue'; useI18n desde 'vue-i18n'; projects desde '@/data/projects'.
    - `const { t } = useI18n()`; `const { prefersReduced } = inject('prm')`; `const project = computed(() => projects.find(p => p.id === props.projectId))`.
    - `const overlayRef = useTemplateRef('overlay')`; `const closeBtnRef = useTemplateRef('closeBtn')`; `let lastFocusedEl = null`.
    - handleKeydown(e): si key === 'Escape' → emit('close'); si key === 'Tab' → trapTab(e).
    - trapTab(e): focusables = overlayRef.querySelectorAll(...); first/last guard; preventDefault + cycle.
    - handleBackdropClick(e): si e.target === overlayRef.value → emit('close').
    - onMounted: lastFocusedEl = document.activeElement; setTimeout(() => closeBtnRef.value?.focus(), 0); document.addEventListener('keydown', handleKeydown).
    - onBeforeUnmount: document.removeEventListener('keydown', handleKeydown); lastFocusedEl?.focus().
    - Template root `<div ref="overlay" class="project-overlay" role="dialog" aria-modal="true" :aria-labelledby="project ? \`project-\${projectId}-title\` : null" @click="handleBackdropClick">`.
    - Close button SIEMPRE presente (ref="closeBtn", type="button", `:aria-label="t('ui.closeOverlay')"`, `@click="emit('close')"`).
    - **Card wrapper con `v-if="project"`** (Warning 9 RESOLVED — null guard): si project existe, renderea title h2 con id matching, year, role, techStack ul, descripción p, link a v-if=project.link target="_blank" rel="noopener noreferrer". Si project es undefined, solo el shell (backdrop + close button) — modal cierra normalmente con ESC/click-outside/close.
    - ProjectOverlay.test.js T1-T6 verdes:
      - T1: ESC cierra.
      - T2: click backdrop cierra.
      - T3: content render con projectId válido (title/role/techStack/desc).
      - T4: aria-label="Cerrar" en close button.
      - T5: rel="noopener noreferrer" en target="_blank" link.
      - **T6 (Warning 9 RESOLVED):** mount con projectId="ch6-nonexistent" → no crash; `wrapper.find('.project-overlay__title').exists()` === false; close button sigue presente y funcional.
    - focus-trap.test.js T1-T3 verdes (Tab cycle, Shift+Tab cycle, focus restore).
  </behavior>
  <action>Reemplazar el contenido COMPLETO de `src/components/ProjectOverlay.vue` (el stub mínimo W3 cae) con la implementación verbatim de RESEARCH §Pattern 10 (líneas 947-1059), **con la adición del null guard `v-if="project"` en el card wrapper** (Warning 9 RESOLVED). Estructura: `<script setup>` con imports + reactive state + handleKeydown + trapTab + handleBackdropClick + onMounted/onBeforeUnmount lifecycle; `<template>` con backdrop root + close button (siempre presente) + card wrapper `v-if="project"` que contiene title + meta (year/role) + techStack ul + descripción + link v-if; `<style scoped>` MÍNIMO (la mayoría del styling synthwave va a chapter-themes.css @layer components Task 2 — solo dejar reglas reutilizables genéricas aquí o nada).

  Verificar manualmente que el código:
  - `role="dialog"` y `aria-modal="true"` están en el root.
  - `aria-labelledby="project-${projectId}-title"` binding al h2 id solo cuando project existe (usar `:aria-labelledby="project ? ... : null"`).
  - `target="_blank"` link tiene `rel="noopener noreferrer"` (threat T-05-W4-02 enforced; test ProjectOverlay.test.js T5 lo verifica).
  - `defineProps({ projectId: { type: String, required: true } })`.
  - `defineEmits(['close'])`.
  - Manual focus trap (NO `@vueuse/integrations useFocusTrap` — Open Q3 RESOLVED decision documented en SUMMARY).
  - **Card content envuelto en `v-if="project"`** (Warning 9 RESOLVED — threat T-05-W4-01 mitigation).

  Per D5-07 (overlay structural distinct) + RESEARCH §Pattern 10 (verbatim skeleton) + threat T-05-W4-02 (tabnabbing) + threat T-05-W4-01 (null guard projectId).</action>
  <verify>
    <automated>npm run test -- --run tests/components/ProjectOverlay.test.js tests/a11y/focus-trap.test.js</automated>
  </verify>
  <acceptance_criteria>
    - ProjectOverlay.vue source contiene `role="dialog"` y `aria-modal="true"` en el root.
    - ProjectOverlay.vue source contiene `const project = computed(() => projects.find(p => p.id === props.projectId))`.
    - **ProjectOverlay.vue template contiene `v-if="project"` wrapper** (o accesos defensive como `t(project?.titleKey)`) que previene crash si project lookup retorna undefined (Warning 9 RESOLVED).
    - ProjectOverlay.vue source contiene `rel="noopener noreferrer"` en `target="_blank"` link (T-05-W4-02 tabnabbing mitigation).
    - ProjectOverlay.vue source contiene `document.addEventListener('keydown', handleKeydown)` en onMounted + matching removeEventListener en onBeforeUnmount.
    - ProjectOverlay.vue source contiene manual focus trap querySelectorAll + first/last cycle (NO `import { useFocusTrap } from '@vueuse/integrations'`).
    - Close button siempre presente y funcional, incluso cuando project es undefined.
    - Mount con `projectId="ch6-nonexistent"` no crashea (test T6 GREEN).
  </acceptance_criteria>
  <done>ProjectOverlay.vue completo con synthwave structure + ESC + click-outside + focus trap manual + restore focus + **null guard v-if="project"**; ProjectOverlay.test.js T1-T6 + focus-trap.test.js T1-T3 verdes; suite global no regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir @layer components reglas synthwave a chapter-themes.css (.project-overlay backdrop + card glow doble + mobile fullscreen + PRM)</name>
  <files>src/styles/chapter-themes.css</files>
  <behavior>
    - chapter-themes.css @layer components añade reglas para .project-overlay siguiendo RESEARCH §Pattern 10 CSS skeleton (líneas 1061-1137).
    - `.project-overlay { position: fixed; inset: 0; z-index: 50; display: flex; align-items: center; justify-content: center; background: rgba(26, 14, 61, 0.7); }` con `@supports backdrop-filter` override que aplica `background: rgba(26, 14, 61, 0.5); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px)` para soportar progressive enhancement.
    - `.project-overlay__card { max-width: min(90vw, 560px); background: #1a0e3d; border: 1px solid #4dffff; border-radius: 8px; padding: var(--sp-lg); box-shadow: 0 0 24px rgba(77, 255, 255, 0.4), 0 0 48px rgba(255, 60, 166, 0.2); animation: overlay-enter 200ms ease-out; }` (glow doble cyan + pink).
    - `@keyframes overlay-enter { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } }`.
    - `@media (max-width: 599px) { .project-overlay__card { max-width: 100vw; height: 100vh; height: 100dvh; border-radius: 0; border-left: none; border-right: none; } }` (mobile fullscreen).
    - `@media (prefers-reduced-motion: reduce) { .project-overlay__card { animation: none; } }` (PRM D5-08 instant).
    - `.project-overlay__close { position: absolute; top: var(--sp-sm); right: var(--sp-sm); background: transparent; border: 1px solid #4dffff; color: #4dffff; width: 32px; height: 32px; border-radius: 4px; font-size: 1.2rem; cursor: pointer; }`.
    - `.project-overlay__title { font-family: 'Audiowide', sans-serif; color: #4dffff; text-shadow: 0 0 8px rgba(77, 255, 255, 0.5); }`.
    - `.project-overlay__year`, `.project-overlay__role`, `.project-overlay__desc` — body text con color #c0e0ff o `var(--c-fg)` (legible sobre deep purple).
    - `.project-overlay__tech` — flex chips estilo synthwave (border cyan rounded, padding 2px 8px, font-size 0.75rem).
    - `.project-overlay__link` — `display: inline-block; padding: 8px 18px; background: #ff3ca6; color: #1a0e3d; text-shadow: 0 0 8px rgba(255, 60, 166, 0.6); border-radius: 4px; text-decoration: none;` con hover state opcional.
    - themes-file.test.js sigue verde (las nuevas reglas no rompen las arquitecturales existentes).
    - theme-isolation.test.js sigue verde — `.project-overlay` no está scoped a `[data-chapter="6"]` para que el modal pueda salir del scope chapter (es position:fixed encima del shell), pero NO debe overridear tokens globales que afectan otras chapters durante snap.
  </behavior>
  <action>Editar `src/styles/chapter-themes.css` añadiendo el sub-bloque "Phase 5 W4 — ProjectOverlay synthwave (D5-07, Pattern 10)" dentro del `@layer components` block (después del bloque Phase 5 W3 ch6 .ch6-layout/.ch6-canvas-host/.ch6-mantra). Las clases son globales (NO `[data-chapter="6"]` prefix) porque el modal es `position: fixed` y vive encima del shell — no necesita scope chapter. Verificar que las reglas siguen patrón `@supports (backdrop-filter)` progressive enhancement + `@media (prefers-reduced-motion: reduce)` PRM override.

  Per D5-07 (synthwave specs) + D5-08 (PRM instant) + RESEARCH §Pattern 10 CSS skeleton.</action>
  <verify>
    <automated>npm run test -- --run tests/styles/themes-file.test.js tests/themes/theme-isolation.test.js tests/components/ProjectOverlay.test.js</automated>
  </verify>
  <done>chapter-themes.css @layer components contiene reglas .project-overlay + .project-overlay__card + .project-overlay__close + .project-overlay__title + .project-overlay__tech + .project-overlay__link + @keyframes overlay-enter + @supports backdrop-filter + @media mobile + @media PRM; ProjectOverlay.test.js sigue verde con render check; themes-file.test.js sin regression; theme-isolation.test.js sin regression. **Slice end-to-end manual check:** dev preview + click planet → overlay synthwave aparece con glow doble + ESC cierra + Tab cycla focus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props.projectId → projects.find | Lookup en array estático; si id inválido, computed devuelve undefined |
| target="_blank" link → external site | Tabnabbing vector vía window.opener manipulation |
| i18n strings → template render | vue-i18n escapa por default; no v-html en este componente |
| document.addEventListener('keydown') → focus trap | Listener global; debe limpiarse en onBeforeUnmount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W4-01 | DoS | projectId inválido → project.value undefined → template crash | mitigate | **Template usa `v-if="project"` wrapper** (Warning 9 RESOLVED — acceptance criteria explícito + test T6 enforced). Chapter6Content (W3) solo setea activeProject a valores del computed ch6Projects (subset confiable), pero defensive null guard en ProjectOverlay garantiza tolerancia ante drift futuro |
| T-05-W4-02 | Tampering | target="_blank" sin rel noopener → tabnabbing window.opener | mitigate | rel="noopener noreferrer" hardcoded; ProjectOverlay.test.js T5 lo verifica via source-regex |
| T-05-W4-03 | Tampering | XSS via i18n title/desc strings con `<script>` injection | accept | Strings vienen de es.json/en.json (commit-tracked, no user input); vue-i18n escapa con `{{ t() }}`; sin `v-html` — risk muy bajo |
| T-05-W4-04 | DoS | document.keydown listener leak si onBeforeUnmount no cleanup | mitigate | `removeEventListener('keydown', handleKeydown)` en onBeforeUnmount + Vue garantiza unmount cuando v-if=false (Chapter6Content @close sets activeProject=null) |
| T-05-W4-05 | Spoofing | emit('close') desde código arbitrario | accept | emit es scope-internal del component; otros componentes no acceden a la instancia. Risk muy bajo |
| T-05-W4-06 | Information disclosure | Focus restoration captura activeElement before opening modal — leak focus historial cross-modal | accept | lastFocusedEl es variable local del component scope; se descarta al unmount; no se persiste |
</threat_model>

<verification>
- `npm run test -- --run tests/components/ProjectOverlay.test.js tests/a11y/focus-trap.test.js`: T1-T6 + T1-T3 verdes.
- `npm run test:run` global: 151 baseline + Phase 5 W0-W4 verdes. Tests pendientes solo W5 (manual checklist Rafael — no automated).
- `npm run dev` + scroll a ch6 + click planet: overlay synthwave aparece + ESC cierra + click backdrop cierra + click close cierra + Tab cycla + restore focus al planet-button.
- Browser DevTools accessibility tree: dialog role detected, aria-modal, aria-labelledby binding correct, close button con aria-label "Cerrar"/"Close" según locale.
- Frontmatter validation: `gsd-sdk query frontmatter.validate ".planning/phases/05-phaser-chapter-6/05-05-PLAN.md" --schema plan`.
</verification>

<success_criteria>
- ProjectOverlay.vue implementación completa synthwave + focus trap manual (~30 LOC) + ESC + click-outside + restore focus + **null guard `v-if="project"`** (Warning 9 RESOLVED).
- chapter-themes.css @layer components contiene reglas .project-overlay con backdrop blur progressive enhancement + card glow doble cyan+pink + mobile fullscreen + PRM no-animation.
- Tests ProjectOverlay.test.js T1-T6 verdes (ESC, backdrop click, content render, aria-label, rel noopener, **null guard projectId inválido**).
- Tests a11y/focus-trap.test.js T1-T3 verdes (Tab cycle, Shift+Tab cycle, restore focus).
- target="_blank" link tiene rel="noopener noreferrer" (T-05-W4-02).
- Slice end-to-end completo: click planet → overlay con detalle correcto + i18n + A11Y + PRM-aware + tolerancia a projectId inválido.
</success_criteria>

<output>
After completion, create `.planning/phases/05-phaser-chapter-6/05-05-SUMMARY.md` que incluya: decisión final focus trap manual vs @vueuse/integrations (Open Q3 RESOLVED rationale), screenshots de los 3 planet overlays (ar-vr, remoose, software-mind) con contenido renderizado, confirmación de A11Y tree en DevTools (dialog/aria-modal/aria-labelledby), confirmación del null guard `v-if="project"` (test T6 GREEN), métricas opcionales de animación 200ms percibida.
</output>
