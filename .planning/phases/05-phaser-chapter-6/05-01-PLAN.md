---
phase: 05-phaser-chapter-6
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/data/chapters.js
  - src/data/projects.js
  - src/styles/chapter-themes.css
  - src/i18n/es.json
  - src/i18n/en.json
  - tests/assets/asset-naming.test.js
  - tests/assets/ch6-assets.test.js
  - tests/data/projects.test.js
  - tests/data/chapters.test.js
  - tests/phaser/factory.test.js
  - tests/phaser/scale.test.js
  - tests/phaser/space-scene-shape.test.js
  - tests/phaser/no-character-animation.test.js
  - tests/phaser/locale-bridge.test.js
  - tests/phaser/prm.test.js
  - tests/components/Chapter6Content.test.js
  - tests/components/Chapter6Content-lazy.test.js
  - tests/components/Chapter6Content-bridge.test.js
  - tests/components/Chapter6Content-resize.test.js
  - tests/components/Chapter6Content-prm.test.js
  - tests/components/ProjectOverlay.test.js
  - tests/a11y/keyboard-planet-buttons.test.js
  - tests/a11y/focus-trap.test.js
  - tests/integration/chapter-overlap-ch6.test.js
autonomous: true
requirements:
  - CON-04
  - CON-05
  - CON-06
  - THM-01
  - THM-03
  - THM-05
  - ART-05
  - ART-06
  - A11Y-05
  - A11Y-06
  - PHA-01
  - PHA-02
  - PHA-03
  - PHA-04
  - PHA-05
  - PHA-06
  - PHA-07
  - PHA-08
  - PHA-09
must_haves:
  truths:
    - "El array `chapters[6].palette` contiene los 4 hex synthwave D5-04 (#1a0e3d, #ff3ca6, #4dffff, #ffd95c)."
    - "`projects.js` declara exactamente 3 items con chapterEra=6 (ch6-ar-vr, ch6-remoose, ch6-software-mind) con shape D3-03 y campos planetSprite/planetOrbit/planetColor poblados."
    - "El bloque `[data-chapter=\"6\"]` en chapter-themes.css declara paleta synthwave + `--bg-image: url('/assets/ch6-bg.png')`."
    - "Las keys i18n chapters.6.mantra, chapters.6.flavor, projects.ch6-{ar-vr,remoose,software-mind}.{title,desc,role}, ui.closeOverlay existen en ES y EN con paridad enforced."
    - "El regex de asset-naming.test.js acepta los descriptors nuevos ch6 (planet-{ar-vr,remoose,software-mind}, ship-{1,2}, bg-stars-far, bg-nebulae-mid) y rechaza naming drift."
    - "Tests RED creados (16 scaffolds) describen los contratos PHA-01..09 + CON-04 + A11Y-05/06 + chapter-overlap defensive, listos para volverse green en waves 1-5."
  artifacts:
    - path: "src/data/chapters.js"
      provides: "chapters[6].palette poblada"
      contains: "'#1a0e3d'"
    - path: "src/data/projects.js"
      provides: "3 items chapterEra=6"
      contains: "ch6-ar-vr"
    - path: "src/styles/chapter-themes.css"
      provides: "[data-chapter=\"6\"] finalizado + --bg-image"
      contains: "--bg-image: url('/assets/ch6-bg.png')"
    - path: "src/i18n/es.json"
      provides: "keys ch6 + mantra + ui.closeOverlay (ES)"
      contains: "mantra"
    - path: "src/i18n/en.json"
      provides: "keys ch6 + mantra + ui.closeOverlay (EN)"
      contains: "mantra"
    - path: "tests/assets/asset-naming.test.js"
      provides: "regex extendido a descriptors ch6"
      contains: "planet-ar-vr"
    - path: "tests/phaser/factory.test.js"
      provides: "RED scaffold contrato PHA-01..03 (Scale.NONE, Math.floor, pixelArt, roundPixels, physics:none, parent:parentEl)"
    - path: "tests/phaser/space-scene-shape.test.js"
      provides: "RED scaffold contrato PHA-05 + locale singleton + arrival camera tween"
    - path: "tests/phaser/no-character-animation.test.js"
      provides: "RED scaffold anti-pattern PHA-08 (anims.create, spritesheet.*frames, playAnimation)"
    - path: "tests/components/Chapter6Content.test.js"
      provides: "RED scaffold PHA-04 lazy import + PHA-02 destroy(true,false)"
    - path: "tests/components/ProjectOverlay.test.js"
      provides: "RED scaffold ESC + click-outside + focus trap"
    - path: "tests/integration/chapter-overlap-ch6.test.js"
      provides: "RED scaffold defensive — no overflow:hidden en .ch6-layout + scroll-snap-stop:always"
  key_links:
    - from: "src/styles/chapter-themes.css"
      to: "/assets/ch6-bg.png"
      via: "--bg-image declaration en [data-chapter=\"6\"]"
      pattern: "--bg-image:\\s*url\\('/assets/ch6-bg\\.png'\\)"
    - from: "src/data/projects.js"
      to: "src/data/chapters.js"
      via: "chapterEra=6 referencia chapter id"
      pattern: "chapterEra:\\s*6"
    - from: "tests/i18n/parity.test.js"
      to: "src/i18n/{es,en}.json"
      via: "flatten keys parity auto-extends a chapters.6.* y projects.ch6-*.*"
      pattern: "ch6-ar-vr"
---

<objective>
W0 scaffolding del Phase 5: poblar data layer (chapters[6].palette + 3 projects ch6 con planet fields D3-03), finalizar `[data-chapter="6"]` theme block en chapter-themes.css (paleta synthwave + `--bg-image`), añadir i18n keys ch6 con paridad ES↔EN, extender regex de asset-naming.test.js, y crear 16 test scaffolds RED que describen los contratos PHA-01..09 + CON-04 + A11Y-05/06 + chapter-overlap defensive.

Purpose: dejar el "esqueleto contractual" del Phase 5 listo. Waves 1-5 implementan código que vuelve esos tests green; el data layer + theme + i18n son los inputs que las waves posteriores consumen (assets W1, factory+SpaceScene W2, Chapter6Content W3, ProjectOverlay W4, manual checklist W5).

Output: data sources finalizadas, theme block finalizado, i18n keys committed, regex extendido, 16 tests scaffold (RED esperado — todos referencian archivos que aún no existen o aserciones contra source que no tiene la forma esperada).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-phaser-chapter-6/05-CONTEXT.md
@.planning/phases/05-phaser-chapter-6/05-RESEARCH.md
@.planning/phases/05-phaser-chapter-6/05-PATTERNS.md
@.planning/phases/05-phaser-chapter-6/05-VALIDATION.md
@src/data/chapters.js
@src/data/projects.js
@src/styles/chapter-themes.css
@src/i18n/es.json
@src/i18n/en.json
@tests/assets/asset-naming.test.js
@tests/i18n/parity.test.js
@CLAUDE.md

<interfaces>
<!-- Contratos D3-03 (shape projects) + D5-04 (paleta synthwave) + D5-01 (3 planets mapping). -->

Shape D3-03 (projects.js — locked):
```
{
  id: string,              // 'ch6-{slug}'
  chapterEra: number,      // 6
  year: number,
  titleKey: string,        // 'projects.ch6-{slug}.title'
  descKey: string,
  link: string | null,
  imageSrc: string | null,
  role: string,
  techStack: string[],
  planetSprite: string,    // '/assets/ch6-planet-{slug}.png'
  planetOrbit: number,     // 0..1 Y normalized within ARRIVAL_DESCENT
  planetColor: string,     // hex of neon-orb halo
}
```

Paleta synthwave D5-04 (chapters[6].palette):
```
['#1a0e3d', '#ff3ca6', '#4dffff', '#ffd95c']
```

Mapping D5-01 (3 planets):
- ch6-ar-vr (Empresa propia AR/VR 2015-18, founder) — planetOrbit: 0.2, planetColor: '#ff3ca6' (hot pink)
- ch6-remoose (Remoose Interactive Vancouver 2023+, full stack) — planetOrbit: 0.5, planetColor: '#4dffff' (cyan)
- ch6-software-mind (Software Mind NA 2023+, QA+AI) — planetOrbit: 0.8, planetColor: '#ffd95c' (amber)
</interfaces>

<analog_pattern>
Phase 4 W0 (`04-01-PLAN.md`): mismo patrón scaffolding antes de generar assets. Asset-naming.test.js extended ANTES de los `art()` commits; data layer poblada antes de waves de componentes.

Existing ch6 stub theme block (chapter-themes.css líneas 134-149): paleta provisional (`#000814`, `#c0e0ff`, `#ffaa00`, `#1a4080`) + font `Press Start 2P`. W0 finaliza con paleta D5-04 synthwave + font `Audiowide` (synthwave-friendly).

Chapter4Content.test.js (referencia para mock i18n + provide scrollState/prm pattern). PattternMap §`tests/components/Chapter4Content.test.js` lines 24-75 verbatim.
</analog_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender asset-naming regex + crear ch6-assets.test.js</name>
  <files>tests/assets/asset-naming.test.js, tests/assets/ch6-assets.test.js</files>
  <behavior>
    - asset-naming.test.js T1 sigue verde para los 13 assets Phase 4 existentes.
    - asset-naming.test.js T1 acepta como válidos los nuevos descriptors ch6: `bg`, `bg-stars-far`, `bg-nebulae-mid`, `planet-ar-vr`, `planet-remoose`, `planet-software-mind`, `ship-1`, `ship-2`.
    - asset-naming.test.js T2 (wave-0-gate Phase 4) sigue verde (7 busts ch{0..6}-bust.png existen).
    - asset-naming.test.js T3 (no underscores) sigue verde.
    - asset-naming.test.js T4 (.gitignore privacy gate) sigue verde.
    - Nuevo ch6-assets.test.js: T1 verifica existencia de ch6-bg.png (RED hasta W1), T2 verifica existencia de los 3 planets, T3 verifica existencia de los 2 ships, T4 verifica file size de ch6-bg.png ≤ 80*1024 bytes (Phase 6 budget carry-forward Pitfall 12).
  </behavior>
  <action>Editar `tests/assets/asset-naming.test.js` reemplazando la constante `ASSET_NAMING_REGEX` por el nuevo enum que incluye ch6 descriptors. El nuevo regex debe ser: `^ch[0-6]-(bust|bg|bg-stars-far|bg-planet-mid|bg-nebulae-mid|fg-panels|fg-ships|hero|planet-(ar-vr|remoose|software-mind)|ship-[12])\.(png|jpg)$`. Actualizar el comentario de líneas 35-46 para mencionar los 8 assets nuevos Phase 5 (1 bg + 2 opt parallax + 3 planets + 2 ships). Mantener T1/T2/T3/T4 intactos. Crear `tests/assets/ch6-assets.test.js` siguiendo el patrón readdirSync+existsSync de asset-naming.test.js (clone shape, 4 tests T1-T4 según behavior). El test T4 usa `statSync(path).size` con threshold `80 * 1024`. Todos los tests T1-T3 fallan RED hasta W1 commit de assets (esperado). T4 también RED. Per D5-04 (paleta synthwave locked) y Pitfall 11 (extend regex BEFORE generation).</action>
  <verify>
    <automated>npm run test -- --run tests/assets/asset-naming.test.js tests/assets/ch6-assets.test.js</automated>
  </verify>
  <done>asset-naming.test.js T1 verde (regex acepta descriptors ch6 sin assets aún, sin offenders activos del Phase 4); T2/T3/T4 verdes; ch6-assets.test.js existe con T1-T4 esperando ch6-bg/planets/ships (RED esperado por W1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Poblar data layer ch6 (chapters palette + 3 projects ch6) + tests data</name>
  <files>src/data/chapters.js, src/data/projects.js, tests/data/chapters.test.js, tests/data/projects.test.js</files>
  <behavior>
    - chapters[6].palette es array de 4 strings hex matching D5-04 (`#1a0e3d`, `#ff3ca6`, `#4dffff`, `#ffd95c`).
    - projects.js declara exactamente 3 items con chapterEra === 6.
    - Los 3 items ch6 son ch6-ar-vr (planetOrbit:0.2, planetColor:'#ff3ca6'), ch6-remoose (planetOrbit:0.5, planetColor:'#4dffff'), ch6-software-mind (planetOrbit:0.8, planetColor:'#ffd95c').
    - Los 3 items declaran planetSprite con path `/assets/ch6-planet-{slug}.png` (slugs ar-vr, remoose, software-mind).
    - Shape D3-03 completo en los 3 items (12 fields incluyendo techStack array, role string, link/imageSrc null inicialmente, titleKey/descKey con prefijo `projects.ch6-{slug}.`).
    - tests/data/projects.test.js: T1 (count) 3 items chapterEra=6, T2 (shape) los 3 tienen las 12 keys D3-03, T3 (Phaser fields) planetSprite/planetOrbit/planetColor != null para los 3, T4 (planetOrbit range) cada planetOrbit entre 0 y 1, T5 (sprite path) planetSprite matchea `/^/assets/ch6-planet-(ar-vr|remoose|software-mind)\.png$/`.
    - tests/data/chapters.test.js: T1 chapters[6].palette.length >= 4, T2 cada elemento matchea `/^#[0-9a-fA-F]{6}$/`, T3 contiene los 4 hex D5-04 exactos.
  </behavior>
  <action>Editar `src/data/chapters.js` reemplazando `chapters[6].palette: []` por el array `['#1a0e3d', '#ff3ca6', '#4dffff', '#ffd95c']` (D5-04 locked, source: 05-CONTEXT §Decisions D5-04). Mantener el resto del shape intacto. Editar `src/data/projects.js` añadiendo 3 items chapterEra=6 al final del array (después de los ch5-* items, antes del comentario "PENDING — ch3 Pink Parrot"). Usar copy stubs para titleKey/descKey (executor refresca post-W5 si Rafael actualiza CONTENT-CHECKLIST §2.5 — patrón D4-09 ya establecido en ch2/ch4/ch5). Por D5-01: ch6-ar-vr (year:2015, role:'Founder / Tech Lead', techStack:['Unity','ARKit','Vuforia'], planetOrbit:0.2, planetColor:'#ff3ca6', planetSprite:'/assets/ch6-planet-ar-vr.png'); ch6-remoose (year:2023, role:'Full Stack Engineer', techStack:['Vue 3','Vite','TypeScript'], planetOrbit:0.5, planetColor:'#4dffff', planetSprite:'/assets/ch6-planet-remoose.png'); ch6-software-mind (year:2023, role:'QA Specialist · AI/Data Science Automation', techStack:['Python','AWS','Playwright'], planetOrbit:0.8, planetColor:'#ffd95c', planetSprite:'/assets/ch6-planet-software-mind.png'). link y imageSrc en null para los 3. Crear `tests/data/projects.test.js` y `tests/data/chapters.test.js` con los tests T1-T5 / T1-T3 descritos en behavior, usando imports estáticos `import { projects } from '../../src/data/projects.js'` y mismo patrón para chapters. Los tests vuelven GREEN tras este task (no requieren waves posteriores).</action>
  <verify>
    <automated>npm run test -- --run tests/data/projects.test.js tests/data/chapters.test.js</automated>
  </verify>
  <done>chapters[6].palette tiene los 4 hex synthwave; projects.js tiene 3 items chapterEra=6 con planet fields poblados; tests/data/{projects,chapters}.test.js verdes; sin afectar tests preexistentes de Phase 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalizar [data-chapter="6"] theme block + i18n keys ch6 paridad ES↔EN</name>
  <files>src/styles/chapter-themes.css, src/i18n/es.json, src/i18n/en.json</files>
  <behavior>
    - Bloque `[data-chapter="6"]` en chapter-themes.css declara los tokens synthwave (--c-bg:#1a0e3d, --c-fg legible, --c-accent:#4dffff, --c-accent-2:#ff3ca6, --c-mantra:#ffd95c, --c-border:#4dffff, --c-focus:#ffd95c, --c-surface:#0a061f, --bg-image:url('/assets/ch6-bg.png'), --font-body:'Audiowide').
    - Tests architectural existentes (themes-file.test.js, theme-isolation.test.js) verdes.
    - tests/i18n/parity.test.js T1 (flatten EN == flatten ES) verde tras añadir keys.
    - Keys nuevas en ES y EN: `chapters.6.flavor`, `chapters.6.mantra`, `projects.ch6-ar-vr.{title,desc}`, `projects.ch6-remoose.{title,desc}`, `projects.ch6-software-mind.{title,desc}`, `ui.closeOverlay`. (NOTA: `chapters.6.title` y `chapters.6.era` ya existen en es.json/en.json — no duplicar; solo añadir flavor y mantra dentro de chapters.6.)
    - chapters.6.mantra es "Y siempre muestra una sonrisa" en ES y "And always show a smile" en EN (D5-03; Rafael ratifica copy ES en W5 si quiere variante).
    - ui.closeOverlay es "Cerrar" en ES y "Close" en EN.
  </behavior>
  <action>Editar `src/styles/chapter-themes.css` reemplazando el bloque `[data-chapter="6"]` actual (líneas ~134-149, stub Phase 2 con `#000814`/`Press Start 2P`) por el bloque synthwave finalizado: declarar los 9 tokens del behavior + comentario contrast `contrast(#c0e0ff, #1a0e3d)` calculado. Mantener el comment header del block actualizado para indicar "Phase 5 W0 — finalizado". NO añadir aún las reglas `@layer components` (.ch6-layout, .ch6-canvas-host, .ch6-mantra, .project-overlay) — esas vienen en W3+W4 cuando los componentes existan. Solo declarar el theme block. Editar `src/i18n/es.json` extendiendo el objeto `chapters.6` con keys `flavor` ("2026. Convergencia: dev + QA + AI en un solo espacio. La nave aterriza.") y `mantra` ("Y siempre muestra una sonrisa"); añadir objeto `projects.ch6-ar-vr` con `title` ("Empresa propia AR/VR") y `desc` ("PENDING — Rafael refresca via CONTENT-CHECKLIST §2.5: empresa AR/VR aplicada a publicidad y entrenamiento corporativo, 2015-18."); añadir objeto `projects.ch6-remoose` con `title` ("Remoose Interactive") y `desc` ("PENDING — Rafael refresca §2.5: full stack remoto Vancouver, 2023-presente."); añadir objeto `projects.ch6-software-mind` con `title` ("Software Mind NA") y `desc` ("PENDING — Rafael refresca §2.5: QA + AI data science automation, 2023-presente."); añadir `ui.closeOverlay` con valor "Cerrar". Editar `src/i18n/en.json` con paridad exacta (mismas keys, valores en inglés equivalentes: flavor="2026. Convergence: dev + QA + AI in a single space. The ship lands.", mantra="And always show a smile", titles iguales en EN (nombres propios), descs marcadas PENDING — Rafael refreshes §2.5, ui.closeOverlay="Close"). Por D5-03 (mantra locked) + D5-10 (overlay i18n keys naming).</action>
  <verify>
    <automated>npm run test -- --run tests/i18n/parity.test.js tests/styles/themes-file.test.js tests/themes/theme-isolation.test.js</automated>
  </verify>
  <done>Bloque [data-chapter="6"] en chapter-themes.css finalizado con tokens synthwave + --bg-image; es.json y en.json tienen las keys nuevas con paridad enforced por parity.test.js T1; themes-file.test.js T4 verde (ch6 selector con paleta y `--bg-image`); theme-isolation.test.js sigue verde para chapters 0-5 (no leak).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Crear 16 test scaffolds RED para Phaser + Chapter6Content + ProjectOverlay + a11y + chapter-overlap defensive</name>
  <files>tests/phaser/factory.test.js, tests/phaser/scale.test.js, tests/phaser/space-scene-shape.test.js, tests/phaser/no-character-animation.test.js, tests/phaser/locale-bridge.test.js, tests/phaser/prm.test.js, tests/components/Chapter6Content.test.js, tests/components/Chapter6Content-lazy.test.js, tests/components/Chapter6Content-bridge.test.js, tests/components/Chapter6Content-resize.test.js, tests/components/Chapter6Content-prm.test.js, tests/components/ProjectOverlay.test.js, tests/a11y/keyboard-planet-buttons.test.js, tests/a11y/focus-trap.test.js, tests/integration/chapter-overlap-ch6.test.js</files>
  <behavior>
    - Los 15 archivos test existen.
    - Cada test usa `vitest` + `readFileSync` defensive try/catch (no crashea si el source file no existe aún — falla con expect message claro).
    - Tests Phaser source-regex (factory, scale, space-scene-shape, no-character-animation, locale-bridge, prm): leen src/phaser/index.js o src/phaser/SpaceScene.js y aseguran patterns PHA-01..09. RED porque archivos no existen (W2 los crea).
    - Tests Chapter6Content (5 files): mount con stubs de @vue/test-utils + mocks de @/phaser, @/data/projects, createTestI18n helper. RED porque Chapter6Content.vue no existe (W3 lo crea).
    - Tests ProjectOverlay: mount + keydown Escape + click outside + focus trap. RED porque ProjectOverlay.vue no existe (W4).
    - Tests a11y (keyboard-planet-buttons, focus-trap): integración con Chapter6Content/ProjectOverlay mocks. RED hasta W3/W4.
    - tests/integration/chapter-overlap-ch6.test.js: source-regex en chapter-themes.css verificando NO `overflow:hidden` en `.ch6-layout`, Y mount con stubs verifica que `<Chapter6Content>` renderea (sección 6) en ScrollShell (NOT placeholder). RED hasta W3.
  </behavior>
  <action>Crear los 15 archivos test usando los skeletons verbatim de 05-PATTERNS.md §Pattern Assignments + 05-RESEARCH.md §Pattern 1-13. Cada archivo sigue el pattern source-regex con `readFileSync` defensive (try/catch → source='' si no existe → expects fallan con mensaje claro indicando "W{N} crea este archivo"). Detalles por archivo:

  `tests/phaser/factory.test.js`: 6 tests source-regex sobre `src/phaser/index.js` verificando `Phaser.Scale.NONE`, `Math.floor`, `pixelArt: true`, `roundPixels: true`, `physics: { default: 'none' }`, `parent: parentEl` (no string id). Skeleton 05-PATTERNS líneas 814-838.

  `tests/phaser/scale.test.js`: 3 tests source-regex verificando `BASE_W = 480`, `BASE_H = 270`, función `computeZoom` con `Math.min` + `Math.floor` + `|| 1` defensive.

  `tests/phaser/space-scene-shape.test.js`: 6 tests source-regex sobre `src/phaser/SpaceScene.js` verificando: (a) `extends Phaser.Scene`, (b) `preload()` carga keys `ch6-bg`, `ch6-planet-ar-vr`, `ch6-planet-remoose`, `ch6-planet-software-mind`, `ch6-ship-1`, `ch6-ship-2`, (c) `create()` instancia 3 planet sprites + 2 ships + camera tween (`scrollY`), (d) registry `prefersReduced` check, (e) singleton i18n import `from '@/i18n'`, (f) emit `vue:arrival-complete` event.

  `tests/phaser/no-character-animation.test.js`: 3 tests source-regex en SpaceScene.js verificando NO match `this\.anims\.create`, NO match `spritesheet.*frames`, NO match `playAnimation`. Skeleton 05-PATTERNS líneas 842-855.

  `tests/phaser/locale-bridge.test.js`: 4 tests source-regex SpaceScene.js: (a) listener `this.game.events.on\(['"]locale-changed['"]` registrado, (b) cleanup en SHUTDOWN, (c) handler re-translates `tooltipTexts`, (d) emit defensive `game.value?.events.emit` en Chapter6Content.

  `tests/phaser/prm.test.js`: 3 tests source-regex SpaceScene.js: (a) `this.registry.get\(['"]prefersReduced['"]\)` lectura, (b) PRM branch sets `cameras.main.setScroll` directo sin tween, (c) `this.tweens.timeScale = 0` cinturón presente en branch PRM. Skeleton 05-PATTERNS líneas 882-899.

  `tests/components/Chapter6Content.test.js`: 4 tests mount integration con `vi.mock('@/phaser', () => ({ createGame: vi.fn(() => mockGame) }))` + mock projects + createTestI18n. T1: lifecycle PHA-04 — activeChapter=6 dispara `createGame` exactamente 1 vez (verifica con `await flushPromises()`). T2: lifecycle PHA-02 — cambiar activeChapter de 6 a 5 llama `mockGame.destroy` con `(true, false)`. T3: shallowRef pattern — source-regex en Chapter6Content.vue verifica `shallowRef\(null\)` (NO `ref(`, NO `reactive(`). T4: registra 2 listeners `vue:show-project` y `vue:arrival-complete` en game.value.events. Skeleton 05-PATTERNS líneas 945-985.

  `tests/components/Chapter6Content-lazy.test.js`: 2 tests source-regex Chapter6Content.vue: (a) presencia `await import\(['"]@/phaser['"]\)` (string literal — Vite splittable), (b) no top-level `import Phaser` ni `import.*from\s+['"]phaser['"]` en Chapter6Content.vue.

  `tests/components/Chapter6Content-bridge.test.js`: 3 tests integration: (a) game emit `vue:show-project` → `activeProject` Vue ref se setea → `<ProjectOverlay>` v-if renderea (verifica con stub component), (b) emit `vue:arrival-complete` → `arrivalDone.value = true` → `.ch6-mantra` v-if visible, (c) cambiar activeChapter de 6 a 5 reset `activeProject` y `arrivalDone` a null/false.

  `tests/components/Chapter6Content-resize.test.js`: 2 tests integration: (a) source-regex confirma `useResizeObserver(document.documentElement` (no `window`), (b) mock ResizeObserver fires → if `newZoom !== game.value.scale.zoom` → `game.value.scale.setZoom(newZoom)` invocado (Pitfall 8 mitigation).

  `tests/components/Chapter6Content-prm.test.js`: 2 tests integration: (a) provide PRM=true → source-regex verifica `prefersReduced.value` se pasa a `createGame({ prefersReduced })` opt, (b) PRM=true → en render, `.ch6-mantra` sigue v-if=arrivalDone pero CSS `@media (prefers-reduced-motion: reduce)` desactiva animation (verifica via source-regex en chapter-themes.css).

  `tests/components/ProjectOverlay.test.js`: 5 tests integration: (a) keydown Escape → emit 'close', (b) click en backdrop (overlay root) → emit 'close', (c) ProjectOverlay con projectId resuelve `projects.find(p => p.id === projectId)` y renderea title/role/techStack/desc, (d) close button presente con `aria-label="Cerrar"` (i18n ES), (e) `target="_blank"` link tiene `rel="noopener noreferrer"` (threat T-05-02 tabnabbing mitigation). Skeleton 05-PATTERNS líneas 995-1013.

  `tests/a11y/keyboard-planet-buttons.test.js`: 3 tests integration con Chapter6Content mount: (a) renderea 3 `<button class="sr-only">` post-canvas con `aria-label` que incluye `t('ui.openProject')`, (b) Enter/Space en button dispara mismo handler que planet pointerdown (setea `activeProject = p.id`), (c) Tab navigation incluye los 3 buttons en orden chronological ar-vr → remoose → software-mind.

  `tests/a11y/focus-trap.test.js`: 3 tests ProjectOverlay: (a) Tab desde último focusable → cycla al primero (close button), (b) Shift+Tab desde primero → cycla al último, (c) onMount: lastFocusedEl captured + closeBtn focused; onUnmount: focus restored to lastFocusedEl.

  `tests/integration/chapter-overlap-ch6.test.js`: 4 tests defensive (Pitfall 13 mitigation): T1 source-regex en ScrollShell.vue verifica `scroll-snap-stop:\s*always` activo. T2 source-regex en chapter-themes.css verifica que el bloque `[data-chapter="6"] .ch6-layout` NO contiene `overflow:\s*hidden` (Pattern 12 mitigation). T3 source-regex en ScrollShell.vue verifica que `<Chapter6Content v-else-if="ch.id === 6"` está presente en template (replace de placeholder). T4 mount ScrollShell con stubs y verifica que sección data-chapter=6 contiene `.ch6-layout` (NO `.chapter-placeholder`). Skeleton 05-PATTERNS líneas 1036-1059.

  Todos los tests siguen la convención de comentar al inicio: "RED scaffold W0 — verde tras W{N}". Por 05-VALIDATION §Wave 0 Requirements (16 tests listados, ch6-assets.test.js ya creado en Task 1).</action>
  <verify>
    <automated>npm run test -- --run tests/phaser tests/components/Chapter6Content tests/components/ProjectOverlay.test.js tests/a11y tests/integration/chapter-overlap-ch6.test.js</automated>
  </verify>
  <done>15 archivos test creados con scaffolds; tests todos RED (esperado — referencian archivos que aún no existen W2/W3/W4); falla con mensajes claros tipo "W{N} crea este archivo"; ningún test verde por accidente; suite global no rompe tests preexistentes Phase 1-4 (correr `npm run test:run` y verificar baseline 151 + nuevos RED = expected ratio).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source files → test runners | Datos hardcoded en chapters.js/projects.js consumidos por tests; jsdom es sandbox aislado |
| i18n JSON → render Vue/Phaser | strings cargados via vue-i18n, posibles vectores XSS si Phase 5 introduce `v-html` con user input |
| Asset filenames → filesystem regex | tests/assets readdir + regex match en filenames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-01 | Tampering | i18n JSON keys mismatch ES↔EN | mitigate | parity.test.js T1 auto-detecta drift entre es.json y en.json post-task 3 |
| T-05-W0-02 | Tampering | Asset filenames fuera de convención | mitigate | asset-naming.test.js regex enum bloquea ch6 descriptors fuera de spec (Pitfall 11) |
| T-05-W0-03 | Information disclosure | Datos personales en stubs (year, role, techStack) | accept | Información ya pública de Rafael (LinkedIn/CV); no PII sensible. Refresh real post-W5 sigue público |
| T-05-W0-04 | Repudiation | Cambio de paleta sin trazabilidad | mitigate | Commit message con `feat(05-01): poblar chapters[6].palette synthwave per D5-04` referencia decisión locked |
</threat_model>

<verification>
- `npm run test:run` completo: tests Phase 1-4 (151 baseline) verdes + tests Phase 5 W0 verdes (Tasks 1-3: ch6-assets RED esperado para asset existence, data tests verdes, parity verde, themes-file verde) + Tasks 4 scaffolds RED esperados.
- `npm run build` (NO en W0, opcional) — verificar que cambios en chapter-themes.css no rompen build.
- Frontmatter validation: `gsd-sdk query frontmatter.validate ".planning/phases/05-phaser-chapter-6/05-01-PLAN.md" --schema plan`.
</verification>

<success_criteria>
- chapters[6].palette = ['#1a0e3d', '#ff3ca6', '#4dffff', '#ffd95c'] (verificado por tests/data/chapters.test.js T3).
- 3 items chapterEra=6 con planet fields poblados (verificado por tests/data/projects.test.js T1-T5).
- [data-chapter="6"] declara `--bg-image: url('/assets/ch6-bg.png')` (verificado por tests/styles/themes-file.test.js T4 ya existente, extends auto).
- i18n keys ch6 + mantra + ui.closeOverlay paridad enforced (tests/i18n/parity.test.js T1).
- 16 test scaffolds creados (15 nuevos + 1 actualizado): suite total Phase 5 W0 contribuye archivos a tests/phaser/, tests/components/, tests/a11y/, tests/integration/, tests/assets/.
- regex de asset-naming.test.js acepta los descriptors ch6 nuevos (verificado por T1 sin offenders post extension).
</success_criteria>

<output>
After completion, create `.planning/phases/05-phaser-chapter-6/05-01-SUMMARY.md`
</output>
