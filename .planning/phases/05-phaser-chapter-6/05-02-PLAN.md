---
phase: 05-phaser-chapter-6
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - public/assets/ch6-bg.png
  - public/assets/ch6-bg-stars-far.png
  - public/assets/ch6-bg-nebulae-mid.png
  - public/assets/ch6-planet-ar-vr.png
  - public/assets/ch6-planet-remoose.png
  - public/assets/ch6-planet-software-mind.png
  - public/assets/ch6-ship-1.png
  - public/assets/ch6-ship-2.png
autonomous: true
requirements:
  - ART-04
  - ART-05
  - ART-06
  - A11Y-06
must_haves:
  truths:
    - "Existe `public/assets/ch6-bg.png` con vibe synthwave (gradient deep purple → cyan, stars sutiles, no characters, no text), tamaño ≤80KB (Phase 6 budget)."
    - "Existen los 3 planet sprites `ch6-planet-{ar-vr,remoose,software-mind}.png` con bg transparente clean alpha, estilo neon-orb halo color D5-04."
    - "Existen los 2 ship sprites `ch6-ship-{1,2}.png` con bg transparente, estilo abstracto glitchy/neural (no naves realistas)."
    - "Existen 2 parallax layers opcionales `ch6-bg-stars-far.png` + `ch6-bg-nebulae-mid.png` (recommended 3-layer per RESEARCH §Pattern 7)."
    - "Cada llamada pixelforge pasa `palette: chapters[6].palette` explícito (ART-06 enforced)."
    - "ch6-assets.test.js verde — todos los 8 assets existen + ch6-bg.png ≤80KB."
    - "asset-naming.test.js T1 sigue verde — todos los nuevos ch6 assets matchean el regex extendido en W0."
  artifacts:
    - path: "public/assets/ch6-bg.png"
      provides: "Background full-frame synthwave gradient + stars sutiles"
      contains: ""
    - path: "public/assets/ch6-bg-stars-far.png"
      provides: "Parallax layer back (scrollFactor 0.2) — stars-far far parallax"
      contains: ""
    - path: "public/assets/ch6-bg-nebulae-mid.png"
      provides: "Parallax layer mid (scrollFactor 0.5) — nebulae mid parallax"
      contains: ""
    - path: "public/assets/ch6-planet-ar-vr.png"
      provides: "Planet sprite ch6-ar-vr (halo hot pink #ff3ca6)"
      contains: ""
    - path: "public/assets/ch6-planet-remoose.png"
      provides: "Planet sprite ch6-remoose (halo cyan #4dffff)"
      contains: ""
    - path: "public/assets/ch6-planet-software-mind.png"
      provides: "Planet sprite ch6-software-mind (halo amber #ffd95c)"
      contains: ""
    - path: "public/assets/ch6-ship-1.png"
      provides: "Ship sprite 1 (abstract glitchy LTR loop)"
      contains: ""
    - path: "public/assets/ch6-ship-2.png"
      provides: "Ship sprite 2 (abstract glitchy RTL loop, flipX)"
      contains: ""
  key_links:
    - from: "src/data/projects.js"
      to: "public/assets/ch6-planet-{ar-vr,remoose,software-mind}.png"
      via: "planetSprite field W0"
      pattern: "/assets/ch6-planet-(ar-vr|remoose|software-mind)\\.png"
    - from: "src/styles/chapter-themes.css [data-chapter=\"6\"]"
      to: "public/assets/ch6-bg.png"
      via: "--bg-image declaration W0"
      pattern: "/assets/ch6-bg\\.png"
---

<objective>
W1 entrega los 8 assets pixel-art ch6 generados con `forge_background` (full-frame bg, NO bg removal) + `forge_sprite` (planets/ships con bg removal preset "night") + Adobe MCP post-process para crop/resize a dimensiones exactas (1440×810 para bgs, ~288×288 para planets, ~96×72 para ships). Cada llamada incluye `palette: chapters[6].palette` explícito (ART-06). Resultado: ch6-assets.test.js verde + asset-naming.test.js T1 verde + tamaño ch6-bg.png ≤80KB (Phase 6 budget Pitfall 12).

Purpose: dejar los assets en `public/assets/` antes de que la wave de Phaser core (W2) los preload-ee. Sin estos PNGs el `SpaceScene.preload()` falla y W2 no puede entregar slice end-to-end.

Output: 8 PNG files (best case) o 6 PNG files (skip optional parallax layers — single-layer fallback). Tests asset existence verdes. Commits atómicos `art(05-02): generate ch6-{asset}` por asset.

**Slice-end-to-end check:** tras este plan, abrir `public/assets/ch6-bg.png` en preview muestra el bg synthwave; el sitio en dev (`npm run dev` + scroll a ch6) ya muestra `--bg-image` crossfade via BackgroundLayers (D2-04), aunque la escena Phaser todavía no exista (W2). Avance VISIBLE sin Phaser code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-phaser-chapter-6/05-CONTEXT.md
@.planning/phases/05-phaser-chapter-6/05-RESEARCH.md
@.planning/phases/05-phaser-chapter-6/05-PATTERNS.md
@.planning/phases/05-phaser-chapter-6/05-01-SUMMARY.md
@src/data/chapters.js
@src/data/projects.js
@.claude/agents/artist-creator.md
@.claude/agents/artist-editor.md
@.claude/skills/crear-arte-pixelforge.md
@.claude/skills/editar-arte-adobe.md
@CLAUDE.md

<interfaces>
<!-- Pixelforge tools (deferred — cargar con ToolSearch antes de invocar). -->
<!-- Adobe MCP tools (requieren adobe_mandatory_init() primero). -->

Pixelforge calls expected:
```
forge_background({
  description: "<prompt>",
  outputPath: "public/assets/ch6-bg.png",
  aspect: "16:9",
  palette: chapters[6].palette  // ['#1a0e3d', '#ff3ca6', '#4dffff', '#ffd95c']
})

forge_sprite({
  description: "<prompt>",
  outputPath: "public/assets/ch6-planet-{slug}.png",
  background: "night",  // NEVER "black"/"white" (Pitfall 6.2)
  palette: chapters[6].palette
})
```

Adobe MCP post-process tools:
```
adobe_mandatory_init()
image_crop_and_resize(inputPath, targetW, targetH, mode: "fit")
image_remove_background(inputPath)  // si alpha no clean
optimize_sprite(inputPath, downscaleTo: 1440x810)  // pixelforge tool
```

Asset specs (Resolution virtual 480×270 zoom×3 = 1440×810):
- ch6-bg.png: full-frame opaque, 1440×810 final, target ≤80KB.
- ch6-bg-stars-far.png + ch6-bg-nebulae-mid.png: 1440×810 final cada uno, opcionales (skip si bandwidth Phase 6 limita; W2 fallback single-layer).
- ch6-planet-{ar-vr,remoose,software-mind}.png: 288×288 final (96×96 base × zoom 3), alpha clean (neon-orb sin halo rectangular).
- ch6-ship-{1,2}.png: 96×72 final (32×24 base × zoom 3), alpha clean, glitchy/neural abstracto.
</interfaces>

<analog_pattern>
Phase 4 W2 (Plan 04-03 Task 2): `ch2-bg.jpg` generado con `forge_background` + Adobe post → tamaño 627KB (sobre budget — deferred polish Phase 6). Aplicar lecciones aprendidas: usar JPG en lugar de PNG para bg full-frame opaco si pixelforge lo permite + Adobe `image_crop_and_resize` con target size estricto.

Phase 4 W3 (Plan 04-04): 4 parallax layers ch4 generados con `forge_background` (stars-far/planet-mid) + `forge_sprite` (panels-fg/ships-fg). Misma estrategia para ch6 multi-layer parallax.

CLAUDE.md §6.1: "Capas intermedias: siluetas planas sin outlines" — aplica al bg-nebulae-mid si decides incluirlo (sin lineart interno que rompa lectura).

CLAUDE.md §6.2: `forge_sprite` con `background: "night"` preset (NUNCA `"black"`/`"white"`).

CLAUDE.md §6.3: `forge_background` para opacos full-frame, NUNCA `forge_sprite`.

Multi-agente routing: este plan delega a `artist-creator` agent (genera con pixelforge) y `artist-editor` agent (post-procesa con Adobe MCP). Ver `.claude/agents/artist-creator.md` y `.claude/agents/artist-editor.md`.
</analog_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generar ch6-bg.png (background synthwave full-frame) + tamaño ≤80KB</name>
  <files>public/assets/ch6-bg.png</files>
  <action>Cargar pixelforge tool schemas via `ToolSearch(query="select:mcp__pixelforge__forge_background,mcp__pixelforge__optimize_sprite", max_results=5)`. Invocar `forge_background` con prompt: "lo-fi synthwave pixel art deep space background, vertical gradient from deep purple #1a0e3d at top to electric cyan #4dffff at bottom, with subtle scattered stars and faint nebulae clouds, vaporwave aesthetic, no characters, no text, no UI elements, no outlines, smooth gradient with subtle grain, pixel art 16-bit style". outputPath `public/assets/ch6-bg.png`, aspect `"16:9"`, `palette: ['#1a0e3d', '#ff3ca6', '#4dffff', '#ffd95c', '#0a061f']` (los 4 D5-04 + opcional near-black para starfield contrast). Después invocar `optimize_sprite` con downscale a 1440×810 nearest-neighbor. Verificar tamaño con `node -e "console.log(require('fs').statSync('public/assets/ch6-bg.png').size)"` — debe ser ≤81920 bytes (80KB). Si excede, invocar `adobe_mandatory_init()` + `image_crop_and_resize` con quality decremento progresivo hasta cumplir budget (Pitfall 12 mitigation). Per D5-04 (paleta locked) + Pitfall 12 (Phase 6 budget blocker).</action>
  <verify>
    <automated>npm run test -- --run tests/assets/ch6-assets.test.js -t "T1"</automated>
  </verify>
  <done>ch6-bg.png existe en public/assets/, tamaño ≤80KB, visual inspection (open file) confirma gradient deep-purple → cyan synthwave sin characters/text/UI elements.</done>
</task>

<task type="auto">
  <name>Task 2: Generar 3 planet sprites (neon-orb halo per project)</name>
  <files>public/assets/ch6-planet-ar-vr.png, public/assets/ch6-planet-remoose.png, public/assets/ch6-planet-software-mind.png</files>
  <action>Cargar pixelforge `forge_sprite` y `process_sprite` schemas. Generar 3 planet sprites con `forge_sprite` (con bg removal automático), background preset `"night"` (NEVER `"black"`/`"white"` — Pitfall 6.2). Cada planet usa palette completa de chapters[6].palette pero highlight color distinto (halo principal):

  Planet 1 — `ch6-planet-ar-vr.png` (halo hot pink #ff3ca6): "pixel art planet orb with neon hot pink #ff3ca6 glow halo, solid filled spherical shape, no outlines, no internal lineart, retro synthwave 16-bit style, abstract not realistic, vapor wave aesthetic, ~96×96 base resolution".

  Planet 2 — `ch6-planet-remoose.png` (halo cyan #4dffff): "pixel art planet orb with neon electric cyan #4dffff glow halo, solid filled spherical shape, no outlines, no internal lineart, retro synthwave 16-bit style".

  Planet 3 — `ch6-planet-software-mind.png` (halo amber #ffd95c): "pixel art planet orb with neon soft amber #ffd95c glow halo, solid filled spherical shape, no outlines, no internal lineart, retro synthwave 16-bit style, neural/AI-themed subtle pattern".

  Tras cada generación, verificar alpha clean (no rectangular halo blanco — Pitfall 10). Si alpha sucio, invocar `adobe_mandatory_init()` + `image_remove_background` + `image_crop_and_resize` a 288×288 (96 base × zoom 3). Per D5-01 (3 planets mapping locked) + D5-04 (paleta) + ART-06 (palette governance enforced).</action>
  <verify>
    <automated>npm run test -- --run tests/assets/ch6-assets.test.js -t "T2"</automated>
  </verify>
  <done>Los 3 planet sprites existen en public/assets/, alpha clean (no halo rectangular), dimensiones ~288×288 cada uno; ch6-assets.test.js T2 (existence) verde.</done>
</task>

<task type="auto">
  <name>Task 3: Generar 2 ship sprites (abstract glitchy/neural) + 2 parallax layers opcionales</name>
  <files>public/assets/ch6-ship-1.png, public/assets/ch6-ship-2.png, public/assets/ch6-bg-stars-far.png, public/assets/ch6-bg-nebulae-mid.png</files>
  <action>**Parte 1 — Ships (mandatory):** generar 2 ship sprites con `forge_sprite`, background preset `"night"`:

  Ship 1 — `ch6-ship-1.png` (LTR top band): "pixel art abstract spaceship, glitchy neural/AI aesthetic, NOT realistic X-wing/TIE-fighter, neon hot pink #ff3ca6 + cyan #4dffff trails, retro synthwave geometry, small ~32×24 base resolution, solid filled shapes, no outlines".

  Ship 2 — `ch6-ship-2.png` (RTL bottom band): mismo prompt con variación "cyan + amber accents" para diferenciar visualmente. NOTA: en runtime W2 usará `setFlipX(true)` en SpaceScene para que ship2 mire a la izquierda — NO generar ya espejado.

  Post: Adobe MCP `image_remove_background` si alpha sucio + `image_crop_and_resize` a 96×72 (32×24 base × zoom 3). Per D5-05 (ships locked).

  **Parte 2 — Parallax layers (OPCIONAL recommended 3-layer):** generar `ch6-bg-stars-far.png` + `ch6-bg-nebulae-mid.png` con `forge_background` (NO bg removal — son backgrounds, no sprites). Per RESEARCH §Pattern 7 + §Open Q3:

  Layer stars-far (scrollFactor 0.2): "pixel art deep space starfield, tiny scattered stars on deep purple #1a0e3d background, very minimal, no large objects, 16:9 aspect, ~1440×810, low contrast for parallax depth illusion".

  Layer nebulae-mid (scrollFactor 0.5): "pixel art faint cosmic nebula clouds in hot pink #ff3ca6 + cyan #4dffff hues, soft glow, on deep purple background, mid-depth parallax layer, ~1440×810, no defined edges". Aplicar CLAUDE.md §6.1 (siluetas planas sin outlines internos para evitar transparencias internas que rompen lectura).

  Si bandwidth Phase 6 es preocupación (cada layer 80KB extra), planner decide skip optional layers — W2 fallback single-layer (`ch6-bg.png` con scrollFactor 1.0 cubriendo descenso). Si se generan, target tamaño ≤80KB cada uno (mismo budget Phase 6). Per ART-04 + ART-05 + Open Q3 (planner decide multi-layer count).</action>
  <verify>
    <automated>npm run test -- --run tests/assets/ch6-assets.test.js tests/assets/asset-naming.test.js</automated>
  </verify>
  <done>2 ship sprites + 2 parallax layers (best case 4 PNGs nuevos) existen en public/assets/; ch6-assets.test.js T1-T4 verdes; asset-naming.test.js T1 verde sin offenders (regex extendido W0 acepta los nuevos descriptors); si planner decidió skip optional parallax, dejar comentario en SUMMARY.md "W2 usa single-layer fallback".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pixelforge MCP (Gemini API) → filesystem | Salida de Gemini escribe directamente a public/assets/; trust limited |
| Adobe MCP → filesystem | Operaciones de crop/resize sobre archivos generados |
| GEMINI_API_KEY env var → pixelforge process | Hereda env (NO --env flag — CLAUDE.md §4.3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W1-01 | Information disclosure | GEMINI_API_KEY accidentally en .claude.json | mitigate | CLAUDE.md §4.3: NUNCA `--env GEMINI_API_KEY=...`; usar `setx` env var; verify `echo $env:GEMINI_API_KEY` antes de invocar pixelforge |
| T-05-W1-02 | Tampering | Asset filename drift (ch6-spaceship-1 vs ch6-ship-1) | mitigate | asset-naming.test.js T1 regex extendido W0 rechaza descriptors no listados (Pitfall 11) |
| T-05-W1-03 | DoS | Bundle size blow ≤80KB exceeded | mitigate | Task 1 verifica tamaño + Adobe MCP fallback (Pitfall 12); test ch6-assets.test.js T4 enforces budget |
| T-05-W1-04 | Tampering | pixelforge generates content fuera del prompt (animal, character) | accept | Inspección visual por executor antes de commit; W5 manual checklist Rafael ratifica vibe (D5-04) |
| T-05-W1-05 | Information disclosure | Output incluye watermark / signature inadvertido de Gemini | accept | Gemini flash-image no añade watermarks; risk muy bajo |
</threat_model>

<verification>
- `npm run test -- --run tests/assets`: ch6-assets.test.js T1-T4 verdes + asset-naming.test.js T1-T4 verdes.
- File size manual: `node -e "console.log(require('fs').statSync('public/assets/ch6-bg.png').size, 'bytes')"` — esperado ≤81920.
- Visual smoke: abrir cada PNG en preview/explorer y verificar vibe synthwave coherente con D5-04.
- `npm run dev` + scroll a ch6: BackgroundLayers (Phase 2 W3) crossfade ch5→ch6 muestra ch6-bg.png — primer signal visible end-to-end.
- Frontmatter validation: `gsd-sdk query frontmatter.validate ".planning/phases/05-phaser-chapter-6/05-02-PLAN.md" --schema plan`.
</verification>

<success_criteria>
- 6-8 PNG files committed en public/assets/ siguiendo naming `ch6-{descriptor}.png` (matching regex W0).
- ch6-bg.png tamaño ≤80KB (Phase 6 budget Pitfall 12).
- Los 3 planet sprites con alpha clean (no halo rectangular blanco — Pitfall 10).
- Los 2 ship sprites con alpha clean, estilo abstracto glitchy.
- Si parallax layers opcionales generadas: tamaño individual ≤80KB cada una.
- Cada asset usa palette de chapters[6] enforced (ART-06).
</success_criteria>

<output>
After completion, create `.planning/phases/05-phaser-chapter-6/05-02-SUMMARY.md` que incluya: lista de assets generados, tamaño bytes de cada uno, decisión sobre parallax layers opcionales (generated / skipped + razón), captura del primer dev preview con ch6-bg crossfade.
</output>
