---
phase: 05-phaser-chapter-6
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/phaser/index.js
  - src/phaser/SpaceScene.js
autonomous: true
requirements:
  - PHA-01
  - PHA-03
  - PHA-05
  - PHA-06
  - PHA-07
  - PHA-08
  - A11Y-05
  - ART-04
must_haves:
  truths:
    - "`src/phaser/index.js` exporta `createGame(parentEl, { prefersReduced })` que devuelve `new Phaser.Game(config)` con `Phaser.Scale.NONE` + `Math.floor` zoom + `pixelArt:true` + `roundPixels:true` + `physics: { default: 'none' }` + `backgroundColor: '#1a0e3d'` (PHA-01, PHA-03)."
    - "`src/phaser/index.js` registra `prefersReduced` en `game.registry` via `callbacks.preBoot` (Pattern 9 Option B)."
    - "`src/phaser/index.js` comenta explícitamente \"stay on Phaser 3.x — do not upgrade to 4.x\" (Pitfall 14)."
    - "`src/phaser/SpaceScene.js` extends `Phaser.Scene`, preload carga las 6 sprite keys (bg + 3 planets + 2 ships), create instancia 3 planets distribuidos verticalmente (Y = planetOrbit * ARRIVAL_DESCENT + 135), 2 ships con tween loop horizontal, parallax layers con setScrollFactor escalonado (0.2 / 0.5 / 1.0), camera arrival tween (3500ms, Power2.easeOut) → emit `vue:arrival-complete` (PHA-05)."
    - "SpaceScene registra `setInteractive` con `Phaser.Geom.Circle` hit area generosa (+16px halo) en cada planet + `pointerdown` emite `vue:show-project` con `projectsData[idx].id` (PHA-07)."
    - "SpaceScene registra listener `game.events.on('locale-changed', handleLocaleChange)` + cleanup en SHUTDOWN; handler re-translates tooltipTexts visibles via `i18n.global.t(titleKey)` (PHA-06, Pattern 5)."
    - "PRM detection via `this.registry.get('prefersReduced')` (Pattern 9): arrival = `cameras.main.setScroll` directo (no tween) + ships static positions + `this.tweens.timeScale = 0` cinturón (D5-08, A11Y-05)."
    - "Cero `this.anims.create`, `spritesheet*frames`, `playAnimation` en SpaceScene (PHA-08, Pitfall character animation)."
    - "Cero captura de eventos `wheel` o `touchmove` que rompen scroll-snap del documento (Pitfall 6)."
  artifacts:
    - path: "src/phaser/index.js"
      provides: "createGame factory function"
      contains: "Phaser.Scale.NONE"
      min_lines: 30
    - path: "src/phaser/SpaceScene.js"
      provides: "SpaceScene class with preload/create/handleLocaleChange"
      contains: "extends Phaser.Scene"
      min_lines: 100
  key_links:
    - from: "src/phaser/index.js"
      to: "src/phaser/SpaceScene.js"
      via: "import + scene array config"
      pattern: "import.*SpaceScene.*from.*SpaceScene"
    - from: "src/phaser/SpaceScene.js"
      to: "src/i18n"
      via: "singleton import for tooltip text"
      pattern: "import.*i18n.*from.*['\"]@/i18n['\"]"
    - from: "src/phaser/SpaceScene.js"
      to: "src/data/projects.js"
      via: "import projects para iterar chapterEra=6"
      pattern: "import.*projects.*from.*['\"]@/data/projects['\"]"
    - from: "SpaceScene.preload"
      to: "public/assets/ch6-{bg,planet-*,ship-*}.png"
      via: "this.load.image() calls"
      pattern: "this\\.load\\.image\\(['\"]ch6-"
---

<objective>
W2 entrega el "corazón Phaser" del chapter 6: factory + SpaceScene completa. Sin componente Vue todavía — esa wire es W3. Pero el módulo Phaser ya es importable y construible (verificable vía tests source-regex sobre la forma del código, dado que jsdom no renderea WebGL).

Purpose: aislar la lógica Phaser en su propio módulo (`src/phaser/`) para que (a) Vite genere chunk separado en build (PHA-04 lazy), (b) los tests de Chapter6Content (W3) puedan mockearlo con `vi.mock('@/phaser', ...)`, (c) el código Phaser viva en JS sandbox aparte del reactive Vue.

Output: 2 archivos JavaScript ESM nuevos. Tests Phase 5 phaser/* todos verdes (factory, scale, space-scene-shape, no-character-animation, locale-bridge, prm).

**Slice-end-to-end check:** tras este plan, importar manualmente `createGame` desde una consola dev no rompe (el game se puede crear standalone si se le pasa un container DOM). En el sitio en sí, ch6 sigue mostrando placeholder porque ScrollShell aún no monta Chapter6Content (eso es W3). Avance INTERNO + tests verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-phaser-chapter-6/05-CONTEXT.md
@.planning/phases/05-phaser-chapter-6/05-RESEARCH.md
@.planning/phases/05-phaser-chapter-6/05-PATTERNS.md
@.planning/phases/05-phaser-chapter-6/05-01-SUMMARY.md
@.planning/phases/05-phaser-chapter-6/05-02-SUMMARY.md
@src/data/projects.js
@src/data/chapters.js
@src/i18n/index.js
@tests/phaser/factory.test.js
@tests/phaser/scale.test.js
@tests/phaser/space-scene-shape.test.js
@tests/phaser/no-character-animation.test.js
@tests/phaser/locale-bridge.test.js
@tests/phaser/prm.test.js
@CLAUDE.md
@.claude/skills/dev-vue3-phaser.md

<interfaces>
<!-- Phaser 3.86 API contracts (npm view phaser → 3.86.x range pinned ^3.86.0). -->

createGame factory contract (consumed by Chapter6Content.vue W3):
```
export function createGame(parentEl: HTMLElement, opts?: { prefersReduced?: boolean }): Phaser.Game
```

SpaceScene contract (consumed by Phaser.Game scene array):
```
class SpaceScene extends Phaser.Scene {
  constructor()           // super({ key: 'SpaceScene' })
  preload()               // this.load.image('ch6-bg', ...), planets, ships
  create()                // bg layers + 3 planets + 2 ships + arrival tween + listeners
  handleLocaleChange(locale: string)  // re-translates tooltipTexts visibles
}
```

Events emitted on game.events:
- `vue:show-project` (id: string) — Phaser → Vue, planeta clickeado
- `vue:arrival-complete` () — Phaser → Vue, arrival tween onComplete o instant cut bajo PRM
- `viewport-resized` () — Phaser → Vue (opcional W3 ResizeObserver wire)

Events listened on game.events:
- `locale-changed` (locale: string) — Vue → Phaser, re-render tooltips

i18n singleton import (Phase 2 W0):
```
import { i18n } from '@/i18n'
// uso: i18n.global.t('projects.ch6-ar-vr.title')
```

projects data (Phase 5 W0 W0):
```
projects.filter(p => p.chapterEra === 6)
// → [{ id: 'ch6-ar-vr', planetSprite, planetOrbit, planetColor, titleKey, ... }, ...]
```

PRM heuristic D5-08 (cinturón cross-cutting):
- arrival: instant cut (`cameras.main.setScroll(0, ARRIVAL_DESCENT - 135)` directo + emit `vue:arrival-complete` sin tween)
- ships: estáticas en posición fija (no tweens)
- parallax layers: scrollFactor 1.0 todas (sin diferencial — D5-08 mandato)
- `this.tweens.timeScale = 0` cinturón si algún tween escapa al PRM check
</interfaces>

<analog_pattern>
**No analog en codebase** — primera integración Phaser. Source-of-truth: RESEARCH.md Pattern 1 (createGame factory verbatim), Pattern 3 (integer scale formula), Pattern 5 (locale bridge listener), Pattern 6 (planet click bridge), Pattern 7 (parallax + arrival tween), Pattern 8 (ships loop), Pattern 9 (PRM via registry), Pattern 13 (i18n singleton).

dev-vue3-phaser.md skill: aplicar patterns documentados pero NOTAR (RESEARCH §Runtime State Inventory): "el skill referencia `src/scenes/ParallaxScene.js` legacy. Phase 5 usa `src/phaser/` instead. Update skill in W5 polish." Plan 05 NO actualiza skill — solo deja la nota para W5.

phaser/index.js skeleton verbatim: RESEARCH lines 354-396.
SpaceScene.js patterns clave: RESEARCH lines 656-707 (locale listener), lines 720-745 (click bridge), lines 800-852 (parallax + arrival), lines 874-909 (ships loop).
</analog_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear src/phaser/index.js — factory createGame() + config Phaser.Scale.NONE + integer zoom + registry PRM</name>
  <files>src/phaser/index.js</files>
  <behavior>
    - Exporta named `createGame(parentEl, { prefersReduced } = {})` que retorna `new Phaser.Game(config)`.
    - Constantes `BASE_W = 480`, `BASE_H = 270` declaradas.
    - Función `computeZoom()` devuelve `Math.min(Math.floor(vw/BASE_W), Math.floor(vh/BASE_H)) || 1` (defensive fallback).
    - Config Phaser: type Phaser.AUTO, parent: parentEl (DOM node NO id string — Pitfall pattern), width/height base, zoom dinámico, pixelArt:true, roundPixels:true, backgroundColor:'#1a0e3d', transparent:false, physics:{default:'none'}, scale:{mode:Phaser.Scale.NONE, autoCenter:Phaser.Scale.CENTER_BOTH}, scene:[SpaceScene].
    - `callbacks.preBoot` setea `game.registry.set('prefersReduced', !!prefersReduced)` (Pattern 9 Option B).
    - Comentario top-level (líneas iniciales): "// Phaser 3.86 — stay on 3.x major; do NOT upgrade to Phaser 4.x publicado 2026-04-30 (Pitfall 14, requires re-validation)."
    - Import top: `import Phaser from 'phaser'` + `import { SpaceScene } from './SpaceScene'`.
    - factory.test.js T1-T6 verdes.
    - scale.test.js T1-T3 verdes (BASE_W=480, BASE_H=270, Math.min+Math.floor+|| 1).
  </behavior>
  <action>Crear `src/phaser/index.js` siguiendo RESEARCH §Pattern 1 skeleton (líneas 354-396) verbatim, con dos adiciones: (a) comentario block top documentando "stay on Phaser 3.x" (Pitfall 14 mitigation), (b) JSDoc-style comments en createGame indicando los args y return type. Importar Phaser default + SpaceScene named. Verificar que el código pasa los regex tests (factory.test.js T1-T6 buscan literales `Phaser.Scale.NONE`, `Math.floor`, `pixelArt: true`, `roundPixels: true`, `physics: { default: 'none' }`, `parent: parentEl`). Per PHA-01 (factory shape) + PHA-03 (integer scale formula) + Pattern 9 Option B (registry PRM) + Pitfall 14 (3.x pinning).</action>
  <verify>
    <automated>npm run test -- --run tests/phaser/factory.test.js tests/phaser/scale.test.js</automated>
  </verify>
  <done>src/phaser/index.js existe, exporta createGame, factory.test.js + scale.test.js verdes; sin top-level side effects (no `new Phaser.Game()` al importar — solo factory).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear src/phaser/SpaceScene.js — preload + create (3 planets + 2 ships + parallax + arrival camera tween) + listeners</name>
  <files>src/phaser/SpaceScene.js</files>
  <behavior>
    - Named export `class SpaceScene extends Phaser.Scene` con constructor `super({ key: 'SpaceScene' })`.
    - Constructor inicializa `this.tooltipTexts = []`, `this.planets = []`.
    - Import top: `import Phaser from 'phaser'` + `import { i18n } from '@/i18n'` + `import { projects } from '@/data/projects'`.
    - preload(): `this.load.image('ch6-bg', '/assets/ch6-bg.png')` + 3 planets (keys: `ch6-planet-ar-vr`, `ch6-planet-remoose`, `ch6-planet-software-mind`) + 2 ships (`ch6-ship-1`, `ch6-ship-2`) + parallax layers opcionales (`ch6-bg-stars-far`, `ch6-bg-nebulae-mid`) si los archivos existen post W1 — usar `this.load.on('loaderror', ...)` defensive para skip silent si los opcionales faltan, single-layer fallback (Open Q3 documented).
    - create():
      - Lee `prefersReduced` de `this.registry.get('prefersReduced')`.
      - Constante `ARRIVAL_DESCENT = 270 * 3` (3 viewport-heights descent).
      - Parallax: si las 2 layers opcionales cargaron, instancia stars-far con scrollFactor 0.2 y nebulae-mid con 0.5 (setDisplaySize 480, 270×4 para cubrir descenso). Si NO cargaron, single bg con scrollFactor 1.0 cubriendo descenso.
      - Filtra `this.projectsData = projects.filter(p => p.chapterEra === 6)` (3 items orden cronológico ar-vr/remoose/software-mind via planetOrbit ascendente).
      - forEach planet (idx 0..2): instancia sprite en `(240, proj.planetOrbit * ARRIVAL_DESCENT + 135)` con key `ch6-planet-${proj.id.replace('ch6-', '')}` + setScrollFactor(1.0) + setInteractive con `Phaser.Geom.Circle` hit area generosa (planet.width/2 + 16) + pointerdown emite `this.game.events.emit('vue:show-project', proj.id)` + pointerover (desktop only — guard `this.sys.game.device.input.touch`) sets `this.input.setDefaultCursor('pointer')` + tooltip text aparece + pointerout limpia cursor + tooltip oculta. Acumular tooltip ref + titleKey en `this.tooltipTexts`.
      - 2 ships: ship1 en (-50, 80) y ship2 en (530, 200) con setScrollFactor(0) (sticky-to-camera) + setDepth(50). ship2 setFlipX(true). Si NOT prefersReduced: this.tweens.add ship1 → x=530 duration:12000 repeat:-1 onRepeat setX(-50); ship2 → x=-50 duration:18000 repeat:-1 onRepeat setX(530) (Pattern 8). Si prefersReduced: ships estáticas posiciones decorativas (ship1.setX(120), ship2.setX(360)).
      - Arrival camera: this.cameras.main.setScroll(0,0). Si prefersReduced: setScroll directo a `(0, ARRIVAL_DESCENT - 135)` + emit `vue:arrival-complete` instant. Si NOT prefersReduced: this.tweens.add cameras.main scrollY → ARRIVAL_DESCENT - 135 duration:3500 ease:'Power2.easeOut' onComplete emit `vue:arrival-complete`.
      - PRM cinturón: si prefersReduced, `this.tweens.timeScale = 0` después de los tweens add (D5-08 safety net).
      - Locale listener: `this.game.events.on('locale-changed', this.handleLocaleChange, this)` + cleanup en `this.events.on(Phaser.Scenes.Events.SHUTDOWN, () => this.game.events.off('locale-changed', this.handleLocaleChange, this))`.
    - handleLocaleChange(locale): forEach tooltipTexts, si tooltip.visible, `tooltip.setText(i18n.global.t(titleKey))`.
    - NO `this.anims.create`, NO `spritesheet*frames`, NO `playAnimation` (PHA-08).
    - NO `this.input.mouse.preventDefaultWheel`, NO listeners en `wheel`/`touchmove` (Pitfall 6).
  </behavior>
  <action>Crear `src/phaser/SpaceScene.js` siguiendo RESEARCH §Pattern 5+6+7+8+9+13 skeletons verbatim, combinados en una sola scene class. Estructura recomendada: comentario header top (referencias D5-01..D5-08 + research patterns), imports, constructor, preload, create (sección parallax → planets → ships → arrival → listeners → PRM cinturón), handleLocaleChange. Verificar manualmente que la implementación:
  - Pasa los regex tests de space-scene-shape.test.js (extends Phaser.Scene + preload keys + planets/ships forEach + camera tween + registry PRM + singleton i18n + emit vue:arrival-complete).
  - Pasa los regex tests de no-character-animation.test.js (NO this.anims.create, NO spritesheet*frames, NO playAnimation).
  - Pasa los regex tests de locale-bridge.test.js (listener + cleanup + handler re-translates).
  - Pasa los regex tests de prm.test.js (this.registry.get('prefersReduced') + PRM branch setScroll + tweens.timeScale=0).

  Sobre la "tooltip in-Phaser" (D5-10 + Claude's discretion): implementar tooltip como `Phaser.GameObjects.Container` con Rectangle (background cyan glow) + Text encima. Tooltip oculto por default, visible solo en pointerover desktop. NO requiere asset pixelforge adicional — pure Phaser geometry. Per D5-10 (locale bridge granularity locked) + D5-04 (paleta).</action>
  <verify>
    <automated>npm run test -- --run tests/phaser/space-scene-shape.test.js tests/phaser/no-character-animation.test.js tests/phaser/locale-bridge.test.js tests/phaser/prm.test.js</automated>
  </verify>
  <done>src/phaser/SpaceScene.js existe con clase SpaceScene completa; los 4 tests source-regex (space-scene-shape, no-character-animation, locale-bridge, prm) verdes; tests phaser/* suite full verde; suite global no regression (151 baseline + nuevos verdes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vue app → Phaser sandbox | Bridge events vía game.events; Vue no accede directamente a scene internals |
| Phaser → DOM | Canvas insertado en parentEl; pointer events capturados solo por Phaser dentro del canvas |
| i18n singleton → Phaser scene | i18n.global.t() read-only en Phaser; Phaser no muta el i18n state |
| projects.js → SpaceScene | Filter chapterEra=6 lee datos estáticos; no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W2-01 | DoS | Memory leak si destroy() no se llama (W3 fix) | mitigate | factory crea instance; W3 Chapter6Content.vue lifecycle PHA-02 destroy(true,false) garantiza cleanup |
| T-05-W2-02 | Tampering | Bridge event `vue:show-project` con ID malformado | mitigate | W3 Chapter6Content valida `projects.find(p => p.id === id)` con null guard; aquí solo emisor |
| T-05-W2-03 | Spoofing | Listener game.events.on('locale-changed') invocado por componente arbitrario | mitigate | game.events es scope dentro del game; solo Chapter6Content.vue (W3) emite. Risk muy bajo |
| T-05-W2-04 | DoS | Phaser captura wheel/touchmove rompiendo scroll-snap | mitigate | Pitfall 6: NO this.input.mouse.preventDefault*; verificado por absence regex en SpaceScene |
| T-05-W2-05 | DoS | character animation memory bloat | mitigate | PHA-08 + no-character-animation.test.js — regex anti-pattern enforced |
| T-05-W2-06 | DoS | Phaser bundle no tree-shaken inflando initial chunk | mitigate | W3 wraps import en dynamic import('@/phaser') — split chunk verificado en lazy.test.js |
</threat_model>

<verification>
- `npm run test -- --run tests/phaser`: los 6 tests Phaser todos verdes (factory, scale, space-scene-shape, no-character-animation, locale-bridge, prm).
- `npm run test:run` global: 151 baseline + Phase 5 W0 (data tests verdes) + Phase 5 W2 (phaser tests verdes) + tests RED de waves siguientes esperados.
- `node -e "import('./src/phaser/index.js').then(m => console.log(typeof m.createGame))"` (smoke import) — debe imprimir "function".
- Frontmatter validation: `gsd-sdk query frontmatter.validate ".planning/phases/05-phaser-chapter-6/05-03-PLAN.md" --schema plan`.
</verification>

<success_criteria>
- src/phaser/index.js exporta `createGame` con config completo PHA-01/03 + registry PRM (Pattern 9 Option B).
- src/phaser/SpaceScene.js implementa preload + create + handleLocaleChange + cleanup SHUTDOWN.
- 3 planets distribuidos verticalmente vía planetOrbit field (W0 data) — Y = planetOrbit * ARRIVAL_DESCENT + 135.
- 2 ships con tween loop horizontal escalonado (12s LTR, 18s RTL); estáticas bajo PRM.
- Camera arrival tween 3500ms Power2.easeOut → emit vue:arrival-complete; instant cut bajo PRM.
- Locale bridge: listener registrado + cleanup en SHUTDOWN + handler re-translates tooltipTexts visibles.
- Cero character animation (PHA-08 enforced); cero captura de wheel/touchmove (Pitfall 6).
- Comentario "stay on Phaser 3.x" en index.js (Pitfall 14 mitigation).
</success_criteria>

<output>
After completion, create `.planning/phases/05-phaser-chapter-6/05-03-SUMMARY.md` que incluya: estructura final del src/phaser/, decisión sobre tooltip styling (Container vs sprite), decisión sobre multi-layer parallax (3-layer vs single fallback según outcome W1), confirmación de PRM heuristic completo D5-08.
</output>
