---
phase: 05-phaser-chapter-6
plan: 06
type: execute
wave: 5
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - .planning/phases/05-phaser-chapter-6/05-MANUAL-CHECKLIST.md
  - .planning/phases/05-phaser-chapter-6/05-VERIFICATION.md
  - src/i18n/es.json
  - src/i18n/en.json
autonomous: false
requirements:
  - CON-04
  - PHA-04
  - PHA-09
  - A11Y-05
  - A11Y-06
  - ART-04
  - ART-06
must_haves:
  truths:
    - "Archivo `05-MANUAL-CHECKLIST.md` existe derivado de 05-VALIDATION.md §Manual-Only Verifications con 12 items concretos a verificar por Rafael."
    - "Archivo `05-VERIFICATION.md` draft existe documentando resultado de `npm run test:run` + `npm run build` + chunk size Phaser + bundle size delta vs Phase 4."
    - "Rafael firma §10 del 05-MANUAL-CHECKLIST.md con verdict (PASS / PASS-with-observations / FAIL + razón) + fecha."
    - "Refresh post-execute de i18n keys ch6 descs en es.json/en.json si Rafael actualizó CONTENT-CHECKLIST §2.5 paralelo (patrón D4-09 / D3-08)."
    - "Si Rafael ratifica mantra ES copy diferente al default \"Y siempre muestra una sonrisa\", chapters.6.mantra ES actualizada (D5-03 + Claude's discretion documented)."
  artifacts:
    - path: ".planning/phases/05-phaser-chapter-6/05-MANUAL-CHECKLIST.md"
      provides: "Checklist visual + functional + A11Y + bundle size para Rafael"
      contains: "§10"
      min_lines: 80
    - path: ".planning/phases/05-phaser-chapter-6/05-VERIFICATION.md"
      provides: "Verification draft con suite results + chunk metrics"
      contains: "verdict"
  key_links:
    - from: ".planning/phases/05-phaser-chapter-6/05-MANUAL-CHECKLIST.md §10"
      to: "STATE.md / ROADMAP.md Phase 5 closed"
      via: "Rafael firma → executor cierra phase en STATE.md"
      pattern: "verdict:"
---

<objective>
W5 cierra Phase 5 con la única wave human-verify gate. Genera `05-MANUAL-CHECKLIST.md` (derivado de 05-VALIDATION.md §Manual-Only Verifications) con 12 items específicos que Rafael ejecuta personalmente — todos cubren behaviors que jsdom no puede verificar (HiDPI pixel-perfect, arrival timing percibido, ships loop visual, locale tooltips in-Phaser, project overlay UX, keyboard navigation real, PRM behaviors, ch5↔ch6 transitions sin leak, chapter-overlap bug vigilancia datapoint, bundle size budget, mantra copy ratification, visual vibe coherence synthwave).

Genera `05-VERIFICATION.md` draft con resultados automáticos (suite test global, build, chunk Phaser size). Rafael firma §10 → executor cierra Phase 5 en STATE.md/ROADMAP.md.

Adicionalmente: si Rafael actualizó CONTENT-CHECKLIST §2.5 con copy real para los 3 proyectos ch6 (paralelo al execute de Waves 1-5, patrón D4-09), refresh i18n keys descs en es.json/en.json reemplazando los stubs "PENDING — Rafael refresca §2.5: ..." con copy real.

Purpose: cerrar el bucle Phase 5 completo con gate humano + documentación de hallazgos (chunk size, chapter-overlap bug datapoint, vibe ratification).

Output: 2 archivos docs nuevos + posibles refresh de 2 archivos i18n. Rafael firma sign-off → Phase 5 cerrada.

**Slice-end-to-end check:** este es el cierre formal. Tras Rafael firma `verdict: PASS` o `PASS-with-observations`, la fase queda CERRADA y STATE.md actualiza progress + Phase 6 (Deploy + Polish) puede arrancar planning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-phaser-chapter-6/05-CONTEXT.md
@.planning/phases/05-phaser-chapter-6/05-VALIDATION.md
@.planning/phases/05-phaser-chapter-6/05-01-SUMMARY.md
@.planning/phases/05-phaser-chapter-6/05-02-SUMMARY.md
@.planning/phases/05-phaser-chapter-6/05-03-SUMMARY.md
@.planning/phases/05-phaser-chapter-6/05-04-SUMMARY.md
@.planning/phases/05-phaser-chapter-6/05-05-SUMMARY.md
@.planning/phases/04-chapters-0-2-4-5/04-MANUAL-CHECKLIST.md
@.planning/phases/02-theme-system-i18n/02-MANUAL-CHECKLIST.md
@.planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/i18n/es.json
@src/i18n/en.json

<interfaces>
<!-- 05-MANUAL-CHECKLIST.md structure (derived from 04-MANUAL-CHECKLIST.md format). -->

Sections (12 items mapped a 05-VALIDATION.md Manual-Only Verifications):
1. HiDPI pixel-perfect rendering (PHA-03, Success Criteria 4)
2. Arrival cinematográfico timing ~3-4s con easing (PHA-05, D5-02)
3. 2 ships loop horizontal escalonado visual (D5-05)
4. Locale toggle in-Phaser tooltips ES↔EN sin reload (PHA-06, Success Criteria 2)
5. Project overlay open/close on planet click + tap mobile + ESC + click-outside + close button (PHA-07, Success Criteria 1)
6. Keyboard navigation post-canvas (3 sr-only buttons + Tab + Enter) (A11Y-02, D5-06)
7. PRM-aware behaviors (arrival instant cut + ships estáticos + mantra sin fade + overlay instant) (A11Y-05, D5-08)
8. ch5↔ch6 transitions sin canvas duplicado o WebGL errors (Success Criteria 3) — 3 ciclos navegación
9. Chapter overlap bug datapoint (Phase 4 deferred vigilancia) — reproduce o no en ch6 (CORE-04 datapoint)
10. Bundle size verification — `npm run build` + chunk Phaser `dist/assets/*phaser*` ≤200KB gzip (PHA-04)
11. ch6-bg.png ≤80KB cumulative budget (Phase 6 blocker carry-forward)
12. Mantra ES copy ratification (CON-04, A11Y-06)
13. Visual vibe synthwave coherence (D5-04)

§10 Sign-off:
- Rafael ejecuta el checklist
- Marca cada item ✓ / ⚠ / ✗ con notas si aplica
- Decide verdict: PASS / PASS-with-observations / FAIL
- Firma con fecha

05-VERIFICATION.md structure (analog 02-VERIFICATION.md / 04-VERIFICATION.md format):
- Header: phase, date, verdict draft
- Test suite results: `npm run test:run` (total, passed, failed, skipped)
- Build results: `npm run build` size deltas, chunk Phaser exact size, gzip estimate
- Test-by-plan breakdown (Plan 05-01..05-05 + tests verde per plan)
- Programmatic gate ✓ + manual gate pending Rafael
</interfaces>

<analog_pattern>
**`04-MANUAL-CHECKLIST.md`** (Phase 4 W5) — closest analog para format del checklist. Sigue el patrón:
- Sections numeradas con título + descripción del comportamiento esperado + steps específicos to verify + criterio PASS/FAIL claro.
- §10 sign-off con verdict template + fecha + notas.

**`02-MANUAL-CHECKLIST.md`** (Phase 2 W5) — analog para sign-off block format + status tracking (✓/⚠/✗).

**`02-VERIFICATION.md`** (Phase 2) y **`04-VERIFICATION.md`** (Phase 4) — analogs para verification document format (test suite results + verdict + caveats).

**CONTENT-CHECKLIST §2.5** (Phase 3, ch6 — pending Rafael): si Rafael actualizó §2.5 con titles/descs reales para los 3 ch6 projects, refresh i18n keys (analog D4-09 pattern Phase 4 que refrescó ch2/ch4/ch5 descs post-W5).
</analog_pattern>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Generar 05-MANUAL-CHECKLIST.md + 05-VERIFICATION.md draft + Rafael ejecuta checklist + firma §10</name>
  <what-built>
    Phase 5 ejecutó 5 planes en 5 waves: W0 scaffolding (data + theme + i18n + 16 test scaffolds RED), W1 8 assets pixelforge + Adobe post, W2 src/phaser/{index,SpaceScene}.js, W3 Chapter6Content.vue + ScrollShell wire + chapter-themes.css ch6 components, W4 ProjectOverlay synthwave + focus trap.

    Resultado: chapter 6 funcional end-to-end con escena Phaser arrival cinematográfico + 3 planet-projects clickables + 2 ships loop + mantra HTML fade-in + overlay synthwave + locale bridge + PRM heuristic completo + 3 sr-only keyboard A11Y. Tests automáticos: suite global Phase 1-4 (151 baseline) + Phase 5 W0-W4 (~30 nuevos) todos verdes.

    Lo que Phase 5 NO cubre programáticamente:
    - HiDPI/Retina pixel-perfect render (jsdom no renderea WebGL).
    - Arrival timing percibido + ease feel (subjective).
    - Tooltip in-Phaser locale switch (visual).
    - Mobile touch tap planeta + mobile fullscreen overlay (real device).
    - PRM behaviors observados en sistema OS prefers-reduced-motion: reduce activo.
    - Bundle size build verificado en tiempo real (chunk Phaser separation).
    - Chapter overlap bug Phase 4 datapoint (vigilancia visual no fix).
    - Mantra copy ES ratification (preferencia lingüística de Rafael).
    - Visual vibe synthwave coherence aesthetic judgment.
  </what-built>
  <how-to-verify>
    **Paso 0 — Generar artefactos antes de checkpoint:**

    Como executor, antes de pasar al checkpoint humano:

    1. Crear `.planning/phases/05-phaser-chapter-6/05-MANUAL-CHECKLIST.md` con 13 sections + §10 sign-off, derivado de 05-VALIDATION §Manual-Only Verifications. Format analog 04-MANUAL-CHECKLIST.md. Cada section debe tener:
       - Título numerado (§1, §2, ... §13).
       - Descripción del comportamiento esperado (1-2 párrafos).
       - Steps específicos (numerados): `1. Abrir http://127.0.0.1:5173/?ch=6`, `2. Verificar X`, etc.
       - Criterio PASS / FAIL claro (e.g., "PASS si planet bordes nítidos sin blur en Retina").
       - Notes: campo libre para Rafael documentar observaciones.
       - Status checkbox: [ ] ✓ Pass / [ ] ⚠ Observations / [ ] ✗ Fail.

       Secciones obligatorias:
       §1 — HiDPI pixel-perfect: abrir `http://127.0.0.1:5173/?ch=6` en monitor Retina/HiDPI; planets + ships borders nítidos sin blur. Math.floor(vw/480, vh/270) zoom integer matching.
       §2 — Arrival cinematográfico: cold mount ch6 → cámara desciende ~3.5s con easing Power2.easeOut → 3 planets revelados secuencialmente arriba→fondo → naves loop empieza solo post-arrival.
       §3 — 2 ships loop horizontal: tras arrival, esperar 30s → ship 1 LTR cada ~12s, ship 2 RTL cada ~18s, sin overlap chocante.
       §4 — Locale toggle tooltips in-Phaser: hover sobre cada planet desktop → tooltip text aparece → cambiar locale ES↔EN → tooltip text cambia sin reload.
       §5 — Project overlay UX completo: click cada planet desktop + tap mobile → overlay aparece con title/role/techStack/desc correcto → ESC cierra → click backdrop cierra → close X cierra → restore focus al planet-button trigger.
       §6 — Keyboard navigation: Tab desde LangToggle → 3 sr-only buttons (chronological order: ar-vr → remoose → software-mind) → Enter abre overlay correcto.
       §7 — PRM-aware: sistema → activar prefers-reduced-motion → recargar ch6 → arrival instant cut (no descenso) + ships estáticos + mantra sin fade + overlay sin scale animation.
       §8 — ch5↔ch6 transitions: navegar ch5→ch6→ch5→ch6→ch5→ch6 (3 ciclos); DevTools Console sin errores WebGL; performance.memory no crece indefinidamente; sin canvas duplicados en DOM inspector.
       §9 — Chapter overlap bug Phase 4 datapoint: scroll desde ch3 → ch6 sequential; documentar SI ch6 agrava el bug existente, SI lo reproduce igual, o SI NO lo reproduce (datapoint útil para root cause analysis posterior). Capturar screenshot si reproduce.
       §10 — Bundle size verification: `npm run build` → ls -la `dist/assets/*phaser*.js` → tamaño raw + gzip estimado (`gzip -c dist/assets/*phaser*.js | wc -c`) → PASS si chunk Phaser ≤200KB gzipped (research estimate ~150KB; >200KB = investigar tree-shaking).
       §11 — ch6-bg.png ≤80KB: `node -e "console.log(require('fs').statSync('public/assets/ch6-bg.png').size, 'bytes')"` → PASS si ≤81920 (Phase 6 budget blocker carry-forward).
       §12 — Mantra ES copy ratification: leer "Y siempre muestra una sonrisa" en chapters.6.mantra ES → Rafael decide si suena natural o quiere alternativa (ej. "Y muestra siempre una sonrisa", "Siempre con una sonrisa", etc.). Si Rafael ajusta, executor refresca i18n key post-firma.
       §13 — Visual vibe synthwave coherence: aesthetic check del render conjunto (bg + planets + ships + overlay + mantra) → Rafael ratifica vibe "lo-fi AI vaporwave" o solicita refresh de algún asset (planet color tweak, ship style ajuste).
       §10 (sign-off, ID separado de §10 bundle por convención): Sign-off block con:
         ```
         verdict: PASS | PASS-with-observations | FAIL
         date: 2026-MM-DD
         signed_by: Rafael Matovelle
         observations: [free text]
         ```

    2. Crear `.planning/phases/05-phaser-chapter-6/05-VERIFICATION.md` draft con:
       - Header: phase 5, gathered date, draft status.
       - Suite results: ejecutar `npm run test:run` y capturar output (total tests, passed, failed). Esperar 151 baseline + ~30 Phase 5 nuevos = ~181 verdes.
       - Build results: ejecutar `npm run build` y capturar bundle size + chunk Phaser size raw + gzip estimate.
       - Phase 5 plans breakdown table (Plan 05-01..05-05 + slice entregada + tests asociados).
       - Programmatic gate: ✓ (todos verdes).
       - Manual gate: pending Rafael (referencia a 05-MANUAL-CHECKLIST §10 sign-off).
       - Conclusion draft: "Verdict pending Rafael §10 sign-off. Programmatic verification PASS."

    3. Si CONTENT-CHECKLIST §2.5 fue actualizado por Rafael en paralelo (patrón D4-09): refresh i18n keys ch6 descs en `src/i18n/es.json` y `src/i18n/en.json` reemplazando los stubs "PENDING — Rafael refresca §2.5: ..." con el copy real. Verificar paridad ES↔EN via `npm run test -- --run tests/i18n/parity.test.js`.

    **Paso 1 — Rafael ejecuta el checklist:**

    1. Abrir `.planning/phases/05-phaser-chapter-6/05-MANUAL-CHECKLIST.md` y leer las 13 secciones.
    2. Para cada section §1-§13, seguir los steps específicos en el orden listado.
    3. Marcar status checkbox: [✓] Pass, [⚠] Observations, [✗] Fail según outcome.
    4. Anotar observaciones en campo Notes de cada section.

    **Paso 2 — Rafael decide verdict en §10 sign-off:**

    1. Llenar `verdict: PASS | PASS-with-observations | FAIL` según outcome global.
    2. `date: 2026-MM-DD` (fecha real del sign-off).
    3. `signed_by: Rafael Matovelle`.
    4. `observations:` campo libre con cualquier issue documentado, decisiones de polish diferido, hallazgos relevantes para Phase 6.

    Si verdict=FAIL: documentar issue bloqueante → executor evalúa si es gap closure (Plan 05-07 follow-up) o re-plan.
    Si verdict=PASS o PASS-with-observations: Phase 5 cierra; executor procede a Paso 3.

    **Paso 3 — Cleanup post sign-off (executor):**

    1. Actualizar `.planning/STATE.md`: marcar Phase 5 status `Closed` con `verdict: PASS|PASS-with-observations`, fecha, observations summary.
    2. Actualizar `.planning/ROADMAP.md`: marcar `Phase 5 Phaser Chapter 6` con [x] complete + reference a 05-VERIFICATION.md verdict.
    3. Commit final: `docs(phase-05): close phase — verdict {verdict}` con files .planning/STATE.md + ROADMAP.md + 05-VERIFICATION.md + 05-MANUAL-CHECKLIST.md.
    4. Si Rafael ajustó mantra copy ES o refresh descs ch6, commit separado `i18n(05-06): refresh ch6 copy per Rafael CONTENT-CHECKLIST §2.5 + mantra ratification`.

  </how-to-verify>
  <resume-signal>Type "approved" o "verdict: PASS" / "verdict: PASS-with-observations" + paste el §10 sign-off completo + describe issues encontrados si hay. Si "verdict: FAIL": describe issue bloqueante + executor pivots a gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual checklist .md → Rafael judgment | Rafael es trust root para verdict de A11Y/visual/perceptual |
| `npm run build` output → 05-VERIFICATION | Build artifact paths, no PII, no secrets |
| CONTENT-CHECKLIST §2.5 → i18n JSON refresh | Copy provided por Rafael (público); paridad enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W5-01 | Repudiation | Verdict firmado sin Rafael presente | mitigate | §10 sign-off includes signed_by name + date; commit log preserva auditoría |
| T-05-W5-02 | DoS | Chunk Phaser exceeds 200KB gzip bloqueando Phase 6 | mitigate | §10 documenta size, executor evalúa post-W5 si requiere lazy-split o tree-shake config — gap closure plan opcional |
| T-05-W5-03 | Tampering | i18n refresh accidental rompe paridad ES↔EN | mitigate | tests/i18n/parity.test.js T1 auto-detecta drift; executor ejecuta antes de commit |
| T-05-W5-04 | Information disclosure | Screenshots in MANUAL-CHECKLIST § exponen layout sensible | accept | Screenshots opcionales solo en chapter-overlap bug datapoint §9; público — no PII; risk muy bajo |
| T-05-W5-05 | Spoofing | Verdict modificado post-sign-off | mitigate | Git commit history is immutable record; verdict change requires new commit + dated rationale |
</threat_model>

<verification>
- `.planning/phases/05-phaser-chapter-6/05-MANUAL-CHECKLIST.md` existe con 13 sections + §10 sign-off.
- `.planning/phases/05-phaser-chapter-6/05-VERIFICATION.md` draft existe con suite + build results capturados.
- Si Rafael actualizó §2.5: `npm run test -- --run tests/i18n/parity.test.js` verde post-refresh.
- Rafael firmó §10 con verdict (PASS / PASS-with-observations / FAIL + observations).
- STATE.md + ROADMAP.md actualizados post sign-off.
- Frontmatter validation: `gsd-sdk query frontmatter.validate ".planning/phases/05-phaser-chapter-6/05-06-PLAN.md" --schema plan`.
</verification>

<success_criteria>
- 05-MANUAL-CHECKLIST.md con 13 sections concretas + §10 sign-off block estructurado.
- 05-VERIFICATION.md draft con test suite results + build size deltas + chunk Phaser metrics.
- Rafael firma §10 con verdict explícito (no implícito).
- Mantra copy ES ratificada (default "Y siempre muestra una sonrisa" mantenido O alternativa documentada).
- i18n descs ch6 refresh aplicado si CONTENT-CHECKLIST §2.5 actualizado.
- STATE.md + ROADMAP.md reflejan Phase 5 cerrada.
- Phase 5 deferred items (si los hay — e.g., chapter overlap bug datapoint, mantra alternative copy, ship vibe refresh) documentados en STATE.md Deferred Items section.
</success_criteria>

<output>
After completion, create `.planning/phases/05-phaser-chapter-6/05-06-SUMMARY.md` que incluya: verdict final firmado, status by section (13 §s), deferred items extraídos del checklist (si los hay), next steps para Phase 6 (e.g., addressear chapter-overlap bug, backgrounds downscale ≤80KB cumulative incluyendo ch6-bg, iOS smoke test que sigue deferred).
</output>
