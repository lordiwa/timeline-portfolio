---
phase: 3
plan: 5
slug: wave4-avatar-busts-pixel-art
wave: 4
type: execute
mode: mvp
autonomous: false
gap_closure: false
requirements: [ART-01, ART-05]
depends_on: ["03-01", "03-03"]
files_modified:
  - public/assets/.refs/.gitkeep
  - .gitignore
  - public/assets/ch0-bust.png
  - public/assets/ch1-bust.png
  - public/assets/ch2-bust.png
  - public/assets/ch3-bust.png
  - public/assets/ch4-bust.png
  - public/assets/ch5-bust.png
  - public/assets/ch6-bust.png
  - src/data/chapters.js
  - src/components/StickyAvatar.vue
  - tests/assets/bust-naming.test.js
notes:
  human_input_gate: >
    Este plan es `autonomous: false` porque Task 5.1 requiere checkpoint:human-input
    para verificar que Rafael completó el CONTENT-CHECKLIST.md §1-§5 con bio, proyectos,
    contacto, SEO, paletas + subió la foto ~30 años a `public/assets/.refs/rafael-age30.{jpg|png}`
    + agregó `public/assets/.refs/` al `.gitignore`. Tasks 5.2-5.4 ejecutan autónomas
    tras el gate.
  multi_agent: >
    Task 5.2 y 5.3 son dispatched al `artist-creator` agent (pixelforge-mcp). Task 5.4
    es dispatched al `frontend-dev` agent (Vue + tests). El executor (gsd-executor) lee
    el `<agent>` hint en cada task XML y decide el subagent type.
  references_chain: >
    Pattern verbatim de RESEARCH §Pattern 3 (líneas 351-403): ch3 es el ANCHOR — se genera
    primero usando la foto privada de Rafael (`public/assets/.refs/rafael-age30.jpg`) como
    `references:`. Los otros 6 busts (ch0, ch1, ch2, ch4, ch5, ch6) usan `references: ["public/assets/ch3-bust.png"]`
    como ancla de personaje. Garantiza coherencia visual: mismo personaje, solo edad cambia.
  privacy_guarantee: >
    La foto privada de Rafael NUNCA se commitea. `public/assets/.refs/` está en `.gitignore`.
    Solo pixelforge la consume como input local. Los outputs `ch{N}-bust.png` SÍ se commitean
    (son los avatares pixel art derivados, no la foto original).
  pixelforge_pitfalls_lock: >
    CRITICAL desde CLAUDE.md §6.2 + .claude/skills/crear-arte-pixelforge.md verbatim:
    - NO usar `background: "black"` o `"white"` con forge_sprite (Gemini ignora el color → bg removal falla)
    - SÍ usar presets nombrados: "sky", "forest", "night", "dungeon", "ocean", "sand", "snow", "lava"
    - NO usar `forge_background` para busts (no aplica bg removal)
    - NO usar `forge_animation` (frames incoherentes entre sí)
    - Cada call DEBE incluir `palette: chapter.palette` explícito desde chapters.js (D3-06)
  palette_dependency: >
    Plan 03-01 debe haber poblado el field `palette: [hex, ...]` en `src/data/chapters.js`
    para los 7 chapters (D3-06). Si Rafael no llenó CONTENT-CHECKLIST §5 antes del gate de
    Task 5.1, el `.palette` field viene vacío y las llamadas a pixelforge no son
    determinísticas → BLOCK con gate hasta que Rafael llene §5.
must_haves:
  truths:
    - "Rafael completó CONTENT-CHECKLIST §1, §2 (al menos ch3), §3, §4.1, §5 (las 5 paletas chapter), §5.6 (avatar humana base) antes del gate Task 5.1"
    - "Foto `public/assets/.refs/rafael-age30.{jpg|png}` existe localmente y `.gitignore` excluye `public/assets/.refs/` antes del gate"
    - "src/data/chapters.js field `palette` está poblado con hex codes para los 7 chapters (no `[]` vacío)"
    - "ch3-bust.png se genera ANTES que los otros 6 busts (orden estricto)"
    - "Los 6 busts non-ch3 usan `references: [\"public/assets/ch3-bust.png\"]` (NO la foto privada)"
    - "Cada forge_sprite call incluye palette explícito de chapters.js (no defaults)"
    - "Cada forge_sprite call usa preset bg nombrado (NO 'black'/'white')"
    - "Tras el batch, `chapters.js` se actualiza con `avatarSrc: '/assets/ch{N}-bust.png'` por chapter"
    - "StickyAvatar.vue placeholder (Phase 1 Plan 04) se reemplaza con render real (img tag con :src=chapter.avatarSrc)"
    - "Suite global zero regression: Phase 1+2+W0/W1/W2/W3 tests siguen verdes"
  artifacts:
    - "7 PNG files en `public/assets/ch{N}-bust.png` (N=0..6)"
    - "`tests/assets/bust-naming.test.js` smoke test verifica los 7 archivos existen"
    - "`src/data/chapters.js` updated con avatarSrc per chapter"
    - "`src/components/StickyAvatar.vue` reemplaza placeholder por img reactivo"
anti_scope:
  - "NO generar pixel art para chapters 0-6 backgrounds (ART-02/03/04 → Phase 4)"
  - "NO generar planet sprites para ch6 (Phase 5 — Phaser scene)"
  - "NO generar OG image (Plan 03-04 ya tiene placeholder 1×1; Phase 6 reemplaza)"
  - "NO modificar `chapters.js` shape (D3-04 LOCKED) — solo poblar `avatarSrc` field"
  - "NO retocar busts con Adobe MCP en este plan (es un seguimiento opcional Phase 4 si la consistencia visual entre busts no es suficiente)"
  - "NO regenerar busts existentes — si una iteración falla, debugear el prompt; no acumular outputs"
key_decisions:
  - "D3-05 LOCKED: Batch 7 busts en Phase 3 (no solo ch3)"
  - "D3-06 LOCKED: Paleta doble fuente CONTENT-CHECKLIST §5 + chapters.js palette field"
  - "D3-07 LOCKED: BASE_PROMPT + diff per chapter (consistencia personaje)"
  - "D3-08 LOCKED: 1 foto Rafael ~30 años como anchor + .gitignore privacy"
  - "RESEARCH Pattern 3 LOCKED: ch3 anchor → references chain a los otros 6 busts"
  - "RESEARCH §Pitfalls LOCKED: presets nombrados, no forge_background, no forge_animation"
threat_model_summary:
  - "T-ART-PRIV (Disclosure): foto privada de Rafael en build pública → mitigated por .gitignore Pitfall 5"
  - "T-ART-ASSET-LEAK (Information): outputs busts contienen la cara de Rafael pixelizada → ACCEPTED (es el deseo explícito del portafolio personal)"
  - "T-PIXELFORGE-API-KEY (Disclosure): GEMINI_API_KEY exposed → mitigated en env var + claude.json gitignore (CLAUDE.md §4.3 verbatim)"
---

# Phase 3 Plan 05 — Wave 4: Avatar Pixel Art Batch 7 Busts

## <objective>
Generar los 7 retratos bust pixel art de Rafael (uno por chapter) usando pixelforge-mcp.
ch3 es el ANCHOR (generado primero usando la foto privada ~30 años). Los otros 6 busts
referencían a ch3 para mantener coherencia de personaje. Actualizar `src/data/chapters.js`
con `avatarSrc` y reemplazar el placeholder Phase 1 de `StickyAvatar.vue` con render real.

Cubre **ART-01** (7 retratos bust consistentes — solo edad cambia) y **ART-05** (naming
convention `ch{N}-{descriptor}[-{variant}].png` en `public/assets/`).
</objective>

## <interfaces>

### pixelforge-mcp `forge_sprite` call shape (per RESEARCH §Pattern 3)

```
forge_sprite(
  prompt: <BASE_PROMPT + diff per chapter>,  ← D3-07 verbatim
  background: <named preset from CLAUDE.md §6.2>,  ← NUNCA "black" / "white"
  palette: chapter.palette,  ← chapters.js field, NO defaults
  references: <see below>,
  output_path: "public/assets/ch{N}-bust.png"  ← ART-05 naming
)
```

**`references` por chapter:**
- ch3 (anchor, generado primero): `references: ["public/assets/.refs/rafael-age30.jpg"]`
- ch0, ch1, ch2, ch4, ch5, ch6: `references: ["public/assets/ch3-bust.png"]`

### BASE_PROMPT template (D3-07)

```
A pixel art portrait bust of a Latin American man with [DIFF.skin_tone] skin tone, [DIFF.complexion]
build, [DIFF.facial_features] facial features. 16-bit retro game art style, character bust framing
(head + shoulders), centered composition. Mood: [DIFF.mood]. [DIFF.age_descriptor] years old.
[DIFF.hair_state]. [DIFF.era_clothing]. [DIFF.accessories]. Consistent character identity across
the 7 chapters of this portfolio — same person, age varies.
```

**Diffs per chapter** (planner sets defaults — Rafael ajusta en review post-Task 5.1):

| Chapter | Age | Era | Hair | Clothing | Mood | Accessories |
|---------|-----|-----|------|----------|------|-------------|
| ch0 (1995) | ~10 | Terminal | dark, short | basic kid clothes, polo | curious | none |
| ch1 (2001) | ~15 | HTML 90s | dark, slightly longer | t-shirt, geek style | excited | none |
| ch2 (2009) | ~22 | Flash era | dark, casual | hoodie, jeans-era | focused | none |
| ch3 (2013) | ~26 | Web 2.0 | dark | smart casual, button-up | confident smile | watch (subtle) |
| ch4 (2015-18) | ~30 | AR/VR | dark, modern cut | tech startup polo | inquisitive | AR glasses (hint) |
| ch5 (2022-23) | ~36 | Modern | dark with hints of gray (subtle) | hoodie + collared shirt | composed | none |
| ch6 (2026) | ~40 | Phaser AI | salt-and-pepper hair (más canas), professional look | smart casual, layered | warm, knowing smile | subtle glasses (older eye) |

**Skin tone, complexion, facial features:** Define DURANTE Task 5.2 (Rafael revisa la foto y el artist-creator deriva 3-4 lines descriptive). LOCK en el plan tras ese review — NO se cambian entre chapters.

### `src/data/chapters.js` field updates (post-bust generation)

```javascript
// From Plan 03-01 (W0 scaffolding), chapters[N] tiene:
{ id: N, year, era, eraKey, titleKey, avatarSrc, themeColor?, palette }

// Plan 03-05 actualiza avatarSrc:
chapters.find(c => c.id === 3).avatarSrc = '/assets/ch3-bust.png'
// (similar para los 6 chapters restantes)
```

### `src/components/StickyAvatar.vue` placeholder replacement (Phase 1 → Phase 3 real)

```vue
<!-- BEFORE (Phase 1 Plan 04 placeholder): -->
<aside class="sticky-avatar" aria-hidden="true">
  <span class="avatar-placeholder">{{ activeChapter }}</span>
</aside>

<!-- AFTER (Phase 3 Plan 05): -->
<aside class="sticky-avatar" aria-hidden="true">
  <img
    :src="currentAvatarSrc"
    :alt="''"
    class="avatar-bust"
    width="64"
    height="64"
  />
</aside>
<script setup>
import { computed, inject } from 'vue'
import { chapters } from '@/data/chapters'  // named export per Plan 03-01 D3-04 contract
const { activeChapter } = inject('scrollState')
const currentAvatarSrc = computed(() =>
  chapters.find(c => c.id === activeChapter.value)?.avatarSrc ?? '/assets/ch3-bust.png'
)
</script>
```

(El swap entre busts mantiene el state-machine pattern del Phase 1 Plan 04 — fade-out 100ms + setTimeout 100 + fade-in 100ms = 200ms total. Si el placeholder Phase 1 tiene el crossfade, conservalo. Si no, déjalo como render directo + transition CSS opacity.)

### `tests/assets/bust-naming.test.js` smoke test shape

```javascript
import { describe, it, expect } from 'vitest'
import { existsSync } from 'node:fs'
import { resolve } from 'node:path'

describe('Avatar busts — ART-05 naming convention', () => {
  for (const N of [0, 1, 2, 3, 4, 5, 6]) {
    it(`ch${N}-bust.png exists in public/assets/`, () => {
      const path = resolve(process.cwd(), `public/assets/ch${N}-bust.png`)
      expect(existsSync(path)).toBe(true)
    })
  }
})
```

</interfaces>

## <tasks>

<task id="5.1">
  <agent>frontend-dev</agent>
  <objective>
    Checkpoint:human-input gate — verificar CONTENT-CHECKLIST + foto + .gitignore + palette field
    en chapters.js. Si algún ítem falta, BLOQUEAR el plan y emitir mensaje claro a Rafael.
  </objective>
  <read_first>
    .planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md
    .planning/phases/03-chapter-3-end-to-end/03-CONTEXT.md (D3-05/06/07/08)
    .planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md §Pattern 3 + §Pitfalls
    src/data/chapters.js (post Plan 03-01)
    .gitignore
  </read_first>
  <action>
    1. Leer `CONTENT-CHECKLIST.md`. Verificar que las siguientes secciones están llenas (NOT empty placeholders `__________`):
       - §1.1 Bio en español (≥1 párrafo completo)
       - §1.2 Bio en inglés (≥1 párrafo completo)
       - §2.2 Chapter 3 — al menos 1 proyecto Pink Parrot con nombre + año + descripción ES + descripción EN
       - §3 Email + LinkedIn + GitHub (3 valores no-empty)
       - §4.1 Title + Description ES + EN
       - §5.2 Paleta ch3 — 5-8 hex codes
       - §5.6 Paleta avatar humana (skin tones + hair + clothing)

    2. Verificar foto: `ls public/assets/.refs/rafael-age30.jpg 2>/dev/null` o `.png`. Si ambos missing → FAIL.

    3. Verificar `.gitignore` contiene `public/assets/.refs/` o equivalente que excluya esa carpeta. Si missing → FAIL.

    4. Verificar `src/data/chapters.js` (post Plan 03-01) — cada chapter debe tener `palette` field NO-vacío:
       ```
       grep -c "palette:" src/data/chapters.js
       # debe ser ≥7 (uno por chapter)
       ```
       Si 0 o `palette: []` en algún chapter → FAIL.

    5. Si todos los checks pasan → emit `checkpoint:human-input-cleared` y continuar a Task 5.2.

    Si algún check falla → emit checkpoint message:
    ```
    [checkpoint:human-input] Plan 03-05 (Wave 4 avatar art) BLOCKED.

    Missing inputs:
    {list of missing items}

    Action required from Rafael:
    1. Llenar CONTENT-CHECKLIST.md sections {N, M, ...}
    2. Subir foto a public/assets/.refs/rafael-age30.{jpg|png}
    3. Verificar .gitignore excluye public/assets/.refs/
    4. Validar src/data/chapters.js palette field poblado por Plan 03-01

    Re-spawn este plan cuando los inputs estén listos.
    ```
    Y NO ejecutar Tasks 5.2-5.4.
  </action>
  <verify>
    <automated>node --input-type=commonjs -e "
      const fs = require('fs');
      const checklist = fs.readFileSync('.planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md', 'utf8');
      const gitignore = fs.readFileSync('.gitignore', 'utf8');
      const chaptersJs = fs.readFileSync('src/data/chapters.js', 'utf8');
      const photoExists = fs.existsSync('public/assets/.refs/rafael-age30.jpg') || fs.existsSync('public/assets/.refs/rafael-age30.png');
      const placeholderCount = (checklist.match(/__________/g) || []).length;
      const paletteCount = (chaptersJs.match(/palette\s*:/g) || []).length;
      const gitignoreHasRefs = /public\/assets\/\.refs\//.test(gitignore);
      console.log({photoExists, placeholderCount, paletteCount, gitignoreHasRefs});
      if (!photoExists || placeholderCount > 5 || paletteCount < 7 || !gitignoreHasRefs) process.exit(1);
    "</automated>
    <fallback_powershell_ps5>
      # CLAUDE.md §1: Windows 11 / PowerShell 5.1 host. Si `node --input-type=commonjs` da problemas, usar:
      $checklist = Get-Content -Raw '.planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md'
      $gitignore = Get-Content -Raw '.gitignore'
      $chaptersJs = Get-Content -Raw 'src/data/chapters.js'
      $photoExists = (Test-Path 'public/assets/.refs/rafael-age30.jpg') -or (Test-Path 'public/assets/.refs/rafael-age30.png')
      $placeholders = ([regex]'__________').Matches($checklist).Count
      $paletteCount = ([regex]'palette\s*:').Matches($chaptersJs).Count
      $gitignoreOk = $gitignore -match 'public/assets/\.refs/'
      "photoExists=$photoExists placeholders=$placeholders paletteCount=$paletteCount gitignoreOk=$gitignoreOk"
      if (-not $photoExists -or $placeholders -gt 5 -or $paletteCount -lt 7 -or -not $gitignoreOk) { exit 1 }
    </fallback_powershell_ps5>
  </verify>
  <acceptance_criteria>
    - CONTENT-CHECKLIST.md §1, §2 (ch3 al menos), §3, §4.1, §5.2, §5.6 sin placeholders críticos
    - Foto `public/assets/.refs/rafael-age30.{jpg|png}` existe localmente
    - `.gitignore` contains `public/assets/.refs/` entry (verifica con grep)
    - `src/data/chapters.js` tiene `palette:` field poblado en los 7 chapters (count ≥7)
    - Si cualquier check falla: plan bloqueado con mensaje claro a Rafael, NO ejecutar Tasks 5.2-5.4
  </acceptance_criteria>
  <done>Gate clarado o bloqueado con mensaje al usuario.</done>
</task>

<task id="5.2">
  <agent>artist-creator</agent>
  <objective>
    Generar ch3-bust.png usando pixelforge-mcp `forge_sprite` con la foto privada como anchor.
    Este es el ANCHOR del batch — la referencia visual para los otros 6 busts.
  </objective>
  <read_first>
    .claude/skills/crear-arte-pixelforge.md (mandatory — error cases §6.1-6.4)
    CLAUDE.md §6 (pitfalls verbatim)
    .planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md §Pattern 3 + §Pitfalls
    src/data/chapters.js (palette field for ch3)
    .planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md §5.2 (paleta ch3) + §5.6 (avatar humana base)
    public/assets/.refs/rafael-age30.{jpg|png} (anchor file — visual verification only, no commit)
  </read_first>
  <action>
    1. **ToolSearch** para cargar pixelforge tools:
       ```
       ToolSearch(query="select:mcp__pixelforge__forge_sprite,mcp__pixelforge__process_sprite,mcp__pixelforge__optimize_sprite", max_results=5)
       ```

    2. Leer la foto de Rafael (visual inspection — describir 3-4 lines: skin tone, complexión, facial features, hair texture). LOCK estos rasgos en notas — son invariantes para los 7 busts.

    3. Construir el prompt para ch3 (~26 años, Web 2.0 era 2013):
       - BASE_PROMPT (de <interfaces> arriba) + diffs ch3 + features locked de step 2
       - Ejemplo:
         ```
         A pixel art portrait bust of a Latin American man with medium-tan skin tone, average build,
         oval face with subtle smile lines. 16-bit retro game art style, character bust framing
         (head + shoulders), centered composition. Mood: confident smile. ~26 years old.
         Dark medium-length hair, slightly groomed. Smart casual button-up shirt (era 2013).
         Subtle wrist watch. Consistent character identity — Pink Parrot UX/Web Dev era.
         ```

    4. Invocar `forge_sprite`:
       ```
       forge_sprite(
         prompt: <prompt step 3>,
         background: "sky",  ← preset nombrado (NUNCA "black"/"white")
         palette: <chapters.find(c=>c.id===3).palette>,  ← desde chapters.js
         references: ["public/assets/.refs/rafael-age30.jpg"],  ← foto privada
         output_path: "public/assets/ch3-bust.png"
       )
       ```

    5. Validar output: `existsSync('public/assets/ch3-bust.png')`. Si missing → debug logs de pixelforge → reintentar con prompt ajustado (max 3 retries).

    6. Visual review: artist-creator inspecciona el output. Si el personaje NO se parece a la foto base, ajustar el prompt e iterar. CRITICAL: este bust es el anchor — debe ser sólido antes de Task 5.3.

    7. Commit: `feat(03-05): ch3 bust anchor generated (ART-01, ART-05)`.
  </action>
  <verify>
    <automated>node --input-type=commonjs -e "if (!require('fs').existsSync('public/assets/ch3-bust.png')) process.exit(1); console.log('ch3-bust.png OK')"</automated>
    <fallback_powershell_ps5>if (-not (Test-Path 'public/assets/ch3-bust.png')) { exit 1 }; "ch3-bust.png OK"</fallback_powershell_ps5>
  </verify>
  <acceptance_criteria>
    - `public/assets/ch3-bust.png` existe + tamaño razonable (>1KB, <500KB pixel art bust)
    - Visual review: el bust se reconoce como Rafael (juicio del artist-creator)
    - Naming convention `ch3-bust.png` (ART-05)
    - Palette aplicada correctamente (los colores del bust matchean los hex de chapters[3].palette)
    - Background limpio (forge_sprite aplica bg removal correctly)
    - Commit atómico
  </acceptance_criteria>
  <done>ch3-bust.png en disco, commiteado, visual review PASS.</done>
</task>

<task id="5.3">
  <agent>artist-creator</agent>
  <objective>
    Generar los 6 busts restantes (ch0, ch1, ch2, ch4, ch5, ch6) usando ch3-bust.png como
    `references:`. Esto mantiene la coherencia de personaje — mismo Rafael, diferentes edades.
  </objective>
  <read_first>
    .claude/skills/crear-arte-pixelforge.md
    public/assets/ch3-bust.png (anchor del paso anterior)
    src/data/chapters.js (palettes per chapter)
    .planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md §Pattern 3
  </read_first>
  <action>
    Para cada chapter N ∈ {0, 1, 2, 4, 5, 6} (orden: 0 → 1 → 2 → 4 → 5 → 6):

    1. Construir prompt N usando BASE_PROMPT + diffs N (de la tabla en <interfaces>):
       - Misma skin tone, complexión, facial features (locked de Task 5.2)
       - Edad: ch0=~10, ch1=~15, ch2=~22, ch4=~30, ch5=~36, ch6=~40
       - Hair, clothing, mood, accessories: ver tabla
       - Ejemplo ch0:
         ```
         A pixel art portrait bust of a Latin American boy (~10 years old) with medium-tan skin tone,
         small build, oval face. 16-bit retro game art style. Mood: curious, with bright eyes.
         Short dark hair. Simple polo shirt (era 1995). No accessories.
         Consistent character identity — same person as ch3 bust, younger age.
         ```

    2. Invocar `forge_sprite`:
       ```
       forge_sprite(
         prompt: <prompt N>,
         background: "sky",  ← o preset apropiado per era (e.g., "night" para ch0 CRT)
         palette: chapters.find(c=>c.id===N).palette,
         references: ["public/assets/ch3-bust.png"],  ← ANCHOR
         output_path: "public/assets/ch{N}-bust.png"
       )
       ```

    3. Validar output existe.

    4. Visual review por chapter: ¿se reconoce como el MISMO personaje que ch3-bust? Si no, ajustar prompt (max 3 retries por chapter).

    5. Commit por chapter (orden estricto): `feat(03-05): ch{N} bust generated using ch3 as reference (ART-01)`.

    6. Continuar hasta los 6 chapters non-ch3.
  </action>
  <verify>
    <automated>node --input-type=commonjs -e "const fs=require('fs'); const missing=[0,1,2,4,5,6].filter(N=>!fs.existsSync('public/assets/ch'+N+'-bust.png')); if(missing.length){console.error('missing:',missing); process.exit(1)}; console.log('all 6 busts present')"</automated>
    <fallback_powershell_ps5>$missing = @(0,1,2,4,5,6) | Where-Object { -not (Test-Path "public/assets/ch$_-bust.png") }; if ($missing) { "missing: $($missing -join ',')"; exit 1 } else { 'all 6 busts present' }</fallback_powershell_ps5>
  </verify>
  <acceptance_criteria>
    - `public/assets/ch{N}-bust.png` existe para N ∈ {0, 1, 2, 4, 5, 6}
    - Visual review: los 6 busts se reconocen como la misma persona (Rafael) envejeciendo
    - Cada bust matchea su palette de chapter (chapters.js)
    - Naming convention `ch{N}-bust.png` correcto
    - Background limpio en todos
    - 6 commits atómicos (uno por chapter)
  </acceptance_criteria>
  <done>7 busts totales en disco (ch0..ch6), commiteados, visual review PASS.</done>
</task>

<task id="5.4">
  <agent>frontend-dev</agent>
  <objective>
    Wire los 7 busts en `src/data/chapters.js` (avatarSrc field), reemplazar el placeholder
    de `src/components/StickyAvatar.vue` con render real, agregar smoke test
    `tests/assets/bust-naming.test.js`.
  </objective>
  <read_first>
    src/data/chapters.js (post Plan 03-01)
    src/components/StickyAvatar.vue (Phase 1 placeholder)
    tests/setup.js
    tests/components/StickyAvatar.test.js (Phase 1 tests — verificar zero regression)
  </read_first>
  <action>
    1. **Update chapters.js** — populate `avatarSrc` field per chapter. CRITICAL: chapters.js
       usa `export const chapters` (named export per Plan 03-01 D3-04 contract). NO cambiar
       a `export default`:
       ```javascript
       export const chapters = [
         { id: 0, year: 1995, era: 'Terminal', ..., avatarSrc: '/assets/ch0-bust.png', palette: [...] },
         { id: 1, year: 2001, era: 'HTML 90s', ..., avatarSrc: '/assets/ch1-bust.png', palette: [...] },
         { id: 2, year: 2009, era: 'Flash', ..., avatarSrc: '/assets/ch2-bust.png', palette: [...] },
         { id: 3, year: 2013, era: 'Web 2.0', ..., avatarSrc: '/assets/ch3-bust.png', palette: [...] },
         { id: 4, year: 2015, era: 'AR/VR', ..., avatarSrc: '/assets/ch4-bust.png', palette: [...] },
         { id: 5, year: 2022, era: 'Modern', ..., avatarSrc: '/assets/ch5-bust.png', palette: [...] },
         { id: 6, year: 2026, era: 'Phaser', ..., avatarSrc: '/assets/ch6-bust.png', palette: [...] },
       ]
       ```
       (Los demás fields preservados — D3-04 LOCKED no cambia el shape.)

    2. **Update StickyAvatar.vue** — reemplazar el placeholder por img tag (ver <interfaces> arriba). Si el componente actual tiene state-machine de crossfade (Phase 1 Plan 04), CONSERVARLO — solo reemplazar el `<span class="avatar-placeholder">{{N}}</span>` por `<img :src="currentAvatarSrc" :alt="''" ...>`. Si no, render directo con `transition: opacity 200ms ease`.

    3. **Crear `tests/assets/bust-naming.test.js`** según shape de <interfaces>. 7 tests (uno por chapter) que verifican existsSync de `public/assets/ch{N}-bust.png`.

    4. **Actualizar `tests/components/StickyAvatar.test.js`** si necesita — el placeholder ya no muestra texto del número, ahora muestra img. Ajustar assertions:
       - Old: `expect(wrapper.find('.avatar-placeholder').text()).toBe('3')`
       - New: `expect(wrapper.find('img.avatar-bust').attributes('src')).toBe('/assets/ch3-bust.png')`
       Mantener test count similar (no perder coverage de aria-hidden, default chapter, transition).

    5. Run `npm run test:run -- tests/assets/ tests/components/StickyAvatar` — esperar GREEN.

    6. Run `npm run test:run` (suite completa) — verificar ≥{baseline_post_w3 + 7 nuevos asset tests} verdes.

    7. Run `npm run build` — verde + dist/assets contiene los 7 busts (Vite los copia desde public/).

    8. Run `npm run dev` smoke (manual): abrir browser, navegar ch0→ch6, verificar que el bust top-left cambia correctamente y se reconoce el personaje.

    9. Commit GREEN: `feat(03-05): wire 7 busts to chapters.js + replace StickyAvatar placeholder + smoke tests (ART-01, ART-05)`.
  </action>
  <verify>
    <automated>npm run test:run -- tests/assets/ tests/components/StickyAvatar &amp;&amp; npm run test:run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/chapters.js` tiene `avatarSrc: '/assets/ch{N}-bust.png'` poblado por chapter
    - `src/components/StickyAvatar.vue` renderiza `<img :src="currentAvatarSrc">` reactivo a `activeChapter`
    - `tests/assets/bust-naming.test.js` corre 7 tests verdes
    - Tests StickyAvatar actualizados — coverage preservada, sin regresión Phase 1
    - Suite global zero regression (Phase 1+2+W0/W1/W2/W3 + W4 nuevo tests)
    - `npm run build` verde + dist/assets contiene los 7 busts copiados desde public/
    - DevTools manual: navegar ch0→ch6 → bust top-left muta correctamente
  </acceptance_criteria>
  <done>chapters.js + StickyAvatar.vue updated, tests verdes, build verde, manual smoke OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public/assets/.refs/rafael-age30.{jpg|png}` → pixelforge-mcp `references:` | Foto privada de Rafael consumida localmente; .gitignore evita commit |
| pixelforge-mcp outputs → `public/assets/ch{N}-bust.png` | Outputs pixel art derivados; SÍ commiteados (deseo explícito del portafolio personal) |
| GEMINI_API_KEY env var | Set via `setx` per CLAUDE.md §4.2; NO en .claude.json (Pitfall §4.3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ART-PRIV | Disclosure | foto privada Rafael en build pública | mitigate | `.gitignore` excluye `public/assets/.refs/`. Task 5.1 verifica entry presente antes de continuar. Pitfall 5 documentado en CLAUDE.md. |
| T-ART-ASSET-LEAK | Information | Outputs busts contienen Rafael pixelizado | accept | Es el deseo explícito del portafolio personal. Mitigación social: el avatar pixelizado es de baja resolución; no es comparable a una foto. |
| T-PIXELFORGE-API-KEY | Disclosure | GEMINI_API_KEY plaintext en .claude.json si mis-configurado | mitigate | CLAUDE.md §4.3 lock: usar `setx GEMINI_API_KEY` para env var, NO `--env GEMINI_API_KEY=...` flag en `claude mcp add`. Plan 03-05 NO toca el config de pixelforge; asume Phase 1 setup correcto. |
| T-PROMPT-INJ | Tampering | Prompt construction con string interpolation podría inyectar via CONTENT-CHECKLIST text | mitigate | CONTENT-CHECKLIST contiene texto plano de Rafael (no user input runtime); prompts construidos en plan-driven flow, no via web form. Acceptable risk para portafolio personal. |
</threat_model>

<verification>
- Comando: `npm run test:run && npm run build`
- Esperado: suite global ≥{baseline_post_W3} + 7 tests asset verdes; build verde con `dist/assets/ch{N}-bust.png` ×7 presentes.
- Visual manual: `npm run dev` → navegar ch0→ch6 → verificar bust top-left muta + se reconoce el mismo personaje envejeciendo.
- Verificación pendiente externa (POST Phase 6 deploy): el avatar es la cara visible del portafolio — un cross-check informal con familia/conocidos confirmando "sí, es Rafael" es la última verificación antes de hacer pública la URL.
</verification>

<success_criteria>
- Gate Task 5.1 cleared (Rafael completó CONTENT-CHECKLIST + foto + .gitignore + chapters.js palette field poblado por Plan 03-01)
- ch3-bust.png generado como anchor usando la foto privada como `references:`
- 6 busts adicionales (ch0, ch1, ch2, ch4, ch5, ch6) generados usando ch3-bust.png como `references:` — consistencia de personaje preservada
- 7 PNG files en `public/assets/ch{N}-bust.png` (ART-05 naming)
- `src/data/chapters.js` populated con `avatarSrc` per chapter
- `src/components/StickyAvatar.vue` reemplaza placeholder con render real reactivo
- `tests/assets/bust-naming.test.js` smoke test 7/7 verde
- Phase 1+2+W0/W1/W2/W3 zero regression (suite global verde)
- `npm run build` verde + dist/ contiene los 7 busts
- Visual smoke (manual): navegación ch0→ch6 muestra el bust correcto en cada chapter y se reconoce como Rafael envejeciendo
- **Caveats documentados**:
  - Foto privada NO se committea (verificado en Task 5.1 + Pitfall 5)
  - Si la consistencia visual entre busts no es satisfactoria post-batch, Phase 4 puede ajustar con Adobe MCP (`image_adjust_color_temperature`, `image_adjust_hsl`) sin regenerar — fuera del scope de Plan 03-05
  - Mantra "And always show a smile" (CON-04) NO se entrega aquí; deferred a Phase 5 (escena Phaser)
</success_criteria>

<output>
After completion, create `.planning/phases/03-chapter-3-end-to-end/03-05-SUMMARY.md` con:
- Gate Task 5.1 outcome (cleared timestamp + qué se verificó)
- 7 busts generated — checksum / size + visual review notes por chapter
- Decision lock (skin tone, complexion, facial features) capturado para Phase 4+ reference
- Prompts usados verbatim (los 7) para repetibilidad / debugging Phase 4
- chapters.js diff (avatarSrc poblado)
- StickyAvatar.vue diff (placeholder → real render)
- bust-naming test creado + 7 tests pasando
- Suite global count post-W4 + zero regression confirmation
- Pending Rafael (NO blocking este plan):
  - Visual review final: ¿los 7 busts son satisfactorios? Si NO en algún chapter, abrir nuevo plan-fix Phase 4 con Adobe MCP retouch (no regenerar via pixelforge)
  - Verification post-Phase-6 deploy: cross-check informal con familia/conocidos antes de publicar URL
- Pending para Phase 4 (otros chapters):
  - Backgrounds parallax pixel art (ART-02/03/04)
  - Possible refinements de busts via Adobe MCP si Phase 4 Visual review identifica issues
- Pending para Phase 5 (Phaser ch6):
  - Planet sprites para proyectos
  - Mantra "And always show a smile" easter egg ch6 (CON-04)
- Pending para Phase 6 (DEPLOY):
  - OG image real (screenshot de ch3 post-Phase-3 polished)
  - Replace siteUrl placeholder con dominio real (de Plan 03-04 SEO config)
</output>
