---
phase: 3
plan: 2
slug: wave1-contact-hud-vertical-slice
wave: 1
type: execute
mode: mvp
autonomous: true
gap_closure: false
requirements: [CON-03]
depends_on: ["03-01"]
files_modified:
  - src/components/ContactHUD.vue
  - src/App.vue
  - tests/components/ContactHUD.test.js
notes:
  phase_2_regression_baseline: >
    Suite Phase 2 baseline = 151 tests verdes; Plan 03-01 añadió 13 tests data (Wave 0).
    Total baseline antes de W1 = 164 tests. W1 añade ≥8 tests ContactHUD + 0 modificaciones a
    tests existentes. COUNT total post-W1 ≥ 172 tests verdes. NO debe disminuir respecto al
    baseline previo — cero regresión en LangToggle, StickyAvatar, StickyTimeline, SkipLink,
    ScrollShell, BackgroundLayers, useScrollState, usePRM, useBackgroundMorph, smoke, i18n suite.
  hud_invariance: >
    ContactHUD es invariante a chapter (D3-10 verbatim) — análogo arquitectural a LangToggle
    (Phase 2 Plan 02). Usa tokens neutros (`--c-surface`, `--c-border`, `--c-fg`, `--c-accent`)
    que vienen del `:root` de App.vue (Phase 1) y son sobreescritos por `[data-chapter="N"]`
    SOLO cuando el HUD vive dentro de un section taggeada. Como `position: fixed` en el viewport
    sale del DOM de las sections, el HUD usa los tokens del `:root` neutro — NO se tiñe por
    chapter. Mismo comportamiento que LangToggle (verificado en Phase 2 W1 tests T7).
must_haves:
  truths:
    - "Existe `src/components/ContactHUD.vue` componente standalone fixed bottom-right (`position: fixed; bottom: env(safe-area-inset-bottom, 0); right: var(--sp-md); z-index: 40`) — D3-10 verbatim"
    - "El HUD renderiza 3 anchor tags con icons SVG inline (email, LinkedIn, GitHub) — cada uno 44×44px tap target mínimo (A11Y) — más el 4to opcional otherUrl si `contact.otherUrl` no es null"
    - "Anchor email: `href=\"mailto:${contact.email}\"` (si contact.email no es ''); si email '', el icon se renderea pero con `aria-disabled='true'` y sin href (no rompe el HUD si Rafael no llenó CONTENT-CHECKLIST §3 aún)"
    - "Anchor LinkedIn: `href=\"${contact.linkedinUrl}\"` + `target=\"_blank\"` + `rel=\"noopener noreferrer\"` (T-CON-03 mitigation verbatim)"
    - "Anchor GitHub: `href=\"${contact.githubUrl}\"` + `target=\"_blank\"` + `rel=\"noopener noreferrer\"`"
    - "Anchor otherUrl: rendered SOLO SI `contact.otherUrl !== null && contact.otherUrl !== ''` — mismo pattern + rel/target"
    - "Aria-labels desde i18n: `aria-label=\"t('contact.emailAria')\"`, `aria-label=\"t('contact.linkedinAria')\"`, `aria-label=\"t('contact.githubAria')\"` — bilingue reactivo (Pitfall 3 evitado)"
    - "Tooltip del email via `title=\"t('contact.emailTooltip')\"` (atributo title nativo del browser; UX simple para v1; el click-to-copy interactivo queda diferido)"
    - "Wrapper del HUD: `<div class=\"contact-hud\" :aria-label=\"t('contact.hudAria')\">` con role labelGroup implícito"
    - "Mobile <600px: el HUD mantiene tap target 44×44 + `right: var(--sp-sm)` + apilados verticales (column flex direction) según RESEARCH Pattern 5 verbatim"
    - "ContactHUD NO declara `outline:` propio — hereda el focus-visible universal de App.vue (Pitfall 7 — análogo a LangToggle verificado en Phase 2 W1 T8)"
    - "App.vue monta `<ContactHUD />` como último hijo del template root (después de LangToggle): orden DOM final post-W1 P3 = `<BackgroundLayers /> → <SkipLink /> → <StickyAvatar /> → <ScrollShell /> → <StickyTimeline /> → <LangToggle /> → <ContactHUD />`"
    - "Threat T-CON-03 verificado: ningún href de external link omite `rel='noopener noreferrer'` (regex check en test)"
    - "Cero regresión: todos los 164 tests baseline siguen verdes; suite total post-W1 ≥ 172 tests verdes"
  artifacts:
    - path: src/components/ContactHUD.vue
      provides: "HUD fixed bottom-right invariante; 3-4 anchors a contact data (email, LinkedIn, GitHub, otherUrl opcional); a11y compliant (44×44, aria-labels i18n reactivos); T-CON-03 mitigation"
      contains: "contact-hud"
    - path: tests/components/ContactHUD.test.js
      provides: "Tests: DOM contract (3-4 anchors), href correcto, rel/target externos, aria-labels reactivas, CSS position/z-index/44px, no outline propio"
      contains: "ContactHUD"
  key_links:
    - from: src/components/ContactHUD.vue
      to: src/data/contact.js
      via: "import { contact } from '@/data/contact'"
      pattern: "from .@/data/contact."
    - from: src/components/ContactHUD.vue
      to: src/i18n/es.json
      via: "useI18n() → t('contact.hudAria') + t('contact.emailAria') + t('contact.linkedinAria') + t('contact.githubAria') + t('contact.emailTooltip')"
      pattern: "contact\\.(hud|email|linkedin|github)Aria"
    - from: src/App.vue
      to: src/components/ContactHUD.vue
      via: "import + <ContactHUD /> como último hijo del template root"
      pattern: "<ContactHUD"
key_decisions:
  - "D3-10 LOCKED — ContactHUD fixed bottom-right INVARIANTE a chapter (análogo arquitectural a LangToggle Phase 2 W1). Usa tokens neutros del :root, NO consume [data-chapter] overrides."
  - "Iconos SVG inline en el template (NOT imports de archivos separados) — keeps componente self-contained, ~80 LOC totales, sin bundle drift. Pattern análogo a StickyAvatar.vue inline (componente standalone simple)."
  - "Email click → href=mailto: nativo (NOT click-to-copy custom) — el v1 entrega mailto que abre el cliente de correo del usuario (UX universal). El click-to-copy interactivo queda diferido a v2 (POL-06 si surge demand) — NO es CON-03 requirement crítico."
  - "Tooltip via atributo `title` nativo HTML (NOT custom tooltip JS) — UX simple, sin libs adicionales, accesible por screen readers via browser default."
  - "Si `contact.email === ''` (Rafael no llenó CONTENT-CHECKLIST §3 aún), el icon se renderea pero sin href + aria-disabled='true' — el HUD se mantiene funcional para los otros 2 links (LinkedIn + GitHub) sin romper el layout. SAME pattern para linkedinUrl/githubUrl '' (raro pero defensive)."
  - "El 4to slot otherUrl es CONDITIONAL render: `v-if=\"contact.otherUrl\"` — si Rafael decide no incluir Twitter/Mastodon/Bluesky, el HUD muestra solo 3 icons sin gap visible."
  - "T-CON-03 mitigation verbatim en cada external link: rel='noopener noreferrer' + target='_blank' — verified por test regex assertion."
anti_scope:
  - "NO implementar click-to-copy interactivo del email (mailto + tooltip nativo cubre v1; POL-06 si surge demand)."
  - "NO instalar libs de iconos (lucide-vue, heroicons-vue, etc.) — SVG inline en template directamente."
  - "NO añadir 5to/6to icon (Twitter, Mastodon, Bluesky son opcionales y caben en `otherUrl` 4to slot si Rafael lo decide)."
  - "NO i18nificar el texto visible (los iconos son SVG sin texto; aria-labels SI son i18n reactive)."
  - "NO modificar el StickyTimeline ni el LangToggle ni ningún otro componente Phase 1/Phase 2 — solo añadir ContactHUD nuevo + montaje en App.vue."
  - "NO consumir chapter.palette ni [data-chapter] (invariante a chapter)."
threat_model_summary:
  - "T-CON-03 (open redirect via external URLs): URLs hardcoded en src/data/contact.js (Plan 03-01) + rel='noopener noreferrer' + target='_blank' en cada anchor LinkedIn/GitHub/otherUrl. Verified por test regex (CSS readFileSync source check)."
  - "T-PHISH (phishing via mailto): N/A — mailto: protocol es safe-by-default (abre cliente de correo, no exfiltración runtime). Defensive: si Rafael cambia email, requiere commit + redeploy (no runtime mutation)."
---

## Phase Goal (MVP Vertical Slice)

**As a** recruiter visitando el portafolio en cualquier chapter, **I want to** poder contactar a Rafael (email, LinkedIn, GitHub) sin hacer scroll adicional ni navegar a una sección "Contact", **so that** el siguiente paso (escribir un email, ver perfil profesional, revisar código) sea de un solo click sin perder el chapter donde estaba.

> **Nota MVP:** Este Wave 1 entrega el primer touchpoint visible NUEVO de Phase 3 — el ContactHUD fijo bottom-right análogo arquitectural al LangToggle (top-right). Tras este plan, el sitio tiene contact info accesible desde los 7 chapters sin recargar ni navegar — funcionalidad CON-03 end-to-end completa.

<objective>
Construir `ContactHUD.vue` — un componente standalone fixed bottom-right que renderea 3-4 anchors (email mailto + LinkedIn + GitHub + opcional otherUrl) consumiendo `contact.js` (Plan 03-01) + aria-labels i18n reactive (Pitfall 3 evitado, análogo a LangToggle Phase 2 W1) + T-CON-03 mitigation verbatim (rel='noopener noreferrer' en cada external) + montaje en App.vue como último hijo del template root.

**Purpose:** Cubre CON-03 (contact info persistente y accesible desde cualquier chapter) + completa la mitigación de T-CON-03 (open redirect). El HUD es invariante a chapter (D3-10) — análogo arquitectural al LangToggle. Plan 03-01 ya creó el shape de data + las keys i18n; este plan los enchufa a UI visible.

**Lo que ESTE plan NO hace:**
- NO crea Chapter3Content (Plan 03-03) ni ProjectCard.
- NO instala @unhead/vue ni implementa SEO (Plan 03-04).
- NO genera arte ni reemplaza StickyAvatar placeholder (Plan 03-05).
- NO migra el array chapters[] de Phase 1 ni toca ScrollShell/StickyTimeline.
- NO implementa click-to-copy del email (mailto: nativo + tooltip cubre v1).
- NO añade libs de iconos (SVG inline en template).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-chapter-3-end-to-end/03-CONTEXT.md
@.planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md
@.planning/phases/03-chapter-3-end-to-end/03-VALIDATION.md
@.planning/phases/03-chapter-3-end-to-end/03-01-SUMMARY.md
@.planning/phases/02-theme-system-i18n/02-02-SUMMARY.md
@src/App.vue
@src/components/LangToggle.vue
@src/data/contact.js
@src/i18n/es.json
@src/i18n/en.json
@tests/components/LangToggle.test.js
@tests/i18n/test-helpers.js

<interfaces>
<!-- src/components/ContactHUD.vue — D3-10 + RESEARCH Pattern 5 verbatim -->

Script setup imports:
- `import { useI18n } from 'vue-i18n'`
- `import { contact } from '@/data/contact'`

Setup:
- `const { t } = useI18n()` — solo necesita `t`, no `locale` (no toggle interno)
- `const showOther = computed(() => contact.otherUrl !== null && contact.otherUrl !== '')` — opcional, puede ser inline `v-if="contact.otherUrl"`
- `const emailDisabled = computed(() => !contact.email || contact.email === '')` — para aria-disabled defensive
- `const linkedinDisabled = computed(() => !contact.linkedinUrl || contact.linkedinUrl === '')` análogo
- `const githubDisabled = computed(() => !contact.githubUrl || contact.githubUrl === '')` análogo

Template shape (D3-10 + RESEARCH Pattern 5 verbatim):

```vue
<template>
  <div class="contact-hud" :aria-label="t('contact.hudAria')">
    <a
      :href="emailDisabled ? undefined : `mailto:${contact.email}`"
      class="contact-icon"
      :aria-label="t('contact.emailAria')"
      :title="t('contact.emailTooltip')"
      :aria-disabled="emailDisabled ? 'true' : undefined"
    >
      <!-- SVG email icon inline (envelope outline) -->
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <rect x="2" y="4" width="20" height="16" rx="2" />
        <path d="m22 7-10 5L2 7" />
      </svg>
    </a>
    <a
      v-if="!linkedinDisabled"
      :href="contact.linkedinUrl"
      class="contact-icon"
      target="_blank"
      rel="noopener noreferrer"
      :aria-label="t('contact.linkedinAria')"
    >
      <!-- SVG LinkedIn icon inline -->
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-4 0v7h-4v-7a6 6 0 0 1 6-6z" />
        <rect x="2" y="9" width="4" height="12" />
        <circle cx="4" cy="4" r="2" />
      </svg>
    </a>
    <a
      v-if="!githubDisabled"
      :href="contact.githubUrl"
      class="contact-icon"
      target="_blank"
      rel="noopener noreferrer"
      :aria-label="t('contact.githubAria')"
    >
      <!-- SVG GitHub icon inline (octocat simplificado) -->
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M12 0C5.37 0 0 5.37 0 12c0 5.3 3.44 9.8 8.21 11.39.6.11.82-.26.82-.58 0-.29-.01-1.04-.02-2.05-3.34.73-4.04-1.61-4.04-1.61-.55-1.39-1.34-1.76-1.34-1.76-1.09-.75.08-.73.08-.73 1.21.08 1.84 1.24 1.84 1.24 1.07 1.84 2.81 1.31 3.5 1 .11-.78.42-1.31.76-1.61-2.67-.3-5.47-1.33-5.47-5.93 0-1.31.47-2.38 1.24-3.22-.12-.3-.54-1.52.12-3.18 0 0 1.01-.32 3.3 1.23a11.5 11.5 0 0 1 6 0c2.29-1.55 3.3-1.23 3.3-1.23.66 1.66.24 2.88.12 3.18.77.84 1.24 1.91 1.24 3.22 0 4.61-2.81 5.63-5.49 5.92.43.37.81 1.1.81 2.22 0 1.61-.02 2.91-.02 3.31 0 .32.22.7.83.58A12 12 0 0 0 24 12c0-6.63-5.37-12-12-12z" />
      </svg>
    </a>
    <a
      v-if="contact.otherUrl"
      :href="contact.otherUrl"
      class="contact-icon"
      target="_blank"
      rel="noopener noreferrer"
      :aria-label="t('contact.otherAria') || 'Other link'"
    >
      <!-- SVG generic external link icon inline -->
      <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
        <polyline points="15 3 21 3 21 9" />
        <line x1="10" y1="14" x2="21" y2="3" />
      </svg>
    </a>
  </div>
</template>
```

NOTA: SVG icons elegidos son simples outlines/fills sin libs externas. Tamaño 20×20 dentro de un 44×44 container con padding (RESEARCH Pattern 5 §537-548 verbatim).

NOTA emailDisabled: si Rafael NO llenó CONTENT-CHECKLIST §3 aún, contact.email === '' (Plan 03-01). El anchor se renderea SIEMPRE (consistencia visual) pero con `aria-disabled='true'` y sin href; clic no hace nada. Cuando Rafael llene §3 + commit, el HUD auto-enables el email click sin tocar componente.

CSS scoped (D3-10 + RESEARCH Pattern 5 §525-554 verbatim):

```css
<style scoped>
.contact-hud {
  position: fixed;
  bottom: env(safe-area-inset-bottom, 0);
  right: var(--sp-md);
  z-index: 40;
  display: flex;
  flex-direction: column;
  gap: var(--sp-xs);
}

.contact-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44px;     /* A11Y tap target mínimo */
  height: 44px;
  background: var(--c-surface);
  border: 1px solid var(--c-border);
  border-radius: 8px;
  color: var(--c-fg);
  text-decoration: none;
  transition: background 150ms ease, color 150ms ease;
}

.contact-icon:hover:not([aria-disabled='true']) {
  background: var(--c-accent);
  color: var(--c-bg);
}

.contact-icon[aria-disabled='true'] {
  opacity: 0.4;
  cursor: not-allowed;
}

@media (max-width: 599px) {
  .contact-hud {
    right: var(--sp-sm);
    bottom: calc(env(safe-area-inset-bottom, 0) + var(--sp-sm));
  }
}
</style>
```

NOTA crítica: NO declarar `outline:` propio en `.contact-icon:focus` ni `:focus-visible`. Pitfall 7 verbatim — el `:focus-visible` universal de App.vue (líneas 117-120 / 147-150 según versión actual) cubre el outline 3px solid var(--c-focus) sobre cada link.

NOTA mobile: el `env(safe-area-inset-bottom, 0)` previene overlap con la Safari toolbar dinámica (iOS-01 carry-forward Phase 1). En portrait sin notch, el inset es 0 — comportamiento idéntico a desktop.

<!-- App.vue extend: append <ContactHUD /> al final del template root -->

Imports:
- `import ContactHUD from './components/ContactHUD.vue'` (junto a los imports existentes líneas 27-36 según versión actual)

Template:
- Orden DOM final post-W1 P3: `<BackgroundLayers /> → <SkipLink /> → <StickyAvatar /> → <ScrollShell ... /> → <StickyTimeline /> → <LangToggle /> → <ContactHUD />`
- `<ContactHUD />` se monta como ÚLTIMO hijo del template root (después de `<LangToggle />`). Order semántico: HUDs invariantes al final del DOM (LangToggle top-right, ContactHUD bottom-right; ambos `position: fixed; z-index: 40`).

<!-- tests/components/ContactHUD.test.js — análogo a LangToggle.test.js Phase 2 W1 -->

Patrón global: usar `createTestI18n` de `tests/i18n/test-helpers.js` (existente Phase 2 W1) + `vi.mock('@/data/contact', () => ({ contact: { email: 'test@example.com', linkedinUrl: 'https://linkedin.com/in/test', githubUrl: 'https://github.com/test', otherUrl: null } }))` para tests con valores conocidos.

Tests mínimos (≥8):
- T1 DOM contract: mount(ContactHUD) con locale='es' → `.contact-hud` existe + aria-label === 'Contacto rápido' (del t('contact.hudAria'))
- T2 anchors renderean: 3 anchors `.contact-icon` (email + LinkedIn + GitHub); `<svg>` dentro de cada uno; otherUrl NULL → 4to anchor NO presente
- T3 href correcto: email anchor href === 'mailto:test@example.com', LinkedIn anchor href === 'https://linkedin.com/in/test', GitHub anchor href === 'https://github.com/test'
- T4 T-CON-03 mitigation: LinkedIn anchor tiene `rel="noopener noreferrer"` y `target="_blank"`; GitHub idem. (Email mailto: NO requiere rel — protocolo distinto.) Test usa `wrapper.findAll('a').filter(a => a.attributes('target') === '_blank')` para extraer externals y asserta `rel === 'noopener noreferrer'` en cada uno.
- T5 aria-labels desde i18n: locale='es' → email anchor aria-label === 'Enviar email a Rafael'; locale='en' → 'Email Rafael' (multiplexar via createTestI18n con {locale: 'en'})
- T6 reactive (Pitfall 3): mount con locale='es' inicial; `i18n.global.locale.value = 'en'` + flushPromises; aria-labels actualizan sin re-mount
- T7 CSS readFileSync (análogo LangToggle T7): source contiene `position:\s*fixed`, `bottom:\s*env\(safe-area-inset-bottom,\s*0\)`, `right:\s*var\(--sp-md\)`, `z-index:\s*40`, `width:\s*44px`, `height:\s*44px`, `flex-direction:\s*column`
- T8 CSS no outline declared (Pitfall 7): source NO contiene `outline:` dentro de `.contact-icon` block — el universal cubre. Test regex: `expect(source.match(/\.contact-icon\s*\{[^}]*outline:/)).toBeNull()`
- T9 (extra valor) otherUrl conditional render: mount con `vi.mock` contact override `{ otherUrl: 'https://mastodon.example/@rafael' }` → 4 anchors visibles, último anchor href === otherUrl + rel/target externos
- T10 (extra valor — defensive disabled): mount con `vi.mock` contact `{ email: '', linkedinUrl: '...', githubUrl: '...', otherUrl: null }` → email anchor renderea PERO `aria-disabled='true'` y NO href (Plan 03-01 placeholder defensive)

Tests usan `mount(ContactHUD, { global: { plugins: [createTestI18n({ locale: 'es' })] } })` y para tests EN re-mountan con `{ locale: 'en' }`. Patrón análogo a LangToggle tests (multiplexar locale en lugar de mutar mid-test, salvo T6 que SÍ muta para verificar reactividad Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Crear ContactHUD.vue + tests RED→GREEN + montar en App.vue</name>
  <files>
    src/components/ContactHUD.vue,
    src/App.vue,
    tests/components/ContactHUD.test.js
  </files>
  <read_first>
    src/components/LangToggle.vue (analog 1: HUD invariante fixed + i18n + a11y — Phase 2 W1 verbatim — copiar patron de import useI18n + provide tokens neutros + min 44×44 + sin outline propio + transition 150ms),
    tests/components/LangToggle.test.js (analog 1 tests: createTestI18n usage + multiplexar locale + flushPromises Pitfall 3 + CSS readFileSync regex),
    tests/i18n/test-helpers.js (helper createTestI18n a importar verbatim — NO crear nuevo),
    src/data/contact.js (Plan 03-01 output — shape de import + valores actuales),
    src/i18n/es.json + src/i18n/en.json (verificar keys contact.* añadidas en Plan 03-01 task 1.2),
    src/App.vue (orden actual del template root + import pattern existente),
    .planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md §Pattern 5 líneas 481-554 (ContactHUD complete source verbatim — copiar template + CSS),
    .planning/phases/03-chapter-3-end-to-end/03-CONTEXT.md D3-10 verbatim
  </read_first>
  <behavior>
    ContactHUD tests RED phase — al menos 10 tests fallan porque ContactHUD.vue no existe aún:

    - T1 DOM contract base: `.contact-hud` existe + aria-label viene de t('contact.hudAria')
    - T2 anchors count + svg children: 3-4 anchors `.contact-icon` con `<svg>` interno; otherUrl conditional v-if
    - T3 href correcto para los 3 mandatory: mailto:email, linkedinUrl, githubUrl
    - T4 T-CON-03: external links (LinkedIn, GitHub, otherUrl si presente) tienen `rel="noopener noreferrer"` Y `target="_blank"` — verificar con `wrapper.findAll('a[target="_blank"]').wrappers.forEach(a => expect(a.attributes('rel')).toBe('noopener noreferrer'))`
    - T5 aria-labels i18n multiplexed: locale='es' → "Contacto rápido" / "Enviar email a Rafael" / "Perfil LinkedIn de Rafael" / "GitHub de Rafael"; locale='en' → "Quick contact" / "Email Rafael" / "Rafael's LinkedIn profile" / "Rafael's GitHub"
    - T6 reactive Pitfall 3: mount locale='es', mutar `i18n.global.locale.value = 'en'`, flushPromises, assert aria-labels en EN sin re-mount
    - T7 CSS readFileSync: source contiene declarations exactas del style scoped (position fixed, bottom env(safe-area-inset-bottom), right var(--sp-md), z-index 40, width/height 44px, flex-direction column, transition 150ms)
    - T8 Pitfall 7 (no outline propio): regex match `/\.contact-icon\s*\{[^}]*outline:/` returns null
    - T9 otherUrl conditional render: mount con vi.mock setting otherUrl → 4to anchor presente con href === otherUrl + externs attrs
    - T10 defensive disabled: mount con email === '' → email anchor renderea con aria-disabled='true' SIN href

    Commit RED: `test(03-02): RED — tests ContactHUD (10 tests, DOM + i18n + T-CON-03 + CSS readFileSync)`.

    GREEN phase — crear ContactHUD.vue según <interfaces> verbatim + montar en App.vue:
    - SVG icons inline en template (NO imports de libs)
    - useI18n() para t() reactive — NO inject de algún store
    - Import contact desde @/data/contact (Plan 03-01)
    - v-if para LinkedIn/GitHub (defensive si '' placeholder) + otherUrl (conditional opcional)
    - email con aria-disabled cuando ''
    - rel="noopener noreferrer" + target="_blank" en cada external

    Commit GREEN: `feat(03-02): GREEN — ContactHUD.vue fixed bottom-right + 3-4 anchors + T-CON-03 mitigation`.

    App.vue mount:
    - import ContactHUD
    - <ContactHUD /> como último hijo del template root

    Commit: `feat(03-02): mount ContactHUD en App.vue (orden DOM: ... → LangToggle → ContactHUD)`.
  </behavior>
  <action>
    1. Leer LangToggle.vue completo (analog arquitectural exacto). Identificar el patrón: import useI18n + setup minimal + template con button + CSS scoped + tokens neutros + sin outline propio + min 44×44 + transition 150ms + mobile @media.

    2. Leer tests/components/LangToggle.test.js completo. Identificar el helper de mount, el uso de createTestI18n, el patrón de multiplexar locale, el flushPromises Pitfall 3, los readFileSync regex.

    3. Escribir `tests/components/ContactHUD.test.js` con los 10 tests del bloque <behavior>. Patrón:
       - `import { mount } from '@vue/test-utils'`
       - `import { flushPromises } from '@vue/test-utils'`
       - `import { vi } from 'vitest'`
       - `import { readFileSync } from 'fs'; import { resolve } from 'path'`
       - `import ContactHUD from '@/components/ContactHUD.vue'`
       - `import { createTestI18n } from '../i18n/test-helpers.js'`
       - Para T9/T10 conditional mocks: usar `vi.mock('@/data/contact', () => ({ contact: { ... } }))` ANTES del import del componente (top of file). Para T1-T8 baseline test usar valores default (decisión: hardcodear el mock al top del file con valores positivos test@example.com / https://linkedin.com/in/test / https://github.com/test / null, y para T9/T10 usar `vi.doMock` dentro del test o re-mockear con `vi.resetModules` + dynamic import; alternativa: crear 2-3 archivos de test separados — SIMPLE: usar `vi.mock` global al top con `{ email: 'test@example.com', linkedinUrl: 'https://linkedin.com/in/test', githubUrl: 'https://github.com/test', otherUrl: null }` y para T9/T10 INLINE mockear via `vi.doMock` antes de un dynamic `await import(...)`).
       - Test pattern más simple alternativo: pasar `contact` como prop al componente (NO recomendado — D3-10 dice "consume contact.js directamente"). MEJOR: usar `vi.mock` con setup async dentro del test usando `vi.resetModules` + dynamic import:
         ```js
         it('T9 otherUrl conditional', async () => {
           vi.resetModules()
           vi.doMock('@/data/contact', () => ({ contact: { email: 'a@b.c', linkedinUrl: 'https://...', githubUrl: 'https://...', otherUrl: 'https://mastodon.example/@rafael' } }))
           const { default: ContactHUD } = await import('@/components/ContactHUD.vue')
           const wrapper = mount(ContactHUD, { global: { plugins: [createTestI18n({locale:'es'})] } })
           expect(wrapper.findAll('a.contact-icon')).toHaveLength(4)
           expect(wrapper.find('a.contact-icon:last-child').attributes('href')).toBe('https://mastodon.example/@rafael')
         })
         ```
       - El executor decide el approach exacto que minimice complejidad de tests; el contrato es: ≥10 tests verdes verificando los behaviors listados.

    4. Run `npm run test:run -- tests/components/ContactHUD` — esperar 10 tests RED.

    5. Commit RED: `test(03-02): RED — tests ContactHUD (10 tests)`.

    6. Crear `src/components/ContactHUD.vue` según <interfaces> verbatim:
       - `<script setup>` con import useI18n + import contact + computed defensive flags
       - `<template>` con `<div class="contact-hud">` + 4 anchors (mailto, LinkedIn, GitHub, otherUrl conditional). SVG icons inline VERBATIM del <interfaces> block — copiar literalmente.
       - `<style scoped>` con CSS del <interfaces> block verbatim. NO declarar `outline:` propio.
       - Header comment al inicio del `<script setup>` análogo a LangToggle.vue (~10 LOC referenciando D3-10, T-CON-03 mitigation, RESEARCH Pattern 5, CON-03).

    7. Run `npm run test:run -- tests/components/ContactHUD` — esperar 10 tests GREEN.

    8. Commit GREEN: `feat(03-02): GREEN — ContactHUD.vue + 3-4 anchors + T-CON-03 + a11y 44×44 + i18n reactive`.

    9. Modificar `src/App.vue`:
       - Añadir `import ContactHUD from './components/ContactHUD.vue'` junto a los imports líneas 27-36.
       - Añadir `<ContactHUD />` como ÚLTIMO hijo del template root (después de `<LangToggle />`). Verificar orden final = `BackgroundLayers → SkipLink → StickyAvatar → ScrollShell → StickyTimeline → LangToggle → ContactHUD`.

    10. Run `npm run test:run` (suite completa) — esperar ≥172 tests verdes. Run `npm run build` — debe pasar.

    11. Commit: `feat(03-02): mount <ContactHUD /> en App.vue como último hijo (orden DOM final post-W1 Phase 3)`.
  </action>
  <verify>
    <automated>npm run test:run -- tests/components/ContactHUD &amp;&amp; npm run test:run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ContactHUD.vue` existe con: import useI18n + contact, 4 anchor templates (email mailto + 3 external con rel/target), SVG icons inline, CSS scoped position fixed bottom-right z-index 40 + min 44×44
    - `tests/components/ContactHUD.test.js` corre ≥10 tests verdes (DOM + i18n multiplexed + T-CON-03 regex + CSS readFileSync + Pitfall 3 + Pitfall 7 + conditional otherUrl + defensive disabled email)
    - CSS source contiene exact strings: `position: fixed`, `bottom: env(safe-area-inset-bottom, 0)`, `right: var(--sp-md)`, `z-index: 40`, `width: 44px`, `height: 44px`, `flex-direction: column`, `transition: background 150ms ease, color 150ms ease`
    - CSS source NO contiene `outline:` dentro de `.contact-icon` block (Pitfall 7)
    - Cada external anchor (`target="_blank"`) tiene `rel="noopener noreferrer"` — verificable con grep: `grep -E 'target="_blank"' src/components/ContactHUD.vue | wc -l` igual a `grep -E 'rel="noopener noreferrer"' src/components/ContactHUD.vue | wc -l` (al menos 2: LinkedIn, GitHub; +1 si otherUrl present en data)
    - `src/App.vue` template contiene literal `<ContactHUD />` como último hijo del template root
    - `npm run build` verde; bundle JS incluye string `contact-hud` (verificable)
    - Suite global ≥172 tests verdes (164 baseline + 10 nuevos ≥ +8 net post deduplicación)
    - DevTools manual (post-merge): HUD visible bottom-right en cada chapter (probar 0..6 mediante click en StickyTimeline ticks); click email abre cliente de correo; click LinkedIn/GitHub abre en nueva pestaña; toggle locale propaga aria-labels al instante
    - Mobile 375×667: HUD aún visible bottom-right, no overlap con StickyTimeline (la timeline está left-side post-redesign 2026-05-13), tap targets 44×44 verificables en DevTools accessibility panel
  </acceptance_criteria>
  <done>ContactHUD.vue funcional + montado en App.vue + ≥10 tests verdes + build verde + T-CON-03 mitigation + Pitfall 7 evitado + i18n reactivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContactHUD external links → user's browser → external sites (LinkedIn, GitHub, otherUrl) | Cross-origin navigation. Open redirect/window.opener risk si los anchors no llevan `rel="noopener noreferrer"`. |
| ContactHUD email link → user's email client (`mailto:` protocol) | Browser handles natively. Safe-by-default. No data exfiltration runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CON-03 | Spoofing / Tampering | External anchors LinkedIn/GitHub/otherUrl | mitigate | `rel="noopener noreferrer"` + `target="_blank"` en cada external anchor (verificado por test T4). URLs hardcoded en `src/data/contact.js` (Plan 03-01), no user input runtime. |
| T-CLICK-MAILTO | Spoofing | `mailto:` href | accept | Browser nativo abre cliente de correo; no exfiltración. Defensa: Rafael puede usar email dedicado/canario si surge abuso de scrapers. |
| T-XSS-i18n | Tampering | t() interpolations en aria-labels | mitigate | vue-i18n escapes by-default; aria-labels no se renderean como HTML. No `v-html` ni `innerHTML` en el componente. |
| T-A11Y-44px | A11Y degradation | Tap targets <44×44 | mitigate | CSS scoped declara `width: 44px; height: 44px` mínimo (verificado por test T7). Mobile @media preserva mínimo. |
</threat_model>

<verification>
- Comando: `npm run test:run && npm run build`
- Esperado: ≥172 tests verdes, build verde, bundle CSS sin grow >1KB (HUD es ~50 LOC CSS), bundle JS sin grow >3KB (HUD componente ~80 LOC + SVG icons)
- DevTools manual (post-merge): `npm run dev` → HUD visible bottom-right en chapters 0..6; click email abre cliente; click LinkedIn abre nueva pestaña (verificar `noopener` en DevTools network tab; el opener window NO debe estar accesible vía window.opener desde el target)
- Mobile simulator 375×667: HUD aún visible bottom-right, no horizontal overflow, tap target 44×44 visible en DevTools accessibility panel
- Threat verification: grep en source: `grep -E 'target="_blank"' src/components/ContactHUD.vue` retorna ≥2 matches; cada uno tiene `rel="noopener noreferrer"` cerca (visible cuando se ejecuta `grep -B1 -A1 'target=' src/components/ContactHUD.vue`)
</verification>

<success_criteria>
- ContactHUD.vue funcional + visible bottom-right en los 7 chapters (CON-03)
- 3-4 anchors (email mailto + LinkedIn + GitHub + opcional otherUrl) con aria-labels i18n reactivos
- T-CON-03 mitigation: rel="noopener noreferrer" + target="_blank" en cada external anchor
- A11Y: 44×44 tap target + focus-visible heredado del universal + aria-labels descriptivos
- Suite global ≥172 tests verdes, build verde
- Phase 1 + Phase 2 zero regression: todos los tests files baseline siguen verdes
- Phase 3 Plan 03-01 zero regression: 13 data tests siguen verdes
</success_criteria>

<output>
After completion, create `.planning/phases/03-chapter-3-end-to-end/03-02-SUMMARY.md` con:
- ContactHUD.vue creado (LOC, principal CSS choices, SVG icons inline elegidos)
- App.vue extendido (orden DOM final)
- Tests añadidos (count + mapping a CON-03 + T-CON-03)
- Decisiones tomadas (e.g., SVG inline vs lib; defensive disabled email; mock approach para tests con vi.mock vs vi.doMock)
- Phase 2 + Phase 3 W0 regression baseline confirmado
- Pending para W2 (Plan 03-03): Chapter3Content + ProjectCard skeumorphic + integración en section ch3 de ScrollShell
- Pending para W3 (Plan 03-04): SEO via @unhead/vue
- Pending para W4 (Plan 03-05): avatar pixel art batch 7 busts (checkpoint:human-input bloqueante)
</output>
