---
phase: 3
plan: 1
slug: wave0-data-scaffolding
wave: 0
type: execute
mode: mvp
autonomous: true
gap_closure: false
requirements: [CON-05, CON-06, CON-03, ART-06]
depends_on: []
files_modified:
  - src/data/chapters.js
  - src/data/projects.js
  - src/data/bio.js
  - src/data/contact.js
  - src/i18n/es.json
  - src/i18n/en.json
  - tests/data/chapters.test.js
  - tests/data/projects.test.js
  - tests/data/contact.test.js
  - tests/data/bio.test.js
notes:
  phase_2_regression_baseline: >
    Suite Phase 2 baseline = 151 tests verdes. Tras W0 los archivos JSON i18n añaden
    keys nuevas (bio.core, projects.pp1.title/desc, contact.hudAria/emailAria/emailTooltip/
    linkedinAria/githubAria, seo.title/description) — `tests/i18n/parity.test.js` debe
    seguir verde (la paridad se mantiene SI las keys se añaden a ambos JSON con valores
    placeholder `"PENDING — CONTENT-CHECKLIST §X"`). Si Rafael NO ha llenado el
    CONTENT-CHECKLIST, los valores son los placeholders; cuando Rafael llene, el
    executor sustituye los placeholders en los mismos paths sin tocar shape.
  content_checklist_gate: >
    Este wave NO requiere que CONTENT-CHECKLIST esté lleno. Los data files se crean
    con shape correcto y los valores reales que TIENE Rafael (si los hay) o
    placeholders (`""` para strings, `null` para campos opcionales). Las keys i18n se
    añaden con texto placeholder `"PENDING — CONTENT-CHECKLIST §X"` que pasa parity
    automáticamente. Plan 03-05 (avatar art) es el que tiene checkpoint:human-input
    bloqueante para CONTENT-CHECKLIST §5 (paletas) + §5.6 (paleta avatar) + foto Rafael.
must_haves:
  truths:
    - "Existe `src/data/chapters.js` que exporta `const chapters` como array de 7 objetos con shape `{ id, year, era, eraKey, titleKey, avatarSrc, palette }` (D3-04 + D3-06)"
    - "Existe `src/data/projects.js` que exporta `const projects` como array (vacío o con proyectos Pink Parrot ch3 si Rafael ya lo llenó) — cada item tiene shape rico D3-03: `{ id, chapterEra, year, titleKey, descKey, link, imageSrc, role, techStack, planetSprite, planetOrbit, planetColor }` (Phase 5 phaser fields como `null` en Phase 3)"
    - "Existe `src/data/bio.js` que exporta `const bio = { coreKey: 'bio.core' }` — solo refs a i18n keys (D3-02)"
    - "Existe `src/data/contact.js` que exporta `const contact = { email, linkedinUrl, githubUrl, otherUrl }` con valores hardcoded de CONTENT-CHECKLIST §3 (si Rafael lo llenó) o strings vacíos placeholder (D3-10 + T-CON-03 mitigation: URLs hardcoded, NO user input)"
    - "Existen 4 archivos de test: `tests/data/chapters.test.js`, `tests/data/projects.test.js`, `tests/data/contact.test.js`, `tests/data/bio.test.js` — cada uno verifica el shape exportado"
    - "`tests/data/chapters.test.js` verifica: T1 array de longitud 7 + T2 cada item tiene `{ id: number, year: number, era: string, eraKey: string, titleKey: string, avatarSrc: string, palette: array }` + T3 ids van 0..6 sin gaps + T4 avatarSrc sigue convención `/assets/ch{N}-bust.png` (ART-05) + T5 cada chapter tiene `palette` array (puede estar vacío en Phase 3 hasta que Rafael llene CONTENT-CHECKLIST §5; el test verifica que el FIELD existe, no que tenga contenido — ART-06 enforcement está en Plan 03-05 antes de generar arte)"
    - "`tests/data/projects.test.js` verifica: T1 import retorna array + T2 cada item tiene los 12 campos shape D3-03 (id, chapterEra, year, titleKey, descKey, link, imageSrc, role, techStack, planetSprite, planetOrbit, planetColor) + T3 planet* fields son `null` (Phase 5 los pobla) + T4 chapterEra es number 0..6"
    - "`tests/data/contact.test.js` verifica: T1 exporta object con 4 keys (email, linkedinUrl, githubUrl, otherUrl) + T2 email es string (puede ser '') + T3 linkedinUrl y githubUrl son strings (T-CON-03 — URLs hardcoded en archivo de código, no user input runtime)"
    - "`tests/data/bio.test.js` verifica: T1 exporta `bio.coreKey === 'bio.core'`"
    - "Las keys i18n nuevas existen en AMBOS JSON con paridad: `bio.core`, `projects.pp1.title`, `projects.pp1.desc` (y `.pp2.*`, `.pp3.*` si Rafael llenó 2-3 proyectos), `contact.hudAria`, `contact.emailAria`, `contact.emailTooltip`, `contact.linkedinAria`, `contact.githubAria`, `seo.title`, `seo.description` — el test `tests/i18n/parity.test.js` (Phase 2) sigue verde automáticamente porque flatten() compara llaves, no contenido"
    - "Suite global ≥151 (Phase 2 baseline) + ≥10 nuevos data tests = ≥161 tests verdes; build verde"
  artifacts:
    - path: src/data/chapters.js
      provides: "Array canonical de 7 chapters (CON-05) — fuente única para ScrollShell + StickyTimeline + StickyAvatar (Phase 1 lo duplica inline, Phase 4 lo consolida)"
      contains: "export const chapters"
    - path: src/data/projects.js
      provides: "Array de proyectos con shape rico D3-03 (CON-06) — filtrable por chapterEra; Phase 5 añade planet* fields para ch6"
      contains: "export const projects"
    - path: src/data/bio.js
      provides: "Ref a i18n key bio.core — Chapter3Content lo consume vía t(bio.coreKey)"
      contains: "export const bio"
    - path: src/data/contact.js
      provides: "Datos hardcoded de contacto (D3-10 + T-CON-03 mitigation)"
      contains: "export const contact"
    - path: tests/data/chapters.test.js
      provides: "Tests CON-05 shape + ART-05 naming + ART-06 palette field existence"
      contains: "chapters"
    - path: tests/data/projects.test.js
      provides: "Tests CON-06 shape D3-03 + chapterEra range"
      contains: "projects"
    - path: tests/data/contact.test.js
      provides: "Tests CON-03 shape + T-CON-03 (URLs hardcoded)"
      contains: "contact"
    - path: tests/data/bio.test.js
      provides: "Test bio.coreKey ref a i18n"
      contains: "bio"
  key_links:
    - from: src/data/chapters.js
      to: src/i18n/es.json
      via: "eraKey + titleKey son references a chapters.N.title (ya existe Phase 2) + nueva key chapters.N.era (si añade) — los consumidores resuelven via t(chapter.titleKey)"
      pattern: "titleKey"
    - from: src/data/projects.js
      to: src/i18n/es.json
      via: "titleKey + descKey son references a projects.<id>.{title,desc} — Chapter3Content resuelve via t(project.titleKey)"
      pattern: "titleKey"
    - from: src/data/bio.js
      to: src/i18n/es.json
      via: "coreKey === 'bio.core' — Chapter3Content resuelve via t(bio.coreKey)"
      pattern: "coreKey"
    - from: src/data/contact.js
      to: src/components/ContactHUD.vue
      via: "ContactHUD importa contact y construye mailto:contact.email + contact.linkedinUrl + contact.githubUrl (Plan 03-02)"
      pattern: "export const contact"
key_decisions:
  - "D3-01 LOCKED — 4 archivos separados en src/data/: chapters.js + projects.js + bio.js + contact.js (no consolidación)"
  - "D3-02 LOCKED — texto bilingual en es.json/en.json; data files solo refs (titleKey, descKey, coreKey, eraKey)"
  - "D3-03 LOCKED — Project shape rico Phase 5-ready: { id, chapterEra, year, titleKey, descKey, link, imageSrc, role, techStack, planetSprite, planetOrbit, planetColor } — planet* nulls en Phase 3"
  - "D3-04 LOCKED — chapters.js shape básico: { id, year, era, eraKey, titleKey, avatarSrc, palette } sin helper functions; joins inline en consumers"
  - "D3-06 LOCKED — palette: [hex, ...] field en chapters.js (puede estar vacío en Phase 3 si Rafael no llenó CONTENT-CHECKLIST §5; el field existe siempre — ART-06 enforcement runtime ocurre en Plan 03-05 cuando pixelforge recibe palette: chapter.palette explícito)"
  - "D3-10 LOCKED — contact shape: { email, linkedinUrl, githubUrl, otherUrl? }; URLs hardcoded en código (T-CON-03 mitigation, no user input)"
  - "I18n keys placeholder strategy — si CONTENT-CHECKLIST está incompleto, las keys se añaden con valor `\"PENDING — CONTENT-CHECKLIST §X\"`. parity.test.js verifica llaves (no contenido), entonces queda verde. Rafael llena los placeholders en es.json/en.json sin tocar shape cuando entregue contenido."
  - "Phase 1 chapters array duplicado en 3 componentes (ScrollShell, StickyTimeline) NO se migra en Plan 03-01 — la consolidación queda para Phase 4 (cuando todos los chapters tengan contenido real). Plan 03-01 SOLO crea el archivo canónico; los 3 componentes Phase 1 mantienen su array inline (decisión locked en Phase 2 W1 SUMMARY §Pending para W2). Risk acceptado: drift temporal Phase 3 → Phase 4."
anti_scope:
  - "NO migrar arrays chapters[] duplicados de ScrollShell.vue ni StickyTimeline.vue a usar src/data/chapters.js — eso es Phase 4 cuando se consoliden todos los chapters. Plan 03-01 SOLO crea el archivo canónico."
  - "NO instalar @unhead/vue (Plan 03-04) ni crear ContactHUD (Plan 03-02) ni Chapter3Content (Plan 03-03) ni generar arte (Plan 03-05)."
  - "NO requerir que CONTENT-CHECKLIST esté lleno — los data files se crean con shape correcto + placeholders donde haga falta. Plan 03-05 es el que tiene el checkpoint blocking."
  - "NO crear Pitfall 5 mitigation aún (`.gitignore` entry para public/assets/.refs/) — eso es prerequisito de Plan 03-05 (arte) donde la foto vive."
  - "NO crear `tests/assets/bust-naming.test.js` ni `tests/seo/head-tags.test.js` ni `tests/components/ContactHUD.test.js` ni `tests/components/Chapter3Content.test.js` ni `tests/components/ProjectCard.test.js` — son responsabilidad de sus respectivos plans (03-02..03-05)."
threat_model_summary:
  - "T-CON-03 (open redirect via contact URLs): URLs hardcoded en src/data/contact.js (no user input runtime). Mitigación en Plan 03-02 también: rel='noopener noreferrer' en links externos."
  - "T-DATA-DRIFT (chapters array drift Phase 1 → Phase 3): documentado en key_decisions; aceptado como deuda técnica corta — Phase 4 lo cierra cuando los 7 chapters tengan contenido real."
---

## Phase Goal (MVP Vertical Slice)

**As a** developer (Claude futuro) que va a construir ContactHUD, Chapter3Content y los avatar busts, **I want to** tener archivos canonical `src/data/*.js` con shape locked + i18n keys placeholder ya presentes en ambos idiomas, **so that** los planes 03-02..03-05 puedan importarlos directamente sin re-decidir shape ni tocar i18n parity.

> **Nota MVP:** Este Wave 0 es de scaffolding — NO produce UI visible nueva. Es el prerequisito que desbloquea los 4 waves siguientes. Tras este plan, el shape de los datos está locked + los placeholders i18n están en su sitio + los tests de shape existen y son la fuente de verdad para Plan 03-02 (que importa `contact`), Plan 03-03 (que importa `bio`, `projects`, `chapters[3]`), Plan 03-04 (que renderea SEO desde i18n keys `seo.*`), y Plan 03-05 (que lee `chapter.palette` antes de cada call a pixelforge).

<objective>
Construir el scaffolding de datos de Phase 3: 4 archivos `src/data/*.js` con shape locked por D3-01..D3-04 + D3-10 + extensión de i18n parity con todas las keys que los demás planes consumirán (placeholders donde Rafael no haya llenado CONTENT-CHECKLIST aún) + tests de shape que sirven como contrato sellado.

**Purpose:** Cubre CON-05 (src/data/chapters.js fuente única) + CON-06 (src/data/projects.js shape rico Phase 5-ready) + parte de CON-03 (contact data — el componente HUD viene en Plan 03-02) + ART-06 (palette field existence — el palette enforcement runtime viene en Plan 03-05).

**Lo que ESTE plan NO hace:**
- NO crea componentes UI (ContactHUD → 03-02; Chapter3Content + ProjectCard → 03-03; SEO/JSON-LD → 03-04).
- NO instala dependencias (`@unhead/vue` → 03-04).
- NO genera arte (→ 03-05).
- NO migra los arrays chapters[] duplicados de ScrollShell.vue / StickyTimeline.vue a usar el nuevo archivo canónico — esa consolidación queda para Phase 4 cuando todos los chapters tengan contenido real.
- NO requiere que CONTENT-CHECKLIST esté lleno — los data files se crean con shape correcto + valores reales (los que Rafael haya entregado) o placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-chapter-3-end-to-end/03-CONTEXT.md
@.planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md
@.planning/phases/03-chapter-3-end-to-end/03-VALIDATION.md
@.planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md
@.planning/REQUIREMENTS.md
@src/i18n/index.js
@src/i18n/es.json
@src/i18n/en.json
@tests/i18n/parity.test.js
@tests/i18n/test-helpers.js
@src/components/ScrollShell.vue
@src/components/StickyTimeline.vue

<interfaces>
<!-- src/data/chapters.js — D3-04 + D3-06 locked shape verbatim -->

Estructura del módulo:
- `export const chapters = [ ... ]` con 7 entradas (id 0..6)
- Cada entrada: `{ id: number, year: number, era: string, eraKey: string, titleKey: string, avatarSrc: string, palette: string[] }`
- Valores derivados:
  - id: 0..6 (number, no string)
  - year: 1995, 2001, 2009, 2013, 2015, 2022, 2026 (numbers — coinciden con Phase 1 ScrollShell.vue y StickyTimeline.vue arrays)
  - era: 'Terminal', 'HTML 90s', 'Flash', 'Web 2.0', 'AR/VR', 'Modern', 'Phaser' (strings — coinciden con Phase 1)
  - eraKey: 'chapters.0.era' .. 'chapters.6.era' (NUEVAS keys i18n — NO existen aún; este plan las añade a es.json/en.json con texto bilingue real: 'Terminal' / 'Terminal', 'HTML 90s crudo' / '90s HTML', etc.)
  - titleKey: 'chapters.0.title' .. 'chapters.6.title' (YA existen desde Phase 2 W0 — verificar; chapters.js solo referencia esas keys)
  - avatarSrc: '/assets/ch0-bust.png' .. '/assets/ch6-bust.png' — convención ART-05 verbatim (NOTA: los archivos físicos no existen aún; Plan 03-05 los genera. Phase 1 StickyAvatar.vue todavía no consume avatarSrc — eso es Plan 03-05 también)
  - palette: array de strings hex `'#RRGGBB'` — para ch0 y ch1 usar valores verbatim de chapter-themes.css (`['#000000', '#00ff41']` para ch0; `['#000080', '#ff00ff', '#ffff00', '#ffffff']` para ch1). Para ch2..ch6: `[]` array vacío SI Rafael NO ha llenado CONTENT-CHECKLIST §5; SI llenó, usar los hex codes de §5.{1..5} verbatim.

Source files a leer para extraer values:
- src/components/ScrollShell.vue líneas 26-34 (chapters array Phase 1 verbatim — copiar year + era exactos)
- src/styles/chapter-themes.css líneas 33-141 (per-chapter [data-chapter="N"] blocks — extraer `--c-bg`, `--c-fg`, `--c-accent`, `--c-border`, `--c-focus` para chapters 0 y 1 que están completos en Phase 2)
- .planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md §5 — si Rafael ha llenado paletas, usar verbatim; si no, arrays vacíos

Header comment del módulo (~10 LOC explicando el shape + ref a D3-04 + D3-06 + CON-05):
```
// src/data/chapters.js — CON-05 single source of truth para los 7 chapters.
// Shape: { id, year, era, eraKey, titleKey, avatarSrc, palette }.
// Decisiones:
//   - D3-04 (locked): shape básico + assets refs; sin helper functions. Joins inline.
//   - D3-06 (locked): palette field doble fuente — CONTENT-CHECKLIST §5 (humano) + replica aquí (programmatic).
//     Cada call a forge_sprite en Plan 03-05 pasa `palette: chapter.palette` explícito (ART-06).
// Consumers Phase 3: Chapter3Content (vía chapters[3]); StickyAvatar (vía avatarSrc cuando se reemplace placeholder); Plan 03-05 (pixelforge palette param).
// Consumers Phase 1 (legacy, NO MIGRADOS aquí): ScrollShell.vue + StickyTimeline.vue duplican el array inline; consolidación queda para Phase 4.
```

<!-- src/data/projects.js — D3-03 locked shape verbatim -->

Estructura del módulo:
- `export const projects = [ ... ]` (puede estar vacío al inicio si CONTENT-CHECKLIST §2.2 está sin llenar — el shape lo establece el header comment y los tests; cuando Rafael llene, executor añade items siguiendo el shape locked)
- Si Rafael YA llenó CONTENT-CHECKLIST §2.2 con 1-3 proyectos Pink Parrot ch3:
  - 1-3 entradas con `chapterEra: 3`, `year: 2013`, `id: 'pp1' | 'pp2' | 'pp3'`
  - titleKey: 'projects.pp1.title' .. 'projects.pp3.title'
  - descKey: 'projects.pp1.desc' .. 'projects.pp3.desc'
  - link: string URL del CHECKLIST §2.2 (NO `null` si el checklist tiene link) — hardcoded en archivo de código
  - imageSrc: `null` por default (Phase 3 no maneja imágenes de proyecto; Phase 4 puede añadir)
  - role: string del checklist §2.2 (e.g., 'UX Lead', 'Web Developer + Team Leader') o `null`
  - techStack: array de strings del checklist §2.2 (e.g., `['CSS3', 'jQuery', 'PHP']`) o `null`
  - planetSprite, planetOrbit, planetColor: `null` SIEMPRE en Phase 3 (Phase 5 los pobla cuando construya la escena Phaser ch6)
- Si Rafael NO ha llenado §2.2: array vacío `[]` con header comment indicando que executor llene cuando Rafael entregue contenido. Los tests verifican el SHAPE de cada item presente — un array vacío pasa el shape check trivialmente; el contenido completo se valida en Plan 03-03 (Chapter3Content render test).

Header comment del módulo (~12 LOC):
```
// src/data/projects.js — CON-06 array canonical de proyectos.
// Shape D3-03 (locked): { id, chapterEra, year, titleKey, descKey, link, imageSrc, role, techStack, planetSprite, planetOrbit, planetColor }.
// Phase 5 fields (planetSprite, planetOrbit, planetColor): `null` en Phase 3; Phase 5 los pobla para ch6.
// Phase 3 SCOPE: solo proyectos ch3 Pink Parrot (1-3 desde CONTENT-CHECKLIST §2.2). Phase 4 añade ch2/ch4/ch5; Phase 5 añade ch6 con planet metadata.
// Convención de IDs: chN-<slug> o ppN (pink parrot N) — string corto único.
```

<!-- src/data/bio.js — minimal -->

```
// src/data/bio.js — CON-01 ref a i18n.
// Bio text vive en es.json/en.json bajo la key bio.core (renderizada por Chapter3Content en Plan 03-03).
// Este archivo solo expone la key — futuras secciones de chapter pueden importarla para mostrar
// la misma bio con estilo de su era.
export const bio = {
  coreKey: 'bio.core',
}
```

<!-- src/data/contact.js — D3-10 + T-CON-03 mitigation verbatim -->

```
// src/data/contact.js — CON-03 datos de contacto hardcoded.
// Decisión D3-10: ContactHUD fixed bottom-right consume estos valores.
// Threat T-CON-03 (open redirect): URLs hardcoded en source code — NO user input runtime,
// NO localStorage, NO query-string. Compromiso: si Rafael cambia un URL, requiere
// commit + redeploy. Aceptable para portafolio estático.
// Source: CONTENT-CHECKLIST §3 — si Rafael NO ha llenado, valores son '' / null.
export const contact = {
  email: '',         // CONTENT-CHECKLIST §3 — Rafael fills
  linkedinUrl: '',
  githubUrl: '',
  otherUrl: null,    // opcional — 4to slot (Twitter/X, Mastodon, Bluesky, sitio personal)
}
```

<!-- src/i18n/es.json — nuevas keys (paridad con en.json mandatoria) -->

Añadir keys nuevas BAJO el objeto root (las existentes Phase 2 NO se tocan):

```json
{
  "chapters": {
    "0": { "title": "Pre-carrera: niñez digital", "era": "Terminal" },
    "1": { "title": "Pre-carrera tardío: HTML 90s", "era": "HTML 90s crudo" },
    "2": { "title": "Flash era: gameplay programmer", "era": "Flash" },
    "3": { "title": "Web 2.0: UX + dev + líder", "era": "Web 2.0" },
    "4": { "title": "AR/VR: empresa propia + Metrodigi", "era": "AR/VR" },
    "5": { "title": "Modern: streaming, QA, frontend lead", "era": "Modern" },
    "6": { "title": "Convergencia: QA + AI", "era": "Convergencia" }
  },
  "ui": { ... existente ... },
  "avatar": { ... existente ... },
  "bio": {
    "core": "PENDING — CONTENT-CHECKLIST §1.1 (Rafael fills bio ES)"
  },
  "projects": {
    "pp1": {
      "title": "PENDING — CONTENT-CHECKLIST §2.2 (project #1 ES)",
      "desc":  "PENDING — CONTENT-CHECKLIST §2.2 (description ES)"
    }
    /* opcional pp2, pp3 si Rafael lo entrega — el executor añade SI hay contenido */
  },
  "contact": {
    "hudAria": "Contacto rápido",
    "emailAria": "Enviar email a Rafael",
    "emailTooltip": "Copiar email",
    "linkedinAria": "Perfil LinkedIn de Rafael",
    "githubAria": "GitHub de Rafael"
  },
  "seo": {
    "title": "PENDING — CONTENT-CHECKLIST §4.1 (title ES ≤60 chars)",
    "description": "PENDING — CONTENT-CHECKLIST §4.1 (description ES ≤155 chars)"
  }
}
```

**NOTA crítica i18n parity:** El objeto `chapters.N` existente Phase 2 tiene solo `title`. Este plan añade `era` como key nueva a cada chapter (necesaria para que el componente eventual pueda mostrar el era localizado en lugar del literal en inglés). El test `tests/i18n/parity.test.js` (Phase 2) usa `flatten()` sobre TODO el objeto y compara — si añades `chapters.0.era` en es.json pero no en en.json, FALLA. Añadirlo a AMBOS verbatim.

**Para en.json** (paridad obligatoria — los textos en EN):
```json
{
  "chapters": {
    "0": { "title": "Pre-career: digital childhood", "era": "Terminal" },
    "1": { "title": "Pre-career late: 90s HTML", "era": "90s HTML" },
    "2": { "title": "Flash era: gameplay programmer", "era": "Flash" },
    "3": { "title": "Web 2.0: UX + dev + lead", "era": "Web 2.0" },
    "4": { "title": "AR/VR: own company + Metrodigi", "era": "AR/VR" },
    "5": { "title": "Modern: streaming, QA, frontend lead", "era": "Modern" },
    "6": { "title": "Convergence: QA + AI", "era": "Convergence" }
  },
  "ui": { ... existente ... },
  "avatar": { ... existente ... },
  "bio": {
    "core": "PENDING — CONTENT-CHECKLIST §1.2 (Rafael fills bio EN)"
  },
  "projects": {
    "pp1": {
      "title": "PENDING — CONTENT-CHECKLIST §2.2 (project #1 EN)",
      "desc":  "PENDING — CONTENT-CHECKLIST §2.2 (description EN)"
    }
  },
  "contact": {
    "hudAria": "Quick contact",
    "emailAria": "Email Rafael",
    "emailTooltip": "Copy email",
    "linkedinAria": "Rafael's LinkedIn profile",
    "githubAria": "Rafael's GitHub"
  },
  "seo": {
    "title": "PENDING — CONTENT-CHECKLIST §4.1 (title EN ≤60 chars)",
    "description": "PENDING — CONTENT-CHECKLIST §4.1 (description EN ≤155 chars)"
  }
}
```

<!-- tests/data/chapters.test.js — shape contract -->

Tests mínimos (5):
- T1 `import { chapters } from '@/data/chapters'` → `expect(chapters).toHaveLength(7)`
- T2 cada `chapters[i]` cumple shape: `id` number 0..6, `year` number, `era` string non-empty, `eraKey` matches `/^chapters\.\d\.era$/`, `titleKey` matches `/^chapters\.\d\.title$/`, `avatarSrc` matches `/^\/assets\/ch\d-bust\.png$/` (ART-05), `palette` Array.isArray
- T3 ids van exactamente `[0,1,2,3,4,5,6]` (no gaps, no duplicates) — `expect(chapters.map(c => c.id)).toEqual([0,1,2,3,4,5,6])`
- T4 years match Phase 1 ScrollShell baseline: `[1995, 2001, 2009, 2013, 2015, 2022, 2026]`
- T5 ART-06 field existence: cada chapter tiene `palette: Array` (puede ser `[]` vacío); chapters 0 y 1 con paleta extraída de chapter-themes.css tienen `palette.length >= 2` (ch0: `['#000000', '#00ff41']`; ch1: `['#000080', '#ff00ff', '#ffff00', '#ffffff']`). Para ch2..ch6 el test NO requiere length >0 (Rafael llena CONTENT-CHECKLIST §5; Plan 03-05 verifica que palette esté lleno antes de pixelforge call).

<!-- tests/data/projects.test.js — shape contract -->

Tests mínimos (4):
- T1 `import { projects } from '@/data/projects'` → `expect(Array.isArray(projects)).toBe(true)`
- T2 si `projects.length > 0`, cada item tiene los 12 campos D3-03: `expect(Object.keys(p).sort()).toEqual(['chapterEra', 'descKey', 'id', 'imageSrc', 'link', 'planetColor', 'planetOrbit', 'planetSprite', 'role', 'techStack', 'titleKey', 'year'].sort())`
- T3 cada item con `chapterEra: 3` (Phase 3 scope) tiene `planetSprite === null && planetOrbit === null && planetColor === null` (Phase 5 los pobla)
- T4 `chapterEra` es number entre 0 y 6: `expect(p.chapterEra).toBeGreaterThanOrEqual(0); expect(p.chapterEra).toBeLessThanOrEqual(6)`

<!-- tests/data/contact.test.js — shape contract + T-CON-03 -->

Tests mínimos (3):
- T1 `import { contact } from '@/data/contact'` → `expect(typeof contact).toBe('object')` + 4 keys exactos
- T2 `expect(typeof contact.email).toBe('string')` + email es '' o matches `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
- T3 (T-CON-03) `linkedinUrl` y `githubUrl` son strings (pueden ser '' placeholder); si non-empty, comienzan con `https://` — `expect(contact.linkedinUrl === '' || contact.linkedinUrl.startsWith('https://')).toBe(true)` análogo githubUrl

<!-- tests/data/bio.test.js — minimal contract -->

Tests mínimos (1):
- T1 `import { bio } from '@/data/bio'` → `expect(bio.coreKey).toBe('bio.core')`

<!-- Existing i18n parity test (NO modificar) -->

`tests/i18n/parity.test.js` ya existe (Phase 2 Plan 02-01) y usa `flatten()` para comparar keys entre es.json y en.json. Las nuevas keys añadidas en este plan (chapters.N.era + bio.core + projects.pp1.title + projects.pp1.desc + contact.* + seo.*) deben aparecer en AMBOS JSON con paridad para que el test siga verde. NO se modifica el test — solo se verifica que pasa post-W0 con las keys nuevas presentes en ambos archivos.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Crear 4 data files + tests RED→GREEN</name>
  <files>
    src/data/chapters.js,
    src/data/projects.js,
    src/data/bio.js,
    src/data/contact.js,
    tests/data/chapters.test.js,
    tests/data/projects.test.js,
    tests/data/bio.test.js,
    tests/data/contact.test.js
  </files>
  <read_first>
    src/components/ScrollShell.vue líneas 26-34 (chapters array Phase 1 — copiar year + era exactos),
    src/styles/chapter-themes.css líneas 33-56 (ch0 y ch1 [data-chapter] blocks completos Phase 2 — extraer hex codes a chapters[].palette),
    .planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md §2.2 + §3 + §5 (si Rafael llenó, usar valores; si no, placeholders),
    src/i18n/es.json + src/i18n/en.json (verificar keys existentes Phase 2 antes de añadir nuevas — NO duplicar chapters.N.title),
    .planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md §Pattern 1 líneas 211-258 (data files con refs a i18n — verbatim shapes y header comments)
  </read_first>
  <behavior>
    Tests RED phase (escribir tests primero, todos fallan porque los data files no existen):

    `tests/data/chapters.test.js` — 5 tests (descritos en <interfaces> verbatim):
    - T1 length === 7
    - T2 shape per item: id number, year number, era string, eraKey regex /^chapters\.\d\.era$/, titleKey regex /^chapters\.\d\.title$/, avatarSrc regex /^\/assets\/ch\d-bust\.png$/, palette Array
    - T3 ids === [0,1,2,3,4,5,6]
    - T4 years === [1995,2001,2009,2013,2015,2022,2026]
    - T5 ch0 palette === ['#000000','#00ff41'] + ch1 palette === ['#000080','#ff00ff','#ffff00','#ffffff'] (extraído de chapter-themes.css); ch2..ch6 palette es Array (puede estar vacío)

    `tests/data/projects.test.js` — 4 tests:
    - T1 Array.isArray
    - T2 si length > 0, keys de cada item === sorted [chapterEra, descKey, id, imageSrc, link, planetColor, planetOrbit, planetSprite, role, techStack, titleKey, year]
    - T3 items con chapterEra===3 tienen planet* === null
    - T4 chapterEra ∈ [0,6]

    `tests/data/contact.test.js` — 3 tests:
    - T1 typeof === 'object' + 4 keys exactos (email, linkedinUrl, githubUrl, otherUrl)
    - T2 typeof contact.email === 'string'
    - T3 (T-CON-03) linkedinUrl/githubUrl son '' o https://...

    `tests/data/bio.test.js` — 1 test:
    - T1 bio.coreKey === 'bio.core'

    Commit RED: `test(03-01): RED — tests para 4 data files (chapters/projects/contact/bio)`.

    Tests GREEN phase (crear los 4 archivos según <interfaces> verbatim, todos los tests pasan):
    - chapters.js: array con 7 items (year+era de Phase 1 verbatim, paletas ch0/ch1 verbatim de chapter-themes.css, ch2..ch6 paletas vacías o CONTENT-CHECKLIST §5 si Rafael llenó)
    - projects.js: array `[]` vacío si Rafael NO llenó §2.2, O 1-3 items con shape D3-03 si SÍ llenó
    - bio.js: `export const bio = { coreKey: 'bio.core' }`
    - contact.js: object con 4 keys, valores de §3 o '' placeholders

    Commit GREEN: `feat(03-01): GREEN — 4 data files Phase 3 (CON-05 + CON-06 + CON-03 + bio ref)`.
  </behavior>
  <action>
    1. Leer CONTENT-CHECKLIST.md para verificar qué tiene Rafael lleno. Si §2.2 / §3 / §5 están con valores reales, usarlos verbatim. Si están sin llenar, usar placeholders.

    2. Escribir los 4 test files con `expect(...).toBe(...)` precisos. Patrón: `import { chapters } from '@/data/chapters'` al inicio. Vitest no necesita imports adicionales (`describe`, `it`, `expect` ya están globales según vite.config — verificar; si no, importar de 'vitest'). Reference: `tests/components/StickyAvatar.test.js` Tests 7-10 (CSS asserts) como ejemplo de estilo, aunque aquí es solo unit tests de shape.

    3. Run `npm run test:run -- tests/data/` — esperar 13 tests RED (todos fallan por module not found).

    4. Commit RED: `test(03-01): RED — tests shape para 4 data files (13 tests)`.

    5. Crear `src/data/chapters.js`:
       - Header comment según <interfaces> (referencias D3-04, D3-06, CON-05).
       - `export const chapters = [ ... ]` con 7 items.
       - Para ch0: `{ id: 0, year: 1995, era: 'Terminal', eraKey: 'chapters.0.era', titleKey: 'chapters.0.title', avatarSrc: '/assets/ch0-bust.png', palette: ['#000000', '#00ff41'] }`.
       - Para ch1: análogo con palette `['#000080', '#ff00ff', '#ffff00', '#ffffff']`.
       - Para ch2..ch6: análogo con palette `[]` (o CONTENT-CHECKLIST §5 si Rafael llenó).

    6. Crear `src/data/projects.js`:
       - Header comment según <interfaces>.
       - `export const projects = [ ... ]` — `[]` vacío si Rafael NO llenó §2.2, O 1-3 items con shape D3-03 verbatim (12 campos, planet* === null).

    7. Crear `src/data/bio.js`:
       - Header comment minimal.
       - `export const bio = { coreKey: 'bio.core' }`.

    8. Crear `src/data/contact.js`:
       - Header comment con ref D3-10 + T-CON-03 mitigation.
       - `export const contact = { email: '<de §3 o ''>', linkedinUrl: '<de §3 o ''>', githubUrl: '<de §3 o ''>', otherUrl: <de §3 o null> }`.
       - Si Rafael llenó §3 con URLs, hardcoded `'https://www.linkedin.com/in/...'` y `'https://github.com/...'` (https obligatorio). Si llenó con HTTP, escalar a https.

    9. Run `npm run test:run -- tests/data/` — esperar 13 tests GREEN.

    10. Commit GREEN: `feat(03-01): GREEN — 4 data files Phase 3 scaffolded (CON-05 + CON-06 + CON-03 + bio ref)`.
  </action>
  <verify>
    <automated>npm run test:run -- tests/data/ &amp;&amp; npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/chapters.js` exporta `const chapters` array de 7 items con shape D3-04 + palette ART-06 (ch0/ch1 hex codes presentes; ch2..ch6 puede ser `[]` o lleno desde §5)
    - `src/data/projects.js` exporta `const projects` array (`[]` o con 1-3 items shape D3-03)
    - `src/data/bio.js` exporta `const bio = { coreKey: 'bio.core' }`
    - `src/data/contact.js` exporta `const contact` con 4 keys exactos (email, linkedinUrl, githubUrl, otherUrl)
    - 13 tests verdes en `tests/data/` (5+4+3+1)
    - Suite global `npm run test:run` ≥164 tests verdes (Phase 2 baseline 151 + 13 nuevos), build verde
  </acceptance_criteria>
  <done>4 data files con shape locked + 13 tests verdes + suite global ≥164.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Extender es.json + en.json con keys nuevas Phase 3 (mantener parity)</name>
  <files>
    src/i18n/es.json,
    src/i18n/en.json
  </files>
  <read_first>
    src/i18n/es.json (estructura completa Phase 2 — verificar que chapters.N.title existen),
    src/i18n/en.json (paridad obligatoria con es.json),
    tests/i18n/parity.test.js (test que enforza paridad — verificar que flatten() compara keys, NO contenidos),
    .planning/phases/03-chapter-3-end-to-end/CONTENT-CHECKLIST.md (si Rafael llenó §1 bio + §2.2 proyectos + §4.1 SEO, usar contenido; si no, placeholders),
    .planning/phases/03-chapter-3-end-to-end/03-RESEARCH.md §Pattern 2 líneas 783-815 (i18n keys nuevas — listado verbatim)
  </read_first>
  <behavior>
    Tras esta tarea:
    - `tests/i18n/parity.test.js` sigue verde (T1 flatten equality, T2 chapters.N.title presentes, T3 ui/avatar keys presentes — los 3 tests Phase 2)
    - Las claves nuevas añadidas existen en AMBOS archivos y son leíbles via t() en cualquier componente que las use (Plans 03-02..03-05 las consumirán)
    - Las claves añadidas son: `chapters.0.era` ... `chapters.6.era` (7 nuevas — DOS keys por chapter ahora: title + era), `bio.core` (1), `projects.pp1.title` + `projects.pp1.desc` (2; opcional pp2 + pp3 si Rafael llenó), `contact.hudAria` + `contact.emailAria` + `contact.emailTooltip` + `contact.linkedinAria` + `contact.githubAria` (5), `seo.title` + `seo.description` (2). Total: 7 + 1 + 2 + 5 + 2 = 17 nuevas keys por idioma (más si Rafael tiene 2-3 proyectos).
  </behavior>
  <action>
    1. Leer `src/i18n/es.json` completo. Verificar que `chapters.0.title` ... `chapters.6.title` existen (Phase 2 W0). Mismo con en.json.

    2. Leer CONTENT-CHECKLIST.md §1 (bio ES + EN), §2.2 (proyectos ch3 Pink Parrot — nombre+desc ES+EN), §4.1 (SEO title+description ES+EN). Si están con valores reales, usar verbatim. Si no, usar placeholders `"PENDING — CONTENT-CHECKLIST §X"`.

    3. Editar `src/i18n/es.json` añadiendo (NO eliminar ni modificar keys existentes):
       - Bajo `chapters.0`..`chapters.6`: añadir `"era": "Terminal"` / `"HTML 90s crudo"` / `"Flash"` / `"Web 2.0"` / `"AR/VR"` / `"Modern"` / `"Convergencia"`.
       - Top-level `"bio": { "core": "<texto bio ES de §1.1 o placeholder>" }`.
       - Top-level `"projects": { "pp1": { "title": "<de §2.2 o placeholder>", "desc": "<de §2.2 o placeholder>" } }` — añadir pp2 + pp3 SI Rafael llenó.
       - Top-level `"contact": { "hudAria": "Contacto rápido", "emailAria": "Enviar email a Rafael", "emailTooltip": "Copiar email", "linkedinAria": "Perfil LinkedIn de Rafael", "githubAria": "GitHub de Rafael" }` — copy verbatim del RESEARCH.
       - Top-level `"seo": { "title": "<de §4.1 ES ≤60 chars o placeholder>", "description": "<de §4.1 ES ≤155 chars o placeholder>" }`.

    4. Editar `src/i18n/en.json` con paridad EXACTA de keys (los valores son distintos pero las llaves idénticas):
       - `chapters.N.era`: 'Terminal' / '90s HTML' / 'Flash' / 'Web 2.0' / 'AR/VR' / 'Modern' / 'Convergence'.
       - `bio.core`: texto EN de §1.2 o placeholder.
       - `projects.pp1.title/desc`: contenido EN de §2.2 o placeholder.
       - `contact.*`: 'Quick contact' / 'Email Rafael' / 'Copy email' / "Rafael's LinkedIn profile" / "Rafael's GitHub".
       - `seo.title/description`: contenido EN de §4.1 o placeholder.

    5. Run `npm run test:run -- tests/i18n/parity.test.js` — debe pasar (T1 flatten compara keys; ambos archivos tienen las mismas keys).

    6. Run `npm run test:run` (suite completa) — verificar ≥161 tests verdes (151 Phase 2 + 13 Wave 0 Task 1.1 = 164; ajustar count exacto).

    7. Commit: `feat(03-01): extender es.json + en.json con keys Phase 3 (chapters.N.era, bio.core, projects.*, contact.*, seo.*) — parity preserved`.
  </action>
  <verify>
    <automated>npm run test:run -- tests/i18n/ &amp;&amp; npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `src/i18n/es.json` contiene las 17+ nuevas keys (chapters.N.era × 7 + bio.core + projects.pp1.{title,desc} + contact × 5 + seo × 2; más si pp2/pp3)
    - `src/i18n/en.json` contiene exactamente las mismas keys con valores en inglés
    - `tests/i18n/parity.test.js` corre 3 tests verdes (T1 paridad + T2 chapter titles + T3 ui/avatar)
    - Suite global: 151 (Phase 2) + 13 (Task 1.1) + 0 (Task 1.2 no añade tests, solo modifica JSON) = ≥164 tests verdes
    - `npm run build` verde; bundle JSON inflado proporcionalmente pero <2KB delta (es texto plano)
  </acceptance_criteria>
  <done>Ambos JSON extendidos con paridad mantenida; parity test verde; suite global ≥164 verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/data/*.js` source code → bundled JS at build | Datos hardcoded en source; el build los ships verbatim. No hay user input runtime que mute estos archivos. |
| `src/i18n/*.json` source → bundled JSON | Idem. Los placeholders se reemplazan vía commit + redeploy, no via runtime mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CON-03 | Spoofing | `src/data/contact.js` (URLs externas) | mitigate | URLs hardcoded en source code; `rel="noopener noreferrer"` aplicado en Plan 03-02 cuando ContactHUD las consuma. No user input runtime. |
| T-DATA-PII | Information disclosure | `src/data/contact.js` (email visible) | accept | El email visible es intencional (D3-10) — portafolio público. Rafael decide si usa email dedicado/canario. |
| T-i18n-INJ | Tampering / Injection | `src/i18n/*.json` interpolations | mitigate | vue-i18n `t(key, { params })` es safe-by-default: interpolations son escaped antes de DOM render. Los placeholders `{chapter}`, `{era}`, `{year}` ya en uso Phase 2 sin issues. |
</threat_model>

<verification>
- Comando: `npm run test:run && npm run build`
- Esperado: ≥164 tests verdes (151 baseline Phase 2 + 13 nuevos data tests), build verde
- Manual smoke: `npm run dev` — el sitio sigue cargando sin errores en console (data files no se renderean aún, pero su import no debe romper anything — verificar que ningún chapter ni componente Phase 1/2 importa accidentalmente desde `@/data/*` aún)
- Mobile: N/A (no UI cambia)
- Threat verification: `grep -E "(http|https)://" src/data/contact.js` retorna solo URLs en es-EC/global hardcoded (T-CON-03 mitigation visible)
</verification>

<success_criteria>
- 4 archivos `src/data/*.js` creados con shape locked por D3-01..D3-04 + D3-10
- 13 tests nuevos en `tests/data/` verdes (chapters 5 + projects 4 + contact 3 + bio 1)
- 17+ keys nuevas en es.json + en.json con paridad verificada por `tests/i18n/parity.test.js`
- Suite global ≥164 tests verdes, build verde
- REQ-IDs cubiertos: CON-05 (chapters.js), CON-06 (projects.js shape D3-03), CON-03 partial (contact data — componente HUD viene en Plan 03-02), ART-06 partial (palette field existence — enforcement runtime en Plan 03-05)
</success_criteria>

<output>
After completion, create `.planning/phases/03-chapter-3-end-to-end/03-01-SUMMARY.md` con:
- 4 data files creados (paths + LOC aprox + shape verbatim per D3-XX)
- Keys i18n añadidas (lista + cobertura paridad)
- Tests añadidos (13 nuevos + mapping a REQ-IDs)
- Si Rafael llenó CONTENT-CHECKLIST §X, qué valores reales se hardcodearon (vs placeholders)
- Decisiones tomadas (e.g., el array `projects.js` quedó vacío vs con 1/2/3 proyectos según §2.2)
- Pending para W1 (Plan 03-02): ContactHUD vertical slice usando `contact.js` data
- Pending para W2 (Plan 03-03): Chapter3Content + ProjectCard usando `bio.js` + `projects.js` + `chapters[3]`
- Pending para W3 (Plan 03-04): SEO via @unhead/vue usando `seo.*` keys
- Pending para W4 (Plan 03-05): Avatar pixel art usando `chapter.palette` + chapter.avatarSrc paths
- Notar si executor decidió migrar arrays chapters[] de ScrollShell/StickyTimeline a usar @/data/chapters (decisión recomendada: NO en W0, queda para Phase 4 según `key_decisions`)
</output>
