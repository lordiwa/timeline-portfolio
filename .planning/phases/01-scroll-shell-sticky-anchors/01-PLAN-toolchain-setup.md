---
phase: 1
plan: 1
slug: toolchain-setup
wave: 0
type: execute
mode: mvp
autonomous: true
gap_closure: false
requirements_covered: []
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - vitest.config.js
  - tests/setup.js
  - tests/smoke.test.js
  - src/composables/.gitkeep
  - src/components/.gitkeep
files_created_for_downstream:
  - src/composables/
  - src/components/
  - tests/
must_haves:
  truths:
    - "package.json declara vue ^3.5.0 (refleja la realidad: 3.5.34 ya instalado)"
    - "@vueuse/core@14.3.0 está en dependencies y resuelve sin warnings de peer"
    - "Vitest + @vue/test-utils + jsdom están en devDependencies"
    - "npm test corre y exit 0 con un smoke test trivial pasando"
    - "npm run dev sirve el dev server en la red LAN (no solo 127.0.0.1)"
  artifacts:
    - path: package.json
      provides: "Manifest actualizado con peer Vue ^3.5.0 + vueuse + Vitest"
      contains: '"@vueuse/core"'
    - path: vite.config.js
      provides: "Dev server expuesto en LAN para acceso desde mobile"
      contains: "host: true"
    - path: vitest.config.js
      provides: "Vitest config con jsdom + Vue plugin + tests/setup.js"
      contains: "environment: 'jsdom'"
    - path: tests/setup.js
      provides: "Mocks globales: IntersectionObserver, ResizeObserver, matchMedia, requestAnimationFrame"
      contains: "IntersectionObserver"
    - path: tests/smoke.test.js
      provides: "Test trivial que garantiza que la suite arranca"
    - path: src/composables/.gitkeep
      provides: "Directorio scaffold para composables (useScrollState, usePRM)"
    - path: src/components/.gitkeep
      provides: "Directorio scaffold para componentes (ScrollShell, StickyAvatar, StickyTimeline, SkipLink)"
  key_links:
    - from: package.json
      to: node_modules/@vueuse/core
      via: "npm install"
      pattern: '"@vueuse/core": "\\^14\\.'
    - from: vitest.config.js
      to: tests/setup.js
      via: "setupFiles"
      pattern: "setupFiles"
---

<objective>
Wave 0 setup: dejar el toolchain listo para que TODOS los plans subsecuentes (W1..W6) puedan compilar, testear y servir el dev server a un iPhone real. Sin esto, ningún plan posterior puede pasar su `<verify>` automatizado.

**Purpose:** El research RECOMMENDATION A cierra el "Vue version blocker" de STATE.md: el scaffold ya tiene Vue 3.5.34 instalado, pero `package.json` aún declara `^3.4.0`. Este plan refleja la realidad en el manifest, añade `@vueuse/core` (peer satisfied), instala el harness de tests, y expone Vite a LAN para el smoke test iOS del Plan 07.

**Output:** Manifests actualizados, deps instaladas, directorios `src/composables/` y `src/components/` creados (vacíos con `.gitkeep`), `tests/setup.js` con mocks globales necesarios para JSDOM (IntersectionObserver, ResizeObserver, matchMedia, RAF — todos referenciados en los plans posteriores).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scroll-shell-sticky-anchors/01-RESEARCH.md
@.planning/phases/01-scroll-shell-sticky-anchors/01-UI-SPEC.md
@.planning/phases/01-scroll-shell-sticky-anchors/01-CONTEXT.md
@package.json
@vite.config.js

<interfaces>
<!-- Nada que extraer del codebase — este es el plan que crea los directorios y harness. -->
<!-- Los plans posteriores extraerán de los archivos creados aquí. -->
</interfaces>

<key-decisions>
- RESEARCH §1 Open-Q-A → **Camino A** (bump manifest a `^3.5.0` + install vueuse 14, NO downgrade, NO composables custom).
- RESEARCH §8 → `host: true` en `vite.config.js` para que el iPhone en la misma WiFi acceda vía `http://<IP-LAN>:5173/`. También `open: false` para no spamear el browser local en modo LAN.
- RESEARCH §12 → Vitest + jsdom + @vue/test-utils como harness. El UI-SPEC y los plans posteriores describen tests específicos que necesitan los mocks globales declarados en `tests/setup.js`.
- CLAUDE.md preferencias: Sin TypeScript. Comandos en PowerShell con paths Windows.
</key-decisions>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1.1: Bump manifest a Vue ^3.5.0 e instalar @vueuse/core@14.3.0</name>
  <files>package.json, package-lock.json (autogenerated)</files>
  <action>
    Editar `package.json` para cambiar `"vue": "^3.4.0"` → `"vue": "^3.5.0"` (refleja la versión real instalada 3.5.34; el research RECOMMENDATION A lo cierra como camino A). NO ejecutar `npm install vue@^3.5.0` — Vue 3.5.34 ya está en `node_modules/` (verificable con `npm list vue`), solo el manifest mentía. Después instalar `@vueuse/core` con `npm install @vueuse/core` desde el working directory (PowerShell). Esto añade vueuse 14.3.0 a dependencies, regenera lockfile, y deja el peer requirement satisfied. NO usar `--save-dev` — vueuse es runtime dependency. Verificar con `npm list @vueuse/core` que muestra `@vueuse/core@14.3.x deduped` sin warnings de peer.
  </action>
  <verify>
    <automated>Ejecutar desde el working directory: `node -e "const p = require('./package.json'); if (p.dependencies.vue !== '^3.5.0') process.exit(1); if (!p.dependencies['@vueuse/core']) process.exit(1); console.log('OK manifests')"`. Exit 0 esperado.</automated>
  </verify>
  <done>
    `package.json` declara `"vue": "^3.5.0"` y `"@vueuse/core": "^14.3.0"` (o equivalente caret en 14.x). `npm list vue @vueuse/core` muestra ambas resueltas sin "missing peer" warnings. Lockfile regenerado.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.2: Instalar harness de tests (Vitest + @vue/test-utils + jsdom)</name>
  <files>package.json (devDependencies + scripts)</files>
  <action>
    Desde el working directory (PowerShell): `npm install --save-dev vitest @vue/test-utils jsdom`. Esto añade los 3 packages a `devDependencies`. Después, editar `package.json` "scripts" para añadir `"test": "vitest"` y `"test:run": "vitest --run"`. Mantener los scripts existentes (`dev`, `build`, `preview`) intactos. Verificar con `npx vitest --version` que Vitest arranca.
  </action>
  <verify>
    <automated>`node -e "const p = require('./package.json'); if (!p.devDependencies.vitest) process.exit(1); if (!p.devDependencies['@vue/test-utils']) process.exit(1); if (!p.devDependencies.jsdom) process.exit(1); if (!p.scripts.test) process.exit(1); if (!p.scripts['test:run']) process.exit(1); console.log('OK test harness')"`. Exit 0 esperado.</automated>
  </verify>
  <done>
    `package.json` lista `vitest`, `@vue/test-utils`, `jsdom` en devDependencies. Scripts `test` y `test:run` presentes. `npx vitest --version` imprime versión válida.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.3: Crear vitest.config.js + tests/setup.js + tests/smoke.test.js</name>
  <files>vitest.config.js, tests/setup.js, tests/smoke.test.js</files>
  <action>
    Crear `vitest.config.js` en root con: `defineConfig` de `vitest/config`, plugin `@vitejs/plugin-vue`, `test.environment: 'jsdom'`, `test.globals: true`, `test.setupFiles: ['./tests/setup.js']`. Crear directorio `tests/` (que el `mkdir` cree automáticamente al escribir el primer archivo). Crear `tests/setup.js` con mocks globales: (a) `IntersectionObserver` stub que guarda callbacks + expone `triggerEntries(entries)` helper para tests, (b) `ResizeObserver` stub similar con `triggerResize(entries)`, (c) `window.matchMedia` mock que devuelve `{ matches: false, media: query, addEventListener: vi.fn(), removeEventListener: vi.fn() }` configurable por test, (d) `requestAnimationFrame` que usa `setTimeout(fn, 16)` y `cancelAnimationFrame` que usa `clearTimeout`. Crear `tests/smoke.test.js` con un test trivial: `import { describe, it, expect } from 'vitest'; describe('smoke', () => { it('vitest is wired', () => { expect(1 + 1).toBe(2) }) })`. Verificar con `npm run test:run` que exit 0 y reporta "1 passed".
  </action>
  <verify>
    <automated>`npm run test:run`. Exit 0 esperado, output incluye "1 passed" (o un único test pasando).</automated>
  </verify>
  <done>
    `vitest.config.js` existe con jsdom + plugin Vue + setupFiles. `tests/setup.js` exporta los 4 mocks globales (IntersectionObserver, ResizeObserver, matchMedia, RAF). `tests/smoke.test.js` pasa. `npm run test:run` exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.4: Scaffold de directorios src/composables/ y src/components/ + actualizar vite.config.js a host LAN</name>
  <files>src/composables/.gitkeep, src/components/.gitkeep, vite.config.js</files>
  <action>
    Crear `src/composables/.gitkeep` (archivo vacío) y `src/components/.gitkeep` (archivo vacío) para que git trackee los directorios. Estos serán llenados por los plans 02-06 (composables: `useScrollState.js`, `usePRM.js`; components: `ScrollShell.vue`, `StickyAvatar.vue`, `StickyTimeline.vue`, `SkipLink.vue`). Después editar `vite.config.js`: cambiar `host: '127.0.0.1'` → `host: true` (equivalente a `0.0.0.0`, expone LAN per RESEARCH §8). Cambiar `open: true` → `open: false` (en modo LAN abrir browser local es spam; el dev abre manualmente). Mantener `port: 5173`. NO añadir nuevas opciones de Vite — solo modificar las 2 keys ya existentes.
  </action>
  <verify>
    <automated>Ejecutar `node -e "const fs = require('fs'); if (!fs.existsSync('./src/composables/.gitkeep')) process.exit(1); if (!fs.existsSync('./src/components/.gitkeep')) process.exit(1); const v = fs.readFileSync('./vite.config.js', 'utf8'); if (!v.includes('host: true')) process.exit(1); if (v.includes(\"host: '127.0.0.1'\")) process.exit(1); console.log('OK scaffold')"`. Exit 0 esperado.</automated>
  </verify>
  <done>
    `src/composables/.gitkeep` y `src/components/.gitkeep` existen. `vite.config.js` declara `host: true` y `open: false`. `port: 5173` sin cambios.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.5: Smoke test E2E del toolchain (npm run dev + npm test exit 0)</name>
  <files>(verificación, ninguno modificado)</files>
  <action>
    Ejecutar `npm run test:run` desde el working directory (PowerShell). Debe exit 0 con el smoke test pasando. Después ejecutar `npm run dev` en background y verificar (con un `node -e` o un `curl` rápido a `http://127.0.0.1:5173/`) que el server responde. Detener el dev server con `Ctrl+C` o `taskkill`. NO levantar el dev server permanentemente — este task solo verifica que arranca; los plans posteriores lo usarán como needed. Cualquier warning de Vite sobre `host: true` exponiendo LAN está esperado y no es error.
  </action>
  <verify>
    <automated>`npm run test:run`. Exit 0 esperado.</automated>
  </verify>
  <done>
    `npm run test:run` exit 0. `npm run dev` arranca sin errores (verificado al menos manualmente o vía un comando que toca el puerto 5173). Toolchain listo para el Plan 02.
  </done>
</task>

</tasks>

<verification>
**Automated:**
```powershell
npm run test:run
```
Debe exit 0 con `1 passed` (smoke test).

**Manual:**
1. `npm run dev` — la consola debe imprimir tanto `Local: http://localhost:5173/` como `Network: http://<IP-LAN>:5173/`.
2. Acceder a `http://localhost:5173/` desde browser local → ve el placeholder actual de App.vue (todavía no se ha tocado).
3. Detener con `Ctrl+C`.
</verification>

<success_criteria>
- [ ] `package.json` declara `vue ^3.5.0` y `@vueuse/core ^14.3.0` en dependencies.
- [ ] `vitest`, `@vue/test-utils`, `jsdom` en devDependencies.
- [ ] Scripts `test` y `test:run` en package.json.
- [ ] `vitest.config.js` existe con jsdom environment + plugin vue + setupFiles.
- [ ] `tests/setup.js` mockea `IntersectionObserver`, `ResizeObserver`, `matchMedia`, `requestAnimationFrame` globalmente.
- [ ] `tests/smoke.test.js` pasa.
- [ ] `src/composables/` y `src/components/` existen (con `.gitkeep` trackeable por git).
- [ ] `vite.config.js` declara `host: true` y `open: false`.
- [ ] `npm run test:run` exit 0.
- [ ] `npm run dev` arranca sin errores y expone LAN.
</success_criteria>

<output>
Tras completar, crear `.planning/phases/01-scroll-shell-sticky-anchors/01-01-SUMMARY.md` documentando:
- Versiones finales instaladas (vue, @vueuse/core, vitest, jsdom, @vue/test-utils).
- IP LAN reportada por Vite al arrancar (`Network: http://<IP>:5173/`).
- Confirmación de que Windows Firewall aceptó el prompt UAC (o link a la regla manual si fue necesario).
- Cualquier desviación de las versiones target (debería ser cero).
</output>
