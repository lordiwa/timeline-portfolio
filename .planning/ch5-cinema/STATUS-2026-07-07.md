# ch5 "Cine" — ESTADO 2026-07-07 (corte de pixellab a mitad)

## Dónde quedó
- **Groom completo** (100 personajes viables): `CHARACTER-ROSTER.md`.
- **`buildCrowd()`** con bolsa barajada (ratio ≤1.2) + `animDelay` + guard: implementado.
- **Creación en pixellab:** **47 de 100** temáticos completados y **bajados** a
  `public/assets/ch5-cinema/{slug}.png`. Los **53 restantes** quedaron sin generar por un
  **corte/incidente de pixellab** (~75% de las gen `standard` devolvían "Generation failed").
  ⚠️ **Los fallos NO consumen cuota** (22 gen reales usadas de 2000).
- **`CAST` cableado a 72 únicos** (25 beat-em-up + 47 temáticos) en `Chapter5Content.vue`.
  Screenshot validado: la multitud temática se lee de espaldas mirando la pantalla. 👍
- **Animación de festejo:** **NO empezada** (bloqueada — también usa pixellab).

## Ratio 1.2 (temporal)
Con 72 únicos y ~146 asientos, el ratio actual es ~2.0 (>1.2). Se mantuvo la composición de
146 asientos que Rafael aprobó ("está perfecto así"). El ratio vuelve a **1.168 ≤ 1.2** cuando
se completen los 53 y el CAST llegue a 125. El guard de dev en `Chapter5Content.vue` lo avisa.

## Cómo RESUMIR cuando pixellab vuelva (todo persistido)
1. **Backfill de los 53:** en `IDS.tsv`, los `status=pending-pixellab` tienen su último
   `character_id` en vuelo. Chequear con `get_character`; los que sigan `failed`, re-crear con
   `create_character` usando la descripción de `DESCRIPTIONS.tsv` (params: standard, 8dir, side,
   humanoid, size). **Disparar en lotes ≤12 y esperar** (el incidente fallaba en masa al saturar).
2. **Bajar** `north.png` de cada uno:
   `https://backblaze.pixellab.ai/file/pixellab-characters/18d52b53-7235-434e-a62f-90a3d1e6d1f1/{id}/rotations/north.png`
   → `public/assets/ch5-cinema/{slug}.png`. (Script de descarga: ver git/scratchpad.)
3. **Añadir los 53 slugs al `CAST`** (bloque "temáticos") → 125 únicos, ratio 1.168.
4. **Animar festejo (125)** = 47+53 nuevos + 25 existentes: `animate_character` `v3`,
   `directions:["north"]`, `frame_count:8`, action_description de festejo (§5 del roster).
   IDs de los 25 existentes en Apéndice D del roster. **1 gen c/u** (~125 gen).
5. **Bajar animado** (GIF/spritesheet) + cambiar el `<img>` del crowd a render animado con
   `animDelay` por asiento (ya calculado en `buildCrowd`). Screenshot `?ch=5`. Ver §5 del roster.

## Archivos clave
- `CHARACTER-ROSTER.md` — groom + specs + spec de animación/ratio (fuente de verdad).
- `IDS.tsv` — slug → character_id → status (done | pending-pixellab).
- `DESCRIPTIONS.tsv` — slug → size → description (para re-crear los pendientes).
- `src/components/Chapter5Content.vue` — CAST (72) + buildCrowd (bolsa barajada + animDelay + guard).
